# Codex Conversation Export

- Exported at: 2026-04-28 03:27:26 EDT
- Sessions: 1

## Session 019dd2e5-ae7a-7f82-83a6-4e6702aeb9f5

- Started: 2026-04-28 03:02:41 EDT
- Source: `/home/ag/.codex/sessions/2026/04/28/rollout-2026-04-28T03-02-41-019dd2e5-ae7a-7f82-83a6-4e6702aeb9f5.jsonl`
- CWD: `/home/ag/projects/codex-history-export`

### User - 2026-04-28 03:02:54 EDT

补一个执行导出命令的简洁readme

### Assistant - 2026-04-28 03:03:02 EDT

我先看一下仓库结构和现有导出入口，确认 README 应该写到哪里、命令怎么调用。

### Tool Call - 2026-04-28 03:03:02 EDT

```
exec_command {"cmd":"pwd","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Call - 2026-04-28 03:03:02 EDT

```
exec_command {"cmd":"rg --files","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-28 03:03:02 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-28 03:03:02 EDT

```
Chunk ID: ed96bc
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 10
Output:
/home/ag/projects/codex-history-export
```

### Tool Output - 2026-04-28 03:03:02 EDT

```
Chunk ID: b13efe
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 259
Output:
__pycache__/export_codex_history.cpython-312.pyc
export_codex_history.py
exports/2026-04-16T04-00-56-019d954e-b3f7-7b52-b71b-f5ce753398af.md
exports/2026-04-25T05-16-23-019dc3ed-02f6-7c90-8dcf-97e7f6b26eaf.md
exports/2026-04-19T22-09-50-019da8a6-b2e9-7451-b09e-e650ffdb6ff1.md
exports/legacy/codex_history.md
exports/legacy/codex_prompts.md
exports/legacy/codex_history_latest.md
exports/2026-04-24T00-48-18-019dbdd1-3704-7791-91d6-666b2dc2ec15.md
exports/2026-04-17T03-01-27-019d9a3e-9cc4-7412-9eed-651eafe03763.md
exports/2026-04-27T23-47-54-019dd233-5c3b-7273-8edf-2a90d521ce8d.md
exports/2026-04-28T00-57-09-019dd272-c234-78e3-881b-efc81ea369ba.md
exports/2026-04-27T23-26-13-019dd21f-81a8-7553-874c-9d73ec55e30f.md
exports/2026-04-28T02-51-48-019dd2db-b91f-71e2-b92e-297aa02a0251.md
exports/2026-04-25T09-03-43-019dc4bd-2749-7941-a0f5-da0eb9611802.md
exports/2026-04-15T02-44-08-019d8fe2-084b-7a20-b3fa-a8636ccd0aeb.md
exports/index.md
exports/prompt-history.md
exports/2026-04-27T23-53-10-019dd238-2d54-71a1-ac74-4359f75a53c4.md
```

### Tool Output - 2026-04-28 03:03:02 EDT

```
Chunk ID: 22434a
Wall time: 0.0000 seconds
Process exited with code 128
Original token count: 18
Output:
fatal: not a git repository (or any of the parent directories): .git
```

### Assistant - 2026-04-28 03:03:10 EDT

这里不是 git 仓库；我会直接补文档文件，不做版本控制操作。接下来读一下脚本参数，避免 README 写错命令。

### Tool Call - 2026-04-28 03:03:10 EDT

```
exec_command {"cmd":"sed -n '1,240p' export_codex_history.py","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-28 03:03:10 EDT

```
exec_command {"cmd":"ls -la","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-28 03:03:10 EDT

```
exec_command {"cmd":"test -f README.md && sed -n '1,200p' README.md","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-28 03:03:10 EDT

````
Chunk ID: baf567
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1840
Output:
#!/usr/bin/env python3
"""Export Codex conversation history to readable Markdown."""

from __future__ import annotations

import argparse
import json
import re
from dataclasses import dataclass, field
from datetime import datetime, timezone
from pathlib import Path
from typing import Any


SECRET_PATTERNS = [
    re.compile(r"gh[pousr]_[A-Za-z0-9_]{20,}"),
    re.compile(r"sk-[A-Za-z0-9_-]{20,}"),
    re.compile(r"\b(token=[REDACTED] re.IGNORECASE),
    re.compile(r"(api[_-]?key[\"':=\s]+)[A-Za-z0-9._-]{12,}", re.IGNORECASE),
]


@dataclass
class Message:
    role: str
    text: str
    timestamp: str | None = None


@dataclass
class Session:
    session_id: str
    source: Path
    output_stem: str
    started_at: str | None = None
    cwd: str | None = None
    messages: list[Message] = field(default_factory=list)


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(
        description="Export Codex sessions or prompt history to Markdown."
    )
    parser.add_argument(
        "--codex-home",
        type=Path,
        default=Path.home() / ".codex",
        help="Codex config/history directory. Default: ~/.codex",
    )
    parser.add_argument(
        "-o",
        "--output",
        type=Path,
        default=None,
        help="Output directory. Default: ./exports next to this script",
    )
    parser.add_argument(
        "--source",
        choices=("sessions", "history"),
        default="sessions",
        help="sessions exports full conversations; history exports user prompts only.",
    )
    parser.add_argument(
        "--session",
        default="all",
        help="Session id prefix, 'latest', or 'all'. Default: all",
    )
    parser.add_argument(
        "--include-tools",
        action="store_true",
        help="Include tool calls and tool outputs in session exports.",
    )
    parser.add_argument(
        "--include-system",
        action="store_true",
        help="Include system/developer messages in session exports.",
    )
    parser.add_argument(
        "--no-redact",
        action="store_true",
        help="Do not redact common secrets from exported text.",
    )
    return parser.parse_args()


def load_jsonl(path: Path) -> list[dict[str, Any]]:
    rows: list[dict[str, Any]] = []
    with path.open("r", encoding="utf-8") as f:
        for line_no, line in enumerate(f, 1):
            line = line.strip()
            if not line:
                continue
            try:
                rows.append(json.loads(line))
            except json.JSONDecodeError as exc:
                raise ValueError(f"{path}:{line_no}: invalid JSONL: {exc}") from exc
    return rows


def local_time(value: str | int | float | None) -> str | None:
    if value is None:
        return None
    if isinstance(value, (int, float)):
        dt = datetime.fromtimestamp(value, tz=timezone.utc)
    else:
        raw = value.replace("Z", "+00:00")
        try:
            dt = datetime.fromisoformat(raw)
        except ValueError:
            return str(value)
    return dt.astimezone().strftime("%Y-%m-%d %H:%M:%S %Z")


def redact(text: str) -> str:
    for pattern in SECRET_PATTERNS:
        text = pattern.sub(lambda m: (m.group(1) if m.groups() else "") + "[REDACTED]", text)
    return text


def extract_text(content: Any) -> str:
    if isinstance(content, str):
        return content
    if not isinstance(content, list):
        return ""

    chunks: list[str] = []
    for item in content:
        if not isinstance(item, dict):
            continue
        if item.get("type") in {"input_text", "output_text", "text"}:
            chunks.append(str(item.get("text", "")))
    return "\n".join(chunk for chunk in chunks if chunk).strip()


def should_skip_message(text: str) -> bool:
    stripped = text.strip()
    return stripped.startswith("<environment_context>") or stripped.startswith("<user_info>")


def parse_session_file(path: Path, include_tools: bool, include_system: bool) -> Session:
    rows = load_jsonl(path)
    stem = path.stem.removeprefix("rollout-")
    session = Session(session_id=stem, output_stem=safe_filename(stem), source=path)

    for row in rows:
        row_type = row.get("type")
        payload = row.get("payload") or {}
        ts = local_time(row.get("timestamp"))

        if row_type == "session_meta":
            session.session_id = payload.get("id") or session.session_id
            session.started_at = local_time(payload.get("timestamp")) or ts
            session.cwd = payload.get("cwd")
            continue

        if row_type != "response_item" or not isinstance(payload, dict):
            continue

        item_type = payload.get("type")
        if item_type == "message":
            role = str(payload.get("role") or "unknown")
            if role not in {"user", "assistant"} and not include_system:
                continue
            text = extract_text(payload.get("content"))
            if text and not should_skip_message(text):
                session.messages.append(Message(role=role, text=text, timestamp=ts))
        elif include_tools and item_type == "function_call":
            name = payload.get("name", "tool")
            args = payload.get("arguments", "")
            session.messages.append(Message("tool_call", f"{name} {args}".strip(), ts))
        elif include_tools and item_type == "function_call_output":
            output = str(payload.get("output", "")).strip()
            if output:
                session.messages.append(Message("tool_output", output, ts))

    return session


def session_files(codex_home: Path, session_filter: str) -> list[Path]:
    files = sorted(
        (codex_home / "sessions").glob("**/*.jsonl"),
        key=lambda p: p.stat().st_mtime,
    )
    if session_filter == "all":
        return files
    if session_filter == "latest":
        return files[-1:] if files else []
    matches = [p for p in files if session_filter in p.stem]
    if not matches:
        raise SystemExit(f"No session matched: {session_filter}")
    return matches


def export_sessions(
    codex_home: Path,
    session_filter: str,
    include_tools: bool,
    include_system: bool,
) -> list[Session]:
    return [
        parse_session_file(path, include_tools, include_system)
        for path in session_files(codex_home, session_filter)
    ]


def export_prompt_history(codex_home: Path) -> list[Session]:
    history_file = codex_home / "history.jsonl"
    session = Session(
        session_id="prompt-history",
        output_stem="prompt-history",
        source=history_file,
    )
    for row in load_jsonl(history_file):
        text = str(row.get("text") or "").strip()
        if text:
            session.messages.append(
                Message(
                    role="user",
                    text=text,
                    timestamp=local_time(row.get("ts")),
                )
            )
    return [session]


def fence(text: str) -> str:
    if "\n" not in text and len(text) < 120:
        return text
    marker = "```"
    while marker in text:
        marker += "`"
    return f"{marker}\n{text.rstrip()}\n{marker}"


def safe_filename(value: str) -> str:
    return re.sub(r"[^A-Za-z0-9._-]+", "_", value).strip("._") or "session"


def render_markdown(sessions: list[Session], do_redact: bool) -> str:
    lines = [
````

### Tool Output - 2026-04-28 03:03:10 EDT

```
Chunk ID: afaab7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 74
Output:
total 28
drwxrwxr-x 4 ag ag  4096 Apr 28 03:03 .
drwxrwxr-x 5 ag ag  4096 Apr 28 03:00 ..
-r--r--r-- 1 ag ag     0 Apr 28 03:03 .codex
drwxrwxr-x 2 ag ag  4096 Apr 28 03:00 __pycache__
-rwxrwxr-x 1 ag ag 10497 Apr 28 03:00 export_codex_history.py
drwxrwxr-x 3 ag ag  4096 Apr 28 03:01 exports
```

### Tool Output - 2026-04-28 03:03:10 EDT

```
Chunk ID: 772504
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 0
Output:
```

### Tool Call - 2026-04-28 03:03:17 EDT

```
exec_command {"cmd":"sed -n '241,520p' export_codex_history.py","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-28 03:03:17 EDT

```
Chunk ID: 42da57
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 785
Output:
        "# Codex Conversation Export",
        "",
        f"- Exported at: {local_time(datetime.now(timezone.utc).isoformat())}",
        f"- Sessions: {len(sessions)}",
        "",
    ]

    role_title = {
        "user": "User",
        "assistant": "Assistant",
        "tool_call": "Tool Call",
        "tool_output": "Tool Output",
    }

    for session in sessions:
        title_time = session.started_at or "unknown time"
        lines.extend(
            [
                f"## Session {session.session_id}",
                "",
                f"- Started: {title_time}",
                f"- Source: `{session.source}`",
            ]
        )
        if session.cwd:
            lines.append(f"- CWD: `{session.cwd}`")
        lines.append("")

        if not session.messages:
            lines.extend(["_No exported messages._", ""])
            continue

        for msg in session.messages:
            text = redact(msg.text) if do_redact else msg.text
            time_suffix = f" - {msg.timestamp}" if msg.timestamp else ""
            lines.extend(
                [
                    f"### {role_title.get(msg.role, msg.role.title())}{time_suffix}",
                    "",
                    fence(text),
                    "",
                ]
            )

    return "\n".join(lines).rstrip() + "\n"


def output_dir_from_args(output: Path | None) -> Path:
    if output is not None:
        return output
    return Path(__file__).resolve().parent / "exports"


def write_exports(sessions: list[Session], output_dir: Path, do_redact: bool) -> list[Path]:
    output_dir.mkdir(parents=True, exist_ok=True)
    written: list[Path] = []

    for session in sessions:
        path = output_dir / f"{session.output_stem}.md"
        path.write_text(render_markdown([session], do_redact), encoding="utf-8")
        written.append(path)

    if len(written) > 1:
        index_lines = [
            "# Codex Conversation Export Index",
            "",
            f"- Exported at: {local_time(datetime.now(timezone.utc).isoformat())}",
            f"- Sessions: {len(sessions)}",
            "",
        ]
        for session, path in zip(sessions, written, strict=True):
            title_time = session.started_at or "unknown time"
            index_lines.append(f"- [{path.name}]({path.name}) - {title_time}")
        (output_dir / "index.md").write_text(
            "\n".join(index_lines).rstrip() + "\n",
            encoding="utf-8",
        )
        written.append(output_dir / "index.md")

    return written


def main() -> None:
    args = parse_args()
    if args.source == "sessions":
        sessions = export_sessions(
            args.codex_home,
            args.session,
            args.include_tools,
            args.include_system,
        )
    else:
        sessions = export_prompt_history(args.codex_home)

    written = write_exports(sessions, output_dir_from_args(args.output), not args.no_redact)
    print(
        f"Wrote {len(written)} file(s) to {written[0].parent} "
        f"({sum(len(s.messages) for s in sessions)} messages)"
    )


if __name__ == "__main__":
    main()
```

### Assistant - 2026-04-28 03:03:24 EDT

脚本默认会从 `~/.codex` 读 sessions，并写到当前项目的 `exports/`。我准备新增一个简洁的 `README.md`，只放常用导出命令和几个可选参数。

### Assistant - 2026-04-28 03:03:39 EDT

README 已补上。我再快速检查一下内容和脚本帮助是否对齐。

### Tool Call - 2026-04-28 03:03:39 EDT

```
exec_command {"cmd":"sed -n '1,220p' README.md","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":6000}
```

### Tool Call - 2026-04-28 03:03:39 EDT

```
exec_command {"cmd":"./export_codex_history.py --help","workdir":"/home/ag/projects/codex-history-export","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-28 03:03:39 EDT

````
Chunk ID: 24da47
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 204
Output:
# Codex History Export

Export Codex conversation history to Markdown.

## Usage

Export all sessions:

```bash
./export_codex_history.py
```

Export the latest session:

```bash
./export_codex_history.py --session latest
```

Export prompt history only:

```bash
./export_codex_history.py --source history
```

Write output to a custom directory:

```bash
./export_codex_history.py -o ./my-exports
```

## Options

- `--include-tools`: include tool calls and tool outputs.
- `--include-system`: include system/developer messages.
- `--no-redact`: disable default redaction of common secrets.
- `--codex-home PATH`: read Codex data from another directory instead of `~/.codex`.

Exports are written to `exports/` by default. When exporting multiple sessions, an
`index.md` file is generated in the output directory.
````

### Tool Output - 2026-04-28 03:03:39 EDT

```
Chunk ID: 7eb0ef
Wall time: 0.0119 seconds
Process exited with code 0
Original token count: 276
Output:
usage: export_codex_history.py [-h] [--codex-home CODEX_HOME] [-o OUTPUT]
                               [--source {sessions,history}]
                               [--session SESSION] [--include-tools]
                               [--include-system] [--no-redact]

Export Codex sessions or prompt history to Markdown.

options:
  -h, --help            show this help message and exit
  --codex-home CODEX_HOME
                        Codex config/history directory. Default: ~/.codex
  -o OUTPUT, --output OUTPUT
                        Output directory. Default: ./exports next to this
                        script
  --source {sessions,history}
                        sessions exports full conversations; history exports
                        user prompts only.
  --session SESSION     Session id prefix, 'latest', or 'all'. Default: all
  --include-tools       Include tool calls and tool outputs in session
                        exports.
  --include-system      Include system/developer messages in session exports.
  --no-redact           Do not redact common secrets from exported text.
```

### Assistant - 2026-04-28 03:03:44 EDT

```
已新增 [README.md](/home/ag/projects/codex-history-export/README.md)，包含常用导出命令：

- 导出全部 sessions
- 导出 latest session
- 只导出 prompt history
- 自定义输出目录
- 常用参数说明

也用 `./export_codex_history.py --help` 对过参数，README 和脚本当前行为一致。
```
