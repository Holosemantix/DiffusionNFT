# Codex Conversation Export

- Exported at: 2026-04-28 03:27:26 EDT
- Sessions: 1

## Session 019dd233-5c3b-7273-8edf-2a90d521ce8d

- Started: 2026-04-27 23:47:54 EDT
- Source: `/home/ag/.codex/sessions/2026/04/27/rollout-2026-04-27T23-47-54-019dd233-5c3b-7273-8edf-2a90d521ce8d.jsonl`
- CWD: `/home/ag/projects/AReaL_v1`

### User - 2026-04-27 23:50:34 EDT

````
# AGENTS.md instructions for /home/ag/projects/AReaL_v1

<INSTRUCTIONS>
<!-- Go-to brief for AI coding agents working on AReaL. -->

# AGENTS.md -- AReaL Agent Operations Guide

## Quick reference

**Tech stack**: Python 3.12+ | PyTorch | FSDP2 / Megatron / Archon | SGLang / vLLM

```bash
# Environment
uv sync --extra cuda            # dependencies (or `uv sync` without CUDA)
source .venv/bin/activate        # activate venv BEFORE pre-commit or git commit if venv exists
pre-commit install               # formatting hooks (Ruff, mdformat, clang-format, nbstripout, autoflake)
pre-commit run --all-files       # lint + format everything

# Tests
uv run pytest tests/test_<topic>.py

# CLI docs
uv run python docs/generate_cli_docs.py
```

**Hard rules** -- never violate:

- No wildcard imports (`from x import *`).
- No hardcoded secrets, paths, or endpoints.
- No skipping pre-commit hooks.
- No guessing cluster configs or rebuilding CUDA/driver stacks.
- Integration tests require multi-node hardware -- explain skips explicitly.

**Always do**:

- Read relevant files before modifying code.
- Run `pre-commit run --all-files` before committing.
- Follow existing code patterns in the same module.
- Add tests for new functionality.
- Use the `question` tool (structured options) when asking the user for decisions or
  clarifications.

**Ask first** before:

- Modifying config structures in `areal/api/cli_args.py`.
- Adding new dependencies.
- Changing launcher or scheduler logic.
- Deleting or renaming public APIs.

When unsure, leave a `TODO(agent)` comment and note the constraint in your response.

______________________________________________________________________

## Repository map

```
areal/                     Core Python package
|-- api/                   Config dataclasses, contracts, IO structs
|-- dataset/               Stateful dataset loaders (GSM8K, Geometry3K, CLEVR, ...)
|-- engine/                Training backends (FSDP2, Megatron) + inference adapters
|-- experimental/          Prototype engines/workflows (Archon MoE engine)
|-- infra/                 Launchers (Local/Ray/Slurm), schedulers, utilities
|-- models/                Model adapters (Megatron-Core, Transformers, custom heads)
|-- reward/                Built-in reward functions + math parsers
|-- tests/                 Unit/integration test suites
|-- trainer/               High-level orchestrators (PPOTrainer, SFTTrainer)
|-- utils/                 Cross-cutting helpers (logging, data, checkpoints, RL ops)
+-- workflow/              RolloutWorkflow implementations (RLVR, multi-turn, vision)

docs/                      Jupyter Book docs (https://inclusionai.github.io/AReaL/)
examples/                  Training scripts and launcher recipes
```

______________________________________________________________________

## Code style & patterns

- **Composition over inheritance** -- keep hierarchies \<= 2 levels; prefer delegation.

| Type             | Pattern         | Example                                   |
| ---------------- | --------------- | ----------------------------------------- |
| Config dataclass | `XxxConfig`     | `GRPOConfig`, `FSDPEngineConfig`          |
| Engine class     | `XxxEngine`     | `FSDPEngine`, `ArchonEngine`              |
| Workflow class   | `XxxWorkflow`   | `RLVRWorkflow`, `MultiTurnWorkflow`       |
| Reward function  | `xxx_reward_fn` | `gsm8k_reward_fn`, `geometry3k_reward_fn` |

**Logging**: `areal.utils.logging.getLogger(name)` with **PascalCase** names -- never
`print` or `logging.__name__`. Per-rank format: `[{Component} Rank {N}]`. Register new
loggers with color in `areal/utils/logging.py`.

**Performance**:

- No GPU-CPU sync in hot paths (`.item()`, `.tolist()`, `print(tensor)`).
- Batch ops over Python loops on tensor elements.
- Explicit `dtype`/`device`; `torch.Size` assertions for shape validation.

**Typing & imports**: explicit type hints; reuse `areal/api/cli_args.py` dataclasses; no
wildcard imports; heavy optional deps inside functions.

**Async**: rollout workflows must stay non-blocking (`await` + `aiofiles`); no sync I/O
in `arun_episode`.

______________________________________________________________________

## Domain experts & skills

Fire the appropriate **expert subagent** or **load a skill** based on what you're
working on. Experts are read-only consultants with deep domain knowledge; skills are
step-by-step implementation guides.

| Working on...                | Fire subagent      | Load skill          |
| ---------------------------- | ------------------ | ------------------- |
| FSDP engine code             | `fsdp-expert`      | --                  |
| Archon engine / new model    | `archon-expert`    | `add-archon-model`  |
| Megatron engine code         | `megatron-expert`  | --                  |
| RL algorithms / PPO / GRPO   | `algorithm-expert` | --                  |
| Launcher / scheduler / infra | `launcher-expert`  | `debug-distributed` |
| New reward function          | --                 | `add-reward`        |
| New dataset loader           | --                 | `add-dataset`       |
| New rollout workflow         | --                 | `add-workflow`      |
| Unit tests                   | --                 | `add-unit-tests`    |
| Distributed debugging        | --                 | `debug-distributed` |

**How to fire an expert**:
`task(subagent_type="fsdp-expert", load_skills=[], run_in_background=true, prompt="...")`

______________________________________________________________________

## Core concepts

**Trainer** orchestrator (`areal/trainer/`, `PPOTrainer`, `SFTTrainer`): manages the
training loop, dataset loading, and workflow execution. Entry point:
`examples/math/gsm8k_rl.py`.

**Rollout workflows** (`areal/workflow/`, `RolloutWorkflow.arun_episode`): define how
episodes are generated. Use `add-workflow` skill for step-by-step guide.

**Engines**: *Inference engines* handle async generation via `engine.agenerate()` and
manage weight updates. *Training engines* consume rollout tensors, compute PPO/GRPO
updates, and broadcast weight versions (FSDP2, Megatron, or Archon).

**Weight versioning**: async workflows require version alignment via `WeightUpdateMeta`
(`areal/api/engine_api.py`). Critical for correctness across distributed training.

**Observability**: emit metrics via `stats_tracker.get()`, persist artifacts under
`dump_dir`, checkpoint via `areal/utils/saver.py` / `recover.py`.

**Launcher / scheduler**: training requires cluster setup (local / Ray / Slurm) via
configs in `areal/infra/launcher/`. See `launcher-expert` for deployment guidance.

______________________________________________________________________

## API & config rules

*Applies to: `areal/api/**`*

- **Field ordering**: required -> common optional -> rare optional -> internal (`_`
  prefix).
- **Validation**: `__post_init__` with `ValueError` and clear message.
- **Backward compat**: add fields with defaults; deprecate before removing; avoid type
  changes.
- **CLI**: use `Literal` for enum choices; all public configs need docstrings with
  constraints.

______________________________________________________________________

## Distributed code rules

*Applies to: `areal/engine/**`, `areal/experimental/**`*

- Never create global process groups at module level; always pass `process_group`
  explicitly.
- `dist.get_rank(group)` not `dist.get_rank()` when group matters.
- DeviceMesh dimensions must match `ArchonParallelDims`: `dp_shard`, `tp`, `cp`, `ep`,
  `etp`.
- All-reduce: all ranks must call. Broadcast: explicit `src`. Barrier: debugging only.

| Issue         | Cause                            | Fix                       |
| ------------- | -------------------------------- | ------------------------- |
| Hang          | Mismatched collective calls      | All ranks call same op    |
| Wrong results | Incorrect `ReduceOp`             | Check SUM vs MEAN         |
| OOM           | Unsharded tensor on wrong device | Verify DTensor placements |

Debug env vars: `TORCH_DISTRIBUTED_DEBUG=DETAIL`, `NCCL_DEBUG=INFO`,
`CUDA_LAUNCH_BLOCKING=1`. See `/debug-distributed` skill for comprehensive guide.

______________________________________________________________________

## Testing rules

*Applies to: `**/tests/**`, `test_*.py`*

| Marker                                  | When                             |
| --------------------------------------- | -------------------------------- |
| `@pytest.mark.slow`                     | > 10s (excluded from default CI) |
| `@pytest.mark.slow` + `@pytest.mark.ci` | Slow but must run in CI          |
| `@pytest.mark.asyncio`                  | Async tests                      |

- Naming: `test_<what>_<condition>_<expected>()` with Arrange/Act/Assert.
- GPU: skip gracefully (`@pytest.mark.skipif(not CUDA_AVAILABLE, reason="...")`).
- Distributed mocking: `torch.distributed.fake_pg`; don't mock FSDP/DTensor internals.
- Assertions: `torch.testing.assert_close()` with explicit `rtol`/`atol`; prefer
  `tmp_path`, `monkeypatch`.

| Suite       | Command                       | GPU       |
| ----------- | ----------------------------- | --------- |
| Unit        | `pytest tests/test_*.py`      | No        |
| GRPO        | `pytest tests/grpo/`          | Yes       |
| FSDP        | `pytest tests/test_fsdp_*.py` | Yes       |
| Distributed | `pytest tests/torchrun/`      | Multi-GPU |

______________________________________________________________________

## Collaboration & review

- **Branches**: kebab-case (`feature/multi-turn-metrics`, `bugfix/fsdp-weight-sync`).
- **Commits**: Conventional Commits (`feat:`, `fix:`, `docs:`), ~72 char subject,
  imperative voice. Squash WIP before PR.
- **Pre-merge**: full pre-commit stack; doc-only edits need at least `mdformat --check`.
- **PRs**: tie to issue, highlight risk areas, list test commands executed, note skipped
  suites with reasons.

| Command / Skill      | Purpose                                              |
| -------------------- | ---------------------------------------------------- |
| `/create-pr`         | Rebase, squash, create PR                            |
| `commit-conventions` | Commit message conventions (auto-triggers on commit) |
| `/review-pr`         | Dynamic agent-allocated PR review                    |

______________________________________________________________________

## Reference material

- **Docs portal**: <https://inclusionai.github.io/AReaL/>
- **Quickstart**: `docs/tutorial/quickstart.md`
- **Architecture**: `docs/tutorial/gsm8k_grpo.md`
- **Customization**: `docs/customization/*.md`
- **Algorithms**: `docs/algorithms/*.md`
- **Best practices**: `docs/best_practices/*.md`
- **CLI reference**: `docs/cli_reference.md`
- **Agent workflow**: `docs/customization/agent.md`

</INSTRUCTIONS>
<environment_context>
  <cwd>/home/ag/projects/AReaL_v1</cwd>
  <shell>zsh</shell>
  <current_date>2026-04-27</current_date>
  <timezone>US/Eastern</timezone>
</environment_context>
````

### User - 2026-04-27 23:50:34 EDT

你的新增指标记录需要改动那么多文件吗，不能做到只在一个文件中传入吗

### Assistant - 2026-04-27 23:50:44 EDT

我先看一下当前工作区里到底改了哪些文件，以及新增指标是沿着哪条调用链传进去的，再判断能不能收敛到单文件改动。

### Tool Call - 2026-04-27 23:50:44 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:50:44 EDT

```
exec_command {"cmd":"git diff --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:50:44 EDT

```
exec_command {"cmd":"git diff --name-only","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:50:44 EDT

```
Chunk ID: ebc4f9
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 41
Output:
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M examples/code/nemotron_code_rl.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Tool Output - 2026-04-27 23:50:44 EDT

```
Chunk ID: 62e2ff
Wall time: 0.0119 seconds
Process exited with code 0
Original token count: 80
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 42 ++++++++++++++++++++++++--------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 ++++++++++++++++++
 4 files changed, 96 insertions(+), 17 deletions(-)
```

### Tool Output - 2026-04-27 23:50:44 EDT

```
Chunk ID: 1df645
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 36
Output:
areal/reward/code/competitive_exec.py
areal/workflow/rlvr_qun_team.py
examples/code/nemotron_code_rl.py
tests/test_code_sandbox_multilang.py
```

### Assistant - 2026-04-27 23:50:51 EDT

现在看起来是 4 个源码/测试文件加一个未跟踪的 `.codex`。我继续看具体 diff，确认哪些是为了指标本身、哪些是测试或示例透传。

### Tool Call - 2026-04-27 23:50:51 EDT

```
exec_command {"cmd":"git diff -- areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:50:51 EDT

```
exec_command {"cmd":"git diff -- areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:50:51 EDT

```
exec_command {"cmd":"git diff -- examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-27 23:50:51 EDT

```
Chunk ID: 7bec1d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 947
Output:
diff --git a/areal/reward/code/competitive_exec.py b/areal/reward/code/competitive_exec.py
index bcb65db3..cd38d4b7 100644
--- a/areal/reward/code/competitive_exec.py
+++ b/areal/reward/code/competitive_exec.py
@@ -82,8 +82,9 @@ def nemotron_competitive_reward_fn(
     memory_mb: int = 2048,
     compile_timeout: float = 30.0,
     java_xmx_mb: int = 2048,
+    return_diagnostics: bool = False,
     **kwargs,
-) -> float:
+) -> float | dict[str, float]:
     """Pass rate over (a sample of) unit tests.
 
     :param language: Source language for the model output. May be overridden
@@ -97,18 +98,40 @@ def nemotron_competitive_reward_fn(
     :param compile_timeout: Wall-clock seconds for the compile step (compiled
         languages only). Compile happens once per submission.
     :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
+    :param return_diagnostics: When true, return a dictionary with the reward
+        and per-sample execution outcome counters for training-time logging.
     """
+    diagnostics = {
+        "reward": 0.0,
+        "no_tests": 0.0,
+        "mismatched_tests": 0.0,
+        "no_code": 0.0,
+        "compile_error": 0.0,
+        "timeout_tests": 0.0,
+        "runtime_error_tests": 0.0,
+        "wrong_answer_tests": 0.0,
+        "passed_tests": 0.0,
+        "sampled_tests": 0.0,
+    }
+
+    def _finish(reward: float) -> float | dict[str, float]:
+        diagnostics["reward"] = reward
+        return diagnostics if return_diagnostics else reward
+
     if not test_inputs or not test_outputs:
-        return 0.0
+        diagnostics["no_tests"] = 1.0
+        return _finish(0.0)
     if len(test_inputs) != len(test_outputs):
         logger.warning(
             f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
         )
-        return 0.0
+        diagnostics["mismatched_tests"] = 1.0
+        return _finish(0.0)
 
     extracted = extract_code(completions, expected_language=language)
     if extracted is None:
-        return 0.0
+        diagnostics["no_code"] = 1.0
+        return _finish(0.0)
     detected_lang, code = extracted
 
     artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
@@ -116,7 +139,8 @@ def nemotron_competitive_reward_fn(
         # Compile failure or unsupported language — caller already logged in
         # CompileResult.error/stderr; treat as zero reward.
         cleanup_compile_result(artifact)
-        return 0.0
+        diagnostics["compile_error"] = 1.0
+        return _finish(0.0)
 
     try:
         n = len(test_inputs)
@@ -127,6 +151,7 @@ def nemotron_competitive_reward_fn(
             # across all rollouts of the same problem in a GRPO group.
             rng = random.Random(prompt)
             idx = rng.sample(range(n), max_tests)
+        diagnostics["sampled_tests"] = float(len(idx))
 
         passed = 0
         for i in idx:
@@ -137,11 +162,18 @@ def nemotron_competitive_reward_fn(
                 memory_mb=memory_mb,
                 java_xmx_mb=java_xmx_mb,
             )
-            if result.timeout or result.error is not None or result.returncode != 0:
+            if result.timeout:
+                diagnostics["timeout_tests"] += 1.0
+                continue
+            if result.error is not None or result.returncode != 0:
+                diagnostics["runtime_error_tests"] += 1.0
                 continue
             if outputs_match(result.stdout, test_outputs[i]):
                 passed += 1
+            else:
+                diagnostics["wrong_answer_tests"] += 1.0
 
-        return passed / len(idx)
+        diagnostics["passed_tests"] = float(passed)
+        return _finish(passed / len(idx))
     finally:
         cleanup_compile_result(artifact)
```

### Tool Output - 2026-04-27 23:50:51 EDT

```
Chunk ID: 0808c0
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1002
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..ed139273 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -1,5 +1,5 @@
-import uuid
 import asyncio
+import uuid
 from collections.abc import Callable
 from typing import Any
 
@@ -57,6 +57,7 @@ class RLVRWorkflow(RolloutWorkflow):
         data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
         | str = default_data_extract_prompt_fn,
         reward_timeout_seconds: float = 15.0,
+        log_reward_diagnostics: bool = False,
     ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
@@ -70,6 +71,7 @@ class RLVRWorkflow(RolloutWorkflow):
         self.enable_thinking = enable_thinking
         self.ig_reward_params = ig_reward_params
         self.reward_timeout_seconds = reward_timeout_seconds
+        self.log_reward_diagnostics = log_reward_diagnostics
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,31 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_kwargs = dict(task_data)
+        if self.log_reward_diagnostics:
+            reward_kwargs["return_diagnostics"] = True
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +147,17 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
 
         return resp, reward
 
@@ -155,7 +177,7 @@ class RLVRWorkflow(RolloutWorkflow):
 
         gt_text = data.get("answer")
         if not gt_text:
-            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
+            logger.warning("[IG-Rollout] Enabled but no 'answer' in data.")
             return None
 
         # 1. 构造 Target 后缀
```

### Tool Output - 2026-04-27 23:50:51 EDT

````
Chunk ID: 4fdb6a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 442
Output:
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..0795284c 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            log_reward_diagnostics=True,
         )
 
         eval_workflow_kwargs_map = {}
diff --git a/tests/test_code_sandbox_multilang.py b/tests/test_code_sandbox_multilang.py
index f759a38a..998a5830 100644
--- a/tests/test_code_sandbox_multilang.py
+++ b/tests/test_code_sandbox_multilang.py
@@ -189,6 +189,30 @@ def test_reward_fn_python_pass_rate():
     assert reward == 1.0
 
 
+def test_reward_fn_diagnostics_mode_reports_test_outcomes():
+    if not TOOLCHAIN.get("python"):
+        pytest.skip("python toolchain not available")
+    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
+    diagnostics = nemotron_competitive_reward_fn(
+        prompt="dummy",
+        completions=completion,
+        prompt_ids=[],
+        completion_ids=[],
+        test_inputs=["1\n", "10\n"],
+        test_outputs=["2", "21"],
+        language="python",
+        per_test_timeout=5.0,
+        max_tests=15,
+        memory_mb=512,
+        return_diagnostics=True,
+    )
+    assert diagnostics["reward"] == 0.5
+    assert diagnostics["sampled_tests"] == 2.0
+    assert diagnostics["passed_tests"] == 1.0
+    assert diagnostics["wrong_answer_tests"] == 1.0
+    assert diagnostics["no_code"] == 0.0
+
+
 def test_reward_fn_cpp_pass_rate():
     if not TOOLCHAIN.get("cpp"):
         pytest.skip("cpp toolchain not available")
````

### Assistant - 2026-04-27 23:51:01 EDT

```
这组改动里，`examples/code/nemotron_code_rl.py` 只是打开开关；真正的指标来源在 reward 函数内部，记录点在 workflow。也就是说“只在调用文件传一个参数”可以作为使用方式，但底层仍要有一次通用支持。
```

### Tool Call - 2026-04-27 23:51:01 EDT

```
exec_command {"cmd":"sed -n '1,240p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:51:01 EDT

```
exec_command {"cmd":"sed -n '1,230p' areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:51:01 EDT

```
exec_command {"cmd":"sed -n '1,120p' examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:51:01 EDT

```
Chunk ID: 9a2d5e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2441
Output:
import asyncio
import uuid
from collections.abc import Callable
from typing import Any

import torch
from transformers import PreTrainedTokenizerFast

from areal import workflow_context
from areal.api.cli_args import GenerationHyperparameters
from areal.api.engine_api import InferenceEngine
from areal.api.io_struct import ModelRequest, ModelResponse
from areal.api.reward_api import AsyncRewardWrapper
from areal.api.workflow_api import RolloutWorkflow
from areal.utils import logging, stats_tracker
from areal.utils.dynamic_import import import_from_string
from areal.utils.perf_tracer import (
    atrace_session_phase,
    session_context,
    trace_session,
)

logger = logging.getLogger("RLVRWorkflow")


def default_get_input_ids_fn(
    data: Any,
    tokenizer: PreTrainedTokenizerFast,
    enable_thinking: bool,
) -> list[int]:
    input_ids = tokenizer.apply_chat_template(
        data,
        tokenize=True,
        add_generation_prompt=True,
        enable_thinking=enable_thinking,
    )
    return list(input_ids)


def default_data_extract_prompt_fn(data: dict[str, Any]) -> Any:
    return data["messages"]


class RLVRWorkflow(RolloutWorkflow):
    """Single-turn reward learning workflow supporting optional thinking tokens."""

    def __init__(
        self,
        reward_fn: Callable[..., Any] | str,
        gconfig: GenerationHyperparameters,
        tokenizer: PreTrainedTokenizerFast | str,
        enable_thinking: bool = False,
        rollout_stat_scope: str = "rollout",
        ig_reward_params=None,
        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
        | str = default_get_input_ids_fn,
        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
        | str = default_data_extract_prompt_fn,
        reward_timeout_seconds: float = 15.0,
        log_reward_diagnostics: bool = False,
    ):
        self.reward_fn = reward_fn
        self.tokenizer = tokenizer
        if isinstance(self.tokenizer, str):
            from areal.utils.hf_utils import load_hf_tokenizer

            tokenizer = load_hf_tokenizer(self.tokenizer)
            self.tokenizer = tokenizer
        self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
        self.rollout_stat_scope = rollout_stat_scope
        self.enable_thinking = enable_thinking
        self.ig_reward_params = ig_reward_params
        self.reward_timeout_seconds = reward_timeout_seconds
        self.log_reward_diagnostics = log_reward_diagnostics
        if not isinstance(reward_fn, str):
            self.async_reward_fn = AsyncRewardWrapper(
                reward_fn, timeout_seconds=reward_timeout_seconds
            )
        # Support string paths for get_input_ids_fn
        if isinstance(get_input_ids_fn, str):
            get_input_ids_fn = import_from_string(get_input_ids_fn)
        self.get_input_ids_fn = get_input_ids_fn
        # Support string paths for data_extract_prompt_fn
        if isinstance(data_extract_prompt_fn, str):
            data_extract_prompt_fn = import_from_string(data_extract_prompt_fn)
        self.data_extract_prompt_fn = data_extract_prompt_fn

    @trace_session("reward")
    async def _compute_rewards(
        self,
        resp: ModelResponse,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[float, dict[str, float]]:
        """Decode completion and compute reward.

        Traces reward phase execution for SessionTracer. Decodes output tokens
        to string, calls async reward function, and logs metric to stats tracker.

        Returns
        -------
        tuple[float, dict[str, float]]
            Reward value and optional diagnostic scalars.
        """
        completions_str = self.tokenizer.decode(resp.output_tokens)
        reward_kwargs = dict(task_data)
        if self.log_reward_diagnostics:
            reward_kwargs["return_diagnostics"] = True
        reward_result = await self.async_reward_fn(
            prompt_str,
            completions_str,
            resp.input_tokens,
            resp.output_tokens,
            **reward_kwargs,
        )

        if isinstance(reward_result, dict):
            reward = float(reward_result["reward"])
            diagnostics = {
                f"reward_diagnostics/{key}": float(value)
                for key, value in reward_result.items()
                if key != "reward" and isinstance(value, int | float)
            }
            return reward, diagnostics

        return float(reward_result), {}

    @session_context()
    async def _collect_samples(
        self,
        engine: InferenceEngine,
        req: ModelRequest,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[ModelResponse, float]:
        """Generate one sample and compute its reward.

        Registers a new session for this sample, calls engine.agenerate,
        computes reward, and logs metrics. SessionTracer automatically
        tracks generate and reward phases via @trace_session decorators.

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)

        scalars = {
            "reward": reward,
            "output_len": resp.output_len,
            "total_len": resp.input_len + resp.output_len,
            "stop_length": float(resp.stop_reason == "length"),
            "stop_abort": float(resp.stop_reason == "abort"),
        }
        scalars.update(reward_diagnostics)
        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)

        return resp, reward

    async def _compute_ig_probes(
            self,
            engine: InferenceEngine,
            resp: ModelResponse,
            data: dict[str, Any],
            outcome_reward: float
    ) -> dict[str, list[float]] | None:
        """
        利用热乎的 KV Cache，异步并发计算所有中间步的 IG Logprobs，并直接结算过程奖励。
        这样完全解耦了 Actor 进程，避免了多卡同步死锁和 FSDP Padding 带来的内存溢出。
        """
        if not self.ig_reward_params:
            return None

        gt_text = data.get("answer")
        if not gt_text:
            logger.warning("[IG-Rollout] Enabled but no 'answer' in data.")
            return None

        # 1. 构造 Target 后缀
        gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
        bridge_text = getattr(self.ig_reward_params, "bridge_text", "\n\nTherefore, the answer is ")
        bridge_ids = self.tokenizer.encode(bridge_text, add_special_tokens=False)
        boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
        boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)

        ig_gt_ids = bridge_ids + boxed_first_half_ids + gt_ids + boxed_second_half_ids
        target_len = len(gt_ids)
        tail_len = len(boxed_second_half_ids)

        # 2. 定位所有步骤的切分点 (根据 sep_token)
        sep_token = getattr(self.ig_reward_params, "sep_token", "\n")
        sep_ids = self.tokenizer.encode(sep_token, add_special_tokens=False)
        if not sep_ids:
            return None
        sep_id = sep_ids[-1]

        step_offsets = [i for i, token_id in enumerate(resp.output_tokens) if token_id == sep_id]
        if not step_offsets or step_offsets[-1] != len(resp.output_tokens) - 1:
            step_offsets.append(len(resp.output_tokens) - 1)

        # 3. 构造异步探测请求
        probe_input_ids_list = [resp.input_tokens + ig_gt_ids]  # p_0 (基准水位)
        for offset in step_offsets:
            probe_input_ids_list.append(resp.input_tokens + resp.output_tokens[:offset + 1] + ig_gt_ids)

        probe_reqs = []
        for p_ids in probe_input_ids_list:
            probe_gconfig = self.gconfig.new(n_samples=1)
            # 防止 max_tokens 计算溢出， max_tokens 设为 prompt 长度 + 新 token 长度 (1)
            probe_gconfig.max_tokens = len(p_ids) + 1
            probe_gconfig.max_new_tokens = 1
            probe_gconfig.temperature = 1.0
            probe_gconfig.prompt_logprobs = 1

            probe_req = ModelRequest(
                rid=uuid.uuid4().hex,
                input_ids=p_ids,
                gconfig=probe_gconfig,
                tokenizer=self.tokenizer,
                metadata={"ig_target_len": target_len,
                          "tail_len": tail_len}  # 透传目标长度给底层，用于精准拦截
            )
            probe_reqs.append(probe_req)

        # 并发执行探测！完全命中 vLLM/SGLang 的 Radix Cache
        probe_responses = []
        chunk_size = 1
        for i in range(0, len(probe_reqs), chunk_size):
            chunk_reqs = probe_reqs[i: i + chunk_size]
            chunk_tasks = [engine.agenerate(req) for req in chunk_reqs]
            chunk_resps = await asyncio.gather(*chunk_tasks)
            probe_responses.extend(chunk_resps)
            # print(f"len(step_offsets): {len(step_offsets)}, step_offsets: {step_offsets}\t"
            #       f"len(chunk_reqs[0].input_ids):{len(chunk_reqs[0].input_ids)}\t"
            #       f"chunk_reqs[0].metadata: {chunk_reqs[0].metadata}\t"
            #       f"len(chunk_resps[0].input_logprobs): {len(chunk_resps[0].input_logprobs)}\t"
```

### Tool Output - 2026-04-27 23:51:01 EDT

````
Chunk ID: fe6eed
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1579
Output:
"""Reward for Nemotron-RL-coding-competitive_coding (and future multi-lang data).

Extracts the last fenced code block from the model completion (Python by
default, configurable per sample), compiles once, runs against stdin/stdout
unit tests, returns pass rate ∈ [0, 1].

Test subsampling is deterministic per ``prompt`` so all rollouts of the same
question in a GRPO group see the same tests — otherwise the group-level
advantage normalization gets noisy.
"""

from __future__ import annotations

import random
import re

from areal.reward.code.sandbox import (
    canonical_language,
    cleanup_compile_result,
    compile_code,
    outputs_match,
    run_artifact,
)
from areal.utils import logging

logger = logging.getLogger("CompetitiveCodeReward")

# Group 1: language tag (optional). Group 2: code body.
_CODE_BLOCK_RE = re.compile(
    r"```(python|py|cpp|c\+\+|cxx|java|javascript|js|node)?\s*\n(.*?)```",
    re.DOTALL | re.IGNORECASE,
)


def extract_code(
    text: str, expected_language: str = "python"
) -> tuple[str, str] | None:
    """Return ``(canonical_language, code)`` of the last useful fenced block.

    Selection:
      1. Prefer the LAST fence whose tag matches ``expected_language``.
      2. Else fall back to the LAST fence with any recognized tag.
      3. Else fall back to the LAST untagged fence, treated as
         ``expected_language``.
      4. Return None if no fenced block is present.
    """
    expected_canon = canonical_language(expected_language) or "python"

    matches = _CODE_BLOCK_RE.findall(text)
    if not matches:
        return None

    matched_expected: tuple[str, str] | None = None
    matched_any_tag: tuple[str, str] | None = None
    matched_untagged: tuple[str, str] | None = None

    for tag, code in matches:
        body = code.strip()
        if not body:
            continue
        canon = canonical_language(tag) if tag else None
        if canon == expected_canon:
            matched_expected = (canon, body)
        elif canon is not None:
            matched_any_tag = (canon, body)
        else:
            matched_untagged = (expected_canon, body)

    return matched_expected or matched_any_tag or matched_untagged


def nemotron_competitive_reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    test_inputs: list[str] | None = None,
    test_outputs: list[str] | None = None,
    language: str = "python",
    per_test_timeout: float = 5.0,
    max_tests: int = 15,
    memory_mb: int = 2048,
    compile_timeout: float = 30.0,
    java_xmx_mb: int = 2048,
    return_diagnostics: bool = False,
    **kwargs,
) -> float | dict[str, float]:
    """Pass rate over (a sample of) unit tests.

    :param language: Source language for the model output. May be overridden
        by ``task_data["language"]`` injected by the dataset loader. Defaults
        to ``"python"`` for Nemotron-RL-coding-competitive_coding.
    :param per_test_timeout: Wall-clock seconds for each test case.
    :param max_tests: Upper bound on tests executed per reward call; tests
        beyond this cap are sampled deterministically per ``prompt``.
    :param memory_mb: Address-space cap for each test subprocess (Python/C++/
        Node). Java uses ``java_xmx_mb`` instead.
    :param compile_timeout: Wall-clock seconds for the compile step (compiled
        languages only). Compile happens once per submission.
    :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
    :param return_diagnostics: When true, return a dictionary with the reward
        and per-sample execution outcome counters for training-time logging.
    """
    diagnostics = {
        "reward": 0.0,
        "no_tests": 0.0,
        "mismatched_tests": 0.0,
        "no_code": 0.0,
        "compile_error": 0.0,
        "timeout_tests": 0.0,
        "runtime_error_tests": 0.0,
        "wrong_answer_tests": 0.0,
        "passed_tests": 0.0,
        "sampled_tests": 0.0,
    }

    def _finish(reward: float) -> float | dict[str, float]:
        diagnostics["reward"] = reward
        return diagnostics if return_diagnostics else reward

    if not test_inputs or not test_outputs:
        diagnostics["no_tests"] = 1.0
        return _finish(0.0)
    if len(test_inputs) != len(test_outputs):
        logger.warning(
            f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
        )
        diagnostics["mismatched_tests"] = 1.0
        return _finish(0.0)

    extracted = extract_code(completions, expected_language=language)
    if extracted is None:
        diagnostics["no_code"] = 1.0
        return _finish(0.0)
    detected_lang, code = extracted

    artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
    if not artifact.ok:
        # Compile failure or unsupported language — caller already logged in
        # CompileResult.error/stderr; treat as zero reward.
        cleanup_compile_result(artifact)
        diagnostics["compile_error"] = 1.0
        return _finish(0.0)

    try:
        n = len(test_inputs)
        if n <= max_tests:
            idx = list(range(n))
        else:
            # Seeding with the prompt keeps the sampled subset identical
            # across all rollouts of the same problem in a GRPO group.
            rng = random.Random(prompt)
            idx = rng.sample(range(n), max_tests)
        diagnostics["sampled_tests"] = float(len(idx))

        passed = 0
        for i in idx:
            result = run_artifact(
                artifact,
                stdin=test_inputs[i],
                timeout=per_test_timeout,
                memory_mb=memory_mb,
                java_xmx_mb=java_xmx_mb,
            )
            if result.timeout:
                diagnostics["timeout_tests"] += 1.0
                continue
            if result.error is not None or result.returncode != 0:
                diagnostics["runtime_error_tests"] += 1.0
                continue
            if outputs_match(result.stdout, test_outputs[i]):
                passed += 1
            else:
                diagnostics["wrong_answer_tests"] += 1.0

        diagnostics["passed_tests"] = float(passed)
        return _finish(passed / len(idx))
    finally:
        cleanup_compile_result(artifact)
````

### Tool Output - 2026-04-27 23:51:01 EDT

```
Chunk ID: cb4ad1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 847
Output:
"""GRPO training on Nemotron-RL-coding-competitive_coding.

Pipeline:
  dataset   areal.dataset.code.nemotron_competitive
  reward    areal.reward.code.competitive_exec (subprocess sandbox, pass rate)
  workflow  areal.workflow.rlvr_qun_team.RLVRWorkflow (with extended reward timeout)

Note:
  ``reward_fn`` and ``workflow`` are passed as import-path strings so they
  survive JSON-RPC serialization to remote workers.  The default kwargs of
  ``areal.reward.code.nemotron_competitive_reward_fn`` match the
  ``code_reward`` fields in the YAML template.
"""

import sys

from examples.code.nemotron_configs import CodeGRPOConfig

from areal.api.cli_args import load_expr_config
from areal.dataset.get_datasets_qun_team import get_multi_custom_dataset
from areal.trainer.rl_trainer_qun_team import PPOTrainer
from areal.utils.hf_utils import load_hf_tokenizer


def main(args):
    config, _ = load_expr_config(args, CodeGRPOConfig)
    tokenizer = load_hf_tokenizer(config.tokenizer_path)

    # Probe sandbox toolchain early so missing g++/java/node show up in the
    # trainer log before any rollouts happen.
    from areal.reward.code import assert_toolchain_or_warn

    assert_toolchain_or_warn(list(config.code_reward.probe_languages))

    if config.actor.ig_reward_params is not None:
        config.actor.ig_reward_params.sep_token_id = tokenizer.encode(
            config.actor.ig_reward_params.sep_token, add_special_tokens=False
        )
        config.actor.ig_reward_params.pad_token_id = tokenizer.pad_token_id

    train_dataset_map = get_multi_custom_dataset(
        split="train",
        dataset_config=config.train_dataset,
        tokenizer=tokenizer,
    )
    assert len(train_dataset_map) == 1, f"{len(train_dataset_map)} != 1"
    train_dataset = next(iter(train_dataset_map.values()))

    valid_dataset_map: dict = {}
    if config.valid_dataset is not None and config.valid_dataset.path:
        valid_dataset_map = get_multi_custom_dataset(
            split="test",
            dataset_config=config.valid_dataset,
            tokenizer=tokenizer,
        )

    with PPOTrainer(
        config,
        train_dataset=train_dataset,
        valid_dataset=valid_dataset_map,
    ) as trainer:
        workflow_kwargs = dict(
            reward_fn="areal.reward.code.nemotron_competitive_reward_fn",
            gconfig=config.gconfig,
            tokenizer=config.tokenizer_path,
            enable_thinking=config.gconfig.enable_thinking,
            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
            log_reward_diagnostics=True,
        )

        eval_workflow_kwargs_map = {}
        for dataset_name in valid_dataset_map.keys():
            eval_workflow_kwargs = workflow_kwargs.copy()
            eval_workflow_kwargs["gconfig"] = config.eval_gconfig
            eval_workflow_kwargs["rollout_stat_scope"] = f"eval-rollout/{dataset_name}"
            eval_workflow_kwargs_map[dataset_name] = eval_workflow_kwargs

        workflow_kwargs["ig_reward_params"] = config.actor.ig_reward_params
        trainer.train(
            workflow="areal.workflow.rlvr_qun_team.RLVRWorkflow",
            workflow_kwargs=workflow_kwargs,
            eval_workflow="areal.workflow.rlvr_qun_team.RLVRWorkflow",
            eval_workflow_kwargs=eval_workflow_kwargs_map,
        )


if __name__ == "__main__":
    main(sys.argv[1:])
```

### Tool Call - 2026-04-27 23:51:10 EDT

```
exec_command {"cmd":"rg -n \"class AsyncRewardWrapper|def scalar|stats_tracker.get\\(\" areal/api areal/utils areal/workflow -g '*.py'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:51:10 EDT

```
exec_command {"cmd":"sed -n '1,220p' areal/api/reward_api.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:51:10 EDT

```
exec_command {"cmd":"sed -n '1,220p' areal/utils/stats_tracker.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:51:11 EDT

```
Chunk ID: 01e236
Wall time: 0.1688 seconds
Process exited with code 0
Original token count: 143
Output:
areal/api/reward_api.py:61:class AsyncRewardWrapper:
areal/utils/stats_tracker.py:107:    def scalar(self, **kwargs):
areal/workflow/multi_turn.py:126:        stats_tracker.get(workflow_context.stat_scope()).scalar(
areal/workflow/rlvr.py:129:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/workflow/rlvr_qun_team.py:160:        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
areal/workflow/vision_rlvr.py:99:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
```

### Tool Output - 2026-04-27 23:51:11 EDT

```
Chunk ID: 6bf7cc
Wall time: 0.1621 seconds
Process exited with code 0
Original token count: 1949
Output:
import asyncio
import os
import threading
import traceback
import weakref
from collections.abc import Callable
from concurrent.futures import ProcessPoolExecutor
from concurrent.futures.process import BrokenProcessPool
from functools import partial

from areal.utils import logging

logger = logging.getLogger("RewardAPI")


def _get_device_count_safely() -> int:
    """
    Safely get device count without initializing CUDA context.
    """
    gpu_types = ["nvidia", "davinci"]
    try:
        if os.path.exists("/dev"):
            for gpu_type in gpu_types:
                devices = [
                    f
                    for f in os.listdir("/dev")
                    if f.startswith(gpu_type) and f[len(gpu_type) :].isdigit()
                ]
                if devices:
                    return len(devices)
    except (OSError, ValueError) as e:
        # /dev doesn't exist or can't read (e.g., Windows, macOS)
        logger.debug(f"Could not read device list from /dev, using fallback: {e}")

    # Fallback: assume 8 devices for cautious max_workers calculation
    return 8


def reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    **kwargs,
):
    """This function is a placeholder for the reward function that will be used in the RLVR pipeline.

    In general, there's no restriction on the signature and implementation of this function in customized rollout workflows.
    It would be convinent to follow this signature and directly use it in our predefined rollout workflows.

    :param prompt: The string representing the task to be completed.
    :param completions: The string representing the trajectory generated by the model.
    :param prompt_ids: The token IDs of the prompt.
    :param completion_ids: The token IDs of the trajectory generated by the model.
    :param kwargs: Other attributes of the data in the dataset, such as solutions, input_outputs, etc.
        Any other attributes in the dataset will be passed as keyword arguments to this function.
    :rtype: float
    """


class AsyncRewardWrapper:
    """
    Wraps a synchronous reward function to make it async with timeout handling.
    Automatically manages ProcessPoolExecutor lifecycle based on instance count.
    Includes automatic recovery from broken process pools.
    """

    _executors = {}
    _instance_counts = {}
    _lock = threading.Lock()

    def __init__(
        self,
        reward_fn: Callable,
        timeout_seconds: float = 15,
        max_workers: int | None = None,
        max_retries: int = 3,
    ):
        self.reward_fn = reward_fn
        self.timeout_seconds = timeout_seconds
        if max_workers is None:
            cpu_count = os.cpu_count() or 1
            device_count = _get_device_count_safely()
            # Heuristic for max_workers: distribute CPU cores across devices,
            # then halve to be conservative, ensuring at least one worker.
            max_workers = max((cpu_count // device_count) // 2, 1)
        self.max_workers = max_workers
        self.max_retries = max_retries
        self._executor_key = max_workers

        with self._lock:
            if self._executor_key not in self._executors:
                self._executors[self._executor_key] = ProcessPoolExecutor(
                    max_workers=max_workers
                )
                self._instance_counts[self._executor_key] = 0
            self._instance_counts[self._executor_key] += 1

        weakref.finalize(self, AsyncRewardWrapper._cleanup_executor, max_workers)

    @classmethod
    def _cleanup_executor(cls, executor_key):
        """Called when an AsyncRewardWrapper instance is garbage collected"""
        with cls._lock:
            if executor_key in cls._instance_counts:
                cls._instance_counts[executor_key] -= 1
                if cls._instance_counts[executor_key] <= 0:
                    if executor_key in cls._executors:
                        executor = cls._executors.pop(executor_key)
                        executor.shutdown(wait=False, cancel_futures=True)
                        logger.debug(
                            f"ProcessPoolExecutor with {executor_key} workers shut down"
                        )
                    cls._instance_counts.pop(executor_key, None)

    @classmethod
    def _recreate_executor(cls, executor_key, max_workers):
        """Recreate a broken ProcessPoolExecutor"""
        with cls._lock:
            if executor_key in cls._executors:
                # Clean up the broken executor
                old_executor = cls._executors[executor_key]
                try:
                    old_executor.shutdown(wait=False)
                except Exception as e:
                    logger.warning(f"Error shutting down broken executor: {e}")

                # Create a new executor
                cls._executors[executor_key] = ProcessPoolExecutor(
                    max_workers=max_workers
                )
                logger.info(f"Recreated ProcessPoolExecutor with {max_workers} workers")
                return cls._executors[executor_key]
        return None

    async def __call__(self, *args, **kwargs) -> float:
        last_exception = None

        for attempt in range(self.max_retries + 1):
            with self._lock:
                executor = self._executors.get(self._executor_key)

            if executor is None:
                raise RuntimeError("ProcessPoolExecutor has been shut down")

            loop = asyncio.get_event_loop()
            try:
                future = loop.run_in_executor(
                    executor,
                    partial(self.reward_fn, *args, **kwargs),
                )
                reward = await asyncio.wait_for(
                    future,
                    timeout=self.timeout_seconds,
                )
                return reward
            except TimeoutError:
                logger.warning(
                    f"Computing reward timeout after {self.timeout_seconds}s. Set reward to 0."
                )
                return 0
            except BrokenProcessPool as e:
                last_exception = e
                logger.warning(
                    f"ProcessPoolExecutor broken (attempt {attempt + 1}/{self.max_retries + 1}). "
                    "Attempting to recreate..."
                )
                if attempt < self.max_retries:
                    # Try to recreate the executor
                    new_executor = self._recreate_executor(
                        self._executor_key, self.max_workers
                    )
                    if new_executor is None:
                        logger.error("Failed to recreate ProcessPoolExecutor")
                        break
                    # Continue to next attempt
                    continue
                else:
                    logger.error("Max retries exceeded for BrokenProcessPool.")
                    traceback.print_exc()
                    raise e
            except Exception as e:
                last_exception = e
                logger.error(f"Unexpected error in reward computation: {e}")
                if attempt < self.max_retries:
                    logger.info(
                        f"Retrying... (attempt {attempt + 1}/{self.max_retries + 1})"
                    )
                    continue
                else:
                    logger.error("Max retries exceeded for unexpected error.")
                    traceback.print_exc()
                    raise e

        # If we get here, all retries failed
        if last_exception:
            traceback.print_exc()
            raise last_exception
        else:
            raise RuntimeError("Reward computation failed after all retries.")
```

### Tool Output - 2026-04-27 23:51:11 EDT

```
Chunk ID: a91301
Wall time: 0.1716 seconds
Process exited with code 0
Original token count: 2041
Output:
import time
from collections import defaultdict
from contextlib import contextmanager
from enum import Enum, auto
from threading import Lock

import torch
import torch.distributed as dist

from areal.infra.platforms import current_platform
from areal.utils import logging
from areal.utils.datapack import flat2d

logger = logging.getLogger("StatsTracker")


class ReduceType(Enum):
    AVG_MIN_MAX = auto()
    AVG = auto()
    SUM = auto()
    MIN = auto()
    MAX = auto()
    SCALAR = auto()


MOE_AUX_LOSSES = {}


class DistributedStatsTracker:
    def __init__(self, name: str = ""):
        self.lock = Lock()
        self.scope_stack = []
        if name:
            self.scope_stack.append(name.strip("/"))
        self.denominators = {}  # key -> denominator key
        self.reduce_types = {}  # key -> ReduceType

        self.stats = defaultdict(list)

    def scope(self, name):
        """Context manager for hierarchical scoping"""
        with self.lock:
            return self.Scope(self, name)

    def scope_func_wrapper(self, name):
        """Decorator for hierarchical scoping"""

        def decorator(func):
            def wrapper(*args, **kwargs):
                with self.scope(name):
                    return func(*args, **kwargs)

            return wrapper

        return decorator

    class Scope:
        def __init__(self, tracker, name):
            self.tracker = tracker
            self.name = name.strip("/")

        def __enter__(self):
            self.tracker.scope_stack.append(self.name)
            return self

        def __exit__(self, exc_type, exc_val, exc_tb):
            self.tracker.scope_stack.pop()

    def _get_full_key(self, key):
        """Combine scope stack with current key"""
        if not self.scope_stack:
            return key
        return "/".join(self.scope_stack + [key])

    @contextmanager
    def disable_scope(self):
        tmp = self.scope_stack
        self.scope_stack = []
        yield
        self.scope_stack = tmp

    @contextmanager
    def record_timing(self, key):
        start_time = time.perf_counter()
        try:
            yield
        finally:
            with self.lock:
                # NOTE: timing records are fixed under the "timeperf" scope
                full_key = f"timeperf/{key}"
                self._set_reduce_type(full_key, ReduceType.SCALAR)
                self.stats[full_key].append(time.perf_counter() - start_time)

    def denominator(self, **kwargs):
        with self.lock:
            for key, value in kwargs.items():
                if not isinstance(value, torch.Tensor) or value.dtype != torch.bool:
                    raise ValueError(
                        f"`{key}` must be a pytorch bool tensor: {value.dtype}"
                    )
                if value.numel() == 0:
                    raise ValueError(f"`{key}` must be non-empty")
                full_key = self._get_full_key(key)
                self._set_reduce_type(full_key, ReduceType.SUM)
                self.stats[full_key].append(value.detach().clone())

    def scalar(self, **kwargs):
        with self.lock:
            for key, value in kwargs.items():
                full_key = self._get_full_key(key)
                self._set_reduce_type(full_key, ReduceType.SCALAR)
                self.stats[full_key].append(float(value))

    def stat(
        self,
        denominator: str,
        reduce_type: ReduceType | None = None,
        **kwargs,
    ):
        """Record multiple values from a dictionary"""
        with self.lock:
            for key, value in kwargs.items():
                if not isinstance(value, torch.Tensor) or value.dtype != torch.float:
                    raise ValueError(
                        f"`{key}` should be a pytorch float tensor: {value.dtype}"
                    )
                if value.numel() == 0:
                    raise ValueError(f"`{key}` should be non-empty")
                if reduce_type == ReduceType.SCALAR:
                    raise ValueError("Cannot use the scalar reduce type for a tensor")
                full_key = self._get_full_key(key)

                denorm = self._get_full_key(denominator)
                if denorm not in self.stats or not self.stats[denorm]:
                    raise ValueError(f"Denominator `{denorm}` does not exist")
                for x, y in zip(self.stats[denorm], self.stats[full_key] + [value]):
                    assert x.shape == y.shape, (x.shape, y.shape)
                self.denominators[full_key] = denorm

                if reduce_type is None:
                    reduce_type = ReduceType.AVG_MIN_MAX
                self._set_reduce_type(full_key, reduce_type)
                self.stats[full_key].append(value.detach().clone())

    def _set_reduce_type(self, key, reduce_type):
        if not isinstance(reduce_type, ReduceType):
            raise ValueError("reduce_type must be a ReduceType enum")
        self.reduce_types[key] = reduce_type

    def export(self, key=None, reduce_group=None, reset=True) -> dict[str, float]:
        """Get aggregated statistics"""
        with self.lock:
            if key is not None:
                full_key = self._get_full_key(key)
                result = self._aggregate(full_key, reduce_group)
                if reset:
                    if full_key in self.denominators:
                        self.denominators.pop(full_key)
                    if full_key in self.reduce_types:
                        self.denominators.pop(full_key)
                    self.stats.pop(full_key)
                return result

            # synchronize keys across ranks
            keys = list(self.stats.keys())
            if reduce_group is not None:
                all_keys = [None for _ in range(dist.get_world_size(reduce_group))]
                dist.all_gather_object(all_keys, keys, group=reduce_group)
                # Should ensure that the orders are the same
                keys = sorted(list(set(flat2d(all_keys))))
            results = {}
            for key in keys:
                results.update(self._aggregate(key, reduce_group))
            if reset:
                self.denominators = {}
                self.reduce_types = {}
                self.stats = defaultdict(list)
            results = {
                k: v.cpu().item() if torch.is_tensor(v) else v
                for k, v in results.items()
            }
            return results

    def _aggregate(self, key, reduce_group):
        reduce_type = self.reduce_types.get(key, ReduceType.SCALAR)

        result = {}
        if reduce_type == ReduceType.AVG_MIN_MAX:
            result["/".join([key, "avg"])] = self._avg_of(key, reduce_group)
            result["/".join([key, "min"])] = self._min_of(key, reduce_group)
            result["/".join([key, "max"])] = self._max_of(key, reduce_group)
        elif reduce_type == ReduceType.AVG:
            result[key] = self._avg_of(key, reduce_group)
        elif reduce_type == ReduceType.SUM:
            result[key] = self._sum_of(key, reduce_group)
        elif reduce_type == ReduceType.MIN:
            result[key] = self._min_of(key, reduce_group)
        elif reduce_type == ReduceType.MAX:
            result[key] = self._max_of(key, reduce_group)
        elif reduce_type == ReduceType.SCALAR:
            if current_platform.is_initialized():
                device = current_platform.device_type
            else:
                device = "cpu"
            value = torch.tensor(
                sum(self.stats[key]), dtype=torch.float32, device=device
            )
            cnt = torch.tensor(len(self.stats[key]), dtype=torch.float32, device=device)
            if reduce_group is not None:
                dist.all_reduce(value, group=reduce_group)
                dist.all_reduce(cnt, group=reduce_group)
            result[key] = float(value / cnt)
            result[key + "__count"] = int(cnt)
        else:
            raise ValueError(f"Unknown reduce type: {reduce_type}")

        keys_to_pop = [k for k, v in result.items() if v is None]
        for k in keys_to_pop:
            result.pop(k)
        return result
```

### Tool Call - 2026-04-27 23:51:18 EDT

```
exec_command {"cmd":"rg -n \"CodeReward|code_reward|reward_timeout|nemotron_competitive\" -S areal examples tests","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:51:18 EDT

```
exec_command {"cmd":"sed -n '1,220p' examples/code/nemotron_configs.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:51:18 EDT

```
Chunk ID: 9edd0d
Wall time: 0.0067 seconds
Process exited with code 0
Original token count: 713
Output:
examples/code/nemotron_configs.py:36:class CodeRewardConfig:
examples/code/nemotron_configs.py:44:    ``areal.reward.code.competitive_exec.nemotron_competitive_reward_fn`` or
examples/code/nemotron_configs.py:52:    reward_timeout_seconds: float = field(default=90.0)
examples/code/nemotron_configs.py:66:    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
examples/code/nemotron_code_rl.py:4:  dataset   areal.dataset.code.nemotron_competitive
examples/code/nemotron_code_rl.py:11:  ``areal.reward.code.nemotron_competitive_reward_fn`` match the
examples/code/nemotron_code_rl.py:12:  ``code_reward`` fields in the YAML template.
examples/code/nemotron_code_rl.py:33:    assert_toolchain_or_warn(list(config.code_reward.probe_languages))
examples/code/nemotron_code_rl.py:63:            reward_fn="areal.reward.code.nemotron_competitive_reward_fn",
examples/code/nemotron_code_rl.py:67:            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
examples/code/grpo_template_code.yaml:151:# reward_timeout_seconds: total AsyncRewardWrapper timeout per sample
examples/code/grpo_template_code.yaml:157:code_reward:
examples/code/grpo_template_code.yaml:161:  reward_timeout_seconds: 90.0
areal/utils/logging.py:80:    "CompetitiveCodeReward": "purple",
areal/workflow/rlvr_qun_team.py:59:        reward_timeout_seconds: float = 15.0,
areal/workflow/rlvr_qun_team.py:73:        self.reward_timeout_seconds = reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:77:                reward_fn, timeout_seconds=reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:341:                self.reward_fn, timeout_seconds=self.reward_timeout_seconds
areal/reward/code/__init__.py:5:    nemotron_competitive_reward_fn,
areal/reward/code/__init__.py:22:    "nemotron_competitive_reward_fn",
areal/reward/code/competitive_exec.py:26:logger = logging.getLogger("CompetitiveCodeReward")
areal/reward/code/competitive_exec.py:72:def nemotron_competitive_reward_fn(
areal/dataset/get_datasets_qun_team.py:181:            from .code.nemotron_competitive import get_nemotron_competitive_rl_dataset
areal/dataset/get_datasets_qun_team.py:183:            dataset_map['nemotron_code'] = get_nemotron_competitive_rl_dataset(
areal/dataset/code/nemotron_competitive.py:41:def get_nemotron_competitive_rl_dataset(
tests/test_code_sandbox_multilang.py:17:    nemotron_competitive_reward_fn,
tests/test_code_sandbox_multilang.py:177:    reward = nemotron_competitive_reward_fn(
tests/test_code_sandbox_multilang.py:196:    diagnostics = nemotron_competitive_reward_fn(
tests/test_code_sandbox_multilang.py:224:    reward = nemotron_competitive_reward_fn(
tests/test_code_sandbox_multilang.py:241:    reward = nemotron_competitive_reward_fn(
tests/test_code_sandbox_multilang.py:257:    reward = nemotron_competitive_reward_fn(
```

### Tool Output - 2026-04-27 23:51:18 EDT

```
Chunk ID: 5c3338
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 611
Output:
"""Configuration dataclasses for Nemotron code RL examples.

These live in a dedicated module so that RPC workers can deserialize them
(via importlib) when they are transmitted across the wire.  Dataclasses
defined directly in a ``__main__`` script cannot be imported by workers.
"""

from dataclasses import dataclass, field

from areal.api.cli_args import GRPOConfig, PPOActorConfig


@dataclass
class InfoGainRewardConfig:
    """Matches the shape expected by rl_trainer_qun_team / actor_qun_team."""

    mini_batch_size: int = field(default=128)
    max_tokens_per_forward: int = field(default=131072)
    compute_backend: str = field(default="rollout")
    bridge_text: str = field(default="")
    sep_token: str = field(default="\n")
    reward_mode: str = field(default="prob")
    beta: float = field(default=0.5)
    lambda_val: float = field(default=0.2)
    enable_varlen_packing: bool = field(default=True)
    use_watermark_selection: bool = field(default=False)
    use_peak_selection: bool = field(default=False)


@dataclass
class InfoGainPPOActorConfig(PPOActorConfig):
    ig_reward_params: InfoGainRewardConfig | None = field(default=None)


@dataclass
class CodeRewardConfig:
    """Budget for the competitive-coding execution reward.

    All values are tuned for H800 + 0.6B–14B Qwen3 rollouts.

    Note: with a string-based ``reward_fn`` (required for RPC serialization),
    the per-test fields below currently document the reward fn's defaults.
    To change them at training time, edit the defaults in
    ``areal.reward.code.competitive_exec.nemotron_competitive_reward_fn`` or
    inject overrides via the dataset loader (each sample's task_data flows
    into the reward fn as kwargs).
    """

    per_test_timeout: float = field(default=5.0)
    max_tests: int = field(default=15)
    memory_mb: int = field(default=2048)
    reward_timeout_seconds: float = field(default=90.0)
    # Multi-language support
    language: str = field(default="python")
    compile_timeout: float = field(default=30.0)
    java_xmx_mb: int = field(default=2048)
    # Toolchain probe at trainer startup
    probe_languages: list[str] = field(
        default_factory=lambda: ["python", "cpp", "java", "javascript"]
    )


@dataclass
class CodeGRPOConfig(GRPOConfig):
    actor: InfoGainPPOActorConfig = field(default_factory=InfoGainPPOActorConfig)
    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
```

### Tool Call - 2026-04-27 23:51:37 EDT

```
exec_command {"cmd":"sed -n '1,260p' areal/dataset/code/nemotron_competitive.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:51:37 EDT

```
exec_command {"cmd":"sed -n '140,220p' areal/dataset/get_datasets_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:51:37 EDT

```
exec_command {"cmd":"rg -n \"task_data|workflow_kwargs|return_diagnostics|log_reward\" areal examples -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-27 23:51:37 EDT

```
Chunk ID: 1ae4a9
Wall time: 0.0727 seconds
Process exited with code 0
Original token count: 676
Output:
"""Loader for ``nvidia/Nemotron-RL-coding-competitive_coding``.

Verified source schema (parquet, per row):
    responses_create_params.input:  list[{"role": "user", "content": str}]
    verifier_metadata.unit_tests:   {"inputs": list[str], "outputs": list[str]}
    hash_id:                        str
    dataset:                        str  (e.g. "open-r1/codeforces")
    source:                         str  (e.g. "codeforces")

Note: the top-level field is ``responses_create_params.input``, NOT ``input``.
The ``input`` alias only appears in the pre-processed training blends.

Supports three ways to specify ``path``:
    * HuggingFace Hub name, e.g. ``nvidia/Nemotron-RL-coding-competitive_coding``
    * Local directory with the HF dataset layout (contains ``data/train-*.parquet``)
    * Local ``.jsonl`` / ``.parquet`` single-file snapshot
"""

from __future__ import annotations

import os
from typing import TYPE_CHECKING

from datasets import load_dataset

if TYPE_CHECKING:
    from transformers.tokenization_utils_fast import PreTrainedTokenizerFast


def _load_raw(path: str, split: str):
    if os.path.isfile(path) and (path.endswith(".jsonl") or path.endswith(".parquet")):
        parent = os.path.dirname(path)
        fname = os.path.basename(path)
        return load_dataset(path=parent, data_files=fname)["train"]
    if os.path.isdir(path):
        return load_dataset(path=path, split=split)
    # HF Hub fallback
    return load_dataset(path=path, split=split)


def get_nemotron_competitive_rl_dataset(
    path: str,
    split: str,
    tokenizer: PreTrainedTokenizerFast,
    max_length: int | None = None,
):
    dataset = _load_raw(path, split)

    def process(sample):
        rcp = sample.get("responses_create_params") or {}
        messages = rcp.get("input") or []
        verifier = sample.get("verifier_metadata") or {}
        unit_tests = verifier.get("unit_tests") or {}
        return {
            "messages": messages,
            "test_inputs": list(unit_tests.get("inputs") or []),
            "test_outputs": list(unit_tests.get("outputs") or []),
            # Nemotron-RL-coding-competitive_coding is python-only per its README.
            # Future multi-language loaders can vary this per sample.
            "language": "python",
        }

    dataset = dataset.map(process, remove_columns=dataset.column_names)
    dataset = dataset.filter(lambda x: len(x["test_inputs"]) > 0)

    if max_length is not None:

        def fits(sample):
            content = sample["messages"][0]["content"] if sample["messages"] else ""
            return len(tokenizer.encode(content)) <= max_length

        dataset = dataset.filter(fits)

    return dataset
```

### Tool Output - 2026-04-27 23:51:37 EDT

```
Chunk ID: 9d553e
Wall time: 0.1330 seconds
Process exited with code 0
Original token count: 691
Output:
        elif "aime24" in path and type == "rl":
            from .aime24 import get_aime24_rl_dataset

            dataset_map['aime24'] = get_aime24_rl_dataset(
                path=path,
                split=split,
                tokenizer=tokenizer,
                max_length=max_length,
                **kwargs,
            )
        elif "aime25" in path and type == "rl":
            from .aime25 import get_aime25_rl_dataset

            dataset_map['aime25'] = get_aime25_rl_dataset(
                path=path,
                split=split,
                tokenizer=tokenizer,
                max_length=max_length,
                **kwargs,
            )
        elif "aime26" in path and type == "rl":
            from .aime26 import get_aime26_rl_dataset

            dataset_map['aime26'] = get_aime26_rl_dataset(
                path=path,
                split=split,
                tokenizer=tokenizer,
                max_length=max_length,
                **kwargs,
            )
        elif "hmmt" in path and type == "rl":
            from .hmmt25 import get_hmmt25_rl_dataset

            dataset_map['hmmt25'] = get_hmmt25_rl_dataset(
                path=path,
                split=split,
                tokenizer=tokenizer,
                max_length=max_length,
                **kwargs,
            )
        elif ("nemotron" in path.lower() and "cod" in path.lower()) and type == "rl":
            from .code.nemotron_competitive import get_nemotron_competitive_rl_dataset

            dataset_map['nemotron_code'] = get_nemotron_competitive_rl_dataset(
                path=path,
                split=split,
                tokenizer=tokenizer,
                max_length=max_length,
                **kwargs,
            )
        else:
            raise ValueError(
                f"Dataset {path} with split {split} and training type {type} is not supported. "
                f"Supported datasets are: {VALID_DATASETS}. "
            )

    return dataset_map


def get_custom_dataset_legacy(
    path: str,
    rank: int,
    world_size: int,
    type: str = "sft",
    split: str | None = None,
    max_length: int | None = None,
    tokenizer: Optional["PreTrainedTokenizerFast"] = None,
    processor: Optional["ProcessorMixin"] = None,
    **kwargs,
) -> "Dataset":
    logger.warning(
        "get_custom_dataset using rank and world_size is deprecated. "
        "Please use DistributedSampler in dataloader instead for distributed training."
    )
    dataset = _get_custom_dataset(
        path=path,
        type=type,
        split=split,
        max_length=max_length,
        tokenizer=tokenizer,
        processor=processor,
```

### Tool Output - 2026-04-27 23:51:37 EDT

```
Chunk ID: ac17f0
Wall time: 0.0443 seconds
Process exited with code 0
Original token count: 4594
Output:
examples/agent_workflow/train.py:30:    workflow_kwargs = dict(
examples/agent_workflow/train.py:41:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/agent_workflow/train.py:42:    eval_workflow_kwargs["temperature"] = 0.6
examples/agent_workflow/train.py:52:            workflow_kwargs=workflow_kwargs,
examples/agent_workflow/train.py:53:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/vlm_npu/geometry3k_grpo.py:51:    workflow_kwargs = dict(
examples/vlm_npu/geometry3k_grpo.py:58:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/vlm_npu/geometry3k_grpo.py:59:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/vlm_npu/geometry3k_grpo.py:68:            workflow_kwargs=workflow_kwargs,
examples/vlm_npu/geometry3k_grpo.py:70:            eval_workflow_kwargs=eval_workflow_kwargs,
areal/engine/sglang_remote.py:377:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/sglang_remote.py:389:            workflow_kwargs=workflow_kwargs,
areal/engine/sglang_remote.py:414:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/sglang_remote.py:425:            workflow_kwargs=workflow_kwargs,
areal/engine/sglang_remote.py:433:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/sglang_remote.py:442:            workflow_kwargs=workflow_kwargs,
examples/vlm_npu/virl39k_grpo.py:52:    workflow_kwargs = dict(
examples/vlm_npu/virl39k_grpo.py:59:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/vlm_npu/virl39k_grpo.py:60:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/vlm_npu/virl39k_grpo.py:69:            workflow_kwargs=workflow_kwargs,
examples/vlm_npu/virl39k_grpo.py:71:            eval_workflow_kwargs=eval_workflow_kwargs,
areal/engine/fsdp_engine.py:438:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/fsdp_engine.py:445:            workflow_kwargs=workflow_kwargs,
areal/engine/fsdp_engine.py:453:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/fsdp_engine.py:462:            workflow_kwargs=workflow_kwargs,
examples/tau2/train.py:92:    workflow_kwargs = dict(
examples/tau2/train.py:102:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/tau2/train.py:103:    eval_workflow_kwargs["gen_args"] = dict(
examples/tau2/train.py:115:            workflow_kwargs=workflow_kwargs,
examples/tau2/train.py:117:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/code/nemotron_configs.py:45:    inject overrides via the dataset loader (each sample's task_data flows
examples/code/nemotron_code_rl.py:62:        workflow_kwargs = dict(
examples/code/nemotron_code_rl.py:68:            log_reward_diagnostics=True,
examples/code/nemotron_code_rl.py:71:        eval_workflow_kwargs_map = {}
examples/code/nemotron_code_rl.py:73:            eval_workflow_kwargs = workflow_kwargs.copy()
examples/code/nemotron_code_rl.py:74:            eval_workflow_kwargs["gconfig"] = config.eval_gconfig
examples/code/nemotron_code_rl.py:75:            eval_workflow_kwargs["rollout_stat_scope"] = f"eval-rollout/{dataset_name}"
examples/code/nemotron_code_rl.py:76:            eval_workflow_kwargs_map[dataset_name] = eval_workflow_kwargs
examples/code/nemotron_code_rl.py:78:        workflow_kwargs["ig_reward_params"] = config.actor.ig_reward_params
examples/code/nemotron_code_rl.py:81:            workflow_kwargs=workflow_kwargs,
examples/code/nemotron_code_rl.py:83:            eval_workflow_kwargs=eval_workflow_kwargs_map,
examples/vlm/geometry3k_grpo.py:51:    workflow_kwargs = dict(
examples/vlm/geometry3k_grpo.py:58:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/vlm/geometry3k_grpo.py:59:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/vlm/geometry3k_grpo.py:68:            workflow_kwargs=workflow_kwargs,
examples/vlm/geometry3k_grpo.py:70:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/vlm/clevr_count_70k_grpo.py:50:    workflow_kwargs = dict(
examples/vlm/clevr_count_70k_grpo.py:57:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/vlm/clevr_count_70k_grpo.py:58:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/vlm/clevr_count_70k_grpo.py:67:            workflow_kwargs=workflow_kwargs,
examples/vlm/clevr_count_70k_grpo.py:69:            eval_workflow_kwargs=eval_workflow_kwargs,
areal/engine/megatron_engine.py:458:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/megatron_engine.py:465:            workflow_kwargs=workflow_kwargs,
areal/engine/megatron_engine.py:473:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/megatron_engine.py:482:            workflow_kwargs=workflow_kwargs,
areal/engine/vllm_remote.py:377:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/vllm_remote.py:389:            workflow_kwargs=workflow_kwargs,
areal/engine/vllm_remote.py:414:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/vllm_remote.py:425:            workflow_kwargs=workflow_kwargs,
areal/engine/vllm_remote.py:433:        workflow_kwargs: dict[str, Any] | None = None,
areal/engine/vllm_remote.py:442:            workflow_kwargs=workflow_kwargs,
examples/openai_agents/train_agents.py:145:    workflow_kwargs = dict(
examples/openai_agents/train_agents.py:152:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/openai_agents/train_agents.py:161:            workflow_kwargs=workflow_kwargs,
examples/openai_agents/train_agents.py:163:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/camel/train.py:117:    workflow_kwargs = dict(
examples/camel/train.py:128:            workflow_kwargs=workflow_kwargs,
examples/multi_turn_math/gsm8k_rl_mt.py:145:    workflow_kwargs = dict(
examples/multi_turn_math/gsm8k_rl_mt.py:152:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/multi_turn_math/gsm8k_rl_mt.py:153:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6, n_samples=1)
examples/multi_turn_math/gsm8k_rl_mt.py:162:            workflow_kwargs=workflow_kwargs,
examples/multi_turn_math/gsm8k_rl_mt.py:164:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/math/gsm8k_rl.py:24:    workflow_kwargs = dict(
examples/math/gsm8k_rl.py:30:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/math/gsm8k_rl.py:31:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/math/gsm8k_rl.py:40:            workflow_kwargs=workflow_kwargs,
examples/math/gsm8k_rl.py:42:            eval_workflow_kwargs=eval_workflow_kwargs,
examples/math/gsm8k_eval.py:79:        workflow_kwargs = dict(
examples/math/gsm8k_eval.py:93:                    workflow_kwargs=workflow_kwargs,
examples/math/boba_grpo.py:68:    workflow_kwargs = dict(
examples/math/boba_grpo.py:84:            workflow_kwargs=workflow_kwargs,
examples/math/bigmath_rl.py:107:        workflow_kwargs = dict(
examples/math/bigmath_rl.py:114:        eval_workflow_kwargs_map = {}
examples/math/bigmath_rl.py:116:            eval_workflow_kwargs = workflow_kwargs.copy()
examples/math/bigmath_rl.py:117:            eval_workflow_kwargs["gconfig"] = config.eval_gconfig
examples/math/bigmath_rl.py:118:            eval_workflow_kwargs["rollout_stat_scope"] = f"eval-rollout/{dataset_name}"
examples/math/bigmath_rl.py:119:            eval_workflow_kwargs_map[dataset_name] = eval_workflow_kwargs
examples/math/bigmath_rl.py:121:        workflow_kwargs['ig_reward_params'] = config.actor.ig_reward_params
examples/math/bigmath_rl.py:124:            workflow_kwargs=workflow_kwargs,
examples/math/bigmath_rl.py:126:            eval_workflow_kwargs=eval_workflow_kwargs_map,
examples/search_agent/tongyi_deepresearch/train.py:154:    workflow_kwargs = dict(
examples/search_agent/tongyi_deepresearch/train.py:167:            workflow_kwargs=workflow_kwargs,
areal/api/engine_api.py:199:        workflow_kwargs: dict[str, Any] | None = None,
areal/api/engine_api.py:215:        workflow_kwargs : dict[str, Any] | None, optional
areal/api/engine_api.py:234:        workflow_kwargs: dict[str, Any] | None = None,
areal/api/engine_api.py:247:        workflow_kwargs : dict[str, Any] | None, optional
areal/api/engine_api.py:707:        workflow_kwargs: dict[str, Any] | None = None,
areal/api/engine_api.py:725:            - A RolloutWorkflow class type (will be instantiated with workflow_kwargs)
areal/api/engine_api.py:727:              and instantiated with workflow_kwargs)
areal/api/engine_api.py:729:        workflow_kwargs : dict[str, Any], optional
areal/api/engine_api.py:813:        workflow_kwargs: dict[str, Any] | None = None,
areal/api/engine_api.py:831:            - A RolloutWorkflow class type (will be instantiated with workflow_kwargs)
areal/api/engine_api.py:833:              and instantiated with workflow_kwargs)
areal/api/engine_api.py:835:        workflow_kwargs : dict[str, Any], optional
areal/api/engine_api.py:856:        workflow_kwargs: dict[str, Any] | None = None,
areal/api/engine_api.py:868:            The ``dataloader``, ``workflow``, ``workflow_kwargs``, ``group_size``,
areal/api/engine_api.py:886:            - A RolloutWorkflow class type (will be instantiated with workflow_kwargs)
areal/api/engine_api.py:888:              and instantiated with workflow_kwargs)
areal/api/engine_api.py:890:        workflow_kwargs : dict[str, Any], optional
areal/experimental/engine/archon_engine.py:606:        workflow_kwargs: dict[str, Any] | None = None,
areal/experimental/engine/archon_engine.py:614:            workflow_kwargs=workflow_kwargs,
areal/experimental/engine/archon_engine.py:622:        workflow_kwargs: dict[str, Any] | None = None,
areal/experimental/engine/archon_engine.py:632:            workflow_kwargs=workflow_kwargs,
examples/countdown/train.py:90:    workflow_kwargs = dict(
examples/countdown/train.py:98:            workflow_kwargs=workflow_kwargs,
examples/tir/train_tir.py:44:    workflow_kwargs = dict(
examples/tir/train_tir.py:51:    eval_workflow_kwargs = workflow_kwargs.copy()
examples/tir/train_tir.py:52:    eval_workflow_kwargs["gconfig"] = config.gconfig.new(temperature=0.6)
examples/tir/train_tir.py:59:            workflow_kwargs=workflow_kwargs,
examples/tir/train_tir.py:61:            eval_workflow_kwargs=eval_workflow_kwargs,
areal/infra/dist_rollout.py:165:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/dist_rollout.py:184:        workflow_kwargs : Dict[str, Any], optional
areal/infra/dist_rollout.py:206:                workflow_kwargs=workflow_kwargs,
areal/infra/dist_rollout.py:219:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/dist_rollout.py:237:        workflow_kwargs : Dict[str, Any], optional
areal/infra/dist_rollout.py:263:                workflow_kwargs=workflow_kwargs,
areal/trainer/rl_trainer.py:276:        workflow_kwargs: dict[str, Any] | None = None,
areal/trainer/rl_trainer.py:277:        eval_workflow_kwargs: dict[str, Any] | None = None,
areal/trainer/rl_trainer.py:332:                    workflow_kwargs=workflow_kwargs,
areal/trainer/rl_trainer.py:468:                    eval_workflow_kwargs=eval_workflow_kwargs,
areal/trainer/rl_trainer.py:797:        eval_workflow_kwargs,
areal/trainer/rl_trainer.py:806:                        eval_workflow_kwargs,
areal/trainer/rl_trainer.py:819:        eval_workflow_kwargs,
areal/trainer/rl_trainer.py:834:                eval_workflow_kwargs=eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:292:        workflow_kwargs: dict[str, Any] | None = None,
areal/trainer/rl_trainer_qun_team.py:293:        eval_workflow_kwargs: dict[str, Any] | None = None,
areal/trainer/rl_trainer_qun_team.py:337:                    eval_workflow_kwargs=eval_workflow_kwargs
areal/trainer/rl_trainer_qun_team.py:363:                    workflow_kwargs=workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:532:                    eval_workflow_kwargs=eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:861:        eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:872:                                eval_workflow_kwargs[dataset_name],
areal/trainer/rl_trainer_qun_team.py:886:                            eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:900:        eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:915:                eval_workflow_kwargs=eval_workflow_kwargs,
areal/trainer/rl_trainer_qun_team.py:927:        eval_workflow_kwargs
areal/trainer/rl_trainer_qun_team.py:933:            eval_workflow_kwargs=eval_workflow_kwargs
areal/workflow/rlvr.py:82:        task_data: dict[str, Any],
areal/workflow/rlvr.py:100:            **task_data,
areal/workflow/rlvr.py:111:        task_data: dict[str, Any],
areal/workflow/rlvr.py:127:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/rlvr_qun_team.py:60:        log_reward_diagnostics: bool = False,
areal/workflow/rlvr_qun_team.py:74:        self.log_reward_diagnostics = log_reward_diagnostics
areal/workflow/rlvr_qun_team.py:93:        task_data: dict[str, Any],
areal/workflow/rlvr_qun_team.py:106:        reward_kwargs = dict(task_data)
areal/workflow/rlvr_qun_team.py:107:        if self.log_reward_diagnostics:
areal/workflow/rlvr_qun_team.py:108:            reward_kwargs["return_diagnostics"] = True
areal/workflow/rlvr_qun_team.py:134:        task_data: dict[str, Any],
areal/workflow/rlvr_qun_team.py:150:        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/vision_rlvr.py:50:        task_data: dict[str, Any],
areal/workflow/vision_rlvr.py:70:            **task_data,
areal/workflow/vision_rlvr.py:81:        task_data: dict[str, Any],
areal/workflow/vision_rlvr.py:97:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/infra/remote_inf_engine.py:528:        workflow_kwargs: dict[str, Any] | None,
areal/infra/remote_inf_engine.py:551:            if workflow_kwargs is not None:
areal/infra/remote_inf_engine.py:553:                    "workflow_kwargs is ignored when workflow is already an instance"
areal/infra/remote_inf_engine.py:559:            if workflow_kwargs is None:
areal/infra/remote_inf_engine.py:561:                    f"workflow_kwargs is required when workflow is a class. "
areal/infra/remote_inf_engine.py:562:                    f"Got workflow={workflow}, but workflow_kwargs=None."
areal/infra/remote_inf_engine.py:564:            resolved = workflow(**workflow_kwargs)
areal/infra/remote_inf_engine.py:579:                if workflow_kwargs is None:
areal/infra/remote_inf_engine.py:581:                        f"workflow_kwargs is required when workflow is a class or string. "
areal/infra/remote_inf_engine.py:582:                        f"Got workflow={workflow}, but workflow_kwargs=None."
areal/infra/remote_inf_engine.py:584:                resolved = imported_obj(**workflow_kwargs)
areal/infra/remote_inf_engine.py:588:                if workflow_kwargs is not None:
areal/infra/remote_inf_engine.py:590:                        "workflow_kwargs is ignored when workflow resolves to an instance"
areal/infra/remote_inf_engine.py:605:                    agent = imported_obj(**(workflow_kwargs or {}))
areal/infra/remote_inf_engine.py:619:            agent = workflow(**(workflow_kwargs or {}))
areal/infra/remote_inf_engine.py:629:            if workflow_kwargs is not None:
areal/infra/remote_inf_engine.py:631:                    "workflow_kwargs is ignored when workflow is already an instance"
areal/infra/remote_inf_engine.py:1007:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/remote_inf_engine.py:1023:        workflow_kwargs : Dict[str, Any], optional
areal/infra/remote_inf_engine.py:1050:            workflow, workflow_kwargs, group_size, proxy_addr=proxy_addr
areal/infra/remote_inf_engine.py:1096:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/remote_inf_engine.py:1110:        workflow_kwargs : Dict[str, Any], optional
areal/infra/remote_inf_engine.py:1127:            workflow, workflow_kwargs, group_size
areal/infra/remote_inf_engine.py:1139:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/remote_inf_engine.py:1152:        workflow_kwargs : Dict[str, Any], optional
areal/infra/remote_inf_engine.py:1173:            workflow, workflow_kwargs, group_size
areal/reward/code/competitive_exec.py:85:    return_diagnostics: bool = False,
areal/reward/code/competitive_exec.py:91:        by ``task_data["language"]`` injected by the dataset loader. Defaults
areal/reward/code/competitive_exec.py:101:    :param return_diagnostics: When true, return a dictionary with the reward
areal/reward/code/competitive_exec.py:119:        return diagnostics if return_diagnostics else reward
areal/infra/controller/rollout_controller.py:54:    workflow_kwargs: dict[str, Any]
areal/infra/controller/rollout_controller.py:790:                    workflow_kwargs=pending_task.workflow_kwargs,
areal/infra/controller/rollout_controller.py:854:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/controller/rollout_controller.py:863:        if workflow_kwargs is None:
areal/infra/controller/rollout_controller.py:864:            workflow_kwargs = {}
areal/infra/controller/rollout_controller.py:874:            workflow_kwargs=workflow_kwargs,
areal/infra/controller/rollout_controller.py:902:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/controller/rollout_controller.py:915:                workflow_kwargs=workflow_kwargs,
areal/infra/controller/rollout_controller.py:928:        workflow_kwargs: dict[str, Any] | None = None,
areal/infra/controller/rollout_controller.py:942:        if workflow_kwargs is None:
areal/infra/controller/rollout_controller.py:943:            workflow_kwargs = {}
areal/infra/controller/rollout_controller.py:951:                        workflow_kwargs=workflow_kwargs,
areal/infra/controller/train_controller.py:535:        workflow_kwargs: dict[str, Any],
areal/infra/controller/train_controller.py:543:            workflow_kwargs=workflow_kwargs,
areal/infra/controller/train_controller.py:553:        workflow_kwargs: dict[str, Any],
areal/infra/controller/train_controller.py:560:            workflow_kwargs=workflow_kwargs,
```

### Assistant - 2026-04-27 23:51:53 EDT

```
我会把这块改成“调用侧只传一个通用 `reward_kwargs`”，而不是在 workflow 上加一个专门的 `log_reward_diagnostics` 参数。底层仍然需要 reward 返回诊断信息、workflow 负责把它写进 `stats_tracker`，因为 reward 是在子进程里跑的，直接在 reward 内记录不到主进程的 tracker。
```

### Assistant - 2026-04-27 23:52:07 EDT

现在调用侧只剩一个通用透传参数：`reward_kwargs={"return_diagnostics": True}`。我再跑一下 diff 和针对性测试，确认没有把行为改歪。

### Tool Call - 2026-04-27 23:52:07 EDT

```
exec_command {"cmd":"git diff -- areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Call - 2026-04-27 23:52:08 EDT

```
exec_command {"cmd":"git diff --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:52:08 EDT

```
Chunk ID: f4b571
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1387
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..e32ed591 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -1,5 +1,5 @@
-import uuid
 import asyncio
+import uuid
 from collections.abc import Callable
 from typing import Any
 
@@ -52,12 +52,13 @@ class RLVRWorkflow(RolloutWorkflow):
         enable_thinking: bool = False,
         rollout_stat_scope: str = "rollout",
         ig_reward_params=None,
-        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
-        | str = default_get_input_ids_fn,
-        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
-        | str = default_data_extract_prompt_fn,
-        reward_timeout_seconds: float = 15.0,
-    ):
+        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
+        | str = default_get_input_ids_fn,
+        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
+        | str = default_data_extract_prompt_fn,
+        reward_timeout_seconds: float = 15.0,
+        reward_kwargs: dict[str, Any] | None = None,
+    ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
         if isinstance(self.tokenizer, str):
@@ -67,9 +68,10 @@ class RLVRWorkflow(RolloutWorkflow):
             self.tokenizer = tokenizer
         self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
         self.rollout_stat_scope = rollout_stat_scope
-        self.enable_thinking = enable_thinking
-        self.ig_reward_params = ig_reward_params
-        self.reward_timeout_seconds = reward_timeout_seconds
+        self.enable_thinking = enable_thinking
+        self.ig_reward_params = ig_reward_params
+        self.reward_timeout_seconds = reward_timeout_seconds
+        self.reward_kwargs = reward_kwargs or {}
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,30 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
-        """
-        completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
-            prompt_str,
-            completions_str,
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
+        """
+        completions_str = self.tokenizer.decode(resp.output_tokens)
+        reward_kwargs = dict(task_data)
+        reward_kwargs.update(self.reward_kwargs)
+        reward_result = await self.async_reward_fn(
+            prompt_str,
+            completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +146,17 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
 
         return resp, reward
 
@@ -155,7 +176,7 @@ class RLVRWorkflow(RolloutWorkflow):
 
         gt_text = data.get("answer")
         if not gt_text:
-            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
+            logger.warning("[IG-Rollout] Enabled but no 'answer' in data.")
             return None
 
         # 1. 构造 Target 后缀
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..03fe8aa4 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            reward_kwargs={"return_diagnostics": True},
         )
 
         eval_workflow_kwargs_map = {}
```

### Tool Output - 2026-04-27 23:52:08 EDT

```
Chunk ID: 67ca56
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 77
Output:
 areal/reward/code/competitive_exec.py | 46 ++++++++++++++++++++----
 areal/workflow/rlvr_qun_team.py       | 67 +++++++++++++++++++++++------------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 +++++++++++++
 4 files changed, 108 insertions(+), 30 deletions(-)
```

### Tool Call - 2026-04-27 23:52:16 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:52:16 EDT

```
exec_command {"cmd":"git diff --numstat -- areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:52:16 EDT

```
Chunk ID: ff349e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 48
Output:
areal/workflow/rlvr_qun_team.py:   Python script, Unicode text, UTF-8 text executable, with CRLF, LF line terminators
examples/code/nemotron_code_rl.py: Python script, ASCII text executable
```

### Tool Output - 2026-04-27 23:52:16 EDT

```
Chunk ID: 61b7f5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 10
Output:
44	23	areal/workflow/rlvr_qun_team.py
```

### Assistant - 2026-04-27 23:52:25 EDT

`rlvr_qun_team.py` 原本是 CRLF 行尾，我刚才的 patch 在局部引入了 LF，导致 diff 变吵。我会先把这个文件行尾恢复成一致的 CRLF，再看真实改动。

### Tool Call - 2026-04-27 23:52:26 EDT

```
exec_command {"cmd":"perl -0pi -e 's/\\R/\\r\\n/g' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:52:26 EDT

```
Chunk ID: f9db5a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:52:33 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:52:33 EDT

```
exec_command {"cmd":"git diff --numstat -- areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:52:33 EDT

```
exec_command {"cmd":"git diff -- areal/workflow/rlvr_qun_team.py | sed -n '1,220p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-27 23:52:33 EDT

```
Chunk ID: 71fa9b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 29
Output:
areal/workflow/rlvr_qun_team.py: Python script, Non-ISO extended-ASCII text executable, with CRLF line terminators
```

### Tool Output - 2026-04-27 23:52:33 EDT

```
Chunk ID: d1a66d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 10
Output:
60	22	areal/workflow/rlvr_qun_team.py
```

### Tool Output - 2026-04-27 23:52:33 EDT

```
Chunk ID: 374253
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2410
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..24446b51 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -1,5 +1,5 @@
-import uuid
 import asyncio
+import uuid
 from collections.abc import Callable
 from typing import Any
 
@@ -57,6 +57,7 @@ class RLVRWorkflow(RolloutWorkflow):
         data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
         | str = default_data_extract_prompt_fn,
         reward_timeout_seconds: float = 15.0,
+        reward_kwargs: dict[str, Any] | None = None,
     ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
@@ -70,6 +71,7 @@ class RLVRWorkflow(RolloutWorkflow):
         self.enable_thinking = enable_thinking
         self.ig_reward_params = ig_reward_params
         self.reward_timeout_seconds = reward_timeout_seconds
+        self.reward_kwargs = reward_kwargs or {}
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,30 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_kwargs = dict(task_data)
+        reward_kwargs.update(self.reward_kwargs)
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +146,17 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
 
         return resp, reward
 
@@ -148,14 +169,17 @@ class RLVRWorkflow(RolloutWorkflow):
     ) -> dict[str, list[float]] | None:
         """
         利用热乎的 KV Cache，异步并发计算所有中间步的 IG Logprobs，并直接结算过程奖励。
-        这样完全解耦了 Actor 进程，避免了多卡同步死锁和 FSDP Padding 带来的内存溢出。
+        这样完�
+�解耦了 Actor 进程，避�
+�了多卡同步死锁和 FSDP Padding 带来的�
+存溢出。
         """
         if not self.ig_reward_params:
             return None
 
         gt_text = data.get("answer")
         if not gt_text:
-            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
+            logger.warning("[IG-Rollout] Enabled but no 'answer' in data.")
             return None
 
         # 1. 构造 Target 后缀
@@ -204,7 +228,8 @@ class RLVRWorkflow(RolloutWorkflow):
             )
             probe_reqs.append(probe_req)
 
-        # 并发执行探测！完全命中 vLLM/SGLang 的 Radix Cache
+        # 并发执行探测！完�
+�命中 vLLM/SGLang 的 Radix Cache
         probe_responses = []
         chunk_size = 1
         for i in range(0, len(probe_reqs), chunk_size):
@@ -247,7 +272,9 @@ class RLVRWorkflow(RolloutWorkflow):
                     # print(f"expected_token_id: {expected_token_id}, p: {p}")
                     if isinstance(p, dict):
                         # ⚠️ 绝对不能用 list(p.values())[0] 盲提！
-                        # 必须用 expected_token_id 去查字典！
+                        # �
+须用 expected_token_id 去查字�
+�！
                         if expected_token_id in p:
                             token_info = p[expected_token_id]
                             step_sum += token_info.logprob if hasattr(token_info, "logprob") else float(token_info)
@@ -351,7 +378,9 @@ class RLVRWorkflow(RolloutWorkflow):
             "rewards": torch.tensor(reward, dtype=torch.float32),
         }
 
-        # [新增配置控制]: 支持通过配置项无缝切换计算后端
+        # [新增�
+�置控制]: 支持通过�
+�置项无缝切换计算后端
         ig_backend = getattr(self.ig_reward_params, "compute_backend", "actor") if self.ig_reward_params else None
 
         if self.ig_reward_params:
@@ -361,14 +390,16 @@ class RLVRWorkflow(RolloutWorkflow):
                 if ig_results:
                     for k, v in ig_results.items():
                         # 直接把 1D 数组转换为 float32 Tensor 塞进去，
-                        # 最后的 unsqueeze(0) 会完美兼容 actor 里的 shape (bs, seq_len)
+                        # 最后的 unsqueeze(0) 会完美�
+�容 actor 里的 shape (bs, seq_len)
                         res[k] = torch.tensor(v, dtype=torch.float32)
 
             # --- 下面这部分无论哪个 backend 都需要计算（为了防崩溃以及向 Actor 提供结构数据） ---
             # 准备ig计算所需的数据：提取并Tokenize标准答案 (Ground Truth)
             # 1. 尝试从 data 中提取标准答案文本
             # 常见的 key 为 'answer', 'solution' 或 'ground_truth'
-            # 如果你的数据集结构比较特殊，建议在 __init__ 中传入一个 data_extract_target_fn
+            # 如果你的数据集结构比较特殊，建议在 __init__ 中传�
+�一个 data_extract_target_fn
             gt_text = data.get("answer")
 
             if gt_text is None:
@@ -381,7 +412,8 @@ class RLVRWorkflow(RolloutWorkflow):
             else:
                 # 2. 编码标准答案
                 # 注意：add_special_tokens=False，因为我们要把它拼接到推理链后面，
-                # 不希望开头出现 BOS 或其他特殊 Token 干扰概率计算
+                # 不希望开头出现 BOS 或�
+�他特殊 Token 干扰概率计算
                 gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
 
             # 2. 识别 Step 边界 (通常是换行符 \n)
@@ -392,19 +424,25 @@ class RLVRWorkflow(RolloutWorkflow):
             res["ig_pad_token_id"] = torch.tensor(self.tokenizer.pad_token_id, dtype=torch.int32)
 
             # 3. 准备 Bridge (连接词)
-            # 这一步非常重要，必须让模型觉得“我要开始回答了”
-            # 比如: "\n\n#### " 或者 "\n\nTherefore, the answer is "
+            # 这一步非常重要，�
+须让模型觉得“我要开始回答了”
+            # 比如: "\n\n#### " 或�
+ "\n\nTherefore, the answer is "
             bridge_ids = self.tokenizer.encode(self.ig_reward_params.bridge_text, add_special_tokens=False)
             boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
             boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)
 
-            # 4. 存入 res 字典
+            # 4. 存�
+� res 字�
+�
             # 下游 Actor/Critic 在计算 Advantage 时，会检查 'ig_gt_ids' 是否存在
             # 注意：此处直接转为 Tensor，数据类型需与 input_ids 保持一致 (int32)
-            # return 语句会自动对其进行 unsqueeze(0) 处理，增加 batch 维度
+            # return 语句会自动对�
+�进行 unsqueeze(0) 处理，增加 batch 维度
             res["ig_gt_ids"] = torch.tensor(bridge_ids + boxed_first_half_ids + gt_ids + boxed_second_half_ids,
                                             dtype=torch.int32)
-            # 同时存入长度，方便后续token切分定位处理
+            # 同时存�
+�长度，方便后续token切分定位处理
             res["ig_bridge_len"] = torch.tensor(len(bridge_ids + boxed_first_half_ids), dtype=torch.int32)
             res["ig_gt_len"] = torch.tensor(len(gt_ids), dtype=torch.int32)
             res["prompt_len"] = torch.tensor(len(resp.input_tokens), dtype=torch.int32)
```

### Assistant - 2026-04-27 23:52:47 EDT

行尾修复那一步把文件里的中文注释编码打坏了，这个是我的操作失误。我现在从 Git 里的原始版本恢复这个文件，再只重放指标相关的小补丁，避免留下编码损坏。

### Tool Call - 2026-04-27 23:52:48 EDT

```
exec_command {"cmd":"git show HEAD:areal/workflow/rlvr_qun_team.py > /tmp/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:52:48 EDT

```
Chunk ID: 49a115
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:52:51 EDT

```
exec_command {"cmd":"cp /tmp/rlvr_qun_team.py areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:52:51 EDT

```
Chunk ID: a7cc83
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:52:56 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:52:56 EDT

```
exec_command {"cmd":"sed -n '1,180p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":14000}
```

### Tool Output - 2026-04-27 23:52:56 EDT

```
Chunk ID: e482eb
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 28
Output:
areal/workflow/rlvr_qun_team.py: Python script, Unicode text, UTF-8 text executable, with CRLF line terminators
```

### Tool Output - 2026-04-27 23:52:56 EDT

```
Chunk ID: 18a089
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1683
Output:
import uuid
import asyncio
from collections.abc import Callable
from typing import Any

import torch
from transformers import PreTrainedTokenizerFast

from areal import workflow_context
from areal.api.cli_args import GenerationHyperparameters
from areal.api.engine_api import InferenceEngine
from areal.api.io_struct import ModelRequest, ModelResponse
from areal.api.reward_api import AsyncRewardWrapper
from areal.api.workflow_api import RolloutWorkflow
from areal.utils import logging, stats_tracker
from areal.utils.dynamic_import import import_from_string
from areal.utils.perf_tracer import (
    atrace_session_phase,
    session_context,
    trace_session,
)

logger = logging.getLogger("RLVRWorkflow")


def default_get_input_ids_fn(
    data: Any,
    tokenizer: PreTrainedTokenizerFast,
    enable_thinking: bool,
) -> list[int]:
    input_ids = tokenizer.apply_chat_template(
        data,
        tokenize=True,
        add_generation_prompt=True,
        enable_thinking=enable_thinking,
    )
    return list(input_ids)


def default_data_extract_prompt_fn(data: dict[str, Any]) -> Any:
    return data["messages"]


class RLVRWorkflow(RolloutWorkflow):
    """Single-turn reward learning workflow supporting optional thinking tokens."""

    def __init__(
        self,
        reward_fn: Callable[..., Any] | str,
        gconfig: GenerationHyperparameters,
        tokenizer: PreTrainedTokenizerFast | str,
        enable_thinking: bool = False,
        rollout_stat_scope: str = "rollout",
        ig_reward_params=None,
        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
        | str = default_get_input_ids_fn,
        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
        | str = default_data_extract_prompt_fn,
        reward_timeout_seconds: float = 15.0,
    ):
        self.reward_fn = reward_fn
        self.tokenizer = tokenizer
        if isinstance(self.tokenizer, str):
            from areal.utils.hf_utils import load_hf_tokenizer

            tokenizer = load_hf_tokenizer(self.tokenizer)
            self.tokenizer = tokenizer
        self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
        self.rollout_stat_scope = rollout_stat_scope
        self.enable_thinking = enable_thinking
        self.ig_reward_params = ig_reward_params
        self.reward_timeout_seconds = reward_timeout_seconds
        if not isinstance(reward_fn, str):
            self.async_reward_fn = AsyncRewardWrapper(
                reward_fn, timeout_seconds=reward_timeout_seconds
            )
        # Support string paths for get_input_ids_fn
        if isinstance(get_input_ids_fn, str):
            get_input_ids_fn = import_from_string(get_input_ids_fn)
        self.get_input_ids_fn = get_input_ids_fn
        # Support string paths for data_extract_prompt_fn
        if isinstance(data_extract_prompt_fn, str):
            data_extract_prompt_fn = import_from_string(data_extract_prompt_fn)
        self.data_extract_prompt_fn = data_extract_prompt_fn

    @trace_session("reward")
    async def _compute_rewards(
        self,
        resp: ModelResponse,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> float:
        """Decode completion and compute reward.

        Traces reward phase execution for SessionTracer. Decodes output tokens
        to string, calls async reward function, and logs metric to stats tracker.

        Returns
        -------
        float
            Reward value.
        """
        completions_str = self.tokenizer.decode(resp.output_tokens)
        reward = await self.async_reward_fn(
            prompt_str,
            completions_str,
            resp.input_tokens,
            resp.output_tokens,
            **task_data,
        )

        return reward

    @session_context()
    async def _collect_samples(
        self,
        engine: InferenceEngine,
        req: ModelRequest,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[ModelResponse, float]:
        """Generate one sample and compute its reward.

        Registers a new session for this sample, calls engine.agenerate,
        computes reward, and logs metrics. SessionTracer automatically
        tracks generate and reward phases via @trace_session decorators.

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward = await self._compute_rewards(resp, prompt_str, task_data)

        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)

        return resp, reward

    async def _compute_ig_probes(
            self,
            engine: InferenceEngine,
            resp: ModelResponse,
            data: dict[str, Any],
            outcome_reward: float
    ) -> dict[str, list[float]] | None:
        """
        利用热乎的 KV Cache，异步并发计算所有中间步的 IG Logprobs，并直接结算过程奖励。
        这样完全解耦了 Actor 进程，避免了多卡同步死锁和 FSDP Padding 带来的内存溢出。
        """
        if not self.ig_reward_params:
            return None

        gt_text = data.get("answer")
        if not gt_text:
            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
            return None

        # 1. 构造 Target 后缀
        gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
        bridge_text = getattr(self.ig_reward_params, "bridge_text", "\n\nTherefore, the answer is ")
        bridge_ids = self.tokenizer.encode(bridge_text, add_special_tokens=False)
        boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
        boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)

        ig_gt_ids = bridge_ids + boxed_first_half_ids + gt_ids + boxed_second_half_ids
        target_len = len(gt_ids)
        tail_len = len(boxed_second_half_ids)

        # 2. 定位所有步骤的切分点 (根据 sep_token)
        sep_token = getattr(self.ig_reward_params, "sep_token", "\n")
        sep_ids = self.tokenizer.encode(sep_token, add_special_tokens=False)
        if not sep_ids:
            return None
        sep_id = sep_ids[-1]

        step_offsets = [i for i, token_id in enumerate(resp.output_tokens) if token_id == sep_id]
        if not step_offsets or step_offsets[-1] != len(resp.output_tokens) - 1:
```

### User - 2026-04-27 23:53:16 EDT

保持linux行尾是我需要的

### Assistant - 2026-04-27 23:53:28 EDT

收到，后面我按 LF/Linux 行尾处理。刚才恢复原文件是为了先消除编码损坏风险；现在我会只做 CRLF -> LF 的行尾规范化，不碰内容字节。

### Tool Call - 2026-04-27 23:53:28 EDT

```
exec_command {"cmd":"perl -pi -e 's/\\r$//' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:53:28 EDT

```
exec_command {"cmd":"perl -pi -e 's/\\r$//' examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:53:29 EDT

```
Chunk ID: 2a983e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:53:29 EDT

```
Chunk ID: 7033e2
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:53:37 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:53:37 EDT

```
exec_command {"cmd":"sed -n '140,180p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:53:37 EDT

```
exec_command {"cmd":"git diff --check -- areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-27 23:53:37 EDT

```
Chunk ID: 20f0dd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 40
Output:
areal/workflow/rlvr_qun_team.py:   Python script, Unicode text, UTF-8 text executable
examples/code/nemotron_code_rl.py: Python script, ASCII text executable
```

### Tool Output - 2026-04-27 23:53:37 EDT

```
Chunk ID: dce1d6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 373
Output:

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)

        scalars = {
            "reward": reward,
            "output_len": resp.output_len,
            "total_len": resp.input_len + resp.output_len,
            "stop_length": float(resp.stop_reason == "length"),
            "stop_abort": float(resp.stop_reason == "abort"),
        }
        scalars.update(reward_diagnostics)
        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)

        return resp, reward

    async def _compute_ig_probes(
            self,
            engine: InferenceEngine,
            resp: ModelResponse,
            data: dict[str, Any],
            outcome_reward: float
    ) -> dict[str, list[float]] | None:
        """
        利用热乎的 KV Cache，异步并发计算所有中间步的 IG Logprobs，并直接结算过程奖励。
        这样完全解耦了 Actor 进程，避免了多卡同步死锁和 FSDP Padding 带来的内存溢出。
        """
        if not self.ig_reward_params:
            return None

        gt_text = data.get("answer")
        if not gt_text:
            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
            return None
```

### Tool Output - 2026-04-27 23:53:37 EDT

```
Chunk ID: b17025
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Assistant - 2026-04-27 23:53:52 EDT

LF 行尾已恢复，中文注释也检查过没有损坏。接下来我看最终 diff 是否只是预期改动；因为 `rlvr_qun_team.py` 原提交是 CRLF，切到 LF 会让 Git 认为整文件行尾变了，这点我会单独说明。

### Tool Call - 2026-04-27 23:53:52 EDT

```
exec_command {"cmd":"git diff --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:53:52 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:53:52 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol -- areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py | sed -n '1,240p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":24000}
```

### Tool Output - 2026-04-27 23:53:53 EDT

```
Chunk ID: dac1f5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 69
Output:
 areal/reward/code/competitive_exec.py |  46 +-
 areal/workflow/rlvr_qun_team.py       | 847 +++++++++++++++++-----------------
 examples/code/nemotron_code_rl.py     |   1 +
 tests/test_code_sandbox_multilang.py  |  24 +
 4 files changed, 498 insertions(+), 420 deletions(-)
```

### Tool Output - 2026-04-27 23:53:53 EDT

```
Chunk ID: 422dd4
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 79
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 37 ++++++++++++++++++++++------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 ++++++++++++++++++
 4 files changed, 93 insertions(+), 15 deletions(-)
```

### Tool Output - 2026-04-27 23:53:53 EDT

```
Chunk ID: f8a489
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 984
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..0c40abba 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -57,6 +57,7 @@ class RLVRWorkflow(RolloutWorkflow):
         data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
         | str = default_data_extract_prompt_fn,
         reward_timeout_seconds: float = 15.0,
+        reward_kwargs: dict[str, Any] | None = None,
     ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
@@ -70,6 +71,7 @@ class RLVRWorkflow(RolloutWorkflow):
         self.enable_thinking = enable_thinking
         self.ig_reward_params = ig_reward_params
         self.reward_timeout_seconds = reward_timeout_seconds
+        self.reward_kwargs = reward_kwargs or {}
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,30 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_kwargs = dict(task_data)
+        reward_kwargs.update(self.reward_kwargs)
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +146,17 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
 
         return resp, reward
 
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..03fe8aa4 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            reward_kwargs={"return_diagnostics": True},
         )
 
         eval_workflow_kwargs_map = {}
```

### Assistant - 2026-04-27 23:53:58 EDT

我还会顺手把新加的长行折一下，避免 formatter 或 pre-commit 再产生额外噪声。

### Tool Call - 2026-04-27 23:54:04 EDT

```
exec_command {"cmd":"uv run pytest tests/test_code_sandbox_multilang.py -k diagnostics","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:54:04 EDT

```
Chunk ID: ad5b72
Wall time: 0.0000 seconds
Process exited with code 127
Original token count: 8
Output:
zsh:1: command not found: uv
```

### Assistant - 2026-04-27 23:54:07 EDT

这里环境里没有 `uv`，我改用系统里的 `python -m pytest` 跑同一个窄测试；如果缺依赖我会明确标出来。

### Tool Call - 2026-04-27 23:54:08 EDT

```
exec_command {"cmd":"python -m pytest tests/test_code_sandbox_multilang.py -k diagnostics","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:54:08 EDT

```
Chunk ID: ce1bd8
Wall time: 0.0000 seconds
Process exited with code 127
Original token count: 9
Output:
zsh:1: command not found: python
```

### Tool Call - 2026-04-27 23:54:11 EDT

```
exec_command {"cmd":"python3 -m pytest tests/test_code_sandbox_multilang.py -k diagnostics","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:54:11 EDT

```
Chunk ID: 646f3a
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 11
Output:
/usr/bin/python3: No module named pytest
```

### Assistant - 2026-04-27 23:54:17 EDT

本机 `python3` 有，但没有安装 `pytest`，所以测试目前跑不起来。我再检查是否存在项目虚拟环境，避免误报。

### Tool Call - 2026-04-27 23:54:17 EDT

```
exec_command {"cmd":"ls -la .venv","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:54:17 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --check","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:54:17 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-27 23:54:17 EDT

```
Chunk ID: 71622e
Wall time: 0.0000 seconds
Process exited with code 2
Original token count: 14
Output:
ls: cannot access '.venv': No such file or directory
```

### Tool Output - 2026-04-27 23:54:17 EDT

```
Chunk ID: 0b4d5e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:54:17 EDT

```
Chunk ID: 728b94
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 80
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 41 +++++++++++++++++++++++++------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 ++++++++++++++++++
 4 files changed, 97 insertions(+), 15 deletions(-)
```

### Tool Call - 2026-04-27 23:54:22 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol -- areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Call - 2026-04-27 23:54:22 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol -- areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":24000}
```

### Tool Call - 2026-04-27 23:54:22 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-27 23:54:22 EDT

```
Chunk ID: 735171
Wall time: 0.0395 seconds
Process exited with code 0
Original token count: 947
Output:
diff --git a/areal/reward/code/competitive_exec.py b/areal/reward/code/competitive_exec.py
index bcb65db3..cd38d4b7 100644
--- a/areal/reward/code/competitive_exec.py
+++ b/areal/reward/code/competitive_exec.py
@@ -82,8 +82,9 @@ def nemotron_competitive_reward_fn(
     memory_mb: int = 2048,
     compile_timeout: float = 30.0,
     java_xmx_mb: int = 2048,
+    return_diagnostics: bool = False,
     **kwargs,
-) -> float:
+) -> float | dict[str, float]:
     """Pass rate over (a sample of) unit tests.
 
     :param language: Source language for the model output. May be overridden
@@ -97,18 +98,40 @@ def nemotron_competitive_reward_fn(
     :param compile_timeout: Wall-clock seconds for the compile step (compiled
         languages only). Compile happens once per submission.
     :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
+    :param return_diagnostics: When true, return a dictionary with the reward
+        and per-sample execution outcome counters for training-time logging.
     """
+    diagnostics = {
+        "reward": 0.0,
+        "no_tests": 0.0,
+        "mismatched_tests": 0.0,
+        "no_code": 0.0,
+        "compile_error": 0.0,
+        "timeout_tests": 0.0,
+        "runtime_error_tests": 0.0,
+        "wrong_answer_tests": 0.0,
+        "passed_tests": 0.0,
+        "sampled_tests": 0.0,
+    }
+
+    def _finish(reward: float) -> float | dict[str, float]:
+        diagnostics["reward"] = reward
+        return diagnostics if return_diagnostics else reward
+
     if not test_inputs or not test_outputs:
-        return 0.0
+        diagnostics["no_tests"] = 1.0
+        return _finish(0.0)
     if len(test_inputs) != len(test_outputs):
         logger.warning(
             f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
         )
-        return 0.0
+        diagnostics["mismatched_tests"] = 1.0
+        return _finish(0.0)
 
     extracted = extract_code(completions, expected_language=language)
     if extracted is None:
-        return 0.0
+        diagnostics["no_code"] = 1.0
+        return _finish(0.0)
     detected_lang, code = extracted
 
     artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
@@ -116,7 +139,8 @@ def nemotron_competitive_reward_fn(
         # Compile failure or unsupported language — caller already logged in
         # CompileResult.error/stderr; treat as zero reward.
         cleanup_compile_result(artifact)
-        return 0.0
+        diagnostics["compile_error"] = 1.0
+        return _finish(0.0)
 
     try:
         n = len(test_inputs)
@@ -127,6 +151,7 @@ def nemotron_competitive_reward_fn(
             # across all rollouts of the same problem in a GRPO group.
             rng = random.Random(prompt)
             idx = rng.sample(range(n), max_tests)
+        diagnostics["sampled_tests"] = float(len(idx))
 
         passed = 0
         for i in idx:
@@ -137,11 +162,18 @@ def nemotron_competitive_reward_fn(
                 memory_mb=memory_mb,
                 java_xmx_mb=java_xmx_mb,
             )
-            if result.timeout or result.error is not None or result.returncode != 0:
+            if result.timeout:
+                diagnostics["timeout_tests"] += 1.0
+                continue
+            if result.error is not None or result.returncode != 0:
+                diagnostics["runtime_error_tests"] += 1.0
                 continue
             if outputs_match(result.stdout, test_outputs[i]):
                 passed += 1
+            else:
+                diagnostics["wrong_answer_tests"] += 1.0
 
-        return passed / len(idx)
+        diagnostics["passed_tests"] = float(passed)
+        return _finish(passed / len(idx))
     finally:
         cleanup_compile_result(artifact)
```

### Tool Output - 2026-04-27 23:54:22 EDT

````
Chunk ID: c80469
Wall time: 0.0430 seconds
Process exited with code 0
Original token count: 1311
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..9a8a5cd0 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -57,6 +57,7 @@ class RLVRWorkflow(RolloutWorkflow):
         data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
         | str = default_data_extract_prompt_fn,
         reward_timeout_seconds: float = 15.0,
+        reward_kwargs: dict[str, Any] | None = None,
     ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
@@ -70,6 +71,7 @@ class RLVRWorkflow(RolloutWorkflow):
         self.enable_thinking = enable_thinking
         self.ig_reward_params = ig_reward_params
         self.reward_timeout_seconds = reward_timeout_seconds
+        self.reward_kwargs = reward_kwargs or {}
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,30 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_kwargs = dict(task_data)
+        reward_kwargs.update(self.reward_kwargs)
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +146,21 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(
+            resp, prompt_str, task_data
+        )
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(
+            **scalars
+        )
 
         return resp, reward
 
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..03fe8aa4 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            reward_kwargs={"return_diagnostics": True},
         )
 
         eval_workflow_kwargs_map = {}
diff --git a/tests/test_code_sandbox_multilang.py b/tests/test_code_sandbox_multilang.py
index f759a38a..998a5830 100644
--- a/tests/test_code_sandbox_multilang.py
+++ b/tests/test_code_sandbox_multilang.py
@@ -189,6 +189,30 @@ def test_reward_fn_python_pass_rate():
     assert reward == 1.0
 
 
+def test_reward_fn_diagnostics_mode_reports_test_outcomes():
+    if not TOOLCHAIN.get("python"):
+        pytest.skip("python toolchain not available")
+    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
+    diagnostics = nemotron_competitive_reward_fn(
+        prompt="dummy",
+        completions=completion,
+        prompt_ids=[],
+        completion_ids=[],
+        test_inputs=["1\n", "10\n"],
+        test_outputs=["2", "21"],
+        language="python",
+        per_test_timeout=5.0,
+        max_tests=15,
+        memory_mb=512,
+        return_diagnostics=True,
+    )
+    assert diagnostics["reward"] == 0.5
+    assert diagnostics["sampled_tests"] == 2.0
+    assert diagnostics["passed_tests"] == 1.0
+    assert diagnostics["wrong_answer_tests"] == 1.0
+    assert diagnostics["no_code"] == 0.0
+
+
 def test_reward_fn_cpp_pass_rate():
     if not TOOLCHAIN.get("cpp"):
         pytest.skip("cpp toolchain not available")
````

### Tool Output - 2026-04-27 23:54:22 EDT

```
Chunk ID: 355286
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 41
Output:
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M examples/code/nemotron_code_rl.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Assistant - 2026-04-27 23:54:28 EDT

我发现 workflow 里保存 `reward_kwargs` 时最好复制一份，避免外部字典后续被改动影响 rollout worker；这个是小的稳健性修正。

### Tool Call - 2026-04-27 23:54:34 EDT

```
exec_command {"cmd":"python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:54:34 EDT

```
Chunk ID: 3437db
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:54:38 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:54:38 EDT

```
Chunk ID: acba07
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 41
Output:
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M examples/code/nemotron_code_rl.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Tool Call - 2026-04-27 23:54:44 EDT

```
exec_command {"cmd":"git diff --check","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:54:44 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:54:44 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py areal/reward/code/competitive_exec.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-27 23:54:44 EDT

```
Chunk ID: 0e7188
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:54:44 EDT

```
Chunk ID: cf536c
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 80
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 41 +++++++++++++++++++++++++------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 ++++++++++++++++++
 4 files changed, 97 insertions(+), 15 deletions(-)
```

### Tool Output - 2026-04-27 23:54:44 EDT

```
Chunk ID: 1fbc80
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 87
Output:
areal/workflow/rlvr_qun_team.py:       Python script, Unicode text, UTF-8 text executable
areal/reward/code/competitive_exec.py: Python script, Unicode text, UTF-8 text executable
examples/code/nemotron_code_rl.py:     Python script, ASCII text executable
tests/test_code_sandbox_multilang.py:  Python script, Unicode text, UTF-8 text executable
```

### Assistant - 2026-04-27 23:54:55 EDT

````
可以，已经改成调用侧只传一个通用参数：

[`examples/code/nemotron_code_rl.py`](/home/ag/projects/AReaL_v1/examples/code/nemotron_code_rl.py:68)
```python
reward_kwargs={"return_diagnostics": True}
```

但实现上不能真正只改这一个文件：reward 在 `AsyncRewardWrapper` 的子进程里跑，子进程里直接写 `stats_tracker` 不会回到主进程，所以必须让 [`competitive_exec.py`](/home/ag/projects/AReaL_v1/areal/reward/code/competitive_exec.py:85) 返回 diagnostics，再由 [`rlvr_qun_team.py`](/home/ag/projects/AReaL_v1/areal/workflow/rlvr_qun_team.py:58) 在主进程记录指标。测试文件只是覆盖 diagnostics 行为。

我也按你的要求把相关文件保持为 Linux LF 行尾了。验证情况：

- `python3 -m py_compile ...` 通过
- `git diff --check` 通过
- `pytest` 没跑起来：当前环境没有 `uv`，没有 `.venv`，系统 `python3` 也没有安装 `pytest`

另外，未跟踪的 `.codex` 我没有动。
````

### User - 2026-04-27 23:58:46 EDT

reward_kwargs传入是不是没有必要，训练code rl直接使用上这些指标就可以了，训math不存在这些指标，单独加一个rewrad_kwargs不太优雅

### Assistant - 2026-04-27 23:58:56 EDT

```
我同意，`reward_kwargs` 这个抽象有点绕，而且会把“代码奖励的诊断开关”暴露到所有 workflow 使用方。更干净的做法是：code reward 默认返回带诊断的结构；workflow 兼容 float 和 dict，只有 dict 才记录额外指标。这样 math reward 继续返回 float，不需要任何新参数。
```

### Tool Call - 2026-04-27 23:58:56 EDT

```
exec_command {"cmd":"rg -n \"def .*reward_fn|return_diagnostics|reward_kwargs\" areal examples tests -g '*.py'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":24000}
```

### Tool Call - 2026-04-27 23:58:56 EDT

```
exec_command {"cmd":"sed -n '1,150p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Call - 2026-04-27 23:58:56 EDT

```
exec_command {"cmd":"sed -n '70,190p' areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:58:57 EDT

```
Chunk ID: ec1d04
Wall time: 0.0409 seconds
Process exited with code 0
Original token count: 698
Output:
examples/vlm_npu/geometry3k_grpo.py:23:def geometry3k_reward_fn(
examples/vlm_npu/virl39k_grpo.py:23:def virl39k_reward_fn(
examples/code/nemotron_code_rl.py:68:            reward_kwargs={"return_diagnostics": True},
examples/vlm/geometry3k_grpo.py:23:def geometry3k_reward_fn(
examples/vlm/clevr_count_70k_grpo.py:18:def clevr_count_70k_reward_fn(
examples/camel/train.py:28:def gsm8k_reward_fn(result, answer):
examples/multi_turn_math/gsm8k_rl_mt.py:19:def gsm8k_reward_fn(result, answer):
examples/math/boba_grpo.py:44:def boba_reward_fn(
areal/api/reward_api.py:39:def reward_fn(
examples/tir/train_tir.py:15:def math_reward_fn(prompt, completions, prompt_ids, completion_ids, answer, **kwargs):
areal/workflow/langchain/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/openai/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/rlvr_qun_team.py:60:        reward_kwargs: dict[str, Any] | None = None,
areal/workflow/rlvr_qun_team.py:74:        self.reward_kwargs = dict(reward_kwargs or {})
areal/workflow/rlvr_qun_team.py:106:        reward_kwargs = dict(task_data)
areal/workflow/rlvr_qun_team.py:107:        reward_kwargs.update(self.reward_kwargs)
areal/workflow/rlvr_qun_team.py:113:            **reward_kwargs,
areal/reward/code/competitive_exec.py:72:def nemotron_competitive_reward_fn(
areal/reward/code/competitive_exec.py:85:    return_diagnostics: bool = False,
areal/reward/code/competitive_exec.py:101:    :param return_diagnostics: When true, return a dictionary with the reward
areal/reward/code/competitive_exec.py:119:        return diagnostics if return_diagnostics else reward
areal/reward/__init__.py:11:def get_custom_reward_fn(path: str, **kwargs):
areal/workflow/anthropic/math_agent.py:10:def math_reward_fn(completions: str, answer: str) -> float:
areal/reward/math_verify_qun_team.py:8:def math_verify_reward_fn(
areal/reward/geometry3k.py:24:def geometry3k_reward_fn(
areal/workflow/anthropic/claude_math_agent.py:18:def math_reward_fn(completions: str, answer: str) -> float:
areal/reward/gsm8k.py:7:def gsm8k_reward_fn(
areal/reward/clevr_count_70k.py:12:def clevr_count_70k_reward_fn(
tests/test_inference_engines.py:30:def _dummy_reward_fn(*args, **kwargs):
tests/test_code_sandbox_multilang.py:173:def test_reward_fn_python_pass_rate():
tests/test_code_sandbox_multilang.py:192:def test_reward_fn_diagnostics_mode_reports_test_outcomes():
tests/test_code_sandbox_multilang.py:207:        return_diagnostics=True,
tests/test_code_sandbox_multilang.py:216:def test_reward_fn_cpp_pass_rate():
tests/test_code_sandbox_multilang.py:240:def test_reward_fn_no_fence_returns_zero():
tests/test_code_sandbox_multilang.py:253:def test_reward_fn_compile_failure_returns_zero():
```

### Tool Output - 2026-04-27 23:58:57 EDT

```
Chunk ID: 597723
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1327
Output:
import uuid
import asyncio
from collections.abc import Callable
from typing import Any

import torch
from transformers import PreTrainedTokenizerFast

from areal import workflow_context
from areal.api.cli_args import GenerationHyperparameters
from areal.api.engine_api import InferenceEngine
from areal.api.io_struct import ModelRequest, ModelResponse
from areal.api.reward_api import AsyncRewardWrapper
from areal.api.workflow_api import RolloutWorkflow
from areal.utils import logging, stats_tracker
from areal.utils.dynamic_import import import_from_string
from areal.utils.perf_tracer import (
    atrace_session_phase,
    session_context,
    trace_session,
)

logger = logging.getLogger("RLVRWorkflow")


def default_get_input_ids_fn(
    data: Any,
    tokenizer: PreTrainedTokenizerFast,
    enable_thinking: bool,
) -> list[int]:
    input_ids = tokenizer.apply_chat_template(
        data,
        tokenize=True,
        add_generation_prompt=True,
        enable_thinking=enable_thinking,
    )
    return list(input_ids)


def default_data_extract_prompt_fn(data: dict[str, Any]) -> Any:
    return data["messages"]


class RLVRWorkflow(RolloutWorkflow):
    """Single-turn reward learning workflow supporting optional thinking tokens."""

    def __init__(
        self,
        reward_fn: Callable[..., Any] | str,
        gconfig: GenerationHyperparameters,
        tokenizer: PreTrainedTokenizerFast | str,
        enable_thinking: bool = False,
        rollout_stat_scope: str = "rollout",
        ig_reward_params=None,
        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
        | str = default_get_input_ids_fn,
        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
        | str = default_data_extract_prompt_fn,
        reward_timeout_seconds: float = 15.0,
        reward_kwargs: dict[str, Any] | None = None,
    ):
        self.reward_fn = reward_fn
        self.tokenizer = tokenizer
        if isinstance(self.tokenizer, str):
            from areal.utils.hf_utils import load_hf_tokenizer

            tokenizer = load_hf_tokenizer(self.tokenizer)
            self.tokenizer = tokenizer
        self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
        self.rollout_stat_scope = rollout_stat_scope
        self.enable_thinking = enable_thinking
        self.ig_reward_params = ig_reward_params
        self.reward_timeout_seconds = reward_timeout_seconds
        self.reward_kwargs = dict(reward_kwargs or {})
        if not isinstance(reward_fn, str):
            self.async_reward_fn = AsyncRewardWrapper(
                reward_fn, timeout_seconds=reward_timeout_seconds
            )
        # Support string paths for get_input_ids_fn
        if isinstance(get_input_ids_fn, str):
            get_input_ids_fn = import_from_string(get_input_ids_fn)
        self.get_input_ids_fn = get_input_ids_fn
        # Support string paths for data_extract_prompt_fn
        if isinstance(data_extract_prompt_fn, str):
            data_extract_prompt_fn = import_from_string(data_extract_prompt_fn)
        self.data_extract_prompt_fn = data_extract_prompt_fn

    @trace_session("reward")
    async def _compute_rewards(
        self,
        resp: ModelResponse,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[float, dict[str, float]]:
        """Decode completion and compute reward.

        Traces reward phase execution for SessionTracer. Decodes output tokens
        to string, calls async reward function, and logs metric to stats tracker.

        Returns
        -------
        tuple[float, dict[str, float]]
            Reward value and optional diagnostic scalars.
        """
        completions_str = self.tokenizer.decode(resp.output_tokens)
        reward_kwargs = dict(task_data)
        reward_kwargs.update(self.reward_kwargs)
        reward_result = await self.async_reward_fn(
            prompt_str,
            completions_str,
            resp.input_tokens,
            resp.output_tokens,
            **reward_kwargs,
        )

        if isinstance(reward_result, dict):
            reward = float(reward_result["reward"])
            diagnostics = {
                f"reward_diagnostics/{key}": float(value)
                for key, value in reward_result.items()
                if key != "reward" and isinstance(value, int | float)
            }
            return reward, diagnostics

        return float(reward_result), {}

    @session_context()
    async def _collect_samples(
        self,
        engine: InferenceEngine,
        req: ModelRequest,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[ModelResponse, float]:
        """Generate one sample and compute its reward.

        Registers a new session for this sample, calls engine.agenerate,
        computes reward, and logs metrics. SessionTracer automatically
        tracks generate and reward phases via @trace_session decorators.

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward, reward_diagnostics = await self._compute_rewards(
            resp, prompt_str, task_data
```

### Tool Output - 2026-04-27 23:58:57 EDT

```
Chunk ID: 23fafc
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1028
Output:


def nemotron_competitive_reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    test_inputs: list[str] | None = None,
    test_outputs: list[str] | None = None,
    language: str = "python",
    per_test_timeout: float = 5.0,
    max_tests: int = 15,
    memory_mb: int = 2048,
    compile_timeout: float = 30.0,
    java_xmx_mb: int = 2048,
    return_diagnostics: bool = False,
    **kwargs,
) -> float | dict[str, float]:
    """Pass rate over (a sample of) unit tests.

    :param language: Source language for the model output. May be overridden
        by ``task_data["language"]`` injected by the dataset loader. Defaults
        to ``"python"`` for Nemotron-RL-coding-competitive_coding.
    :param per_test_timeout: Wall-clock seconds for each test case.
    :param max_tests: Upper bound on tests executed per reward call; tests
        beyond this cap are sampled deterministically per ``prompt``.
    :param memory_mb: Address-space cap for each test subprocess (Python/C++/
        Node). Java uses ``java_xmx_mb`` instead.
    :param compile_timeout: Wall-clock seconds for the compile step (compiled
        languages only). Compile happens once per submission.
    :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
    :param return_diagnostics: When true, return a dictionary with the reward
        and per-sample execution outcome counters for training-time logging.
    """
    diagnostics = {
        "reward": 0.0,
        "no_tests": 0.0,
        "mismatched_tests": 0.0,
        "no_code": 0.0,
        "compile_error": 0.0,
        "timeout_tests": 0.0,
        "runtime_error_tests": 0.0,
        "wrong_answer_tests": 0.0,
        "passed_tests": 0.0,
        "sampled_tests": 0.0,
    }

    def _finish(reward: float) -> float | dict[str, float]:
        diagnostics["reward"] = reward
        return diagnostics if return_diagnostics else reward

    if not test_inputs or not test_outputs:
        diagnostics["no_tests"] = 1.0
        return _finish(0.0)
    if len(test_inputs) != len(test_outputs):
        logger.warning(
            f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
        )
        diagnostics["mismatched_tests"] = 1.0
        return _finish(0.0)

    extracted = extract_code(completions, expected_language=language)
    if extracted is None:
        diagnostics["no_code"] = 1.0
        return _finish(0.0)
    detected_lang, code = extracted

    artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
    if not artifact.ok:
        # Compile failure or unsupported language — caller already logged in
        # CompileResult.error/stderr; treat as zero reward.
        cleanup_compile_result(artifact)
        diagnostics["compile_error"] = 1.0
        return _finish(0.0)

    try:
        n = len(test_inputs)
        if n <= max_tests:
            idx = list(range(n))
        else:
            # Seeding with the prompt keeps the sampled subset identical
            # across all rollouts of the same problem in a GRPO group.
            rng = random.Random(prompt)
            idx = rng.sample(range(n), max_tests)
        diagnostics["sampled_tests"] = float(len(idx))

        passed = 0
        for i in idx:
            result = run_artifact(
                artifact,
                stdin=test_inputs[i],
                timeout=per_test_timeout,
                memory_mb=memory_mb,
                java_xmx_mb=java_xmx_mb,
            )
            if result.timeout:
                diagnostics["timeout_tests"] += 1.0
                continue
            if result.error is not None or result.returncode != 0:
                diagnostics["runtime_error_tests"] += 1.0
                continue
            if outputs_match(result.stdout, test_outputs[i]):
                passed += 1
            else:
                diagnostics["wrong_answer_tests"] += 1.0

        diagnostics["passed_tests"] = float(passed)
        return _finish(passed / len(idx))
    finally:
        cleanup_compile_result(artifact)
```

### Assistant - 2026-04-27 23:59:11 EDT

```
我准备把这版收敛成两个约定：普通 reward 返回 `float`；需要额外指标的 reward 可以返回 `{"reward": ..., ...}`。workflow 不知道 code/math 的区别，只是兼容这两种返回值；code reward 默认返回 dict，所以 code RL 自动有这些指标，math reward 仍然是 float。
```

### Tool Call - 2026-04-27 23:59:11 EDT

```
exec_command {"cmd":"sed -n '160,285p' tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":14000}
```

### Tool Call - 2026-04-27 23:59:12 EDT

```
exec_command {"cmd":"rg -n \"nemotron_competitive_reward_fn\\(\" -S .","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:59:12 EDT

````
Chunk ID: e6bf45
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 817
Output:
def test_extract_code_picks_last_fence():
    text = (
        "First try:\n```python\nprint('first')\n```\n"
        "Second try:\n```python\nprint('second')\n```\n"
    )
    got = extract_code(text, expected_language="python")
    assert got is not None
    assert "second" in got[1]


# end-to-end reward fn ---------------------------------------------------------


def test_reward_fn_python_pass_rate():
    if not TOOLCHAIN.get("python"):
        pytest.skip("python toolchain not available")
    completion = "Here's the answer:\n```python\nx = int(input())\nprint(x * 2)\n```\n"
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n", "10\n", "100\n"],
        test_outputs=["2", "20", "200"],
        language="python",
        per_test_timeout=5.0,
        max_tests=15,
        memory_mb=512,
    )
    assert reward == 1.0


def test_reward_fn_diagnostics_mode_reports_test_outcomes():
    if not TOOLCHAIN.get("python"):
        pytest.skip("python toolchain not available")
    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
    diagnostics = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n", "10\n"],
        test_outputs=["2", "21"],
        language="python",
        per_test_timeout=5.0,
        max_tests=15,
        memory_mb=512,
        return_diagnostics=True,
    )
    assert diagnostics["reward"] == 0.5
    assert diagnostics["sampled_tests"] == 2.0
    assert diagnostics["passed_tests"] == 1.0
    assert diagnostics["wrong_answer_tests"] == 1.0
    assert diagnostics["no_code"] == 0.0


def test_reward_fn_cpp_pass_rate():
    if not TOOLCHAIN.get("cpp"):
        pytest.skip("cpp toolchain not available")
    completion = (
        "```cpp\n"
        "#include <iostream>\nint main(){int x; std::cin >> x; std::cout << x * 2 << '\\n';}\n"
        "```\n"
    )
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["3\n", "7\n"],
        test_outputs=["6", "14"],
        language="cpp",
        per_test_timeout=10.0,
        max_tests=15,
        memory_mb=1024,
        compile_timeout=30.0,
    )
    assert reward == 1.0


def test_reward_fn_no_fence_returns_zero():
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions="I would solve this by ...",  # no code block
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n"],
        test_outputs=["2"],
        language="python",
    )
    assert reward == 0.0


def test_reward_fn_compile_failure_returns_zero():
    if not TOOLCHAIN.get("cpp"):
        pytest.skip("cpp toolchain not available")
    completion = "```cpp\nint main(){ return 0 }\n```"
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n"],
        test_outputs=["2"],
        language="cpp",
        compile_timeout=10.0,
    )
    assert reward == 0.0
````

### Tool Output - 2026-04-27 23:59:12 EDT

```
Chunk ID: 5ec417
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 131
Output:
./areal/reward/code/competitive_exec.py:72:def nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:177:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:196:    diagnostics = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:224:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:241:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:257:    reward = nemotron_competitive_reward_fn(
```

### Assistant - 2026-04-27 23:59:49 EDT

```
已按这个方向改掉 `reward_kwargs`：code RL 不再传任何额外开关，code reward 自己返回诊断 dict；workflow 只是识别 dict 并记录额外 scalar。Math reward 仍然返回 float，所以不会出现这些 code 指标。
```

### Tool Call - 2026-04-27 23:59:49 EDT

```
exec_command {"cmd":"rg -n \"return_diagnostics|reward_kwargs\" areal examples tests -g '*.py'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:59:49 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:59:49 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol -- areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py | sed -n '1,220p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-27 23:59:49 EDT

```
Chunk ID: 737679
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:59:49 EDT

```
Chunk ID: 9a5036
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 69
Output:
 areal/reward/code/competitive_exec.py | 43 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 29 +++++++++++++++++------
 tests/test_code_sandbox_multilang.py  | 39 ++++++++++++++++++++++++-------
 3 files changed, 89 insertions(+), 22 deletions(-)
```

### Tool Output - 2026-04-27 23:59:49 EDT

```
Chunk ID: d29909
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 581
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..3717f9a5 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -89,7 +89,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,11 +97,11 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
@@ -109,7 +109,16 @@ class RLVRWorkflow(RolloutWorkflow):
             **task_data,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +142,15 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(
+            resp, prompt_str, task_data
+        )
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {"reward": reward}
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(
+            **scalars
+        )
 
         return resp, reward
```

### Tool Call - 2026-04-27 23:59:56 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol -- areal/reward/code/competitive_exec.py tests/test_code_sandbox_multilang.py | sed -n '1,260p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":26000}
```

### Tool Call - 2026-04-27 23:59:56 EDT

```
exec_command {"cmd":"git diff --check","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:59:56 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py areal/reward/code/competitive_exec.py tests/test_code_sandbox_multilang.py examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-27 23:59:56 EDT

````
Chunk ID: a1b2c6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1645
Output:
diff --git a/areal/reward/code/competitive_exec.py b/areal/reward/code/competitive_exec.py
index bcb65db3..8062c467 100644
--- a/areal/reward/code/competitive_exec.py
+++ b/areal/reward/code/competitive_exec.py
@@ -83,7 +83,7 @@ def nemotron_competitive_reward_fn(
     compile_timeout: float = 30.0,
     java_xmx_mb: int = 2048,
     **kwargs,
-) -> float:
+) -> dict[str, float]:
     """Pass rate over (a sample of) unit tests.
 
     :param language: Source language for the model output. May be overridden
@@ -98,17 +98,37 @@ def nemotron_competitive_reward_fn(
         languages only). Compile happens once per submission.
     :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
     """
+    diagnostics = {
+        "reward": 0.0,
+        "no_tests": 0.0,
+        "mismatched_tests": 0.0,
+        "no_code": 0.0,
+        "compile_error": 0.0,
+        "timeout_tests": 0.0,
+        "runtime_error_tests": 0.0,
+        "wrong_answer_tests": 0.0,
+        "passed_tests": 0.0,
+        "sampled_tests": 0.0,
+    }
+
+    def _finish(reward: float) -> dict[str, float]:
+        diagnostics["reward"] = reward
+        return diagnostics
+
     if not test_inputs or not test_outputs:
-        return 0.0
+        diagnostics["no_tests"] = 1.0
+        return _finish(0.0)
     if len(test_inputs) != len(test_outputs):
         logger.warning(
             f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
         )
-        return 0.0
+        diagnostics["mismatched_tests"] = 1.0
+        return _finish(0.0)
 
     extracted = extract_code(completions, expected_language=language)
     if extracted is None:
-        return 0.0
+        diagnostics["no_code"] = 1.0
+        return _finish(0.0)
     detected_lang, code = extracted
 
     artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
@@ -116,7 +136,8 @@ def nemotron_competitive_reward_fn(
         # Compile failure or unsupported language — caller already logged in
         # CompileResult.error/stderr; treat as zero reward.
         cleanup_compile_result(artifact)
-        return 0.0
+        diagnostics["compile_error"] = 1.0
+        return _finish(0.0)
 
     try:
         n = len(test_inputs)
@@ -127,6 +148,7 @@ def nemotron_competitive_reward_fn(
             # across all rollouts of the same problem in a GRPO group.
             rng = random.Random(prompt)
             idx = rng.sample(range(n), max_tests)
+        diagnostics["sampled_tests"] = float(len(idx))
 
         passed = 0
         for i in idx:
@@ -137,11 +159,18 @@ def nemotron_competitive_reward_fn(
                 memory_mb=memory_mb,
                 java_xmx_mb=java_xmx_mb,
             )
-            if result.timeout or result.error is not None or result.returncode != 0:
+            if result.timeout:
+                diagnostics["timeout_tests"] += 1.0
+                continue
+            if result.error is not None or result.returncode != 0:
+                diagnostics["runtime_error_tests"] += 1.0
                 continue
             if outputs_match(result.stdout, test_outputs[i]):
                 passed += 1
+            else:
+                diagnostics["wrong_answer_tests"] += 1.0
 
-        return passed / len(idx)
+        diagnostics["passed_tests"] = float(passed)
+        return _finish(passed / len(idx))
     finally:
         cleanup_compile_result(artifact)
diff --git a/tests/test_code_sandbox_multilang.py b/tests/test_code_sandbox_multilang.py
index f759a38a..4340119f 100644
--- a/tests/test_code_sandbox_multilang.py
+++ b/tests/test_code_sandbox_multilang.py
@@ -174,7 +174,7 @@ def test_reward_fn_python_pass_rate():
     if not TOOLCHAIN.get("python"):
         pytest.skip("python toolchain not available")
     completion = "Here's the answer:\n```python\nx = int(input())\nprint(x * 2)\n```\n"
-    reward = nemotron_competitive_reward_fn(
+    result = nemotron_competitive_reward_fn(
         prompt="dummy",
         completions=completion,
         prompt_ids=[],
@@ -186,7 +186,30 @@ def test_reward_fn_python_pass_rate():
         max_tests=15,
         memory_mb=512,
     )
-    assert reward == 1.0
+    assert result["reward"] == 1.0
+
+
+def test_reward_fn_reports_test_outcomes():
+    if not TOOLCHAIN.get("python"):
+        pytest.skip("python toolchain not available")
+    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
+    result = nemotron_competitive_reward_fn(
+        prompt="dummy",
+        completions=completion,
+        prompt_ids=[],
+        completion_ids=[],
+        test_inputs=["1\n", "10\n"],
+        test_outputs=["2", "21"],
+        language="python",
+        per_test_timeout=5.0,
+        max_tests=15,
+        memory_mb=512,
+    )
+    assert result["reward"] == 0.5
+    assert result["sampled_tests"] == 2.0
+    assert result["passed_tests"] == 1.0
+    assert result["wrong_answer_tests"] == 1.0
+    assert result["no_code"] == 0.0
 
 
 def test_reward_fn_cpp_pass_rate():
@@ -197,7 +220,7 @@ def test_reward_fn_cpp_pass_rate():
         "#include <iostream>\nint main(){int x; std::cin >> x; std::cout << x * 2 << '\\n';}\n"
         "```\n"
     )
-    reward = nemotron_competitive_reward_fn(
+    result = nemotron_competitive_reward_fn(
         prompt="dummy",
         completions=completion,
         prompt_ids=[],
@@ -210,11 +233,11 @@ def test_reward_fn_cpp_pass_rate():
         memory_mb=1024,
         compile_timeout=30.0,
     )
-    assert reward == 1.0
+    assert result["reward"] == 1.0
 
 
 def test_reward_fn_no_fence_returns_zero():
-    reward = nemotron_competitive_reward_fn(
+    result = nemotron_competitive_reward_fn(
         prompt="dummy",
         completions="I would solve this by ...",  # no code block
         prompt_ids=[],
@@ -223,14 +246,14 @@ def test_reward_fn_no_fence_returns_zero():
         test_outputs=["2"],
         language="python",
     )
-    assert reward == 0.0
+    assert result["reward"] == 0.0
 
 
 def test_reward_fn_compile_failure_returns_zero():
     if not TOOLCHAIN.get("cpp"):
         pytest.skip("cpp toolchain not available")
     completion = "```cpp\nint main(){ return 0 }\n```"
-    reward = nemotron_competitive_reward_fn(
+    result = nemotron_competitive_reward_fn(
         prompt="dummy",
         completions=completion,
         prompt_ids=[],
@@ -240,4 +263,4 @@ def test_reward_fn_compile_failure_returns_zero():
         language="cpp",
         compile_timeout=10.0,
     )
-    assert reward == 0.0
+    assert result["reward"] == 0.0
````

### Tool Output - 2026-04-27 23:59:56 EDT

```
Chunk ID: 053982
Wall time: 0.0289 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:59:56 EDT

```
Chunk ID: 8725c1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 87
Output:
areal/workflow/rlvr_qun_team.py:       Python script, Unicode text, UTF-8 text executable
areal/reward/code/competitive_exec.py: Python script, Unicode text, UTF-8 text executable
tests/test_code_sandbox_multilang.py:  Python script, Unicode text, UTF-8 text executable
examples/code/nemotron_code_rl.py:     Python script, ASCII text executable
```

### Tool Call - 2026-04-28 00:00:07 EDT

```
exec_command {"cmd":"python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-28 00:00:07 EDT

```
Chunk ID: badfdf
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-28 00:00:13 EDT

```
exec_command {"cmd":"git diff --check","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-28 00:00:13 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-28 00:00:13 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-28 00:00:13 EDT

```
Chunk ID: 0b7b1e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-28 00:00:13 EDT

```
Chunk ID: 93aaad
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 69
Output:
 areal/reward/code/competitive_exec.py | 45 ++++++++++++++++++++++++++++-------
 areal/workflow/rlvr_qun_team.py       | 29 ++++++++++++++++------
 tests/test_code_sandbox_multilang.py  | 39 +++++++++++++++++++++++-------
 3 files changed, 90 insertions(+), 23 deletions(-)
```

### Tool Output - 2026-04-28 00:00:13 EDT

```
Chunk ID: 8e8cb7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 32
Output:
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Assistant - 2026-04-28 00:00:24 EDT

```
对，这样更合理。我已经把 `reward_kwargs` 去掉了。

现在逻辑是：

- code reward 默认返回 `dict[str, float]`，里面包含 `reward` 和 code execution diagnostics。
- math reward 仍然返回 `float`，所以不会产生这些 code 指标。
- `RLVRWorkflow` 只做通用兼容：如果 reward 返回 dict，就取 `reward` 作为训练奖励，并把其它数值字段记录成 `reward_diagnostics/...`；如果返回 float，就按原逻辑走。

现在改动收敛到 3 个文件：

- [`areal/reward/code/competitive_exec.py`](/home/ag/projects/AReaL_v1/areal/reward/code/competitive_exec.py:72)：code reward 直接返回诊断 dict
- [`areal/workflow/rlvr_qun_team.py`](/home/ag/projects/AReaL_v1/areal/workflow/rlvr_qun_team.py:89)：兼容 float/dict reward 返回
- [`tests/test_code_sandbox_multilang.py`](/home/ag/projects/AReaL_v1/tests/test_code_sandbox_multilang.py:174)：更新测试断言

`examples/code/nemotron_code_rl.py` 不再需要改，训练 code RL 会自动记录这些指标。

验证：
- `python3 -m py_compile ...` 通过
- `git diff --check` 通过
- 全量 pytest 仍没跑：当前环境没有 `uv`，系统 `python3` 没有 `pytest`。
```

### User - 2026-04-28 00:01:35 EDT

推上去吧

### Assistant - 2026-04-28 00:01:46 EDT

我会按发布流程处理：先确认当前分支和待提交范围，只提交这 3 个代码/测试文件，不碰未跟踪的 `.codex`。

### Tool Call - 2026-04-28 00:01:47 EDT

```
exec_command {"cmd":"sed -n '1,220p' /home/ag/.codex/plugins/cache/openai-curated/github/6807e4de/skills/yeet/SKILL.md","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-28 00:01:47 EDT

```
Chunk ID: a81e1a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1005
Output:
---
name: "yeet"
description: "Publish local changes to GitHub by confirming scope, committing intentionally, pushing the branch, and opening a draft PR through the GitHub app from this plugin, with `gh` used only as a fallback where connector coverage is insufficient."
---

# GitHub Publish Changes

## Overview

Use this skill only when the user explicitly wants the full publish flow from the local checkout: branch setup if needed, staging, commit, push, and opening a pull request.

This workflow is hybrid:

- Use local `git` for branch creation, staging, commit, and push.
- Prefer the GitHub app from this plugin for pull request creation after the branch is on the remote.
- Use `gh` as a fallback for current-branch PR discovery, auth checks, or PR creation when the connector path cannot infer the repository or head branch cleanly.

## Prerequisites

- Require GitHub CLI `gh`. Check `gh --version`. If missing, ask the user to install `gh` and stop.
- Require authenticated `gh` session. Run `gh auth status`. If not authenticated, ask the user to run `gh auth login` (and re-run `gh auth status`) before continuing.
- Require a local git repository with a clean understanding of which changes belong in the PR.

## Naming conventions

- Branch: `codex/{description}` when starting from main/master/default.
- Commit: `{description}` (terse).
- PR title: `[codex] {description}` summarizing the full diff.

## Workflow

1. Confirm intended scope.
   - Run `git status -sb` and inspect the diff before staging.
   - If the working tree contains unrelated changes, do not default to `git add -A`. Ask the user which files belong in the PR.
2. Determine the branch strategy.
   - If on `main`, `master`, or another default branch, create `codex/{description}`.
   - Otherwise stay on the current branch.
3. Stage only the intended changes.
   - Prefer explicit file paths when the worktree is mixed.
   - Use `git add -A` only when the user has confirmed the whole worktree belongs in scope.
4. Commit tersely with the confirmed description.
5. Run the most relevant checks available if they have not already been run.
   - If checks fail due to missing dependencies or tools, install what is needed and rerun once.
6. Push with tracking: `git push -u origin $(git branch --show-current)`.
7. Open a draft PR.
   - Prefer the GitHub app from this plugin for PR creation after the push succeeds.
   - Derive `repository_full_name` from the remote, for example by normalizing `git remote get-url origin` or by using `gh repo view --json nameWithOwner`.
   - Derive `head_branch` from `git branch --show-current`.
   - Derive `base_branch` from the user request when specified; otherwise use the remote default branch, for example via `gh repo view --json defaultBranchRef`.
   - If the branch is being pushed from a fork or the PR target differs from the remote that was just pushed, prefer `gh pr create` fallback because the connector PR creation flow expects one repository target and may not encode cross-repo head semantics cleanly.
   - If connector-based PR creation cannot infer the repository or branch cleanly, fall back to `gh pr create --draft --fill --head $(git branch --show-current)`.
   - Write the PR body to a temp file with real newlines when using CLI fallback so the markdown renders cleanly.
8. Summarize the result with branch name, commit, PR target, validation, and anything the user still needs to confirm.

## Write Safety

- Never stage unrelated user changes silently.
- Never push without confirming scope when the worktree is mixed.
- Default to a draft PR unless the user explicitly asks for a ready-for-review PR.
- If the repository does not appear to be connected to an accessible GitHub remote, stop and explain the blocker before making assumptions.

## PR Body Expectations

The PR description should use real Markdown prose and cover:

- what changed
- why it changed
- the user or developer impact
- the root cause when the PR is a fix
- the checks used to validate it
```

### Assistant - 2026-04-28 00:01:57 EDT

我会使用 GitHub 发布技能里的本地提交/推送部分；你没有要求开 PR，所以我先不创建 PR。

### Tool Call - 2026-04-28 00:01:57 EDT

```
exec_command {"cmd":"git status -sb","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-28 00:01:57 EDT

```
exec_command {"cmd":"git branch --show-current","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-28 00:01:57 EDT

```
exec_command {"cmd":"git remote -v","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-28 00:01:57 EDT

```
exec_command {"cmd":"git diff --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-28 00:01:57 EDT

```
Chunk ID: edd917
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 42
Output:
## v1.0.1_dev_ag...origin/v1.0.1_dev_ag
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Tool Output - 2026-04-28 00:01:57 EDT

```
Chunk ID: 6e59eb
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 4
Output:
v1.0.1_dev_ag
```

### Tool Output - 2026-04-28 00:01:57 EDT

```
Chunk ID: e9bf49
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 140
Output:
origin	https://x-access-token:[REDACTED]@github.com/Holosemantix/AReaL_v1.git (fetch)
origin	https://x-access-token:[REDACTED]@github.com/Holosemantix/AReaL_v1.git (push)
origin	https://x-access-token:[REDACTED]@github.com/qun-team/AReaL_v1.git (push)
qun	https://x-access-token:[REDACTED]@github.com/qun-team/AReaL_v1.git (fetch)
qun	https://x-access-token:[REDACTED]@github.com/qun-team/AReaL_v1.git (push)
```

### Tool Output - 2026-04-28 00:01:57 EDT

```
Chunk ID: 9741f4
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 69
Output:
 areal/reward/code/competitive_exec.py | 45 ++++++++++++++++++++++++++++-------
 areal/workflow/rlvr_qun_team.py       | 29 ++++++++++++++++------
 tests/test_code_sandbox_multilang.py  | 39 +++++++++++++++++++++++-------
 3 files changed, 90 insertions(+), 23 deletions(-)
```

### Assistant - 2026-04-28 00:02:05 EDT

当前分支是 `v1.0.1_dev_ag`，工作区里只有这 3 个相关修改和未跟踪 `.codex`。我会显式 stage 这 3 个文件，提交后推当前分支到 `origin`。

### Tool Call - 2026-04-28 00:02:05 EDT

```
exec_command {"cmd":"git diff --check","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-28 00:02:05 EDT

```
exec_command {"cmd":"python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-28 00:02:05 EDT

```
Chunk ID: a450a5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-28 00:02:05 EDT

```
Chunk ID: 790a10
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-28 00:02:09 EDT

```
exec_command {"cmd":"git add areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-28 00:02:10 EDT

```
Chunk ID: 428124
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-28 00:02:13 EDT

```
exec_command {"cmd":"git status -sb","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-28 00:02:13 EDT

```
exec_command {"cmd":"git diff --cached --ignore-space-at-eol --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Output - 2026-04-28 00:02:13 EDT

```
Chunk ID: dcd3a1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 42
Output:
## v1.0.1_dev_ag...origin/v1.0.1_dev_ag
M  areal/reward/code/competitive_exec.py
M  areal/workflow/rlvr_qun_team.py
M  tests/test_code_sandbox_multilang.py
?? .codex
```

### Tool Output - 2026-04-28 00:02:13 EDT

```
Chunk ID: 36f846
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 69
Output:
 areal/reward/code/competitive_exec.py | 45 ++++++++++++++++++++++++++++-------
 areal/workflow/rlvr_qun_team.py       | 29 ++++++++++++++++------
 tests/test_code_sandbox_multilang.py  | 39 +++++++++++++++++++++++-------
 3 files changed, 90 insertions(+), 23 deletions(-)
```

### Assistant - 2026-04-28 00:02:16 EDT

验证通过，stage 范围也只包含预期 3 个文件。现在提交。

### Tool Call - 2026-04-28 00:02:17 EDT

```
exec_command {"cmd":"git commit -m \"feat: record code reward diagnostics\"","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-28 00:02:18 EDT

```
Chunk ID: 924aa7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 29
Output:
[v1.0.1_dev_ag 152e635b] feat: record code reward diagnostics
 3 files changed, 496 insertions(+), 429 deletions(-)
```

### Assistant - 2026-04-28 00:02:23 EDT

提交完成。这个提交里行数看起来偏大，主要是 `rlvr_qun_team.py` 按你的要求从 CRLF 规范成了 LF；语义改动仍是刚才那 3 处。现在推送到 `origin/v1.0.1_dev_ag`。

### Tool Call - 2026-04-28 00:02:25 EDT

```
exec_command {"cmd":"git -C /home/ag/projects/AReaL_v1 push origin v1.0.1_dev_ag","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":30000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-28 00:02:26 EDT

```
Chunk ID: a81c8f
Wall time: 1.7561 seconds
Process exited with code 0
Original token count: 50
Output:
To https://github.com/Holosemantix/AReaL_v1.git
   c29bf431..152e635b  v1.0.1_dev_ag -> v1.0.1_dev_ag
To https://github.com/qun-team/AReaL_v1.git
   c29bf431..152e635b  v1.0.1_dev_ag -> v1.0.1_dev_ag
```

### Tool Call - 2026-04-28 00:02:33 EDT

```
exec_command {"cmd":"git status -sb","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-28 00:02:33 EDT

```
exec_command {"cmd":"git log -1 --oneline","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-28 00:02:33 EDT

```
Chunk ID: b8f987
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 13
Output:
## v1.0.1_dev_ag...origin/v1.0.1_dev_ag
?? .codex
```

### Tool Output - 2026-04-28 00:02:33 EDT

```
Chunk ID: 323976
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 12
Output:
152e635b feat: record code reward diagnostics
```

### Assistant - 2026-04-28 00:02:41 EDT

```
已推上去。

Commit: `152e635b feat: record code reward diagnostics`  
Branch: `v1.0.1_dev_ag`

Push 结果：
- `Holosemantix/AReaL_v1`: `v1.0.1_dev_ag -> v1.0.1_dev_ag`
- `qun-team/AReaL_v1`: `v1.0.1_dev_ag -> v1.0.1_dev_ag`

验证已跑：
- `git diff --check`
- `python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py tests/test_code_sandbox_multilang.py`

工作区现在只剩未跟踪的 `.codex`，我没动。
```
