# Codex Conversation Export

- Exported at: 2026-04-28 03:27:26 EDT
- Sessions: 1

## Session 019dd21f-81a8-7553-874c-9d73ec55e30f

- Started: 2026-04-27 23:26:13 EDT
- Source: `/home/ag/.codex/sessions/2026/04/27/rollout-2026-04-27T23-26-13-019dd21f-81a8-7553-874c-9d73ec55e30f.jsonl`
- CWD: `/home/ag/projects/AReaL_v1`

### User - 2026-04-27 23:28:33 EDT

````
# AGENTS.md instructions for /home/ag/projects/AReaL_v1

<INSTRUCTIONS>
<!-- Go-to brief for AI coding agents working on AReaL. -->

# AGENTS.md -- AReaL Agent Operations Guide

## Quick reference

**Tech stack**: Python 3.12+ | PyTorch | FSDP2 / Megatron / Archon | SGLang / vLLM

```bash
# Environment
uv sync --extra cuda            # dependencies (or `uv sync` without CUDA)
source .venv/bin/activate        # activate venv BEFORE pre-commit or git commit if venv exists
pre-commit install               # formatting hooks (Ruff, mdformat, clang-format, nbstripout, autoflake)
pre-commit run --all-files       # lint + format everything

# Tests
uv run pytest tests/test_<topic>.py

# CLI docs
uv run python docs/generate_cli_docs.py
```

**Hard rules** -- never violate:

- No wildcard imports (`from x import *`).
- No hardcoded secrets, paths, or endpoints.
- No skipping pre-commit hooks.
- No guessing cluster configs or rebuilding CUDA/driver stacks.
- Integration tests require multi-node hardware -- explain skips explicitly.

**Always do**:

- Read relevant files before modifying code.
- Run `pre-commit run --all-files` before committing.
- Follow existing code patterns in the same module.
- Add tests for new functionality.
- Use the `question` tool (structured options) when asking the user for decisions or
  clarifications.

**Ask first** before:

- Modifying config structures in `areal/api/cli_args.py`.
- Adding new dependencies.
- Changing launcher or scheduler logic.
- Deleting or renaming public APIs.

When unsure, leave a `TODO(agent)` comment and note the constraint in your response.

______________________________________________________________________

## Repository map

```
areal/                     Core Python package
|-- api/                   Config dataclasses, contracts, IO structs
|-- dataset/               Stateful dataset loaders (GSM8K, Geometry3K, CLEVR, ...)
|-- engine/                Training backends (FSDP2, Megatron) + inference adapters
|-- experimental/          Prototype engines/workflows (Archon MoE engine)
|-- infra/                 Launchers (Local/Ray/Slurm), schedulers, utilities
|-- models/                Model adapters (Megatron-Core, Transformers, custom heads)
|-- reward/                Built-in reward functions + math parsers
|-- tests/                 Unit/integration test suites
|-- trainer/               High-level orchestrators (PPOTrainer, SFTTrainer)
|-- utils/                 Cross-cutting helpers (logging, data, checkpoints, RL ops)
+-- workflow/              RolloutWorkflow implementations (RLVR, multi-turn, vision)

docs/                      Jupyter Book docs (https://inclusionai.github.io/AReaL/)
examples/                  Training scripts and launcher recipes
```

______________________________________________________________________

## Code style & patterns

- **Composition over inheritance** -- keep hierarchies \<= 2 levels; prefer delegation.

| Type             | Pattern         | Example                                   |
| ---------------- | --------------- | ----------------------------------------- |
| Config dataclass | `XxxConfig`     | `GRPOConfig`, `FSDPEngineConfig`          |
| Engine class     | `XxxEngine`     | `FSDPEngine`, `ArchonEngine`              |
| Workflow class   | `XxxWorkflow`   | `RLVRWorkflow`, `MultiTurnWorkflow`       |
| Reward function  | `xxx_reward_fn` | `gsm8k_reward_fn`, `geometry3k_reward_fn` |

**Logging**: `areal.utils.logging.getLogger(name)` with **PascalCase** names -- never
`print` or `logging.__name__`. Per-rank format: `[{Component} Rank {N}]`. Register new
loggers with color in `areal/utils/logging.py`.

**Performance**:

- No GPU-CPU sync in hot paths (`.item()`, `.tolist()`, `print(tensor)`).
- Batch ops over Python loops on tensor elements.
- Explicit `dtype`/`device`; `torch.Size` assertions for shape validation.

**Typing & imports**: explicit type hints; reuse `areal/api/cli_args.py` dataclasses; no
wildcard imports; heavy optional deps inside functions.

**Async**: rollout workflows must stay non-blocking (`await` + `aiofiles`); no sync I/O
in `arun_episode`.

______________________________________________________________________

## Domain experts & skills

Fire the appropriate **expert subagent** or **load a skill** based on what you're
working on. Experts are read-only consultants with deep domain knowledge; skills are
step-by-step implementation guides.

| Working on...                | Fire subagent      | Load skill          |
| ---------------------------- | ------------------ | ------------------- |
| FSDP engine code             | `fsdp-expert`      | --                  |
| Archon engine / new model    | `archon-expert`    | `add-archon-model`  |
| Megatron engine code         | `megatron-expert`  | --                  |
| RL algorithms / PPO / GRPO   | `algorithm-expert` | --                  |
| Launcher / scheduler / infra | `launcher-expert`  | `debug-distributed` |
| New reward function          | --                 | `add-reward`        |
| New dataset loader           | --                 | `add-dataset`       |
| New rollout workflow         | --                 | `add-workflow`      |
| Unit tests                   | --                 | `add-unit-tests`    |
| Distributed debugging        | --                 | `debug-distributed` |

**How to fire an expert**:
`task(subagent_type="fsdp-expert", load_skills=[], run_in_background=true, prompt="...")`

______________________________________________________________________

## Core concepts

**Trainer** orchestrator (`areal/trainer/`, `PPOTrainer`, `SFTTrainer`): manages the
training loop, dataset loading, and workflow execution. Entry point:
`examples/math/gsm8k_rl.py`.

**Rollout workflows** (`areal/workflow/`, `RolloutWorkflow.arun_episode`): define how
episodes are generated. Use `add-workflow` skill for step-by-step guide.

**Engines**: *Inference engines* handle async generation via `engine.agenerate()` and
manage weight updates. *Training engines* consume rollout tensors, compute PPO/GRPO
updates, and broadcast weight versions (FSDP2, Megatron, or Archon).

**Weight versioning**: async workflows require version alignment via `WeightUpdateMeta`
(`areal/api/engine_api.py`). Critical for correctness across distributed training.

**Observability**: emit metrics via `stats_tracker.get()`, persist artifacts under
`dump_dir`, checkpoint via `areal/utils/saver.py` / `recover.py`.

**Launcher / scheduler**: training requires cluster setup (local / Ray / Slurm) via
configs in `areal/infra/launcher/`. See `launcher-expert` for deployment guidance.

______________________________________________________________________

## API & config rules

*Applies to: `areal/api/**`*

- **Field ordering**: required -> common optional -> rare optional -> internal (`_`
  prefix).
- **Validation**: `__post_init__` with `ValueError` and clear message.
- **Backward compat**: add fields with defaults; deprecate before removing; avoid type
  changes.
- **CLI**: use `Literal` for enum choices; all public configs need docstrings with
  constraints.

______________________________________________________________________

## Distributed code rules

*Applies to: `areal/engine/**`, `areal/experimental/**`*

- Never create global process groups at module level; always pass `process_group`
  explicitly.
- `dist.get_rank(group)` not `dist.get_rank()` when group matters.
- DeviceMesh dimensions must match `ArchonParallelDims`: `dp_shard`, `tp`, `cp`, `ep`,
  `etp`.
- All-reduce: all ranks must call. Broadcast: explicit `src`. Barrier: debugging only.

| Issue         | Cause                            | Fix                       |
| ------------- | -------------------------------- | ------------------------- |
| Hang          | Mismatched collective calls      | All ranks call same op    |
| Wrong results | Incorrect `ReduceOp`             | Check SUM vs MEAN         |
| OOM           | Unsharded tensor on wrong device | Verify DTensor placements |

Debug env vars: `TORCH_DISTRIBUTED_DEBUG=DETAIL`, `NCCL_DEBUG=INFO`,
`CUDA_LAUNCH_BLOCKING=1`. See `/debug-distributed` skill for comprehensive guide.

______________________________________________________________________

## Testing rules

*Applies to: `**/tests/**`, `test_*.py`*

| Marker                                  | When                             |
| --------------------------------------- | -------------------------------- |
| `@pytest.mark.slow`                     | > 10s (excluded from default CI) |
| `@pytest.mark.slow` + `@pytest.mark.ci` | Slow but must run in CI          |
| `@pytest.mark.asyncio`                  | Async tests                      |

- Naming: `test_<what>_<condition>_<expected>()` with Arrange/Act/Assert.
- GPU: skip gracefully (`@pytest.mark.skipif(not CUDA_AVAILABLE, reason="...")`).
- Distributed mocking: `torch.distributed.fake_pg`; don't mock FSDP/DTensor internals.
- Assertions: `torch.testing.assert_close()` with explicit `rtol`/`atol`; prefer
  `tmp_path`, `monkeypatch`.

| Suite       | Command                       | GPU       |
| ----------- | ----------------------------- | --------- |
| Unit        | `pytest tests/test_*.py`      | No        |
| GRPO        | `pytest tests/grpo/`          | Yes       |
| FSDP        | `pytest tests/test_fsdp_*.py` | Yes       |
| Distributed | `pytest tests/torchrun/`      | Multi-GPU |

______________________________________________________________________

## Collaboration & review

- **Branches**: kebab-case (`feature/multi-turn-metrics`, `bugfix/fsdp-weight-sync`).
- **Commits**: Conventional Commits (`feat:`, `fix:`, `docs:`), ~72 char subject,
  imperative voice. Squash WIP before PR.
- **Pre-merge**: full pre-commit stack; doc-only edits need at least `mdformat --check`.
- **PRs**: tie to issue, highlight risk areas, list test commands executed, note skipped
  suites with reasons.

| Command / Skill      | Purpose                                              |
| -------------------- | ---------------------------------------------------- |
| `/create-pr`         | Rebase, squash, create PR                            |
| `commit-conventions` | Commit message conventions (auto-triggers on commit) |
| `/review-pr`         | Dynamic agent-allocated PR review                    |

______________________________________________________________________

## Reference material

- **Docs portal**: <https://inclusionai.github.io/AReaL/>
- **Quickstart**: `docs/tutorial/quickstart.md`
- **Architecture**: `docs/tutorial/gsm8k_grpo.md`
- **Customization**: `docs/customization/*.md`
- **Algorithms**: `docs/algorithms/*.md`
- **Best practices**: `docs/best_practices/*.md`
- **CLI reference**: `docs/cli_reference.md`
- **Agent workflow**: `docs/customization/agent.md`

</INSTRUCTIONS>
<environment_context>
  <cwd>/home/ag/projects/AReaL_v1</cwd>
  <shell>zsh</shell>
  <current_date>2026-04-27</current_date>
  <timezone>US/Eastern</timezone>
</environment_context>
````

### User - 2026-04-27 23:28:33 EDT

```
使用小模型训练，启动命令如下，/opt/schedule-train/algorithm/examples/code/nemotron_code_rl.py --config examples/code/grpo_template_code.yaml experiment_name=ailab_slm_0_5b_think_nemotron_coding trial_name=mtp_grpo_muon_16k_groupsize_16_lr_4e-5_20260425 total_train_epochs=500 allocation_mode=sglang:d8p1t1+fsdp:d8p1t1 cluster.fileroot=/opt/dataset/ag_data/logs/areal/experiments cluster.name_resolve.nfs_record_root=/opt/dataset/ag_data/logs/areal/name_resolve gconfig.n_samples=16 gconfig.temperature=1 gconfig.max_new_tokens=16384 gconfig.enable_thinking=true eval_gconfig.n_samples=32 eval_gconfig.temperature=0.7 eval_gconfig.top_p=0.95 eval_gconfig.top_k=20 eval_gconfig.max_new_tokens=16384 rollout.max_concurrent_rollouts=256 rollout.max_head_offpolicyness=2 rollout.dump_to_file=false actor.path=/opt/dataset/ag_models_nlp/ailab/ailab_slm_0_5b____v2____4m-lr_3e-4-profit_weight_0.3-iter32800 actor.optimizer.lr=4e-5 actor.optimizer.type=muon actor.importance_sampling_level=token actor.kl_ctl=0 actor.use_sapo_loss=false actor.mb_spec.max_tokens_per_mb=18432 ref.path=/opt/dataset/ag_models_nlp/ailab/ailab_slm_0_5b____v2____4m-lr_3e-4-profit_weight_0.3-iter32800 sglang.mem_fraction_static=0.6 sglang.context_length=18432 train_dataset.path=/opt/dataset/ag_data/data/nvidia/Nemotron-RL-coding-competitive_coding train_dataset.batch_size=32 train_dataset.max_length=2048 valid_dataset.batch_size=1 saver.freq_epochs=null saver.freq_steps=null recover.mode=auto evaluator.freq_epochs=1 evaluator.freq_steps=100 stats_logger.swanlab.mode=cloud stats_logger.swanlab.logdir=/opt/dataset/ag_data/logs/areal/swanlab stats_logger.swanlab.api_key=[REDACTED] cluster.n_nodes=2 cluster.n_gpus_per_node=8 scheduler.type=ray ，训练数据都是python 代码，rollout/rewrad一直0.2-0.4之间横盘震荡，是因为太难了吗，需要筛一下数据南都
```

### Assistant - 2026-04-27 23:28:51 EDT

我先看一下这条训练入口和代码任务的 reward/数据处理逻辑，重点确认 0.2-0.4 这个 reward 到底代表什么，以及是否存在长度截断、格式失败或测试样例通过率的问题。

### Tool Call - 2026-04-27 23:28:51 EDT

```
exec_command {"cmd":"rg -n \"Nemotron|nemotron|reward|pass|code|competitive\" examples areal tests -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:28:52 EDT

```
exec_command {"cmd":"sed -n '1,260p' examples/code/grpo_template_code.yaml","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:28:52 EDT

```
exec_command {"cmd":"sed -n '1,320p' examples/code/nemotron_code_rl.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:28:52 EDT

```
Chunk ID: 67bae4
Wall time: 0.0283 seconds
Process exited with code 0
Original token count: 57371
Output:
Total output lines: 2463

areal/version.py:54:                f"{version} ('-dirty' means there are uncommitted code changes in git)"
areal/version.py:67:                .decode("utf-8")
areal/version.py:76:                .decode("utf-8")
areal/version.py:84:            self.__is_dirty__ = git_diff_result.returncode != 0
areal/version.py:86:            pass
examples/agent_workflow/config.yaml:74:  reward_scaling: 10.0
examples/agent_workflow/config.yaml:75:  reward_bias: -0.5
examples/agent_workflow/config.yaml:81:  reward_norm: null
examples/agent_workflow/config_claude.yaml:1:experiment_name: gsm8k-grpo-claude-code
examples/agent_workflow/config_claude.yaml:75:  reward_scaling: 10.0
examples/agent_workflow/config_claude.yaml:76:  reward_bias: -0.5
examples/agent_workflow/config_claude.yaml:82:  reward_norm: null
examples/agent_workflow/README.md:25:   final reward, or a dict where the reward of each interaction is keyed by the
examples/agent_workflow/README.md:42:3. Place your agent code in a path that can be imported in Python (e.g.,
examples/experimental/prox_approx/gsm8k_grpo_prox_approx.yaml:63:  reward_scaling: 10.0
examples/experimental/prox_approx/gsm8k_grpo_prox_approx.yaml:64:  reward_bias: -0.5
examples/experimental/prox_approx/gsm8k_grpo_prox_approx.yaml:70:  reward_norm:
examples/experimental/prox_approx/gsm8k_grpo_prox_approx_eval.yaml:63:  reward_scaling: 10.0
examples/experimental/prox_approx/gsm8k_grpo_prox_approx_eval.yaml:64:  reward_bias: -0.5
examples/experimental/prox_approx/gsm8k_grpo_prox_approx_eval.yaml:70:  reward_norm:
areal/engine/vllm_ext/vllm_worker_extension.py:20:    Iherited from vllm codebase
areal/engine/vllm_ext/vllm_worker_extension.py:153:        # NOTE: This code relies on vLLM private APIs: _adapter_manager, _registered_adapters,
examples/vlm_npu/geometry3k_grpo.py:12:def format_reward(predict_str: str) -> float:
examples/vlm_npu/geometry3k_grpo.py:18:def acc_reward(predict_str: str, ground_truth: str) -> float:
examples/vlm_npu/geometry3k_grpo.py:23:def geometry3k_reward_fn(
examples/vlm_npu/geometry3k_grpo.py:26:    format_reward_val = format_reward(completions)
examples/vlm_npu/geometry3k_grpo.py:27:    acc_reward_val = acc_reward(completions, answer)
examples/vlm_npu/geometry3k_grpo.py:29:    score = (1.0 - format_score) * (acc_reward_val) + format_score * format_reward_val
examples/vlm_npu/geometry3k_grpo.py:52:        reward_fn=geometry3k_reward_fn,
areal/engine/vllm_ext/areal_vllm_server.py:99:        return JSONResponse(content, status_code=200)
areal/engine/vllm_ext/areal_vllm_server.py:101:        return JSONResponse(content, status_code=400)
examples/vlm_npu/qwen3_vl_2b_geometry3k_grpo.yaml:64:  reward_scaling: 10.0
examples/vlm_npu/qwen3_vl_2b_geometry3k_grpo.yaml:65:  reward_bias: -0.5
examples/vlm_npu/qwen3_vl_2b_geometry3k_grpo.yaml:71:  reward_norm:
areal/engine/sglang_remote.py:110:                pybase64.b64decode(routed_experts.encode("utf-8")), dtype=np.int32
examples/vlm_npu/virl39k_grpo.py:12:def format_reward(predict_str: str) -> float:
examples/vlm_npu/virl39k_grpo.py:18:def acc_reward(predict_str: str, ground_truth: str) -> float:
examples/vlm_npu/virl39k_grpo.py:23:def virl39k_reward_fn(
examples/vlm_npu/virl39k_grpo.py:26:    format_reward_val = format_reward(completions)
examples/vlm_npu/virl39k_grpo.py:27:    acc_reward_val = acc_reward(completions, answer)
examples/vlm_npu/virl39k_grpo.py:29:    score = (1.0 - format_score) * (acc_reward_val) + format_score * format_reward_val
examples/vlm_npu/virl39k_grpo.py:53:        reward_fn=virl39k_reward_fn,
examples/vlm_npu/qwen2_5_vl_3b_geometry3k_grpo.yaml:64:  reward_scaling: 10.0
examples/vlm_npu/qwen2_5_vl_3b_geometry3k_grpo.yaml:65:  reward_bias: -0.5
examples/vlm_npu/qwen2_5_vl_3b_geometry3k_grpo.yaml:71:  reward_norm:
areal/engine/fsdp_utils/__init__.py:41:        pass
examples/vlm_npu/README.md:108:26.3 vs. 26.5). With additional training (Epoch 3), AReaL surpasses verl in overall
examples/tau2/config_8b_airline.yaml:73:  reward_scaling: 10.0
examples/tau2/config_8b_airline.yaml:74:  reward_bias: -0.5
examples/tau2/config_8b_airline.yaml:80:  reward_norm: null
examples/tau2/config_235b_moe_airline.yaml:108:  reward_scaling: 10.0
examples/tau2/config_235b_moe_airline.yaml:109:  reward_bias: -0.5
examples/tau2/config_235b_moe_airline.yaml:110:  reward_clip: 20.0
examples/tau2/utils.py:98:    reward: float
examples/tau2/utils.py:103:    reward_info: RewardInfo | None = None
examples/tau2/utils.py:107:        s = f"[REWARD]: {self.reward}\n\n"
examples/tau2/utils.py:110:        if self.reward_info:
examples/tau2/utils.py:112:            s += yaml.dump(self.reward_info.model_dump()) + "\n"
areal/engine/fsdp_engine.py:133:    """Context passed through FSDP forward/backward pipeline.
areal/engine/fsdp_engine.py:138:        The prepared inputs passed to the model (may include Ulysses slicing).
areal/engine/fsdp_engine.py:183:        # when multiple FSDP actors concurrently load trust_remote_code models.
areal/engine/fsdp_engine.py:195:            trust_remote_code=True,
areal/engine/fsdp_engine.py:579:            # Release tree attention metadata after forward pass
areal/engine/fsdp_engine.py:782:                trust_remote_code=True,
areal/engine/fsdp_engine.py:818:                    trust_remote_code=True,
areal/engine/fsdp_engine.py:1759:            logger.warning("mb_spec.granularity must be 2 for reward modeling")
examples/tau2/train.py:63:    return x["rewards"].mean() <= 0.95
examples/tau2/config_30b_moe_airline.yaml:108:  reward_scaling: 10.0
examples/tau2/config_30b_moe_airline.yaml:109:  reward_bias: -0.5
examples/tau2/config_30b_moe_airline.yaml:110:  reward_clip: 20.0
examples/tau2/agent.py:231:            reward=0.0,
examples/tau2/agent.py:236:            reward_info=None,
examples/tau2/agent.py:251:                f"Error running simulation: {e}. Setting reward to 0.0"
examples/tau2/agent.py:258:            reward_info = evaluate_simulation(
examples/tau2/agent.py:265:            run_info.reward_info = reward_info
examples/tau2/agent.py:266:            run_info.reward = reward_info.reward
examples/tau2/agent.py:271:                f"Error evaluating simulation: {e}. Setting reward to 0.0"
examples/tau2/agent.py:273:            run_info.reward_info = None
examples/tau2/agent.py:280:            f"Reward: {reward_info.reward}"
examples/tau2/agent.py:325:            float: The reward from the simulation
examples/tau2/agent.py:392:        # Return the reward
examples/tau2/agent.py:393:        # The proxy server handles tracking completions and assigning rewards
examples/tau2/agent.py:394:        return run_info.reward
areal/engine/megatron_utils/packed_context_parallel.py:162:            f"Error occurred in packed context parallel forward pass on model {model} "
examples/tau2/README.md:126:The following figure shows the training reward curves for the two configurations,
examples/tau2/README.md:130:  <img src="reward.png" width="400">
examples/tau2/README.md:138:figure shows the training reward curve of Qwen3-30B-A3B model trained using the Megatron
examples/tau2/README.md:142:  <img src="reward_moe.png" width="400">
examples/tau2/README.md:145:For reward curves of experiments on a larger scale, please refer to the
examples/tau2/config_1.7b_airline.yaml:73:  reward_scaling: 10.0
examples/tau2/config_1.7b_airline.yaml:74:  reward_bias: -0.5
examples/tau2/config_1.7b_airline.yaml:80:  reward_norm: null
examples/docs/debug/cmp_rollout.py:20:            byte_image = base64.b64encode(buffer.read()).decode("utf-8")
examples/docs/debug/cmp_rollout.py:82:    hf_results = processor.decode(generation, skip_special_tokens=True)
examples/docs/debug/cmp_rollout.py:96:    sglang_results = processor.decode(
examples/code/nemotron_configs.py:1:"""Configuration dataclasses for Nemotron code RL examples.
examples/code/nemotron_configs.py:22:    reward_mode: str = field(default="prob")
examples/code/nemotron_configs.py:32:    ig_reward_params: InfoGainRewardConfig | None = field(default=None)
examples/code/nemotron_configs.py:37:    """Budget for the competitive-coding execution reward.
examples/code/nemotron_configs.py:41:    Note: with a string-based ``reward_fn`` (required for RPC serialization),
examples/code/nemotron_configs.py:42:    the per-test fields below currently document the reward fn's defaults.
examples/code/nemotron_configs.py:44:    ``areal.reward.code.competitive_exec.nemotron_competitive_reward_fn`` or
examples/code/nemotron_configs.py:46:    into the reward fn as kwargs).
examples/code/nemotron_configs.py:52:    reward_timeout_seconds: float = field(default=90.0)
examples/code/nemotron_configs.py:66:    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
areal/engine/megatron_utils/fp8/quantize.py:96:        if megatron_name.startswith("decoder."):
areal/engine/megatron_utils/fp8/quantize.py:101:    decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/fp8/quantize.py:102:    match = re.match(decoder_layers_pattern, megatron_name)
examples/code/nemotron_code_rl.py:1:"""GRPO training on Nemotron-RL-coding-competitive_coding.
examples/code/nemotron_code_rl.py:4:  dataset   areal.dataset.code.nemotron_competitive
examples/code/nemotron_code_rl.py:5:  reward    areal.reward.code.competitive_exec (subprocess sandbox, pass rate)
examples/code/nemotron_code_rl.py:6:  workflow  areal.workflow.rlvr_qun_team.RLVRWorkflow (with extended reward timeout)
examples/code/nemotron_code_rl.py:9:  ``reward_fn`` and ``workflow`` are passed as import-path strings so they
examples/code/nemotron_code_rl.py:11:  ``areal.reward.code.nemotron_competitive_reward_fn`` match the
examples/code/nemotron_code_rl.py:12:  ``code_reward`` fields in the YAML template.
examples/code/nemotron_code_rl.py:17:from examples.code.nemotron_configs import CodeGRPOConfig
examples/code/nemotron_code_rl.py:31:    from areal.reward.code import assert_toolchain_or_warn
examples/code/nemotron_code_rl.py:33:    assert_toolchain_or_warn(list(config.code_reward.probe_languages))
examples/code/nemotron_code_rl.py:35:    if config.actor.ig_reward_params is not None:
examples/code/nemotron_code_rl.py:36:        config.actor.ig_reward_params.sep_token_id = tokenizer.encode(
examples/code/nemotron_code_rl.py:37:            config.actor.ig_reward_params.sep_token, add_special_tokens=False
examples/code/nemotron_code_rl.py:39:        config.actor.ig_reward_params.pad_token_id = tokenizer.pad_token_id
examples/code/nemotron_code_rl.py:63:            reward_fn="areal.reward.code.nemotron_competitive_reward_fn",
examples/code/nemotron_code_rl.py:67:            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
examples/code/nemotron_code_rl.py:77:        workflow_kwargs["ig_reward_params"] = config.actor.ig_reward_params
examples/code/grpo_template_code.yaml:1:experiment_name: qwen3_0p6b_nemotron_code_grpo
examples/code/grpo_template_code.yaml:82:  reward_scaling: 1.0
examples/code/grpo_template_code.yaml:83:  reward_bias: 0.0
examples/code/grpo_template_code.yaml:93:  reward_norm:
examples/code/grpo_template_code.yaml:147:# Reward — competitive coding execution budget (per-test / per-sample)
examples/code/grpo_template_code.yaml:149:# max_tests             : upper bound on tests run per reward call (subsample)
examples/code/grpo_template_code.yaml:151:# reward_timeout_seconds: total AsyncRewardWrapper timeout per sample
examples/code/grpo_template_code.yaml:157:code_reward:
examples/code/grpo_template_code.yaml:161:  reward_timeout_seconds: 90.0
examples/code/grpo_template_code.yaml:172:# `path` accepts: HF hub name (e.g., `nvidia/Nemotron-RL-coding-competitive_coding`),
examples/code/grpo_template_code.yaml:179:  path: /opt/huawei/dataset/ag_data_nlp/RL/Nemotron-RL-coding-competitive_coding
examples/vlm/geometry3k_grpo.py:12:def format_reward(predict_str: str) -> float:
examples/vlm/geometry3k_grpo.py:18:def acc_reward(predict_str: str, ground_truth: str) -> float:
examples/vlm/geometry3k_grpo.py:23:def geometry3k_reward_fn(
examples/vlm/geometry3k_grpo.py:26:    format_reward_val = format_reward(completions)
examples/vlm/geometry3k_grpo.py:27:    acc_reward_val = acc_reward(completions, answer)
examples/vlm/geometry3k_grpo.py:29:    score = (1.0 - format_score) * (acc_reward_val) + format_score * format_reward_val
examples/vlm/geometry3k_grpo.py:52:        reward_fn="examples.vlm.geometry3k_grpo.geometry3k_reward_fn",
areal/engine/megatron_utils/megatron.py:142:    if name == "module.module.decoder.final_layernorm.weight":
areal/engine/megatron_utils/megatron.py:158:    decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/megatron.py:159:    match = re.match(decoder_layers_pattern, name)
areal/engine/megatron_utils/megatron.py:299:    if name == "module.module.decoder.final_layernorm.weight":
areal/engine/megatron_utils/megatron.py:315:    decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/megatron.py:316:    match = re.match(decoder_layers_pattern, name)
areal/engine/megatron_utils/megatron.py:389:    if name == "module.module.decoder.final_layernorm.weight":
areal/engine/megatron_utils/megatron.py:402:    decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/megatron.py:403:    match = re.match(decoder_layers_pattern, name)
areal/engine/megatron_utils/megatron.py:618:            decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/megatron.py:619:            match = re.match(decoder_layers_pattern, name)
areal/engine/megatron_utils/megatron.py:646:            # this is hardcoded for te grouped matmul
areal/engine/megatron_utils/megatron.py:653:                    f"module.module.decoder.layers.{layer_idx}.mlp.experts.{rest}.weight{expert_idx}",
areal/engine/megatron_utils/megatron.py:657:                yield f"module.module.decoder.layers.{layer_idx}.{rest}", param
areal/engine/megatron_utils/megatron.py:667:            decoder_layers_pattern = r"module\.module\.decoder\.layers\.(\d+)\.(.+)"
areal/engine/megatron_utils/megatron.py:668:            match = re.match(decoder_layers_pattern, name)
areal/engine/megatron_utils/megatron.py:674:                yield f"module.module.decoder.layers.{layer_idx}.{rest}", buffer
examples/vlm/clevr_count_70k_grpo.yaml:64:  reward_scaling: 10.0
examples/vlm/clevr_count_70k_grpo.yaml:65:  reward_bias: -0.5
examples/vlm/clevr_count_70k_grpo.yaml:71:  reward_norm:
examples/vlm/clevr_count_70k_grpo.py:18:def clevr_count_70k_reward_fn(
examples/vlm/clevr_count_70k_grpo.py:51:        reward_fn="examples.vlm.clevr_count_70k_grpo.clevr_count_70k_reward_fn",
areal/engine/megatron_utils/pipeline_parallel.py:58:        stage_layers.extend(["decoder"] * length)
areal/engine/megatron_utils/pipeline_parallel.py:92:        "Configured pipeline layout (per-stage decoder counts / params): %s / %s (pp=%s, vpp=%s)",
examples/vlm/geometry3k_grpo.yaml:64:  reward_scaling: 10.0
examples/vlm/geometry3k_grpo.yaml:65:  reward_bias: -0.5
examples/vlm/geometry3k_grpo.yaml:71:  reward_norm:
examples/openai_agents/config.yaml:9:reward_fn_path: "areal.reward.gsm8k.gsm8k_reward_fn"
examples/openai_agents/config.yaml:67:  reward_scaling: 10.0
examples/openai_agents/config.yaml:68:  reward_bias: -0.5
areal/engine/megatron_engine.py:178:            # NOTE: device_id **SHOULD NOT** be passed into init_process_group,
areal/engine/megatron_engine.py:600:            # Release tree attention metadata after forward pass
areal/engine/megatron_engine.py:862:        # fp8_param_gather is passed from make_mcore_model()
areal/engine/megatron_engine.py:1435:        # the expected **total** number of tokens per micro-batch **in the forward pass**.
areal/engine/megatron_engine.py:1438:        # GPU in a forward pass here will be `max_tokens_per_mb / cp_size`.
areal/engine/megatron_engine.py:1674:            rw_logger.warning("mb_spec.granularity must be 2 for reward modeling")
examples/openai_agents/train_agents.py:10:from areal.api.reward_api import AsyncRewardWrapper
examples/openai_agents/train_agents.py:24:        reward_fn_path: str,
examples/openai_agents/train_agents.py:30:        self.async_reward_fn = AsyncRewardWrapper(import_from_string(reward_fn_path))
examples/openai_agents/train_agents.py:47:        reward = await self.async_reward_fn(
examples/openai_agents/train_agents.py:60:        client.set_last_reward(reward)
examples/openai_agents/train_agents.py:61:        return reward
examples/openai_agents/train_agents.py:69:        reward_fn_path: str,
examples/openai_agents/train_agents.py:86:            reward_fn_path=reward_fn_path,
examples/openai_agents/train_agents.py:95:        reward = await self.agent.run_agent(
examples/openai_agents/train_agents.py:99:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
examples/openai_agents/train_agents.py:101:        client.apply_reward_discount(turn_discount=0.9)
examples/openai_agents/train_agents.py:102:        interactions_with_reward = client.export_interactions(style="individual")
examples/openai_agents/train_agents.py:103:        return interactions_with_reward
examples/openai_agents/train_agents.py:108:    reward_fn_path: str = field(
examples/openai_agents/train_agents.py:109:        default="areal.reward.gsm8k.gsm8k_reward_fn",
examples/openai_agents/train_agents.py:111:            "help": "The path to the reward function. Should follow the API in `areal/api/reward_api.py`."
examples/openai_agents/train_agents.py:148:        reward_fn_path=config.reward_fn_path,
examples/camel/train.py:8:from areal.api.reward_api import AsyncRewardWrapper
examples/camel/train.py:13:from areal.reward import get_math_verify_worker
examples/camel/train.py:28:def gsm8k_reward_fn(result, answer):
examples/camel/train.py:46:        self.async_reward_fn = AsyncRewardWrapper(gsm8k_reward_fn)
examples/camel/train.py:64:        reward = await self.async_reward_fn(result=content, answer=data["answer"])
examples/camel/train.py:65:        client.set_last_reward(reward)
examples/camel/train.py:66:        return reward
examples/camel/train.py:96:        reward = await self.agent.run_agent(
examples/camel/train.py:100:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
examples/camel/train.py:102:        client.apply_reward_discount(turn_discount=0.9)
examples/camel/train.py:103:        interactions_with_reward = client.export_interactions(style="individual")
examples/camel/train.py:104:        return interactions_with_reward
examples/camel/config.yaml:67:  reward_scaling: 10.0
examples/camel/config.yaml:68:  reward_bias: -0.5
areal/tools/validate_docker_installation.py:118:            # Enable autocasting for the forward pass
areal/tools/validate_docker_installation.py:140:            # Test a forward-backward pass
areal/tools/validate_docker_installation.py:221:                    "✓ flash-linear-attention (fla) - MultiScaleRetention forward pass OK"
areal/tools/profile_engines.py:431:    config = AutoConfig.from_pretrained(model_path, trust_remote_code=True)
examples/multi_turn_math/gsm8k_grpo_mt.yaml:68:  reward_scaling: 10.0
examples/multi_turn_math/gsm8k_grpo_mt.yaml:69:  reward_bias: -0.5
examples/multi_turn_math/gsm8k_grpo_mt.yaml:75:  reward_norm:
areal/tools/plot_session_trace.py:25:    "reward_s",
areal/tools/plot_session_trace.py:28:DEFAULT_PHASE_ORDER: tuple[str, ...] = ("generate", "reward", "toolcall")
areal/tools/plot_session_trace.py:38:    "reward": {"label": "Reward", "color": "#dc2626"},  # Red for reward
areal/tools/plot_session_trace.py:56:    "reward_s": {"label": "Reward calculation duration", "color": "#10b981"},
areal/tools/plot_session_trace.py:161:            except json.JSONDecodeError as exc:  # pragma: no cover - defensive
areal/tools/plot_session_trace.py:198:    processing. For each phase (g…47371 tokens truncated…tr(config.actor.ig_reward_params, "compute_backend", "actor") if config.actor.ig_reward_params else None
areal/trainer/rl_trainer_qun_team.py:397:            if config.actor.ig_reward_params and ig_backend == "actor":
areal/trainer/rl_trainer_qun_team.py:399:                    stats_tracker.record_timing("compute_ig_reward"),
areal/trainer/rl_trainer_qun_team.py:401:                        "train.compute_ig_reward",
areal/trainer/rl_trainer_qun_team.py:420:                    # 3. 计算最终 IG rewards 并合入 rollout_batch
areal/trainer/rl_trainer_qun_team.py:421:                    rollout_batch = self.actor.process_ig_rewards(rollout_batch, ig_logprobs_list, ig_metadata)
areal/trainer/rl_trainer_qun_team.py:423:                    self.actor.get_device_stats().log("compute ig reward")
areal/workflow/openai/math_agent.py:16:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/openai/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/openai/math_agent.py:42:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:43:        return await reward_fn(
areal/workflow/openai/math_agent.py:59:        rewards = {}
areal/workflow/openai/math_agent.py:71:            reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:72:            reward = await reward_fn(completions=message.content, answer=data["answer"])
areal/workflow/openai/math_agent.py:73:            rewards[response.id] = reward
areal/workflow/openai/math_agent.py:74:            if reward == 1:
areal/workflow/openai/math_agent.py:80:                        "content": "Your answer is either wrong or not parsable to the reward function. You may misunderstand the original question. "
areal/workflow/openai/math_agent.py:84:        return rewards
areal/workflow/openai/math_agent.py:143:            model="default",  # no need to pass
areal/workflow/openai/math_agent.py:164:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:165:        reward = await reward_fn(completions=result.final_output, answer=data["answer"])
areal/workflow/openai/math_agent.py:166:        return reward
areal/workflow/rlvr_qun_team.py:13:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/rlvr_qun_team.py:45:    """Single-turn reward learning workflow supporting optional thinking tokens."""
areal/workflow/rlvr_qun_team.py:49:        reward_fn: Callable[..., Any] | str,
areal/workflow/rlvr_qun_team.py:54:        ig_reward_params=None,
areal/workflow/rlvr_qun_team.py:59:        reward_timeout_seconds: float = 15.0,
areal/workflow/rlvr_qun_team.py:61:        self.reward_fn = reward_fn
areal/workflow/rlvr_qun_team.py:71:        self.ig_reward_params = ig_reward_params
areal/workflow/rlvr_qun_team.py:72:        self.reward_timeout_seconds = reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:73:        if not isinstance(reward_fn, str):
areal/workflow/rlvr_qun_team.py:74:            self.async_reward_fn = AsyncRewardWrapper(
areal/workflow/rlvr_qun_team.py:75:                reward_fn, timeout_seconds=reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:86:    @trace_session("reward")
areal/workflow/rlvr_qun_team.py:87:    async def _compute_rewards(
areal/workflow/rlvr_qun_team.py:93:        """Decode completion and compute reward.
areal/workflow/rlvr_qun_team.py:95:        Traces reward phase execution for SessionTracer. Decodes output tokens
areal/workflow/rlvr_qun_team.py:96:        to string, calls async reward function, and logs metric to stats tracker.
areal/workflow/rlvr_qun_team.py:103:        completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/rlvr_qun_team.py:104:        reward = await self.async_reward_fn(
areal/workflow/rlvr_qun_team.py:112:        return reward
areal/workflow/rlvr_qun_team.py:122:        """Generate one sample and compute its reward.
areal/workflow/rlvr_qun_team.py:125:        computes reward, and logs metrics. SessionTracer automatically
areal/workflow/rlvr_qun_team.py:126:        tracks generate and reward phases via @trace_session decorators.
areal/workflow/rlvr_qun_team.py:131:            Model response and reward value.
areal/workflow/rlvr_qun_team.py:136:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/rlvr_qun_team.py:138:        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
areal/workflow/rlvr_qun_team.py:140:        return resp, reward
areal/workflow/rlvr_qun_team.py:147:            outcome_reward: float
areal/workflow/rlvr_qun_team.py:153:        if not self.ig_reward_params:
areal/workflow/rlvr_qun_team.py:162:        gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:163:        bridge_text = getattr(self.ig_reward_params, "bridge_text", "\n\nTherefore, the answer is ")
areal/workflow/rlvr_qun_team.py:164:        bridge_ids = self.tokenizer.encode(bridge_text, add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:165:        boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:166:        boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:173:        sep_token = getattr(self.ig_reward_params, "sep_token", "\n")
areal/workflow/rlvr_qun_team.py:174:        sep_ids = self.tokenizer.encode(sep_token, add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:266:        from areal.utils.functional.functional import _compute_rewards_logic
areal/workflow/rlvr_qun_team.py:274:        calculated_rewards, calculated_penalties = _compute_rewards_logic(
areal/workflow/rlvr_qun_team.py:276:            outcome_reward=outcome_reward,
areal/workflow/rlvr_qun_team.py:277:            beta=getattr(self.ig_reward_params, "beta", 0.5),
areal/workflow/rlvr_qun_team.py:278:            use_peak_selection=getattr(self.ig_reward_params, "use_peak_selection", False),
areal/workflow/rlvr_qun_team.py:279:            use_watermark_selection=getattr(self.ig_reward_params, "use_watermark_selection", False),
areal/workflow/rlvr_qun_team.py:280:            reward_mode=getattr(self.ig_reward_params, "reward_mode", "prob_diff"),
areal/workflow/rlvr_qun_team.py:289:        token_level_rewards = [0.0] * seq_len
areal/workflow/rlvr_qun_team.py:291:        step_reward_mask = [0.0] * seq_len
areal/workflow/rlvr_qun_team.py:294:        for r, p, offset in zip(calculated_rewards, calculated_penalties, step_offsets):
areal/workflow/rlvr_qun_team.py:296:            reward_idx = abs_idx - 1  # 映射到步骤结束符的前一个 token (对齐 Actor 逻辑)
areal/workflow/rlvr_qun_team.py:298:            if 0 <= reward_idx < seq_len:
areal/workflow/rlvr_qun_team.py:299:                token_level_rewards[reward_idx] = r.item() if isinstance(r, torch.Tensor) else float(r)
areal/workflow/rlvr_qun_team.py:300:                token_level_len_penalties[reward_idx] = p.item() if isinstance(p, torch.Tensor) else float(p)
areal/workflow/rlvr_qun_team.py:301:                step_reward_mask[reward_idx] = 1.0
areal/workflow/rlvr_qun_team.py:306:            "token_level_rewards": token_level_rewards,
areal/workflow/rlvr_qun_team.py:308:            "step_reward_mask": step_reward_mask,
areal/workflow/rlvr_qun_team.py:315:        # NOTE: load reward function dynamically if given as string
areal/workflow/rlvr_qun_team.py:316:        if isinstance(self.reward_fn, str):
areal/workflow/rlvr_qun_team.py:317:            self.reward_fn = import_from_string(self.reward_fn)
areal/workflow/rlvr_qun_team.py:318:            self.async_reward_fn = AsyncRewardWrapper(
areal/workflow/rlvr_qun_team.py:319:                self.reward_fn, timeout_seconds=self.reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:334:        prompt_str = self.tokenizer.decode(input_ids)
areal/workflow/rlvr_qun_team.py:336:        # Generate single response and compute reward
areal/workflow/rlvr_qun_team.py:337:        resp, reward = await self._collect_samples(engine, req, prompt_str, data)
areal/workflow/rlvr_qun_team.py:351:            "rewards": torch.tensor(reward, dtype=torch.float32),
areal/workflow/rlvr_qun_team.py:355:        ig_backend = getattr(self.ig_reward_params, "compute_backend", "actor") if self.ig_reward_params else None
areal/workflow/rlvr_qun_team.py:357:        if self.ig_reward_params:
areal/workflow/rlvr_qun_team.py:360:                ig_results = await self._compute_ig_probes(engine, resp, data, reward)
areal/workflow/rlvr_qun_team.py:385:                gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:390:            sep_id = self.tokenizer.encode(self.ig_reward_params.sep_token, add_special_tokens=False)[-1]
areal/workflow/rlvr_qun_team.py:397:            bridge_ids = self.tokenizer.encode(self.ig_reward_params.bridge_text, add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:398:            boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
areal/workflow/rlvr_qun_team.py:399:            boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)
areal/workflow/anthropic/math_agent.py:7:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/anthropic/math_agent.py:10:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/anthropic/math_agent.py:77:        # Calculate reward
areal/workflow/anthropic/math_agent.py:78:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/anthropic/math_agent.py:79:        return await reward_fn(completion_text, data["answer"])
areal/workflow/anthropic/claude_math_agent.py:15:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/anthropic/claude_math_agent.py:18:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/anthropic/claude_math_agent.py:119:        # not from ClaudeAgentOptions.env (which only passes to subprocess)
areal/workflow/anthropic/claude_math_agent.py:158:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/anthropic/claude_math_agent.py:159:        reward = await reward_fn(completions=final_output, answer=data["answer"])
areal/workflow/anthropic/claude_math_agent.py:160:        return reward
areal/optimizer/distrib_optimizer_muon_core_13_1.py:297:        These groups encompass both: 1) groups used by this class, for
areal/optimizer/distrib_optimizer_muon_core_13_1.py:738:            #   initialized, we skip this code block to avoid reallocating
areal/optimizer/distrib_optimizer_muon_core_13_1.py:1555:        were saved using code from before Feb 13, 2024. Starting on this date, a
areal/optimizer/distrib_optimizer_muon_core_13_1.py:2201:        asynchorous all-gathers that get overlapped with the next forward pass.
areal/workflow/vision_rlvr.py:12:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/vision_rlvr.py:29:        reward_fn: Callable[..., Any] | str,
areal/workflow/vision_rlvr.py:36:            reward_fn,
areal/workflow/vision_rlvr.py:45:    @trace_session("reward")
areal/workflow/vision_rlvr.py:46:    async def _compute_rewards(
areal/workflow/vision_rlvr.py:52:        """Decode completion and compute reward.
areal/workflow/vision_rlvr.py:54:        Traces reward phase execution for SessionTracer. Decodes output tokens
areal/workflow/vision_rlvr.py:55:        to string, calls async reward function with keyword arguments, and logs
areal/workflow/vision_rlvr.py:64:        completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/vision_rlvr.py:65:        reward = await self.async_reward_fn(
areal/workflow/vision_rlvr.py:73:        return reward
areal/workflow/vision_rlvr.py:83:        """Generate one sample and compute its reward.
areal/workflow/vision_rlvr.py:86:        computes reward, and logs metrics. SessionTracer automatically
areal/workflow/vision_rlvr.py:87:        tracks generate and reward phases via @trace_session decorators.
areal/workflow/vision_rlvr.py:92:            Model response and reward value.
areal/workflow/vision_rlvr.py:97:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/vision_rlvr.py:99:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/workflow/vision_rlvr.py:101:        return resp, reward
areal/workflow/vision_rlvr.py:106:        # NOTE: load reward function dynamically if given as string
areal/workflow/vision_rlvr.py:107:        if isinstance(self.reward_fn, str):
areal/workflow/vision_rlvr.py:108:            self.reward_fn = import_from_string(self.reward_fn)
areal/workflow/vision_rlvr.py:109:            self.async_reward_fn = AsyncRewardWrapper(self.reward_fn)
areal/workflow/vision_rlvr.py:134:        prompt_str = self.tokenizer.decode(input_ids)
areal/workflow/vision_rlvr.py:136:        # Generate single response and compute reward
areal/workflow/vision_rlvr.py:137:        resp, reward = await self._collect_samples(engine, req, prompt_str, data)
areal/workflow/vision_rlvr.py:161:            "rewards": torch.tensor(reward, dtype=torch.float32).unsqueeze(0),
areal/workflow/langchain/math_agent.py:16:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/langchain/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/langchain/math_agent.py:20:    """Calculate reward based on math answer correctness."""
areal/workflow/langchain/math_agent.py:74:    rewards based on math answer correctness.
areal/workflow/langchain/math_agent.py:109:        # Calculate reward
areal/workflow/langchain/math_agent.py:110:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/langchain/math_agent.py:111:        return await reward_fn(completions=completion_text, answer=data["answer"])
areal/workflow/langchain/math_agent.py:180:        # Calculate reward
areal/workflow/langchain/math_agent.py:181:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/langchain/math_agent.py:182:        return await reward_fn(completions=final_answer, answer=data["answer"])
areal/reward/code/toolchain.py:1:"""Toolchain availability probe for multi-language code execution.
areal/reward/code/toolchain.py:5:zero rewards during training.
areal/reward/code/toolchain.py:53:            f"{missing_bins}. Samples in those languages will receive 0 reward."
areal/reward/code/__init__.py:1:"""Code RL reward functions and sandbox primitives."""
areal/reward/code/__init__.py:3:from areal.reward.code.competitive_exec import (
areal/reward/code/__init__.py:4:    extract_code,
areal/reward/code/__init__.py:5:    nemotron_competitive_reward_fn,
areal/reward/code/__init__.py:7:from areal.reward.code.sandbox import (
areal/reward/code/__init__.py:13:    compile_code,
areal/reward/code/__init__.py:19:from areal.reward.code.toolchain import assert_toolchain_or_warn, check_toolchain
areal/reward/code/__init__.py:22:    "nemotron_competitive_reward_fn",
areal/reward/code/__init__.py:23:    "extract_code",
areal/reward/code/__init__.py:24:    "compile_code",
areal/reward/math_verify_qun_team.py:8:def math_verify_reward_fn(
areal/reward/math_verify_qun_team.py:17:        logger.warning("Exception in math_verify_reward_fn", exc_info=True)
areal/reward/geometry3k.py:3:from areal.reward import get_math_verify_worker
areal/reward/geometry3k.py:24:def geometry3k_reward_fn(
areal/reward/geometry3k.py:37:        logger.warning("Exception in geometry3k_reward_fn", exc_info=True)
areal/reward/code/sandbox.py:1:"""Subprocess sandbox for executing untrusted code in multiple languages.
areal/reward/code/sandbox.py:37:    returncode: int
areal/reward/code/sandbox.py:47:    missing. The reward fn treats a failed compile as 0 reward.
areal/reward/code/sandbox.py:147:def compile_code(
areal/reward/code/sandbox.py:149:    code: str,
areal/reward/code/sandbox.py:154:    The returned ``CompileResult`` owns a tempdir; pass it to ``run_artifact``
areal/reward/code/sandbox.py:168:    if not code.strip():
areal/reward/code/sandbox.py:169:        return CompileResult(ok=False, workdir="", run_cmd=[], error="empty code")
areal/reward/code/sandbox.py:171:    workdir = tempfile.mkdtemp(prefix=f"arealcode_{canon}_")
areal/reward/code/sandbox.py:175:            f.write(code)
areal/reward/code/sandbox.py:209:            if proc.returncode != 0:
areal/reward/code/sandbox.py:255:    pass a single value for both the address-space cap (skipped for Java) and
areal/reward/code/sandbox.py:262:            returncode=-1,
areal/reward/code/sandbox.py:300:                returncode=proc.returncode,
areal/reward/code/sandbox.py:307:                pass
areal/reward/code/sandbox.py:315:                returncode=-1,
areal/reward/code/sandbox.py:320:            stdout="", stderr="", returncode=-1, timeout=False, error=str(e)
areal/reward/code/sandbox.py:327:                pass
areal/reward/code/sandbox.py:329:            stdout="", stderr="", returncode=-1, timeout=False, error=str(e)
areal/reward/code/sandbox.py:334:    code: str,
areal/reward/code/sandbox.py:340:    artifact = compile_code("python", code)
areal/reward/gsm8k.py:1:from areal.reward import get_math_verify_worker
areal/reward/gsm8k.py:7:def gsm8k_reward_fn(
areal/reward/gsm8k.py:14:        logger.warning("Exception in gsm8k_reward_fn", exc_info=True)
areal/reward/clevr_count_70k.py:12:def clevr_count_70k_reward_fn(
areal/reward/code/competitive_exec.py:1:"""Reward for Nemotron-RL-coding-competitive_coding (and future multi-lang data).
areal/reward/code/competitive_exec.py:3:Extracts the last fenced code block from the model completion (Python by
areal/reward/code/competitive_exec.py:5:unit tests, returns pass rate ∈ [0, 1].
areal/reward/code/competitive_exec.py:17:from areal.reward.code.sandbox import (
areal/reward/code/competitive_exec.py:20:    compile_code,
areal/reward/code/competitive_exec.py:28:# Group 1: language tag (optional). Group 2: code body.
areal/reward/code/competitive_exec.py:35:def extract_code(
areal/reward/code/competitive_exec.py:38:    """Return ``(canonical_language, code)`` of the last useful fenced block.
areal/reward/code/competitive_exec.py:57:    for tag, code in matches:
areal/reward/code/competitive_exec.py:58:        body = code.strip()
areal/reward/code/competitive_exec.py:72:def nemotron_competitive_reward_fn(
areal/reward/code/competitive_exec.py:91:        to ``"python"`` for Nemotron-RL-coding-competitive_coding.
areal/reward/code/competitive_exec.py:93:    :param max_tests: Upper bound on tests executed per reward call; tests
areal/reward/code/competitive_exec.py:109:    extracted = extract_code(completions, expected_language=language)
areal/reward/code/competitive_exec.py:112:    detected_lang, code = extracted
areal/reward/code/competitive_exec.py:114:    artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
areal/reward/code/competitive_exec.py:117:        # CompileResult.error/stderr; treat as zero reward.
areal/reward/code/competitive_exec.py:131:        passed = 0
areal/reward/code/competitive_exec.py:140:            if result.timeout or result.error is not None or result.returncode != 0:
areal/reward/code/competitive_exec.py:143:                passed += 1
areal/reward/code/competitive_exec.py:145:        return passed / len(idx)
areal/reward/__init__.py:11:def get_custom_reward_fn(path: str, **kwargs):
areal/reward/__init__.py:13:        from .clevr_count_70k import clevr_count_70k_reward_fn
areal/reward/__init__.py:15:        return clevr_count_70k_reward_fn
areal/reward/__init__.py:17:        from .geometry3k import geometry3k_reward_fn
areal/reward/__init__.py:19:        return geometry3k_reward_fn
areal/reward/__init__.py:23:            f"Supported reward functions are: {VALID_REWARD_FN}. "
```

### Tool Output - 2026-04-27 23:28:52 EDT

```
Chunk ID: d2bfa3
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1491
Output:
experiment_name: qwen3_0p6b_nemotron_code_grpo
trial_name: debug_20260423

seed: 1
enable_offload: false
total_train_epochs: 5
tokenizer_path: ${actor.path}

cluster:
  n_nodes: 1
  n_gpus_per_node: 8
  fileroot: /opt/huawei/dataset/ag_data/logs/areal/experiments
  name_resolve:
    type: nfs
    nfs_record_root: /opt/huawei/dataset/ag_data/logs/areal/name_resolve

allocation_mode: sglang:d4p1t1+d4p1t1

scheduler:
  type: null

rollout:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  max_concurrent_rollouts: 128
  queue_size: null
  consumer_batch_size: ${train_dataset.batch_size}
  max_head_offpolicyness: 2
  enable_rollout_tracing: false
  scheduling_spec: ${actor.scheduling_spec}
  fileroot: ${cluster.fileroot}
  tokenizer_path: ${tokenizer_path}
  dump_to_file: true

gconfig:
  n_samples: 16
  min_new_tokens: 0
  max_new_tokens: 16384
  greedy: false
  temperature: 1.0
  enable_thinking: False
  stop_token_ids:
    - 151643  # "<|endoftext|>"
    - 151645  # "<|im_end|>"

eval_gconfig:
  n_samples: 8
  min_new_tokens: 0
  max_new_tokens: 16384
  greedy: false
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  enable_thinking: ${gconfig.enable_thinking}
  stop_token_ids:
    - 151643  # "<|endoftext|>"
    - 151645  # "<|im_end|>"

actor:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: /opt/huawei/dataset/ag_data_models/Qwen3-0.6B
  init_from_scratch: false
  disable_dropout: true
  gradient_checkpointing: true
  dtype: bfloat16
  mb_spec:
    max_tokens_per_mb: 20480
  optimizer:
    type: adam
    lr: 1.0e-6
    weight_decay: 0.01
    beta1: 0.9
    beta2: 0.999
    eps: 1e-8
    lr_scheduler_type: constant
    gradient_clipping: 1.0
    warmup_steps_proportion: 0.01
  eps_clip: 0.2
  eps_clip_higher: 0.28
  temperature: ${gconfig.temperature}
  reward_scaling: 1.0
  reward_bias: 0.0
  kl_ctl: 0.0
  kl_estimator: k1
  use_sapo_loss: false
  sapo_tau_pos: 1.0
  sapo_tau_neg: 1.05
  ppo_n_minibatches: 1
  recompute_logprob: true
  use_decoupled_loss: true
  behave_imp_weight_cap: 5.0
  reward_norm:
    denominator: std
    mean_level: group
    std_level: group
    group_size: ${gconfig.n_samples}
  adv_norm:
    mean_level: batch
    std_level: batch
  # GRPO: token, GSPO: sequence
  importance_sampling_level: token
  weight_update_mode: xccl
  max_new_tokens: ${gconfig.max_new_tokens}
  scheduling_spec:
    - task_type: worker
      port_count: 2
      gpu: 1
      mem: 32
      cmd: python3 -m areal.infra.rpc.rpc_server
      env_vars: {}

ref:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: ${actor.path}
  init_from_scratch: false
  disable_dropout: true
  dtype: ${actor.dtype}
  mb_spec:
    max_tokens_per_mb: ${actor.mb_spec.max_tokens_per_mb}
  optimizer: null
  scheduling_strategy:
    type: colocation
    target: actor
  scheduling_spec: ${actor.scheduling_spec}

# SGLang
sglang:
  model_path: ${actor.path}
  random_seed: ${seed}
  skip_tokenizer_init: true
  dtype: ${actor.dtype}
  max_running_requests: null
  context_length: ${actor.mb_spec.max_tokens_per_mb}
  mem_fraction_static: 0.6
  max_prefill_tokens: 10240

vllm:
  model: ${actor.path}
  seed: ${seed}
  skip_tokenizer_init: false
  dtype: ${actor.dtype}
  max_model_len: ${actor.mb_spec.max_tokens_per_mb}
  gpu_memory_utilization: 0.6

# Reward — competitive coding execution budget (per-test / per-sample)
# per_test_timeout      : wall-clock seconds for a single stdin/stdout test
# max_tests             : upper bound on tests run per reward call (subsample)
# memory_mb             : address-space cap for each test subprocess (Python/C++/Node)
# reward_timeout_seconds: total AsyncRewardWrapper timeout per sample
# language              : default source language; per-sample 'language' from the
#                         dataset loader overrides. Supported: python, cpp, java, javascript
# compile_timeout       : wall-clock seconds for the compile step (compiled langs only)
# java_xmx_mb           : JVM heap cap (-Xmx) for Java submissions (RLIMIT_AS is skipped for Java)
# probe_languages       : languages probed at trainer startup; a missing toolchain logs a warning
code_reward:
  per_test_timeout: 5.0
  max_tests: 15
  memory_mb: 2048
  reward_timeout_seconds: 90.0
  language: python
  compile_timeout: 30.0
  java_xmx_mb: 2048
  probe_languages:
    - python
    - cpp
    - java
    - javascript

# datasets
# `path` accepts: HF hub name (e.g., `nvidia/Nemotron-RL-coding-competitive_coding`),
#                 local HF dataset dir, or local .jsonl / .parquet file.
train_dataset:
  batch_size: 128
  shuffle: true
  pin_memory: true
  num_workers: 4
  path: /opt/huawei/dataset/ag_data_nlp/RL/Nemotron-RL-coding-competitive_coding
  type: rl
  max_length: 4096
  drop_last: true

valid_dataset:
  batch_size: 128
  pin_memory: true
  num_workers: 4
  path: ""
  type: rl
  drop_last: false

# Utilities
saver:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: null

recover:
  mode: auto
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: null

evaluator:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: 1
  freq_steps: null
  freq_secs: null

stats_logger:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  tensorboard:
    path: ${cluster.fileroot}/tensorboard/${experiment_name}/${trial_name}
  wandb:
    mode: disabled
  swanlab:
    project: ${experiment_name}
    name: ${trial_name}
    config: null
    logdir: null
    mode: disabled
    api_key: null

perf_tracer:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  enabled: false
  session_tracer:
    enabled: false
```

### Tool Output - 2026-04-27 23:28:52 EDT

```
Chunk ID: 54064e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 836
Output:
"""GRPO training on Nemotron-RL-coding-competitive_coding.

Pipeline:
  dataset   areal.dataset.code.nemotron_competitive
  reward    areal.reward.code.competitive_exec (subprocess sandbox, pass rate)
  workflow  areal.workflow.rlvr_qun_team.RLVRWorkflow (with extended reward timeout)

Note:
  ``reward_fn`` and ``workflow`` are passed as import-path strings so they
  survive JSON-RPC serialization to remote workers.  The default kwargs of
  ``areal.reward.code.nemotron_competitive_reward_fn`` match the
  ``code_reward`` fields in the YAML template.
"""

import sys

from examples.code.nemotron_configs import CodeGRPOConfig

from areal.api.cli_args import load_expr_config
from areal.dataset.get_datasets_qun_team import get_multi_custom_dataset
from areal.trainer.rl_trainer_qun_team import PPOTrainer
from areal.utils.hf_utils import load_hf_tokenizer


def main(args):
    config, _ = load_expr_config(args, CodeGRPOConfig)
    tokenizer = load_hf_tokenizer(config.tokenizer_path)

    # Probe sandbox toolchain early so missing g++/java/node show up in the
    # trainer log before any rollouts happen.
    from areal.reward.code import assert_toolchain_or_warn

    assert_toolchain_or_warn(list(config.code_reward.probe_languages))

    if config.actor.ig_reward_params is not None:
        config.actor.ig_reward_params.sep_token_id = tokenizer.encode(
            config.actor.ig_reward_params.sep_token, add_special_tokens=False
        )
        config.actor.ig_reward_params.pad_token_id = tokenizer.pad_token_id

    train_dataset_map = get_multi_custom_dataset(
        split="train",
        dataset_config=config.train_dataset,
        tokenizer=tokenizer,
    )
    assert len(train_dataset_map) == 1, f"{len(train_dataset_map)} != 1"
    train_dataset = next(iter(train_dataset_map.values()))

    valid_dataset_map: dict = {}
    if config.valid_dataset is not None and config.valid_dataset.path:
        valid_dataset_map = get_multi_custom_dataset(
            split="test",
            dataset_config=config.valid_dataset,
            tokenizer=tokenizer,
        )

    with PPOTrainer(
        config,
        train_dataset=train_dataset,
        valid_dataset=valid_dataset_map,
    ) as trainer:
        workflow_kwargs = dict(
            reward_fn="areal.reward.code.nemotron_competitive_reward_fn",
            gconfig=config.gconfig,
            tokenizer=config.tokenizer_path,
            enable_thinking=config.gconfig.enable_thinking,
            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
        )

        eval_workflow_kwargs_map = {}
        for dataset_name in valid_dataset_map.keys():
            eval_workflow_kwargs = workflow_kwargs.copy()
            eval_workflow_kwargs["gconfig"] = config.eval_gconfig
            eval_workflow_kwargs["rollout_stat_scope"] = f"eval-rollout/{dataset_name}"
            eval_workflow_kwargs_map[dataset_name] = eval_workflow_kwargs

        workflow_kwargs["ig_reward_params"] = config.actor.ig_reward_params
        trainer.train(
            workflow="areal.workflow.rlvr_qun_team.RLVRWorkflow",
            workflow_kwargs=workflow_kwargs,
            eval_workflow="areal.workflow.rlvr_qun_team.RLVRWorkflow",
            eval_workflow_kwargs=eval_workflow_kwargs_map,
        )


if __name__ == "__main__":
    main(sys.argv[1:])
```

### Assistant - 2026-04-27 23:29:03 EDT

```
入口确认了：这里的 reward 是执行单测后的 pass rate，训练曲线 0.2-0.4 基本可以直接理解成“抽样测试通过比例”，不是一个额外 shaping 后的分数。我继续看数据 loader 和 reward 的具体默认参数，判断是不是命令里的 `code_reward` 覆盖没有实际生效。
```

### Tool Call - 2026-04-27 23:29:03 EDT

```
exec_command {"cmd":"sed -n '1,240p' areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:29:03 EDT

```
exec_command {"cmd":"sed -n '1,260p' areal/dataset/code/nemotron_competitive.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:29:03 EDT

```
exec_command {"cmd":"sed -n '1,140p' examples/code/nemotron_configs.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:29:03 EDT

```
exec_command {"cmd":"rg -n \"code_reward|nemotron_competitive_reward_fn\\(|reward_timeout_seconds|per_test_timeout|max_tests\" -S .","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:29:03 EDT

````
Chunk ID: bf8988
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1270
Output:
"""Reward for Nemotron-RL-coding-competitive_coding (and future multi-lang data).

Extracts the last fenced code block from the model completion (Python by
default, configurable per sample), compiles once, runs against stdin/stdout
unit tests, returns pass rate ∈ [0, 1].

Test subsampling is deterministic per ``prompt`` so all rollouts of the same
question in a GRPO group see the same tests — otherwise the group-level
advantage normalization gets noisy.
"""

from __future__ import annotations

import random
import re

from areal.reward.code.sandbox import (
    canonical_language,
    cleanup_compile_result,
    compile_code,
    outputs_match,
    run_artifact,
)
from areal.utils import logging

logger = logging.getLogger("CompetitiveCodeReward")

# Group 1: language tag (optional). Group 2: code body.
_CODE_BLOCK_RE = re.compile(
    r"```(python|py|cpp|c\+\+|cxx|java|javascript|js|node)?\s*\n(.*?)```",
    re.DOTALL | re.IGNORECASE,
)


def extract_code(
    text: str, expected_language: str = "python"
) -> tuple[str, str] | None:
    """Return ``(canonical_language, code)`` of the last useful fenced block.

    Selection:
      1. Prefer the LAST fence whose tag matches ``expected_language``.
      2. Else fall back to the LAST fence with any recognized tag.
      3. Else fall back to the LAST untagged fence, treated as
         ``expected_language``.
      4. Return None if no fenced block is present.
    """
    expected_canon = canonical_language(expected_language) or "python"

    matches = _CODE_BLOCK_RE.findall(text)
    if not matches:
        return None

    matched_expected: tuple[str, str] | None = None
    matched_any_tag: tuple[str, str] | None = None
    matched_untagged: tuple[str, str] | None = None

    for tag, code in matches:
        body = code.strip()
        if not body:
            continue
        canon = canonical_language(tag) if tag else None
        if canon == expected_canon:
            matched_expected = (canon, body)
        elif canon is not None:
            matched_any_tag = (canon, body)
        else:
            matched_untagged = (expected_canon, body)

    return matched_expected or matched_any_tag or matched_untagged


def nemotron_competitive_reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    test_inputs: list[str] | None = None,
    test_outputs: list[str] | None = None,
    language: str = "python",
    per_test_timeout: float = 5.0,
    max_tests: int = 15,
    memory_mb: int = 2048,
    compile_timeout: float = 30.0,
    java_xmx_mb: int = 2048,
    **kwargs,
) -> float:
    """Pass rate over (a sample of) unit tests.

    :param language: Source language for the model output. May be overridden
        by ``task_data["language"]`` injected by the dataset loader. Defaults
        to ``"python"`` for Nemotron-RL-coding-competitive_coding.
    :param per_test_timeout: Wall-clock seconds for each test case.
    :param max_tests: Upper bound on tests executed per reward call; tests
        beyond this cap are sampled deterministically per ``prompt``.
    :param memory_mb: Address-space cap for each test subprocess (Python/C++/
        Node). Java uses ``java_xmx_mb`` instead.
    :param compile_timeout: Wall-clock seconds for the compile step (compiled
        languages only). Compile happens once per submission.
    :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
    """
    if not test_inputs or not test_outputs:
        return 0.0
    if len(test_inputs) != len(test_outputs):
        logger.warning(
            f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
        )
        return 0.0

    extracted = extract_code(completions, expected_language=language)
    if extracted is None:
        return 0.0
    detected_lang, code = extracted

    artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
    if not artifact.ok:
        # Compile failure or unsupported language — caller already logged in
        # CompileResult.error/stderr; treat as zero reward.
        cleanup_compile_result(artifact)
        return 0.0

    try:
        n = len(test_inputs)
        if n <= max_tests:
            idx = list(range(n))
        else:
            # Seeding with the prompt keeps the sampled subset identical
            # across all rollouts of the same problem in a GRPO group.
            rng = random.Random(prompt)
            idx = rng.sample(range(n), max_tests)

        passed = 0
        for i in idx:
            result = run_artifact(
                artifact,
                stdin=test_inputs[i],
                timeout=per_test_timeout,
                memory_mb=memory_mb,
                java_xmx_mb=java_xmx_mb,
            )
            if result.timeout or result.error is not None or result.returncode != 0:
                continue
            if outputs_match(result.stdout, test_outputs[i]):
                passed += 1

        return passed / len(idx)
    finally:
        cleanup_compile_result(artifact)
````

### Tool Output - 2026-04-27 23:29:03 EDT

```
Chunk ID: 54b7a6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 676
Output:
"""Loader for ``nvidia/Nemotron-RL-coding-competitive_coding``.

Verified source schema (parquet, per row):
    responses_create_params.input:  list[{"role": "user", "content": str}]
    verifier_metadata.unit_tests:   {"inputs": list[str], "outputs": list[str]}
    hash_id:                        str
    dataset:                        str  (e.g. "open-r1/codeforces")
    source:                         str  (e.g. "codeforces")

Note: the top-level field is ``responses_create_params.input``, NOT ``input``.
The ``input`` alias only appears in the pre-processed training blends.

Supports three ways to specify ``path``:
    * HuggingFace Hub name, e.g. ``nvidia/Nemotron-RL-coding-competitive_coding``
    * Local directory with the HF dataset layout (contains ``data/train-*.parquet``)
    * Local ``.jsonl`` / ``.parquet`` single-file snapshot
"""

from __future__ import annotations

import os
from typing import TYPE_CHECKING

from datasets import load_dataset

if TYPE_CHECKING:
    from transformers.tokenization_utils_fast import PreTrainedTokenizerFast


def _load_raw(path: str, split: str):
    if os.path.isfile(path) and (path.endswith(".jsonl") or path.endswith(".parquet")):
        parent = os.path.dirname(path)
        fname = os.path.basename(path)
        return load_dataset(path=parent, data_files=fname)["train"]
    if os.path.isdir(path):
        return load_dataset(path=path, split=split)
    # HF Hub fallback
    return load_dataset(path=path, split=split)


def get_nemotron_competitive_rl_dataset(
    path: str,
    split: str,
    tokenizer: PreTrainedTokenizerFast,
    max_length: int | None = None,
):
    dataset = _load_raw(path, split)

    def process(sample):
        rcp = sample.get("responses_create_params") or {}
        messages = rcp.get("input") or []
        verifier = sample.get("verifier_metadata") or {}
        unit_tests = verifier.get("unit_tests") or {}
        return {
            "messages": messages,
            "test_inputs": list(unit_tests.get("inputs") or []),
            "test_outputs": list(unit_tests.get("outputs") or []),
            # Nemotron-RL-coding-competitive_coding is python-only per its README.
            # Future multi-language loaders can vary this per sample.
            "language": "python",
        }

    dataset = dataset.map(process, remove_columns=dataset.column_names)
    dataset = dataset.filter(lambda x: len(x["test_inputs"]) > 0)

    if max_length is not None:

        def fits(sample):
            content = sample["messages"][0]["content"] if sample["messages"] else ""
            return len(tokenizer.encode(content)) <= max_length

        dataset = dataset.filter(fits)

    return dataset
```

### Tool Output - 2026-04-27 23:29:03 EDT

```
Chunk ID: f5aa2b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 611
Output:
"""Configuration dataclasses for Nemotron code RL examples.

These live in a dedicated module so that RPC workers can deserialize them
(via importlib) when they are transmitted across the wire.  Dataclasses
defined directly in a ``__main__`` script cannot be imported by workers.
"""

from dataclasses import dataclass, field

from areal.api.cli_args import GRPOConfig, PPOActorConfig


@dataclass
class InfoGainRewardConfig:
    """Matches the shape expected by rl_trainer_qun_team / actor_qun_team."""

    mini_batch_size: int = field(default=128)
    max_tokens_per_forward: int = field(default=131072)
    compute_backend: str = field(default="rollout")
    bridge_text: str = field(default="")
    sep_token: str = field(default="\n")
    reward_mode: str = field(default="prob")
    beta: float = field(default=0.5)
    lambda_val: float = field(default=0.2)
    enable_varlen_packing: bool = field(default=True)
    use_watermark_selection: bool = field(default=False)
    use_peak_selection: bool = field(default=False)


@dataclass
class InfoGainPPOActorConfig(PPOActorConfig):
    ig_reward_params: InfoGainRewardConfig | None = field(default=None)


@dataclass
class CodeRewardConfig:
    """Budget for the competitive-coding execution reward.

    All values are tuned for H800 + 0.6B–14B Qwen3 rollouts.

    Note: with a string-based ``reward_fn`` (required for RPC serialization),
    the per-test fields below currently document the reward fn's defaults.
    To change them at training time, edit the defaults in
    ``areal.reward.code.competitive_exec.nemotron_competitive_reward_fn`` or
    inject overrides via the dataset loader (each sample's task_data flows
    into the reward fn as kwargs).
    """

    per_test_timeout: float = field(default=5.0)
    max_tests: int = field(default=15)
    memory_mb: int = field(default=2048)
    reward_timeout_seconds: float = field(default=90.0)
    # Multi-language support
    language: str = field(default="python")
    compile_timeout: float = field(default=30.0)
    java_xmx_mb: int = field(default=2048)
    # Toolchain probe at trainer startup
    probe_languages: list[str] = field(
        default_factory=lambda: ["python", "cpp", "java", "javascript"]
    )


@dataclass
class CodeGRPOConfig(GRPOConfig):
    actor: InfoGainPPOActorConfig = field(default_factory=InfoGainPPOActorConfig)
    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
```

### Tool Output - 2026-04-27 23:29:03 EDT

```
Chunk ID: 909542
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 826
Output:
./examples/code/nemotron_configs.py:49:    per_test_timeout: float = field(default=5.0)
./examples/code/nemotron_configs.py:50:    max_tests: int = field(default=15)
./examples/code/nemotron_configs.py:52:    reward_timeout_seconds: float = field(default=90.0)
./examples/code/nemotron_configs.py:66:    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
./examples/code/nemotron_code_rl.py:12:  ``code_reward`` fields in the YAML template.
./examples/code/nemotron_code_rl.py:33:    assert_toolchain_or_warn(list(config.code_reward.probe_languages))
./examples/code/nemotron_code_rl.py:67:            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
./examples/code/grpo_template_code.yaml:148:# per_test_timeout      : wall-clock seconds for a single stdin/stdout test
./examples/code/grpo_template_code.yaml:149:# max_tests             : upper bound on tests run per reward call (subsample)
./examples/code/grpo_template_code.yaml:151:# reward_timeout_seconds: total AsyncRewardWrapper timeout per sample
./examples/code/grpo_template_code.yaml:157:code_reward:
./examples/code/grpo_template_code.yaml:158:  per_test_timeout: 5.0
./examples/code/grpo_template_code.yaml:159:  max_tests: 15
./examples/code/grpo_template_code.yaml:161:  reward_timeout_seconds: 90.0
./areal/workflow/rlvr_qun_team.py:59:        reward_timeout_seconds: float = 15.0,
./areal/workflow/rlvr_qun_team.py:72:        self.reward_timeout_seconds = reward_timeout_seconds
./areal/workflow/rlvr_qun_team.py:75:                reward_fn, timeout_seconds=reward_timeout_seconds
./areal/workflow/rlvr_qun_team.py:319:                self.reward_fn, timeout_seconds=self.reward_timeout_seconds
./areal/reward/code/sandbox.py:6:critical for GRPO with ``n_samples * max_tests`` executions per problem.
./areal/reward/code/competitive_exec.py:72:def nemotron_competitive_reward_fn(
./areal/reward/code/competitive_exec.py:80:    per_test_timeout: float = 5.0,
./areal/reward/code/competitive_exec.py:81:    max_tests: int = 15,
./areal/reward/code/competitive_exec.py:92:    :param per_test_timeout: Wall-clock seconds for each test case.
./areal/reward/code/competitive_exec.py:93:    :param max_tests: Upper bound on tests executed per reward call; tests
./areal/reward/code/competitive_exec.py:123:        if n <= max_tests:
./areal/reward/code/competitive_exec.py:129:            idx = rng.sample(range(n), max_tests)
./areal/reward/code/competitive_exec.py:136:                timeout=per_test_timeout,
./notebook/math_reflection_zh.ipynb:325:    "REWARD_TIMEOUT_SECONDS = 15\n",
./notebook/math_reflection_en.ipynb:327:    "REWARD_TIMEOUT_SECONDS = 15\n",
./tests/test_code_sandbox_multilang.py:177:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:185:        per_test_timeout=5.0,
./tests/test_code_sandbox_multilang.py:186:        max_tests=15,
./tests/test_code_sandbox_multilang.py:200:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:208:        per_test_timeout=10.0,
./tests/test_code_sandbox_multilang.py:209:        max_tests=15,
./tests/test_code_sandbox_multilang.py:217:    reward = nemotron_competitive_reward_fn(
./tests/test_code_sandbox_multilang.py:233:    reward = nemotron_competitive_reward_fn(
```

### Assistant - 2026-04-27 23:29:18 EDT

```
还有一个关键点：`CodeRewardConfig` 的注释写明了，当前只有 `reward_timeout_seconds` 会从 config 传进 workflow；`per_test_timeout/max_tests/memory_mb` 这类参数不会因为命令行 override 自动生效，除非数据样本里带同名字段或改 reward_fn 默认值。你这条命令没改这些值，所以影响不大，但这是后续调参时容易误判的地方。
```

### Tool Call - 2026-04-27 23:29:18 EDT

```
exec_command {"cmd":"ls -la /opt/dataset/ag_data/data/nvidia/Nemotron-RL-coding-competitive_coding","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:29:18 EDT

```
exec_command {"cmd":"find /opt/dataset/ag_data/data/nvidia/Nemotron-RL-coding-competitive_coding -maxdepth 3 -type f | head -20","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:29:18 EDT

```
exec_command {"cmd":"rg -n \"dump_to_file|rollout.*json|responses|completions|reward\" areal/trainer areal/workflow areal/utils examples/code -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:29:18 EDT

```
Chunk ID: 8281fc
Wall time: 0.0000 seconds
Process exited with code 2
Original token count: 30
Output:
ls: cannot access '/opt/dataset/ag_data/data/nvidia/Nemotron-RL-coding-competitive_coding': No such file or directory
```

### Tool Output - 2026-04-27 23:29:18 EDT

```
Chunk ID: 98f081
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 28
Output:
find: ‘/opt/dataset/ag_data/data/nvidia/Nemotron-RL-coding-competitive_coding’: No such file or directory
```

### Tool Output - 2026-04-27 23:29:18 EDT

```
Chunk ID: 584a38
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 10595
Output:
Total output lines: 456

areal/trainer/ppo/actor.py:27:    reward_overlong_penalty,
areal/trainer/ppo/actor.py:40:        self.reward_bias = config.reward_bias
areal/trainer/ppo/actor.py:41:        self.reward_scaling = config.reward_scaling
areal/trainer/ppo/actor.py:42:        self.reward_clip = config.reward_clip
areal/trainer/ppo/actor.py:48:        self.reward_norm = (
areal/trainer/ppo/actor.py:49:            Normalization(config.reward_norm) if config.reward_norm else None
areal/trainer/ppo/actor.py:113:            f"  reward_norm: {config.reward_norm if config.reward_norm else 'DISABLED (None)'}"
areal/trainer/ppo/actor.py:135:        # Reward Penalty on length
areal/trainer/ppo/actor.py:136:        if self.config.overlong_reward_penalty:
areal/trainer/ppo/actor.py:142:            data = reward_overlong_penalty(
areal/trainer/ppo/actor.py:149:        # Reward Scaling
areal/trainer/ppo/actor.py:150:        reward_score = data["rewards"]
areal/trainer/ppo/actor.py:151:        reward_score = (reward_score + self.reward_bias) * self.reward_scaling
areal/trainer/ppo/actor.py:152:        reward_score = torch.clip(
areal/trainer/ppo/actor.py:153:            reward_score, max=self.reward_clip, min=-self.reward_clip
areal/trainer/ppo/actor.py:155:        if self.reward_norm:
areal/trainer/ppo/actor.py:156:            reward_score = self.reward_norm(reward_score)
areal/trainer/ppo/actor.py:181:        # Compute KL-regularized rewards.
areal/trainer/ppo/actor.py:185:        rewards = -self.kl_ctl * self.kl_estimator(old_logp, ref_logp)
areal/trainer/ppo/actor.py:186:        kl_rewards = rewards.clone()
areal/trainer/ppo/actor.py:187:        # KL rewards at the next token after eos is zero.
areal/trainer/ppo/actor.py:188:        rewards[batch_indices, seqlens - 1] = 0
areal/trainer/ppo/actor.py:191:            rewards[batch_indices, indices] += torch.where(
areal/trainer/ppo/actor.py:192:                seq_no_eos_mask, 0, reward_score
areal/trainer/ppo/actor.py:195:            rewards[batch_indices, indices] += reward_score
areal/trainer/ppo/actor.py:199:            values = torch.zeros_like(rewards)
areal/trainer/ppo/actor.py:208:            delta = rewards[:, t] + self.discount * nextvalues - values[:, t]
areal/trainer/ppo/actor.py:226:        data["kl_rewards"] = kl_rewards
areal/trainer/ppo/actor.py:227:        data["tot_rewards"] = rewards
areal/trainer/ppo/actor.py:239:        reward_score = data["rewards"]
areal/trainer/ppo/actor.py:244:            "correct_n_seqs": (reward_score > 0).bool(),
areal/trainer/ppo/actor.py:245:            "incorrect_n_seqs": (reward_score <= 0).bool(),
areal/trainer/ppo/actor.py:259:            n_seqs=torch.ones_like(reward_score, dtype=torch.bool),
areal/trainer/ppo/actor.py:274:            kl_rewards=data["kl_rewards"],
areal/trainer/ppo/actor.py:275:            final_reward=data["tot_rewards"],
areal/trainer/ppo/actor.py:282:            task_reward=reward_score.float(),
areal/trainer/ppo/actor.py:309:        for key in ["rewards", "tot_rewards", "kl_rewards"]:
areal/workflow/multi_turn.py:12:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/multi_turn.py:20:    """Multi-attempt workflow that retries generation until the reward is positive."""
areal/workflow/multi_turn.py:24:        reward_fn: Callable[..., Any],
areal/workflow/multi_turn.py:35:        self.reward_fn = reward_fn
areal/workflow/multi_turn.py:40:        self.async_reward_fn = AsyncRewardWrapper(reward_fn)
areal/workflow/multi_turn.py:49:                "content": "Your answer is either wrong or not parsable to the reward function. You may misunderstand the original question. "
areal/workflow/multi_turn.py:75:        reward = 0.0
areal/workflow/multi_turn.py:78:        completions_str = ""
areal/workflow/multi_turn.py:79:        while reward == 0.0 and t < self.max_turns:
areal/workflow/multi_turn.py:88:            # compute reward: 1 for correct and 0 otherwise
areal/workflow/multi_turn.py:90:            completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/multi_turn.py:91:            reward = await self.async_reward_fn(
areal/workflow/multi_turn.py:93:                completions_str,
areal/workflow/multi_turn.py:113:            if reward == 0.0 and t < self.max_turns:
areal/workflow/multi_turn.py:123:        reward = float(reward * discount)
areal/workflow/multi_turn.py:125:        # Log reward.
areal/workflow/multi_turn.py:127:            reward=reward, num_turns=t
areal/workflow/multi_turn.py:135:            rewards=torch.tensor(reward, dtype=torch.float32),
areal/workflow/rlvr.py:12:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/rlvr.py:44:    """Single-turn reward learning workflow supporting optional thinking tokens."""
areal/workflow/rlvr.py:48:        reward_fn: Callable[..., Any] | str,
areal/workflow/rlvr.py:57:        self.reward_fn = reward_fn
areal/workflow/rlvr.py:66:        if not isinstance(reward_fn, str):
areal/workflow/rlvr.py:67:            self.async_reward_fn = AsyncRewardWrapper(reward_fn)
areal/workflow/rlvr.py:77:    @trace_session("reward")
areal/workflow/rlvr.py:78:    async def _compute_rewards(
areal/workflow/rlvr.py:84:        """Decode completion and compute reward.
areal/workflow/rlvr.py:86:        Traces reward phase execution for SessionTracer. Decodes output tokens
areal/workflow/rlvr.py:87:        to string, calls async reward function, and logs metric to stats tracker.
areal/workflow/rlvr.py:92:            Reward value.
areal/workflow/rlvr.py:94:        completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/rlvr.py:95:        reward = await self.async_reward_fn(
areal/workflow/rlvr.py:97:            completions_str,
areal/workflow/rlvr.py:103:        return reward
areal/workflow/rlvr.py:113:        """Generate one sample and compute its reward.
areal/workflow/rlvr.py:116:        computes reward, and logs metrics. SessionTracer automatically
areal/workflow/rlvr.py:117:        tracks generate and reward phases via @trace_session decorators.
areal/workflow/rlvr.py:122:            Model response and reward value.
areal/workflow/rlvr.py:127:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/rlvr.py:129:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/workflow/rlvr.py:131:        return resp, reward
areal/workflow/rlvr.py:136:        # NOTE: load reward function dynamically if given as string
areal/workflow/rlvr.py:137:        if isinstance(self.reward_fn, str):
areal/workflow/rlvr.py:138:            self.reward_fn = import_from_string(self.reward_fn)
areal/workflow/rlvr.py:139:            self.async_reward_fn = AsyncRewardWrapper(self.reward_fn)
areal/workflow/rlvr.py:155:        # Generate single response and compute reward
areal/workflow/rlvr.py:156:        resp, reward = await self._collect_samples(engine, req, prompt_str, data)
areal/workflow/rlvr.py:170:            "rewards": torch.tensor(reward, dtype=torch.float32),
areal/trainer/ppo/actor_qun_team.py:31:    reward_overlong_penalty,
areal/trainer/ppo/actor_qun_team.py:44:        self.reward_bias = config.reward_bias
areal/trainer/ppo/actor_qun_team.py:45:        self.reward_scaling = config.reward_scaling
areal/trainer/ppo/actor_qun_team.py:46:        self.reward_clip = config.reward_clip
areal/trainer/ppo/actor_qun_team.py:52:        self.reward_norm = (
areal/trainer/ppo/actor_qun_team.py:53:            Normalization(config.reward_norm) if config.reward_norm else None
areal/trainer/ppo/actor_qun_team.py:117:            f"  reward_norm: {config.reward_norm if config.reward_norm else 'DISABLED (None)'}"
areal/trainer/ppo/actor_qun_team.py:231:            ig_params = self.config.ig_reward_params
areal/trainer/ppo/actor_qun_team.py:273:            min_mbs = self.config.ig_reward_params.mini_batch_size
areal/trainer/ppo/actor_qun_team.py:322:    @trace_perf("ppo_actor.process_ig_rewards", category="compute")
areal/trainer/ppo/actor_qun_team.py:323:    def process_ig_rewards(self, data: dict[str, Any], logprobs_list: list[torch.Tensor], metadata: Any) -> dict[
areal/trainer/ppo/actor_qun_team.py:326:            ig_params = self.config.ig_reward_params
areal/trainer/ppo/actor_qun_team.py:337:            from areal.utils.functional.functional import assign_ig_rewards
areal/trainer/ppo/actor_qun_team.py:338:            return assign_ig_rewards(
areal/trainer/ppo/actor_qun_team.py:345:                reward_mode=getattr(ig_params, "reward_mode", "prob")
areal/trainer/ppo/actor_qun_team.py:348:            self._brutal_crash_reporter("process_ig_rewards", e)
areal/trainer/ppo/actor_qun_team.py:358:        # Reward Penalty on length
areal/trainer/ppo/actor_qun_team.py:359:        if self.config.overlong_reward_penalty:
areal/trainer/ppo/actor_qun_team.py:364:            data = reward_overlong_penalty(
areal/trainer/ppo/actor_qun_team.py:371:        # Reward Scaling
areal/trainer/ppo/actor_qun_team.py:372:        reward_score = data["rewards"]
areal/trainer/ppo/actor_qun_team.py:373:        reward_score = (reward_score + self.reward_bias) * self.reward_scaling
areal/trainer/ppo/actor_qun_team.py:374:        reward_score = torch.clip(
areal/trainer/ppo/actor_qun_team.py:375:            reward_score, max=self.reward_clip, min=-self.reward_clip
areal/trainer/ppo/actor_qun_team.py:377:        if self.reward_norm:
areal/trainer/ppo/actor_qun_team.py:378:            reward_score = self.reward_norm(reward_score)
areal/trainer/ppo/actor_qun_team.py:402:        # Compute KL-regularized rewards.
areal/trainer/ppo/actor_qun_team.py:407:        rewards = -self.kl_ctl * self.kl_estimator(old_logp, ref_logp)
areal/trainer/ppo/actor_qun_team.py:408:        kl_rewards = rewards.clone()
areal/trainer/ppo/actor_qun_team.py:409:        rewards[batch_indices, seqlens - 1] = 0
areal/trainer/ppo/actor_qun_team.py:413:            rewards[batch_indices, indices] += torch.where(
areal/trainer/ppo/actor_qun_team.py:414:                seq_no_eos_mask, 0, reward_score
areal/trainer/ppo/actor_qun_team.py:417:            rewards[batch_indices, indices] += reward_score
areal/trainer/ppo/actor_qun_team.py:420:            values = torch.zeros_like(rewards)
areal/trainer/ppo/actor_qun_team.py:426:        # 1. Main Stream: Outcome + KL Reward
areal/trainer/ppo/actor_qun_team.py:428:            rewards=rewards,
areal/trainer/ppo/actor_qun_team.py:434:        # 2. IG Stream: Step-based Information Gain Reward
areal/trainer/ppo/actor_qun_team.py:437:        if "token_level_rewards" in data:
areal/trainer/ppo/actor_qun_team.py:438:            raw_step_rewards = data["token_level_rewards"]
areal/trainer/ppo/actor_qun_team.py:439:            step_mask = (raw_step_rewards != 0).float()
areal/trainer/ppo/actor_qun_team.py:443:            scaled_step_rewards = torch.sign(raw_step_rewards) * torch.log1p(torch.abs(raw_step_rewards))
areal/trainer/ppo/actor_qun_team.py:445:            len_penalties = data.get("token_level_len_penalties", torch.zeros_like(raw_step_rewards))
areal/trainer/ppo/actor_qun_team.py:446:            scaled_step_rewards += len_penalties
areal/trainer/ppo/actor_qun_team.py:449:            if self.config.ig_reward_params:
areal/trainer/ppo/actor_qun_team.py:450:                ig_lambda = self.config.ig_reward_params.lambda_val
areal/trainer/ppo/actor_qun_team.py:452:            final_step_rewards = scaled_step_rewards * ig_lambda
areal/trainer/ppo/actor_qun_team.py:457:                step_boundary_mask = torch.ones_like(rewards, dtype=torch.float32)
areal/trainer/ppo/actor_qun_team.py:461:                rewards=final_step_rewards,
areal/trainer/ppo/actor_qun_team.py:472:                    ig_mean = final_step_rewards[step_mask.bool()].mean().item() if step_count > 0 else 0.0
areal/trainer/ppo/actor_qun_team.py:473:                    ig_std = final_step_rewards[step_mask.bool()].std().item() if step_count > 0 else 0.0
areal/trainer/ppo/actor_qun_team.py:474:                    ig_max = final_step_rewards[step_mask.bool()].max().item() if step_count > 0 else 0.0
areal/trainer/ppo/actor_qun_team.py:475:                    ig_min = final_step_rewards[step_mask.bool()].min().item() if step_count > 0 else 0.0
areal/trainer/ppo/actor_qun_team.py:476:                    logger.info(f"[IG-Debug] Step Rewards Non-Zero Count: {step_count}, Mean: {ig_mean}, Std: {ig_std}, Max: {ig_max}, Min: {ig_min}")
areal/trainer/ppo/actor_qun_team.py:479:                        f"[IG-Debug] Step Rewards Non-Zero Count: {step_count}")
areal/trainer/ppo/actor_qun_team.py:481:            rewards += final_step_rewards
areal/trainer/ppo/actor_qun_team.py:484:            # [新增] 将 Step Reward 相关指标存入 data 字典，供 ppo_update 收集
areal/trainer/ppo/actor_qun_team.py:486:            data["raw_step_rewards"] = raw_step_rewards
areal/trainer/ppo/actor_qun_team.py:487:            data["final_step_rewards"] = final_step_rewards
areal/trainer/ppo/actor_qun_team.py:501:        data["kl_rewards"] = kl_rewards
areal/trainer/ppo/actor_qun_team.py:502:        data["tot_rewards"] = rewards
areal/trainer/ppo/actor_qun_team.py:510:            rewards: torch.Tensor,
areal/trainer/ppo/actor_qun_team.py:516:        bs = rewards.shape[0]
areal/trainer/ppo/actor_qun_team.py:517:        max_seqlen = rewards.shape[1]
areal/trainer/ppo/actor_qun_team.py:526:            delta = rewards[:, t] + self.discount * nextvalues - values[:, t]
areal/trainer/ppo/actor_qun_team.py:545:        reward_score = data["rewards"]
areal/trainer/ppo/actor_qun_team.py:550:            "correct_n_seqs": (reward_score > 0).bool(),
areal/trainer/ppo/actor_qun_team.py:551:            "incorrect_n_seqs": (reward_score <= 0).bool(),
areal/trainer/ppo/actor_qun_team.py:565:            n_seqs=torch.ones_like(reward_score, dtype=torch.bool),
areal/trainer/ppo/actor_qun_team.py:587:            kl_rewards=data["kl_rewards"],
areal/trainer/ppo/actor_qun_team.py:588:            final_reward=data["tot_rewards"],
areal/trainer/ppo/actor_qun_team.py:593:        # [新增] 正式提交 Step Reward 统计指标
areal/trainer/ppo/actor_qun_team.py:595:        if "final_step_rewards" in data:
areal/trainer/ppo/actor_qun_team.py:598:                raw_step_reward=data["raw_step_rewards"],
areal/trainer/ppo/actor_qun_team.py:599:                final_step_reward=data["final_step_rewards"],
areal/trainer/ppo/actor_qun_team.py:603:            seq_step_rewards = data["final_step_rewards"].sum(dim=-1)
areal/trainer/ppo/actor_qun_team.py:606:                seq_step_reward_sum=seq_step_rewards,
areal/trainer/ppo/actor_qun_team.py:610:            # 3. 计算并记录全局 Step Reward 的标准差 (Std)
areal/trainer/ppo/actor_qun_team.py:612:            valid_final_rewards = data["final_step_rewards"][step_mask_bool]
areal/trainer/ppo/actor_qun_team.py:613:            valid_raw_rewards = data["raw_step_rewards"][step_mask_bool]
areal/trainer/ppo/actor_qun_team.py:616:            final_std = valid_final_rewards.std() if valid_final_rewards.numel() > 1 else torch.tensor(0.0)
areal/trainer/ppo/actor_qun_team.py:617:            raw_std = valid_raw_rewards.std() if valid_raw_rewards.numel() > 1 else torch.tensor(0.0)
areal/trainer/ppo/actor_qun_team.py:620:                final_step_reward_std=final_std.item(),
areal/trainer/ppo/actor_qun_team.py:621:                raw_step_reward_std=raw_std.item()
areal/trainer/ppo/actor_qun_team.py:627:            task_reward=reward_score.float(),
areal/trainer/ppo/actor_qun_team.py:655:        for key in ["rewards", "tot_rewards", "kl_rewards", "raw_step_rewards", "final_step_rewards", "step_mask"]:
areal/workflow/openai/math_agent.py:16:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/openai/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/openai/math_agent.py:20:    ans = parse(completions)
areal/workflow/openai/math_agent.py:38:        comp: ChatCompletion = await client.chat.completions.create(
areal/workflow/openai/math_agent.py:42:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:43:        return await reward_fn(
areal/workflow/openai/math_agent.py:44:            completions=comp.choices[0].message.content, answer=data["answer"]
areal/workflow/openai/math_agent.py:59:        rewards = {}
areal/workflow/openai/math_agent.py:64:            response: ChatCompletion = await client.chat.completions.create(
areal/workflow/openai/math_agent.py:71:            reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:72:            reward = await reward_fn(completions=message.content, answer=data["answer"])
areal/workflow/openai/math_agent.py:73:            rewards[response.id] = reward
areal/workflow/openai/math_agent.py:74:            if reward == 1:
areal/workflow/openai/math_agent.py:80:                        "content": "Your answer is either wrong or not parsable to the reward function. You may misunderstand the original question. "
areal/workflow/openai/math_agent.py:84:        return rewards
areal/workflow/openai/math_agent.py:164:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/openai/math_agent.py:165:        reward = await reward_fn(completions=result.final_output, answer=data["answer"])
areal/workflow/openai/math_agent.py:166:        return reward
areal/workflow/rlvr_qun_team.py:13:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/rlvr_qun_team.py:45:    """Single-turn reward learning workflow supporting optional thinking tokens."""
areal/workflow/rlvr_qun_team.py:49:        reward_fn: Callable[..., Any] | str,
areal/workflow/rlvr_qun_team.py:54:        ig_reward_params=None,
areal/workflow/rlvr_qun_team.py:59:        reward_timeout_seconds: float = 15.0,
areal/workflow/rlvr_qun_team.py:61:        self.reward_fn = reward_fn
areal/workflow/rlvr_qun_team.py:71:        self.ig_reward_params = ig_reward_params
areal/workflow/rlvr_qun_team.py:72:        self.reward_timeout_seconds = reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:73:        if not isinstance(reward_fn, str):
areal/workflow/rlvr_qun_team.py:74:            self.async_reward_fn = AsyncRewardWrapper(
areal/workflow/rlvr_qun_team.py:75:                reward_fn, timeout_seconds=reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:86:    @trace_session("reward")
areal/workflow/rlvr_qun_team.py:87:    async def _compute_rewards(
areal/workflow/rlvr_qun_team.py:93:        """Decode completion and compute reward.
areal/workflow/rlvr_qun_team.py:95:        Traces reward phase execution for SessionTracer. Decodes output tokens
areal/workflow/rlvr_qun_team.py:96:        to string, calls async reward function, and logs metric to stats tracker.
areal/workflow/rlvr_qun_team.py:101:            Reward value.
areal/workflow/rlvr_qun_team.py:103:        completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/rlvr_qun_team.py:104:        reward = await self.async_reward_fn(
areal/workflow/rlvr_qun_team.py:106:            completions_str,
areal/workflow/rlvr_qun_team.py:112:        return reward
areal/workflow/rlvr_qun_team.py:122:        """Generate one sample and compute its reward.
areal/workflow/rlvr_qun_team.py:125:        computes reward, and logs metrics. SessionTracer automatically
areal/workflow/rlvr_qun_team.py:126:        tracks generate and reward phases via @trace_session decorators.
areal/workflow/rlvr_qun_team.py:131:            Model response and reward value.
areal/workflow/rlvr_qun_team.py:136:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/rlvr_qun_team.py:138:        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
areal/workflow/rlvr_qun_team.py:140:        return resp, reward
areal/workflow/rlvr_qun_team.py:147:       …595 tokens truncated…308:            "step_reward_mask": step_reward_mask,
areal/workflow/rlvr_qun_team.py:315:        # NOTE: load reward function dynamically if given as string
areal/workflow/rlvr_qun_team.py:316:        if isinstance(self.reward_fn, str):
areal/workflow/rlvr_qun_team.py:317:            self.reward_fn = import_from_string(self.reward_fn)
areal/workflow/rlvr_qun_team.py:318:            self.async_reward_fn = AsyncRewardWrapper(
areal/workflow/rlvr_qun_team.py:319:                self.reward_fn, timeout_seconds=self.reward_timeout_seconds
areal/workflow/rlvr_qun_team.py:336:        # Generate single response and compute reward
areal/workflow/rlvr_qun_team.py:337:        resp, reward = await self._collect_samples(engine, req, prompt_str, data)
areal/workflow/rlvr_qun_team.py:351:            "rewards": torch.tensor(reward, dtype=torch.float32),
areal/workflow/rlvr_qun_team.py:355:        ig_backend = getattr(self.ig_reward_params, "compute_backend", "actor") if self.ig_reward_params else None
areal/workflow/rlvr_qun_team.py:357:        if self.ig_reward_params:
areal/workflow/rlvr_qun_team.py:360:                ig_results = await self._compute_ig_probes(engine, resp, data, reward)
areal/workflow/rlvr_qun_team.py:376:                    f"IG Reward is enabled but no ground truth found in data. "
areal/workflow/rlvr_qun_team.py:390:            sep_id = self.tokenizer.encode(self.ig_reward_params.sep_token, add_special_tokens=False)[-1]
areal/workflow/rlvr_qun_team.py:397:            bridge_ids = self.tokenizer.encode(self.ig_reward_params.bridge_text, add_special_tokens=False)
areal/workflow/anthropic/math_agent.py:7:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/anthropic/math_agent.py:10:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/anthropic/math_agent.py:11:    ans = parse(completions)
areal/workflow/anthropic/math_agent.py:33:            Reward value for this trajectory
areal/workflow/anthropic/math_agent.py:77:        # Calculate reward
areal/workflow/anthropic/math_agent.py:78:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/anthropic/math_agent.py:79:        return await reward_fn(completion_text, data["answer"])
areal/workflow/anthropic/claude_math_agent.py:15:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/anthropic/claude_math_agent.py:18:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/anthropic/claude_math_agent.py:19:    ans = parse(completions)
areal/workflow/anthropic/claude_math_agent.py:107:            Reward value for this trajectory
areal/workflow/anthropic/claude_math_agent.py:158:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/anthropic/claude_math_agent.py:159:        reward = await reward_fn(completions=final_output, answer=data["answer"])
areal/workflow/anthropic/claude_math_agent.py:160:        return reward
areal/workflow/vision_rlvr.py:12:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/vision_rlvr.py:29:        reward_fn: Callable[..., Any] | str,
areal/workflow/vision_rlvr.py:36:            reward_fn,
areal/workflow/vision_rlvr.py:45:    @trace_session("reward")
areal/workflow/vision_rlvr.py:46:    async def _compute_rewards(
areal/workflow/vision_rlvr.py:52:        """Decode completion and compute reward.
areal/workflow/vision_rlvr.py:54:        Traces reward phase execution for SessionTracer. Decodes output tokens
areal/workflow/vision_rlvr.py:55:        to string, calls async reward function with keyword arguments, and logs
areal/workflow/vision_rlvr.py:61:            Reward value.
areal/workflow/vision_rlvr.py:64:        completions_str = self.tokenizer.decode(resp.output_tokens)
areal/workflow/vision_rlvr.py:65:        reward = await self.async_reward_fn(
areal/workflow/vision_rlvr.py:67:            completions=completions_str,
areal/workflow/vision_rlvr.py:73:        return reward
areal/workflow/vision_rlvr.py:83:        """Generate one sample and compute its reward.
areal/workflow/vision_rlvr.py:86:        computes reward, and logs metrics. SessionTracer automatically
areal/workflow/vision_rlvr.py:87:        tracks generate and reward phases via @trace_session decorators.
areal/workflow/vision_rlvr.py:92:            Model response and reward value.
areal/workflow/vision_rlvr.py:97:        reward = await self._compute_rewards(resp, prompt_str, task_data)
areal/workflow/vision_rlvr.py:99:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/workflow/vision_rlvr.py:101:        return resp, reward
areal/workflow/vision_rlvr.py:106:        # NOTE: load reward function dynamically if given as string
areal/workflow/vision_rlvr.py:107:        if isinstance(self.reward_fn, str):
areal/workflow/vision_rlvr.py:108:            self.reward_fn = import_from_string(self.reward_fn)
areal/workflow/vision_rlvr.py:109:            self.async_reward_fn = AsyncRewardWrapper(self.reward_fn)
areal/workflow/vision_rlvr.py:136:        # Generate single response and compute reward
areal/workflow/vision_rlvr.py:137:        resp, reward = await self._collect_samples(engine, req, prompt_str, data)
areal/workflow/vision_rlvr.py:161:            "rewards": torch.tensor(reward, dtype=torch.float32).unsqueeze(0),
areal/workflow/langchain/math_agent.py:16:from areal.api.reward_api import AsyncRewardWrapper
areal/workflow/langchain/math_agent.py:19:def math_reward_fn(completions: str, answer: str) -> float:
areal/workflow/langchain/math_agent.py:20:    """Calculate reward based on math answer correctness."""
areal/workflow/langchain/math_agent.py:21:    ans = parse(completions)
areal/workflow/langchain/math_agent.py:73:    This agent uses the AReaL proxy to generate responses and calculates
areal/workflow/langchain/math_agent.py:74:    rewards based on math answer correctness.
areal/workflow/langchain/math_agent.py:88:            Reward value for this trajectory
areal/workflow/langchain/math_agent.py:109:        # Calculate reward
areal/workflow/langchain/math_agent.py:110:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/langchain/math_agent.py:111:        return await reward_fn(completions=completion_text, answer=data["answer"])
areal/workflow/langchain/math_agent.py:132:            Reward value for this trajectory
areal/workflow/langchain/math_agent.py:180:        # Calculate reward
areal/workflow/langchain/math_agent.py:181:        reward_fn = AsyncRewardWrapper(math_reward_fn)
areal/workflow/langchain/math_agent.py:182:        return await reward_fn(completions=final_answer, answer=data["answer"])
examples/code/nemotron_configs.py:14:class InfoGainRewardConfig:
examples/code/nemotron_configs.py:22:    reward_mode: str = field(default="prob")
examples/code/nemotron_configs.py:32:    ig_reward_params: InfoGainRewardConfig | None = field(default=None)
examples/code/nemotron_configs.py:36:class CodeRewardConfig:
examples/code/nemotron_configs.py:37:    """Budget for the competitive-coding execution reward.
examples/code/nemotron_configs.py:41:    Note: with a string-based ``reward_fn`` (required for RPC serialization),
examples/code/nemotron_configs.py:42:    the per-test fields below currently document the reward fn's defaults.
examples/code/nemotron_configs.py:44:    ``areal.reward.code.competitive_exec.nemotron_competitive_reward_fn`` or
examples/code/nemotron_configs.py:46:    into the reward fn as kwargs).
examples/code/nemotron_configs.py:52:    reward_timeout_seconds: float = field(default=90.0)
examples/code/nemotron_configs.py:66:    code_reward: CodeRewardConfig = field(default_factory=CodeRewardConfig)
examples/code/nemotron_code_rl.py:5:  reward    areal.reward.code.competitive_exec (subprocess sandbox, pass rate)
examples/code/nemotron_code_rl.py:6:  workflow  areal.workflow.rlvr_qun_team.RLVRWorkflow (with extended reward timeout)
examples/code/nemotron_code_rl.py:9:  ``reward_fn`` and ``workflow`` are passed as import-path strings so they
examples/code/nemotron_code_rl.py:11:  ``areal.reward.code.nemotron_competitive_reward_fn`` match the
examples/code/nemotron_code_rl.py:12:  ``code_reward`` fields in the YAML template.
examples/code/nemotron_code_rl.py:31:    from areal.reward.code import assert_toolchain_or_warn
examples/code/nemotron_code_rl.py:33:    assert_toolchain_or_warn(list(config.code_reward.probe_languages))
examples/code/nemotron_code_rl.py:35:    if config.actor.ig_reward_params is not None:
examples/code/nemotron_code_rl.py:36:        config.actor.ig_reward_params.sep_token_id = tokenizer.encode(
examples/code/nemotron_code_rl.py:37:            config.actor.ig_reward_params.sep_token, add_special_tokens=False
examples/code/nemotron_code_rl.py:39:        config.actor.ig_reward_params.pad_token_id = tokenizer.pad_token_id
examples/code/nemotron_code_rl.py:63:            reward_fn="areal.reward.code.nemotron_competitive_reward_fn",
examples/code/nemotron_code_rl.py:67:            reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
examples/code/nemotron_code_rl.py:77:        workflow_kwargs["ig_reward_params"] = config.actor.ig_reward_params
examples/code/grpo_template_code.yaml:33:  dump_to_file: true
examples/code/grpo_template_code.yaml:82:  reward_scaling: 1.0
examples/code/grpo_template_code.yaml:83:  reward_bias: 0.0
examples/code/grpo_template_code.yaml:93:  reward_norm:
examples/code/grpo_template_code.yaml:147:# Reward — competitive coding execution budget (per-test / per-sample)
examples/code/grpo_template_code.yaml:149:# max_tests             : upper bound on tests run per reward call (subsample)
examples/code/grpo_template_code.yaml:151:# reward_timeout_seconds: total AsyncRewardWrapper timeout per sample
examples/code/grpo_template_code.yaml:157:code_reward:
examples/code/grpo_template_code.yaml:161:  reward_timeout_seconds: 90.0
areal/trainer/rw/rw_engine.py:21:        """Train on a batch(reward model)"""
areal/trainer/rl_trainer_qun_team.py:393:            # --- IG Reward Computation (Explicit loop for alignment) ---
areal/trainer/rl_trainer_qun_team.py:395:            ig_backend = getattr(config.actor.ig_reward_params, "compute_backend", "actor") if config.actor.ig_reward_params else None
areal/trainer/rl_trainer_qun_team.py:397:            if config.actor.ig_reward_params and ig_backend == "actor":
areal/trainer/rl_trainer_qun_team.py:399:                    stats_tracker.record_timing("compute_ig_reward"),
areal/trainer/rl_trainer_qun_team.py:401:                        "train.compute_ig_reward",
areal/trainer/rl_trainer_qun_team.py:420:                    # 3. 计算最终 IG rewards 并合入 rollout_batch
areal/trainer/rl_trainer_qun_team.py:421:                    rollout_batch = self.actor.process_ig_rewards(rollout_batch, ig_logprobs_list, ig_metadata)
areal/trainer/rl_trainer_qun_team.py:423:                    self.actor.get_device_stats().log("compute ig reward")
areal/utils/data.py:196:            # Skip 1D tensors like rewards
areal/utils/perf_tracer.py:253:    - Phase boundaries: GENERATE_START, GENERATE_END, REWARD_START, REWARD_END,
areal/utils/perf_tracer.py:267:    REWARD_START : str
areal/utils/perf_tracer.py:268:        Reward computation phase has started.
areal/utils/perf_tracer.py:269:    REWARD_END : str
areal/utils/perf_tracer.py:270:        Reward computation phase has completed.
areal/utils/perf_tracer.py:289:    REWARD_START = "reward_start"
areal/utils/perf_tracer.py:290:    REWARD_END = "reward_end"
areal/utils/perf_tracer.py:331:        Phase name (e.g., "generate", "reward", "execution").
areal/utils/perf_tracer.py:436:    2. Phase executions: generate and reward phases with start/end times, supporting
areal/utils/perf_tracer.py:438:    3. Derived metrics: computed from timestamps (total_s, generate_s, reward_s)
areal/utils/perf_tracer.py:509:                name="reward",
areal/utils/perf_tracer.py:510:                start_event=SessionTraceEvent.REWARD_START,
areal/utils/perf_tracer.py:511:                end_event=SessionTraceEvent.REWARD_END,
areal/utils/perf_tracer.py:612:    def _compute_reward_time(record: SessionRecord) -> float | None:
areal/utils/perf_tracer.py:613:        return record._phase_total_duration("reward")
areal/utils/perf_tracer.py:641:                compute=cls._compute_reward_time,
areal/utils/perf_tracer.py:642:                key="reward_s",
areal/utils/perf_tracer.py:678:    "mark_reward_start": SessionTraceEvent.REWARD_START,
areal/utils/perf_tracer.py:679:    "mark_reward_end": SessionTraceEvent.REWARD_END,
areal/utils/perf_tracer.py:705:        "mark_generate_end", "mark_reward_start", "mark_reward_end", "increment_counter".
areal/utils/perf_tracer.py:830:        Phase name (e.g., "generate", "reward", "execution").
areal/utils/perf_tracer.py:847:    >>> with trace_session_phase("reward"):
areal/utils/perf_tracer.py:848:    ...     reward = reward_fn(prompt, completion)
areal/utils/perf_tracer.py:882:        Phase name (e.g., "generate", "reward").
areal/utils/perf_tracer.py:899:    >>> async with atrace_session_phase("reward"):
areal/utils/perf_tracer.py:900:    ...     reward = await reward_fn(prompt, completion)
areal/utils/perf_tracer.py:925:    executions (generate, reward, etc.), and computes derived metrics like total
areal/utils/perf_tracer.py:937:    - Derived metric computation (total_s, generate_s, reward_s, etc.)
areal/utils/perf_tracer.py:2051:        Phase name (e.g., "generate", "reward", "execution").
areal/utils/perf_tracer.py:2060:    ...     results = await self._compute_rewards(resps)
areal/utils/perf_tracer.py:2068:    >>> @trace_session("reward")
areal/utils/perf_tracer.py:2069:    ... async def _compute_rewards(self, resps):
areal/utils/perf_tracer.py:2071:    ...         reward = await self.async_reward_fn(...)
areal/trainer/ppo/critic.py:40:        for key in ["rewards", "tot_rewards", "kl_rewards", "versions"]:
areal/utils/functional/functional.py:477:# code modified from VERL: https://github.com/volcengine/verl/blob/main/verl/workers/reward_manager/dapo.py
areal/utils/functional/functional.py:478:def reward_overlong_penalty(
areal/utils/functional/functional.py:484:    reward_score = data["rewards"]
areal/utils/functional/functional.py:489:        reward_score_cur = reward_score[sample_idx]
areal/utils/functional/functional.py:493:        overlong_reward = min(
areal/utils/functional/functional.py:496:        reward_score_cur += overlong_reward
areal/utils/functional/functional.py:497:        reward_score[sample_idx] = reward_score_cur
areal/utils/functional/functional.py:499:    data["rewards"] = reward_score
areal/utils/functional/functional.py:504:# New Functions for Decomposed IG / PRM Reward Calculation
areal/utils/functional/functional.py:518:    outcome_rewards = data["rewards"]
areal/utils/functional/functional.py:542:        curr_outcome = outcome_rewards[i]
areal/utils/functional/functional.py:568:            "outcome_reward": curr_outcome,
areal/utils/functional/functional.py:633:def assign_ig_rewards(
areal/utils/functional/functional.py:640:        reward_mode: str = "prob_diff"
areal/utils/functional/functional.py:648:    if "token_level_rewards" not in data:
areal/utils/functional/functional.py:649:        data["token_level_rewards"] = torch.zeros_like(input_ids, dtype=torch.float32, device=device)
areal/utils/functional/functional.py:654:    # 新增：显式的步骤掩码。之前使用 (raw_step_rewards != 0) 过滤，
areal/utils/functional/functional.py:657:    if "step_reward_mask" not in data:
areal/utils/functional/functional.py:658:        data["step_reward_mask"] = torch.zeros_like(input_ids, dtype=torch.float32, device=device)
areal/utils/functional/functional.py:687:        outcome_reward = info["outcome_reward"]
areal/utils/functional/functional.py:707:        calculated_rewards, calculated_penalties = _compute_rewards_logic(
areal/utils/functional/functional.py:709:            outcome_reward=outcome_reward,
areal/utils/functional/functional.py:713:            reward_mode=reward_mode,
areal/utils/functional/functional.py:718:        for r, p, idx in zip(calculated_rewards, calculated_penalties, step_end_indices):
areal/utils/functional/functional.py:721:                data["token_level_rewards"][sample_idx, idx - 1] += r
areal/utils/functional/functional.py:725:                data["step_reward_mask"][sample_idx, idx - 1] = 1.0
areal/utils/functional/functional.py:846:def _compute_rewards_logic(
areal/utils/functional/functional.py:848:        outcome_reward,
areal/utils/functional/functional.py:852:        max_abs_step_reward=5.0,
areal/utils/functional/functional.py:853:        reward_mode="prob_diff",
areal/utils/functional/functional.py:858:    is_correct = outcome_reward > 0
areal/utils/functional/functional.py:859:    final_rewards = []
areal/utils/functional/functional.py:883:            if reward_mode == "prob_diff":
areal/utils/functional/functional.py:888:            elif reward_mode == "prob_diff_hindsight":
areal/utils/functional/functional.py:896:            elif reward_mode == "prob":
areal/utils/functional/functional.py:903:        clipped_r_t = torch.clamp(raw_r_t, -max_abs_step_reward, max_abs_step_reward)
areal/utils/functional/functional.py:914:        final_rewards.append(clipped_r_t)
areal/utils/functional/functional.py:918:        r = final_rewards[idx]
areal/utils/functional/functional.py:927:    if use_watermark_selection and len(final_rewards) > 0:
areal/utils/functional/functional.py:928:        new_rewards = []
areal/utils/functional/functional.py:932:        for i in range(len(final_rewards)):
areal/utils/functional/functional.py:934:            if i == len(final_rewards) - 1:
areal/utils/functional/functional.py:935:                new_rewards.append(torch.tensor(0.0).to(device) if device else 0.0)
areal/utils/functional/functional.py:947:                    -max_abs_step_reward, max_abs_step_reward
areal/utils/functional/functional.py:949:                new_rewards.append(clipped_r)
areal/utils/functional/functional.py:955:                new_rewards.append(torch.tensor(0.0, dtype=torch.float32, device=device) if device else 0.0)
areal/utils/functional/functional.py:958:        final_rewards = new_rewards
areal/utils/functional/functional.py:962:    elif use_peak_selection and len(final_rewards) > 0:
areal/utils/functional/functional.py:963:        new_rewards = []
areal/utils/functional/functional.py:965:        n = len(final_rewards)
areal/utils/functional/functional.py:977:                new_rewards.append(final_rewards[i])
areal/utils/functional/functional.py:980:                new_rewards.append(torch.tensor(0.0).to(device) if device else 0.0)
areal/utils/functional/functional.py:983:        final_rewards = new_rewards
areal/utils/functional/functional.py:986:    return final_rewards, final_penalties
areal/utils/functional/__init__.py:6:    reward_overlong_penalty,
areal/utils/functional/__init__.py:20:    "reward_overlong_penalty",
areal/utils/logging.py:33:#   - light_purple/purple: Workflows, Rewards, OpenAI (RL-specific)
areal/utils/logging.py:75:    # Rewards - purple
areal/utils/logging.py:76:    "GSM8KReward": "purple",
areal/utils/logging.py:77:    "Geometry3KReward": "purple",
areal/utils/logging.py:78:    "RewardUtils": "purple",
areal/utils/logging.py:79:    "RewardAPI": "purple",
areal/utils/logging.py:80:    "CompetitiveCodeReward": "purple",
areal/utils/logging.py:104:    "TokenLogpReward": "light_purple",
areal/utils/logging.py:439:    print("  - light_purple/purple: Workflows, Rewards, OpenAI (RL-specific)")
areal/utils/logging.py:458:        ("GSM8KReward", "Computing rewards..."),
areal/utils/testing_utils.py:18:from areal.experimental.openai.types import InteractionWithTokenLogpReward
areal/utils/testing_utils.py:157:    ) -> dict[str, Any] | None | dict[str, InteractionWithTokenLogpReward]:
areal/utils/testing_utils.py:175:            rewards=torch.randn(1),
```

### Tool Call - 2026-04-27 23:29:25 EDT

```
exec_command {"cmd":"rg -n \"class Normalization|def __call__|reward_norm|group_size\" areal -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Call - 2026-04-27 23:29:25 EDT

```
exec_command {"cmd":"sed -n '330,460p' areal/trainer/ppo/actor_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Call - 2026-04-27 23:29:25 EDT

```
exec_command {"cmd":"sed -n '300,380p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Output - 2026-04-27 23:29:25 EDT

```
Chunk ID: 67b729
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 2695
Output:
areal/engine/sglang_remote.py:379:        group_size: int = 1,
areal/engine/sglang_remote.py:391:            group_size=group_size,
areal/engine/sglang_remote.py:415:        group_size: int = 1,
areal/engine/sglang_remote.py:426:            group_size=group_size,
areal/engine/sglang_remote.py:435:        group_size: int = 1,
areal/engine/sglang_remote.py:444:            group_size=group_size,
areal/api/reward_api.py:136:    async def __call__(self, *args, **kwargs) -> float:
areal/api/cli_args.py:75:    group_size: int = field(
areal/api/cli_args.py:92:        ) and self.group_size < 1:
areal/api/cli_args.py:94:                f"group_size must be a positive integer when using group normalization, got {self.group_size}"
areal/api/cli_args.py:1060:    reward_norm: NormConfig | None = field(
areal/api/workflow_api.py:48:    def __call__(cls, *args, **kwargs):
areal/api/engine_api.py:200:        group_size: int = 1,
areal/api/engine_api.py:217:        group_size : int, optional
areal/api/engine_api.py:225:            Each trajectory contains tensors with shape [group_size, seqlen, ...].
areal/api/engine_api.py:236:        group_size: int = 1,
areal/api/engine_api.py:251:        group_size : int, optional
areal/api/engine_api.py:709:        group_size: int = 1,
areal/api/engine_api.py:736:        group_size : int, optional
areal/api/engine_api.py:814:        group_size: int = 1,
areal/api/engine_api.py:839:        group_size : int, optional
areal/api/engine_api.py:858:        group_size: int = 1,
areal/api/engine_api.py:868:            The ``dataloader``, ``workflow``, ``workflow_kwargs``, ``group_size``,
areal/api/engine_api.py:896:        group_size : int, optional
areal/experimental/engine/archon_runner.py:130:        pp_group_size: int,
areal/experimental/engine/archon_runner.py:137:        self.pp_group_size = pp_group_size
areal/experimental/engine/archon_runner.py:150:            pp_degree=self.pp_group_size,
areal/experimental/engine/archon_runner.py:310:    pp_group_size: int = 1,
areal/experimental/engine/archon_runner.py:322:            pp_group_size=pp_group_size,
areal/experimental/engine/archon_engine.py:334:            pp_group_size=self.parallel_dims.pp,
areal/experimental/engine/archon_engine.py:607:        group_size: int = 1,
areal/experimental/engine/archon_engine.py:615:            group_size=group_size,
areal/experimental/engine/archon_engine.py:624:        group_size: int = 1,
areal/experimental/engine/archon_engine.py:634:            group_size=group_size,
areal/experimental/models/archon/model_spec.py:39:    def __call__(
areal/experimental/models/archon/model_spec.py:72:    def __call__(
areal/engine/fsdp_engine.py:439:        group_size: int = 1,
areal/engine/fsdp_engine.py:446:            group_size=group_size,
areal/engine/fsdp_engine.py:455:        group_size: int = 1,
areal/engine/fsdp_engine.py:464:            group_size=group_size,
areal/engine/megatron_engine.py:459:        group_size: int = 1,
areal/engine/megatron_engine.py:466:            group_size=group_size,
areal/engine/megatron_engine.py:475:        group_size: int = 1,
areal/engine/megatron_engine.py:484:            group_size=group_size,
areal/engine/vllm_remote.py:379:        group_size: int = 1,
areal/engine/vllm_remote.py:391:            group_size=group_size,
areal/engine/vllm_remote.py:415:        group_size: int = 1,
areal/engine/vllm_remote.py:426:            group_size=group_size,
areal/engine/vllm_remote.py:435:        group_size: int = 1,
areal/engine/vllm_remote.py:444:            group_size=group_size,
areal/utils/data.py:1154:class Normalization:
areal/utils/data.py:1169:        self.group_size = config.group_size
areal/utils/data.py:1174:    def __call__(
areal/utils/data.py:1201:            for i in range(0, bs // self.group_size):
areal/utils/data.py:1202:                s = slice(i * self.group_size, (i + 1) * self.group_size)
areal/utils/data.py:1206:                # Special case: with group_size=1 and leave_one_out=True, mean should be 0
areal/utils/data.py:1207:                if self.group_size == 1 and self.mean_leave1out:
areal/utils/data.py:1252:            for i in range(0, bs // self.group_size):
areal/utils/data.py:1253:                s = slice(i * self.group_size, (i + 1) * self.group_size)
areal/utils/data.py:1258:                # Special case: with group_size=1 and std_unbiased=True, std should be 1 for numerical stability
areal/utils/data.py:1259:                if self.group_size == 1 and self.std_unbiased:
areal/utils/data.py:1286:                norm_x = norm_x * self.group_size
areal/utils/data.py:1405:    def __call__(
areal/models/tree_attn/triton_kernel.py:358:        gqa_group_size = H // H_kv
areal/models/tree_attn/triton_kernel.py:417:            GQA_GROUP_SIZE=gqa_group_size,
areal/models/tree_attn/triton_kernel.py:804:        gqa_group_size = H // H_kv
areal/models/tree_attn/triton_kernel.py:876:            GQA_GROUP_SIZE=gqa_group_size,
areal/models/tree_attn/triton_kernel.py:927:            GQA_GROUP_SIZE=gqa_group_size,
areal/infra/dist_rollout.py:166:        group_size: int = 1,
areal/infra/dist_rollout.py:186:        group_size : int, optional
areal/infra/dist_rollout.py:207:                group_size=group_size,
areal/infra/dist_rollout.py:221:        group_size: int = 1,
areal/infra/dist_rollout.py:241:        group_size : int, optional
areal/infra/dist_rollout.py:265:                group_size=group_size,
areal/infra/launcher/ray.py:400:        node_group_size = max(1, allocation_mode.gen_instance_size // n_gpus_per_node)
areal/infra/launcher/ray.py:417:            n_tasks: int, task_group_size: int, placement_group: PlacementGroup
areal/infra/launcher/ray.py:421:            for i in range(0, n_tasks, task_group_size):
areal/infra/launcher/ray.py:432:                        AREAL_SGLANG_MULTI_NODE_RANK=str(i % task_group_size),
areal/infra/launcher/ray.py:434:                            i // task_group_size
areal/infra/launcher/ray.py:437:                            master_ports[i // task_group_size]
areal/infra/launcher/ray.py:464:                partial(sglang_env_hook, n_sglang_nodes, node_group_size)
areal/infra/controller/rollout_controller.py:57:    group_size: int = 1
areal/infra/controller/rollout_controller.py:794:                    group_size=pending_task.group_size,
areal/infra/controller/rollout_controller.py:858:        group_size: int = 1,
areal/infra/controller/rollout_controller.py:878:            group_size=group_size,
areal/infra/controller/rollout_controller.py:904:        group_size: int = 1,
areal/infra/controller/rollout_controller.py:917:                group_size=group_size,
areal/infra/controller/rollout_controller.py:930:        group_size: int = 1,
areal/infra/controller/rollout_controller.py:954:                        group_size=group_size,
areal/infra/controller/train_controller.py:537:        group_size: int = 1,
areal/infra/controller/train_controller.py:545:            group_size=group_size,
areal/infra/controller/train_controller.py:555:        group_size: int = 1,
areal/infra/controller/train_controller.py:562:            group_size=group_size,
areal/trainer/ppo/actor.py:48:        self.reward_norm = (
areal/trainer/ppo/actor.py:49:            Normalization(config.reward_norm) if config.reward_norm else None
areal/trainer/ppo/actor.py:113:            f"  reward_norm: {config.reward_norm if config.reward_norm else 'DISABLED (None)'}"
areal/trainer/ppo/actor.py:155:        if self.reward_norm:
areal/trainer/ppo/actor.py:156:            reward_score = self.reward_norm(reward_score)
areal/infra/launcher/slurm.py:489:            node_group_size = max(
areal/infra/launcher/slurm.py:525:                            f"{prefix}_MULTI_NODE_RANK": i % node_group_size,
areal/infra/launcher/slurm.py:526:                            f"{prefix}_MULTI_NODE_MASTER_ADDR": f"${{master_addrs[{i // node_group_size * node_group_size}]}}",
areal/infra/launcher/slurm.py:527:                            f"{prefix}_MULTI_NODE_MASTER_PORT": f"${{master_ports[{i // node_group_size * node_group_size}]}}",
areal/infra/remote_inf_engine.py:64:        group_size: int,
areal/infra/remote_inf_engine.py:67:        if group_size < 1:
areal/infra/remote_inf_engine.py:68:            raise ValueError(f"group_size must be >= 1, got {group_size}")
areal/infra/remote_inf_engine.py:70:        self.group_size = group_size
areal/infra/remote_inf_engine.py:79:            *[self.workflow.arun_episode(engine, data) for _ in range(self.group_size)]
areal/infra/remote_inf_engine.py:529:        group_size: int = 1,
areal/infra/remote_inf_engine.py:545:            if group_size > 1:
areal/infra/remote_inf_engine.py:546:                resolved = GroupedRolloutWorkflow(resolved, group_size, self.logger)
areal/infra/remote_inf_engine.py:635:        # Wrap with GroupedRolloutWorkflow if group_size > 1
areal/infra/remote_inf_engine.py:636:        if group_size > 1:
areal/infra/remote_inf_engine.py:637:            resolved = GroupedRolloutWorkflow(resolved, group_size, self.logger)
areal/infra/remote_inf_engine.py:1009:        group_size: int = 1,
areal/infra/remote_inf_engine.py:1027:        group_size : int
areal/infra/remote_inf_engine.py:1050:            workflow, workflow_kwargs, group_size, proxy_addr=proxy_addr
areal/infra/remote_inf_engine.py:1097:        group_size: int = 1,
areal/infra/remote_inf_engine.py:1112:        group_size : int
areal/infra/remote_inf_engine.py:1127:            workflow, workflow_kwargs, group_size
areal/infra/remote_inf_engine.py:1141:        group_size: int = 1,
areal/infra/remote_inf_engine.py:1156:        group_size : int
areal/infra/remote_inf_engine.py:1173:            workflow, workflow_kwargs, group_size
areal/trainer/rl_trainer_qun_team.py:365:                    group_size=config.gconfig.n_samples,
areal/trainer/rl_trainer_qun_team.py:873:                                group_size=self.config.eval_gconfig.n_samples,
areal/trainer/rl_trainer_qun_team.py:887:                            group_size=self.config.eval_gconfig.n_samples,
areal/trainer/rl_trainer.py:334:                    group_size=config.gconfig.n_samples,
areal/trainer/rl_trainer.py:807:                        group_size=self.config.eval_gconfig.n_samples,
areal/trainer/ppo/actor_qun_team.py:52:        self.reward_norm = (
areal/trainer/ppo/actor_qun_team.py:53:            Normalization(config.reward_norm) if config.reward_norm else None
areal/trainer/ppo/actor_qun_team.py:117:            f"  reward_norm: {config.reward_norm if config.reward_norm else 'DISABLED (None)'}"
areal/trainer/ppo/actor_qun_team.py:377:        if self.reward_norm:
areal/trainer/ppo/actor_qun_team.py:378:            reward_score = self.reward_norm(reward_score)
```

### Tool Output - 2026-04-27 23:29:25 EDT

```
Chunk ID: 914489
Wall time: 0.0003 seconds
Process exited with code 0
Original token count: 1406
Output:
            for i, logprobs in enumerate(logprobs_list):
                if i < len(valid_batch_sizes):
                    original_bs = valid_batch_sizes[i]
                    cleaned_logprobs_list.append(logprobs[:original_bs])
                else:
                    cleaned_logprobs_list.append(logprobs)

            from areal.utils.functional.functional import assign_ig_rewards
            return assign_ig_rewards(
                data=data,
                logprobs_list=cleaned_logprobs_list,
                metadata_dict=metadata,
                beta=ig_params.beta,
                use_peak_selection=getattr(ig_params, "use_peak_selection", False),
                use_watermark_selection=getattr(ig_params, "use_watermark_selection", False),  # [新增透传] 读取 config 传递给底层
                reward_mode=getattr(ig_params, "reward_mode", "prob")
            )
        except Exception as e:
            self._brutal_crash_reporter("process_ig_rewards", e)

    @trace_perf("ppo_actor.compute_advantages", category="compute")
    def compute_advantages(self, data: dict[str, Any]) -> dict[str, Any]:
        bs = data["input_ids"].shape[0]
        max_seqlen = data["input_ids"].shape[1]
        batch_indices = torch.arange(
            bs, device=data["input_ids"].device, dtype=torch.long
        )

        # Reward Penalty on length
        if self.config.overlong_reward_penalty:
            overlong_tokens = self.config.overlong_tokens
            overlong_penalty_factor = self.config.overlong_penalty_factor
            assert overlong_tokens is not None
            assert overlong_penalty_factor is not None
            data = reward_overlong_penalty(
                data,
                overlong_tokens=overlong_tokens,
                overlong_penalty_factor=overlong_penalty_factor,
                max_response_length=self.config.max_new_tokens,
            )

        # Reward Scaling
        reward_score = data["rewards"]
        reward_score = (reward_score + self.reward_bias) * self.reward_scaling
        reward_score = torch.clip(
            reward_score, max=self.reward_clip, min=-self.reward_clip
        )
        if self.reward_norm:
            reward_score = self.reward_norm(reward_score)

        loss_mask = data["loss_mask"].float()
        loss_mask = torch.roll(loss_mask, shifts=-1, dims=-1)

        # Apply the mask to log probabilities.
        if not self.config.use_decoupled_loss and self.config.recompute_logprob:
            prox_logp_value = data["prox_logp"]
            if prox_logp_value is None:
                raise ValueError(
                    "prox_logp is None but recompute_logprob=True. "
                    "This indicates compute_logp() was skipped incorrectly."
                )
            old_logp = data["logprobs"] = prox_logp_value
        else:
            old_logp = torch.roll(data["logprobs"], shifts=-1, dims=-1)
            if not self.config.use_decoupled_loss:
                data["prox_logp"] = old_logp
        ref_logp = data.get("ref_logp")
        if ref_logp is None:
            ref_logp = torch.zeros_like(old_logp)
        ref_logp *= loss_mask
        old_logp *= loss_mask

        # Compute KL-regularized rewards.
        attn_mask = data["attention_mask"]
        seqlens = attn_mask.sum(-1).long()
        seq_no_eos_mask = seqlens == attn_mask.shape[1]

        rewards = -self.kl_ctl * self.kl_estimator(old_logp, ref_logp)
        kl_rewards = rewards.clone()
        rewards[batch_indices, seqlens - 1] = 0
        indices = torch.clip(seqlens - 2, min=0)

        if self.mask_no_eos_with_zero:
            rewards[batch_indices, indices] += torch.where(
                seq_no_eos_mask, 0, reward_score
            )
        else:
            rewards[batch_indices, indices] += reward_score

        if "values" not in data:
            values = torch.zeros_like(rewards)
        else:
            values = data["values"]

        # --- Dual-Stream GAE Computation ---

        # 1. Main Stream: Outcome + KL Reward
        adv_outcome = self._compute_general_advantage_estimation(
            rewards=rewards,
            values=values,
            loss_mask=loss_mask,
            seq_no_eos_mask=seq_no_eos_mask
        )

        # 2. IG Stream: Step-based Information Gain Reward
        adv_ig = torch.zeros_like(adv_outcome)

        if "token_level_rewards" in data:
            raw_step_rewards = data["token_level_rewards"]
            step_mask = (raw_step_rewards != 0).float()

            # 公式: sign(x) * ln(|x| + 1)
            # 作用: 完美保留正负号物理意义，将巨大差异的数值平滑压缩，且 0 值依然保持为 0
            scaled_step_rewards = torch.sign(raw_step_rewards) * torch.log1p(torch.abs(raw_step_rewards))

            len_penalties = data.get("token_level_len_penalties", torch.zeros_like(raw_step_rewards))
            scaled_step_rewards += len_penalties

            ig_lambda = 0.0
            if self.config.ig_reward_params:
                ig_lambda = self.config.ig_reward_params.lambda_val

            final_step_rewards = scaled_step_rewards * ig_lambda

            if "step_boundary_mask" in data:
                step_boundary_mask = data["step_boundary_mask"]
            else:
                step_boundary_mask = torch.ones_like(rewards, dtype=torch.float32)

            zeros_values = torch.zeros_like(values)
            adv_ig = self._compute_general_advantage_estimation(
```

### Tool Output - 2026-04-27 23:29:25 EDT

```
Chunk ID: 93df61
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 972
Output:
                token_level_len_penalties[reward_idx] = p.item() if isinstance(p, torch.Tensor) else float(p)
                step_reward_mask[reward_idx] = 1.0
            if abs_idx < seq_len:
                step_boundary_mask[abs_idx] = 0.0

        return {
            "token_level_rewards": token_level_rewards,
            "token_level_len_penalties": token_level_len_penalties,
            "step_reward_mask": step_reward_mask,
            "step_boundary_mask": step_boundary_mask,
        }

    async def arun_episode(
        self, engine: InferenceEngine, data: dict[str, Any]
    ) -> dict[str, torch.Tensor]:
        # NOTE: load reward function dynamically if given as string
        if isinstance(self.reward_fn, str):
            self.reward_fn = import_from_string(self.reward_fn)
            self.async_reward_fn = AsyncRewardWrapper(
                self.reward_fn, timeout_seconds=self.reward_timeout_seconds
            )

        input_ids = self.get_input_ids_fn(
            self.data_extract_prompt_fn(data),
            self.tokenizer,
            self.enable_thinking,
        )
        req = ModelRequest(
            rid=uuid.uuid4().hex,
            input_ids=input_ids,
            gconfig=self.gconfig.new(n_samples=1),
            tokenizer=self.tokenizer,
        )

        prompt_str = self.tokenizer.decode(input_ids)

        # Generate single response and compute reward
        resp, reward = await self._collect_samples(engine, req, prompt_str, data)

        # Build result tensor dict with batch dim 1
        seq = resp.input_tokens + resp.output_tokens
        logprobs = [0.0] * resp.input_len + resp.output_logprobs
        loss_mask = [0] * resp.input_len + [1] * resp.output_len
        versions = [-1] * resp.input_len + resp.output_versions

        res = {
            "input_ids": torch.tensor(seq, dtype=torch.int32),
            "loss_mask": torch.tensor(loss_mask, dtype=torch.int32),
            "logprobs": torch.tensor(logprobs, dtype=torch.float32),
            "versions": torch.tensor(versions, dtype=torch.int32),
            "attention_mask": torch.ones(len(seq), dtype=torch.bool),
            "rewards": torch.tensor(reward, dtype=torch.float32),
        }

        # [新增配置控制]: 支持通过配置项无缝切换计算后端
        ig_backend = getattr(self.ig_reward_params, "compute_backend", "actor") if self.ig_reward_params else None

        if self.ig_reward_params:
            if ig_backend == "rollout":
                # [无缝切换] 走 Rollout 异步探测分支
                ig_results = await self._compute_ig_probes(engine, resp, data, reward)
                if ig_results:
                    for k, v in ig_results.items():
                        # 直接把 1D 数组转换为 float32 Tensor 塞进去，
                        # 最后的 unsqueeze(0) 会完美兼容 actor 里的 shape (bs, seq_len)
                        res[k] = torch.tensor(v, dtype=torch.float32)

            # --- 下面这部分无论哪个 backend 都需要计算（为了防崩溃以及向 Actor 提供结构数据） ---
            # 准备ig计算所需的数据：提取并Tokenize标准答案 (Ground Truth)
            # 1. 尝试从 data 中提取标准答案文本
            # 常见的 key 为 'answer', 'solution' 或 'ground_truth'
            # 如果你的数据集结构比较特殊，建议在 __init__ 中传入一个 data_extract_target_fn
            gt_text = data.get("answer")

            if gt_text is None:
                logger.warning(
                    f"IG Reward is enabled but no ground truth found in data. "
                    f"Available keys: {list(data.keys())}. "
                    f"IG calculation will likely fail or return 0."
                )
                gt_ids = []
```

### Tool Call - 2026-04-27 23:29:31 EDT

```
exec_command {"cmd":"sed -n '1154,1296p' areal/utils/data.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:29:31 EDT

```
Chunk ID: 1ac683
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1375
Output:
class Normalization:
    """
    Adaptive normalization with different levels.

    Supports independent specification of normalization level for mean and std:
    - "batch": normalize across entire batch (with optional all_reduce in distributed setting)
    - "group": normalize within fixed-size groups
    - None: no centering or no std scaling
    """

    def __init__(self, config: NormConfig):
        self.mean_level = config.mean_level
        self.mean_leave1out = config.mean_leave1out
        self.std_level = config.std_level
        self.std_unbiased = config.std_unbiased
        self.group_size = config.group_size
        self.denominator = config.denominator
        self.eps = config.eps

    @torch.no_grad()
    def __call__(
        self,
        x: torch.Tensor,
        loss_mask: torch.Tensor | None = None,
        high_precision: bool = True,
        reduce_group=None,
    ) -> torch.Tensor:
        bs = x.size(0)
        eps = self.eps

        # Early return if no elements are active (all masked out)
        if loss_mask is not None and loss_mask.sum().item() == 0:
            return x.float()

        # Step 1: Compute mean
        if self.mean_level == "batch":
            mean = self._compute_mean(
                x,
                loss_mask,
                high_precision=high_precision,
                leave_one_out=self.mean_leave1out,
                all_reduce=True,
                reduce_group=reduce_group,
            )
            mean = mean.expand_as(x)
        elif self.mean_level == "group":
            mean = torch.zeros_like(x)
            for i in range(0, bs // self.group_size):
                s = slice(i * self.group_size, (i + 1) * self.group_size)
                xx = x[s]
                m = loss_mask[s] if loss_mask is not None else None

                # Special case: with group_size=1 and leave_one_out=True, mean should be 0
                if self.group_size == 1 and self.mean_leave1out:
                    dtype = torch.float64 if high_precision else torch.float32
                    group_mean = torch.zeros(
                        (1, *xx.shape[1:]), dtype=dtype, device=xx.device
                    )
                else:
                    group_mean = self._compute_mean(
                        xx,
                        m,
                        high_precision=high_precision,
                        leave_one_out=self.mean_leave1out,
                        all_reduce=False,
                        reduce_group=None,
                    )
                mean[s] = group_mean.expand_as(xx)
        else:  # mean_level == "none"
            mean = torch.zeros_like(x)

        # Subtract mean
        x_centered = x - mean
        # mask unrelevant elements as 0
        if loss_mask is not None:
            x_centered = x_centered * loss_mask

        # Step 2: Compute denominator (Std or K:correct_nums or N:all_nums)
        if self.denominator == "k":
            compute_denominator = self._compute_k
        elif self.denominator == "n":
            compute_denominator = self._compute_n
        else:
            compute_denominator = self._compute_std

        if self.std_level == "batch":
            denominator = compute_denominator(
                x,
                loss_mask,
                mean,
                unbiased=self.std_unbiased,
                high_precision=high_precision,
                all_reduce=True,
                reduce_group=reduce_group,
            )
            denominator = denominator.expand_as(x)
        elif self.std_level == "group":
            denominator = torch.zeros_like(x)
            for i in range(0, bs // self.group_size):
                s = slice(i * self.group_size, (i + 1) * self.group_size)
                xx = x[s]
                m = loss_mask[s] if loss_mask is not None else None
                group_mean_slice = mean[s]  # already computed and expanded

                # Special case: with group_size=1 and std_unbiased=True, std should be 1 for numerical stability
                if self.group_size == 1 and self.std_unbiased:
                    dtype = torch.float64 if high_precision else torch.float32
                    group_denominator = torch.ones(
                        (1, *xx.shape[1:]), dtype=dtype, device=xx.device
                    )
                else:
                    group_denominator = compute_denominator(
                        xx,
                        m,
                        group_mean_slice,
                        unbiased=self.std_unbiased,
                        high_precision=high_precision,
                        all_reduce=False,
                        reduce_group=reduce_group,
                    )
                denominator[s] = group_denominator.expand_as(xx)
        else:
            denominator = torch.ones_like(x)
            eps = 0.0

        # Normalize
        norm_x = (x_centered / (denominator + eps)).float()

        # Scaling correction for 'k' and 'n' methods to counter-act mean-reduction loss
        # The user requested to multiply by n (group size or batch size)
        if self.denominator in ["n", "k"]:
            if self.std_level == "group":
                norm_x = norm_x * self.group_size
            elif self.std_level == "batch":
                norm_x = norm_x * bs

        return norm_x

    @staticmethod
    def _compute_mean(
        x: torch.Tensor,
        mask: torch.Tensor | None,
        high_precision: bool,
```

### Tool Call - 2026-04-27 23:29:46 EDT

```
exec_command {"cmd":"sed -n '1,180p' areal/api/reward_api.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":9000}
```

### Tool Output - 2026-04-27 23:29:46 EDT

```
Chunk ID: 194547
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1750
Output:
import asyncio
import os
import threading
import traceback
import weakref
from collections.abc import Callable
from concurrent.futures import ProcessPoolExecutor
from concurrent.futures.process import BrokenProcessPool
from functools import partial

from areal.utils import logging

logger = logging.getLogger("RewardAPI")


def _get_device_count_safely() -> int:
    """
    Safely get device count without initializing CUDA context.
    """
    gpu_types = ["nvidia", "davinci"]
    try:
        if os.path.exists("/dev"):
            for gpu_type in gpu_types:
                devices = [
                    f
                    for f in os.listdir("/dev")
                    if f.startswith(gpu_type) and f[len(gpu_type) :].isdigit()
                ]
                if devices:
                    return len(devices)
    except (OSError, ValueError) as e:
        # /dev doesn't exist or can't read (e.g., Windows, macOS)
        logger.debug(f"Could not read device list from /dev, using fallback: {e}")

    # Fallback: assume 8 devices for cautious max_workers calculation
    return 8


def reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    **kwargs,
):
    """This function is a placeholder for the reward function that will be used in the RLVR pipeline.

    In general, there's no restriction on the signature and implementation of this function in customized rollout workflows.
    It would be convinent to follow this signature and directly use it in our predefined rollout workflows.

    :param prompt: The string representing the task to be completed.
    :param completions: The string representing the trajectory generated by the model.
    :param prompt_ids: The token IDs of the prompt.
    :param completion_ids: The token IDs of the trajectory generated by the model.
    :param kwargs: Other attributes of the data in the dataset, such as solutions, input_outputs, etc.
        Any other attributes in the dataset will be passed as keyword arguments to this function.
    :rtype: float
    """


class AsyncRewardWrapper:
    """
    Wraps a synchronous reward function to make it async with timeout handling.
    Automatically manages ProcessPoolExecutor lifecycle based on instance count.
    Includes automatic recovery from broken process pools.
    """

    _executors = {}
    _instance_counts = {}
    _lock = threading.Lock()

    def __init__(
        self,
        reward_fn: Callable,
        timeout_seconds: float = 15,
        max_workers: int | None = None,
        max_retries: int = 3,
    ):
        self.reward_fn = reward_fn
        self.timeout_seconds = timeout_seconds
        if max_workers is None:
            cpu_count = os.cpu_count() or 1
            device_count = _get_device_count_safely()
            # Heuristic for max_workers: distribute CPU cores across devices,
            # then halve to be conservative, ensuring at least one worker.
            max_workers = max((cpu_count // device_count) // 2, 1)
        self.max_workers = max_workers
        self.max_retries = max_retries
        self._executor_key = max_workers

        with self._lock:
            if self._executor_key not in self._executors:
                self._executors[self._executor_key] = ProcessPoolExecutor(
                    max_workers=max_workers
                )
                self._instance_counts[self._executor_key] = 0
            self._instance_counts[self._executor_key] += 1

        weakref.finalize(self, AsyncRewardWrapper._cleanup_executor, max_workers)

    @classmethod
    def _cleanup_executor(cls, executor_key):
        """Called when an AsyncRewardWrapper instance is garbage collected"""
        with cls._lock:
            if executor_key in cls._instance_counts:
                cls._instance_counts[executor_key] -= 1
                if cls._instance_counts[executor_key] <= 0:
                    if executor_key in cls._executors:
                        executor = cls._executors.pop(executor_key)
                        executor.shutdown(wait=False, cancel_futures=True)
                        logger.debug(
                            f"ProcessPoolExecutor with {executor_key} workers shut down"
                        )
                    cls._instance_counts.pop(executor_key, None)

    @classmethod
    def _recreate_executor(cls, executor_key, max_workers):
        """Recreate a broken ProcessPoolExecutor"""
        with cls._lock:
            if executor_key in cls._executors:
                # Clean up the broken executor
                old_executor = cls._executors[executor_key]
                try:
                    old_executor.shutdown(wait=False)
                except Exception as e:
                    logger.warning(f"Error shutting down broken executor: {e}")

                # Create a new executor
                cls._executors[executor_key] = ProcessPoolExecutor(
                    max_workers=max_workers
                )
                logger.info(f"Recreated ProcessPoolExecutor with {max_workers} workers")
                return cls._executors[executor_key]
        return None

    async def __call__(self, *args, **kwargs) -> float:
        last_exception = None

        for attempt in range(self.max_retries + 1):
            with self._lock:
                executor = self._executors.get(self._executor_key)

            if executor is None:
                raise RuntimeError("ProcessPoolExecutor has been shut down")

            loop = asyncio.get_event_loop()
            try:
                future = loop.run_in_executor(
                    executor,
                    partial(self.reward_fn, *args, **kwargs),
                )
                reward = await asyncio.wait_for(
                    future,
                    timeout=self.timeout_seconds,
                )
                return reward
            except TimeoutError:
                logger.warning(
                    f"Computing reward timeout after {self.timeout_seconds}s. Set reward to 0."
                )
                return 0
            except BrokenProcessPool as e:
                last_exception = e
                logger.warning(
                    f"ProcessPoolExecutor broken (attempt {attempt + 1}/{self.max_retries + 1}). "
                    "Attempting to recreate..."
                )
                if attempt < self.max_retries:
                    # Try to recreate the executor
                    new_executor = self._recreate_executor(
                        self._executor_key, self.max_workers
                    )
                    if new_executor is None:
                        logger.error("Failed to recreate ProcessPoolExecutor")
                        break
                    # Continue to next attempt
                    continue
                else:
                    logger.error("Max retries exceeded for BrokenProcessPool.")
                    traceback.print_exc()
```

### Tool Call - 2026-04-27 23:29:51 EDT

```
exec_command {"cmd":"sed -n '180,260p' areal/api/reward_api.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":5000}
```

### Tool Output - 2026-04-27 23:29:51 EDT

```
Chunk ID: aae278
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 210
Output:
                    traceback.print_exc()
                    raise e
            except Exception as e:
                last_exception = e
                logger.error(f"Unexpected error in reward computation: {e}")
                if attempt < self.max_retries:
                    logger.info(
                        f"Retrying... (attempt {attempt + 1}/{self.max_retries + 1})"
                    )
                    continue
                else:
                    logger.error("Max retries exceeded for unexpected error.")
                    traceback.print_exc()
                    raise e

        # If we get here, all retries failed
        if last_exception:
            traceback.print_exc()
            raise last_exception
        else:
            raise RuntimeError("Reward computation failed after all retries.")
```

### Tool Call - 2026-04-27 23:30:06 EDT

```
exec_command {"cmd":"rg -n \"mask_no_eos_with_zero|overlong_reward_penalty|overlong_tokens|reward_clip\" areal/api/cli_args.py examples/code/grpo_template_code.yaml -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":6000}
```

### Tool Output - 2026-04-27 23:30:06 EDT

```
Chunk ID: 508be9
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 85
Output:
areal/api/cli_args.py:1068:    reward_clip: float = field(
areal/api/cli_args.py:1071:    overlong_reward_penalty: bool = field(
areal/api/cli_args.py:1075:    overlong_tokens: int | None = field(
areal/api/cli_args.py:1083:    mask_no_eos_with_zero: bool = field(
areal/api/cli_args.py:1268:    mask_no_eos_with_zero: bool = field(
```

### Tool Call - 2026-04-27 23:30:10 EDT

```
exec_command {"cmd":"sed -n '1048,1092p' areal/api/cli_args.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":5000}
```

### Tool Output - 2026-04-27 23:30:10 EDT

```
Chunk ID: 680449
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 408
Output:
    )
    c_clip: float | None = field(
        default=None,
        metadata={
            "help": "Dual clipping factor for policy ratio, must be > 1.0. None disables dual clipping."
        },
    )
    # M2PO
    m2_threshold: float | None = field(
        default=None, metadata={"help": "The second momentum threshold for M2PO."}
    )
    # Reward
    reward_norm: NormConfig | None = field(
        default=None,
        metadata={"help": "Normalization configuration for rewards"},
    )
    reward_scaling: float = field(
        default=1.0, metadata={"help": "Reward scaling factor"}
    )
    reward_bias: float = field(default=0.0, metadata={"help": "Reward bias"})
    reward_clip: float = field(
        default=20.0, metadata={"help": "Maximum absolute value for reward clipping"}
    )
    overlong_reward_penalty: bool = field(
        default=False,
        metadata={"help": "Penalty for overlong sequences. Used within DAPO."},
    )
    overlong_tokens: int | None = field(
        default=None,
        metadata={"help": "Number of tokens in the tail that will receive a penalty"},
    )
    overlong_penalty_factor: float | None = field(
        default=None,
        metadata={"help": "Penalty factor for tokens in the tail"},
    )
    mask_no_eos_with_zero: bool = field(
        default=False,
        metadata={
            "help": "Mask truncated generations (no EOS token) and exclude from training"
        },
    )

    # Advantage Estimation
    discount: float = field(
        default=1.0, metadata={"help": "Discount factor for future rewards"}
```

### Assistant - 2026-04-27 23:30:34 EDT

```
是的，**很可能数据对 0.5B 小模型偏难，需要筛难度**，但先别只按直觉筛。你这个 reward 是代码执行单测 pass rate，见 [competitive_exec.py](/home/ag/projects/AReaL_v1/areal/reward/code/competitive_exec.py:72)。`0.2-0.4` 大概就是每次抽样最多 15 个 test 里过 3-6 个，未必代表真的 solve。

我建议先做这几个诊断：

1. 打开 `rollout.dump_to_file=true` 跑一小段，看 100-500 条样本里：
   - 没有 fenced code block 的比例
   - compile error 比例
   - timeout/runtime error 比例
   - partial pass 比例
   - `reward == 1.0` 的比例
   - 是否大量输出打满 `max_new_tokens=16384`

2. 用当前 base model 离线对每题采样 `n=16/32`，按题统计：
   - `all reward == 0`：太难，早期先丢掉
   - `all reward > 0.8` 或大量满分：太简单，学习信号弱
   - `0 < pass_count < n_samples` 且组内 reward 有方差：最适合 GRPO
   - 早期可以保留 `mean_reward` 在 `0.05-0.6`、`std_reward > 0` 的题，后面 curriculum 再加难题

3. 对 0.5B 来说，`max_new_tokens=16384` 偏激进。长 trajectory 会稀释 credit assignment，也更容易截断、格式乱、reward 计算慢。建议先试：
   - `gconfig.max_new_tokens=4096` 或 `8192`
   - `sglang.context_length` 相应降到 `6144/10240`
   - 观察 `reward==1`、`no_eos`、输出长度分布是否改善

4. 注意一个代码细节：[nemotron_configs.py](/home/ag/projects/AReaL_v1/examples/code/nemotron_configs.py:37) 里已经写了，当前 `code_reward.per_test_timeout/max_tests/memory_mb` 这些配置**不会自动传进 reward_fn**，只有 `reward_timeout_seconds` 被传了。你这条命令没改这些值所以问题不大，但后面如果想调 `max_tests/per_test_timeout`，需要改 reward 默认值或让 dataset 样本带这些字段。

我的判断：如果确认格式/执行错误不高，那 `0.2-0.4` 横盘主要就是 **0.5B + competitive coding + sparse execution reward 太难**。应该先做难度筛选或课程学习，而不是直接全量 Nemotron coding 数据硬训 500 epochs。最有价值的筛法是按当前模型的 pass@16/pass@32 分桶，而不是只按 prompt 长度或测试数量筛。
```

### User - 2026-04-27 23:37:53 EDT

平均seq长度统计在13k附近，另外你说的第1点的指标训练直接统计是不是更方便，直接展示到logging里

### Assistant - 2026-04-27 23:38:02 EDT

13k 平均长度对 0.5B 已经很长，确实会让训练信号很稀。把 reward 诊断直接接进训练日志更合适，我会在 code reward 函数里返回/记录失败类型相关统计，尽量不改变 reward 数值本身。

### Tool Call - 2026-04-27 23:38:02 EDT

```
exec_command {"cmd":"rg -n \"class StatsTracker|def scalar|stats_tracker|get\\(\" areal -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":14000}
```

### Tool Call - 2026-04-27 23:38:03 EDT

```
exec_command {"cmd":"sed -n '1,180p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Call - 2026-04-27 23:38:03 EDT

```
exec_command {"cmd":"sed -n '1,380p' areal/reward/code/sandbox.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Output - 2026-04-27 23:38:03 EDT

```
Chunk ID: a1510a
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 12897
Output:
Total output lines: 553

areal/api/reward_api.py:141:                executor = self._executors.get(self._executor_key)
areal/api/cli_args.py:314:            key = mapping.get(k, k)
areal/engine/sglang_remote.py:31:from areal.utils import perf_tracer, stats_tracker
areal/engine/sglang_remote.py:72:        if req.metadata.get("return_routed_experts", False):
areal/engine/sglang_remote.py:84:            target_len = req.metadata.get("ig_target_len")
areal/engine/sglang_remote.py:85:            tail_len = req.metadata.get("tail_len")
areal/engine/sglang_remote.py:100:        stop_message = finish_reason.get("message", "")
areal/engine/sglang_remote.py:103:        routed_experts = meta_info.get("routed_experts", None)
areal/engine/sglang_remote.py:255:        model_path = server_args.get("model_path")
areal/engine/sglang_remote.py:290:        triton_cache_path = _env.get("TRITON_CACHE_PATH", TRITON_CACHE_PATH)
areal/engine/sglang_remote.py:473:        return stats_tracker.export_all(reduce_group=None)
areal/api/alloc_mode.py:895:            strategy_kwargs.get("pipeline_parallel_size", 1) > 1
areal/api/alloc_mode.py:896:            or strategy_kwargs.get("expert_parallel_size", 1) > 1
areal/api/alloc_mode.py:931:            strategy_kwargs.get("pipeline_parallel_size", 1) > 1
areal/api/alloc_mode.py:932:            or strategy_kwargs.get("expert_parallel_size", 1) > 1
areal/api/engine_api.py:429:            with `stats_tracker`.
areal/engine/fsdp_engine.py:106:    stats_tracker,
areal/engine/fsdp_engine.py:701:        return stats_tracker.export_all(reduce_group=self.data_parallel_group)
areal/engine/fsdp_engine.py:1436:            position_ids = mb_item.padded_mb.get("position_ids", None)
areal/engine/fsdp_engine.py:1509:        labels = inputs.get(
areal/engine/fsdp_engine.py:1539:        labels = inputs.get(
areal/experimental/engine/archon_engine.py:87:from areal.utils import logging, perf_tracer, stats_tracker
areal/experimental/engine/archon_engine.py:712:        data = stats_tracker.export_all(reduce_group=self.data_parallel_group)
areal/engine/megatron_utils/packed_context_parallel.py:129:    cu_seqlens = input_.get("cu_seqlens", None)
areal/engine/megatron_utils/packed_context_parallel.py:133:    attention_mask = input_.get("attention_mask", None)
areal/engine/megatron_utils/packed_context_parallel.py:134:    tree_triton_data = input_.get("tree_triton_data", None)
areal/experimental/openai/client.py:71:os.environ["OPENAI_API_KEY"] = os.environ.get("OPENAI_API_KEY", "none")
areal/experimental/openai/client.py:72:os.environ["OPENAI_BASE_URL"] = os.environ.get("OPENAI_BASE_URL", "none")
areal/experimental/openai/client.py:192:        **extra_body.get("chat_template_kwargs", {}),
areal/experimental/openai/client.py:407:                **extra_body.get("chat_template_kwargs", {}),
areal/experimental/openai/client.py:780:                and item.get("type") == "function_call_output"
areal/experimental/openai/client.py:803:                            and content.get("type") == "output_text"
areal/experimental/openai/client.py:854:                **extra_body.get("chat_template_kwargs", {}),
areal/experimental/openai/client.py:891:        stop = kwargs.get("stop", None)
areal/experimental/openai/client.py:1086:        return self._cache.get(id)
areal/engine/megatron_utils/fp8/config.py:33:    weight_block_size = quantization_config.get("weight_block_size", None)
areal/engine/megatron_utils/fp8/tensor_helper.py:385:            tensors = args[0] if args else kwargs.get("tensors", [])
areal/engine/megatron_utils/fp8/tensor_helper.py:389:                dim = kwargs.get("dim", 0) if not args or len(args) < 2 else args[1]
areal/engine/megatron_utils/fp8/tensor_helper.py:399:                    args[1] if len(args) > 1 else kwargs.get("split_size_or_sections")
areal/engine/megatron_utils/fp8/tensor_helper.py:401:                dim = args[2] if len(args) > 2 else kwargs.get("dim", 0)
areal/engine/megatron_utils/fp8/tensor_helper.py:408:                chunks = args[1] if len(args) > 1 else kwargs.get("chunks")
areal/engine/megatron_utils/fp8/tensor_helper.py:409:                dim = args[2] if len(args) > 2 else kwargs.get("dim", 0)
areal/engine/megatron_utils/fp8/quantize.py:91:    weight_block_size = quantization_config.get("weight_block_size", None)
areal/experimental/openai/proxy/proxy_gateway.py:118:    auth = request.headers.get("authorization", "")
areal/experimental/openai/proxy/proxy_gateway.py:237:    @app.get("/health")
areal/experimental/openai/proxy/proxy_gateway.py:331:            n = end_data.get("interaction_count", -1)
areal/experimental/openai/proxy/proxy_gateway.py:359:        task_id = parsed.get("task_id", "?")
areal/experimental/openai/proxy/proxy_gateway.py:360:        requested_key = parsed.get("api_key") or None
areal/experimental/openai/proxy/proxy_gateway.py:403:                            ready_workers.get(), timeout=remaining
areal/experimental/openai/proxy/proxy_gateway.py:587:        route = routes.get(token) if token else None
areal/experimental/openai/proxy/proxy_gateway.py:599:        route = routes.get(token) if token else None
areal/experimental/openai/proxy/proxy_gateway.py:611:        route = routes.get(token) if token else None
areal/experimental/openai/proxy/proxy_gateway.py:623:        route = routes.get(token) if token else None
areal/engine/megatron_engine.py:84:from areal.utils import logging, name_resolve, names, perf_tracer, stats_tracker
areal/engine/megatron_engine.py:238:        self.alloc_mode: AllocationMode | None = kwargs.get("alloc_mode", None)
areal/engine/megatron_engine.py:574:            cu_seqlens = mb_input.padded_mb.get("cu_seqlens", None)
areal/engine/megatron_engine.py:582:                trie_node = mb_input.padded_mb.get("trie_node", None)
areal/engine/megatron_engine.py:759:        data = stats_tracker.export_all(reduce_group=self.data_parallel_group)
areal/engine/megatron_engine.py:873:            and self.quantization_config.get("quant_method", None) == "fp8"
areal/engine/megatron_engine.py:1501:                trie_node = inputs.get("trie_node")
areal/experimental/openai/proxy/proxy_rollout_server.py:71:_warn_once_enabled = os.environ.get("AREAL_PROXY_WARN_ONCE", "0") == "1"
areal/experimental/openai/proxy/proxy_rollout_server.py:136:    content_type = raw_request.headers.get("content-type", "").lower()
areal/experimental/openai/proxy/proxy_rollout_server.py:162:    auth_header = request.headers.get("authorization", "")
areal/experimental/openai/proxy/proxy_rollout_server.py:166:    x_api_key = [REDACTED]("x-api-key", "")
areal/experimental/openai/proxy/proxy_rollout_server.py:190:        session_id = _api_key_to_session.get(token)
areal/experimental/openai/proxy/proxy_rollout_server.py:215:@app.get("/health")
areal/experimental/openai/proxy/proxy_rollout_server.py:237:        count = data.get("count")
areal/experimental/openai/proxy/proxy_rollout_server.py:289:    config = deserialize_value(data.get("config"))
areal/experimental/openai/proxy/proxy_rollout_server.py:290:    rank = data.get("rank", 0)
areal/experimental/openai/proxy/proxy_rollout_server.py:298:    for key, value in data.get("env", {}).items():
areal/experimental/openai/proxy/proxy_rollout_server.py:310:    engine_class = import_from_string(data.get("engine"))
areal/experimental/openai/proxy/proxy_rollout_server.py:311:    init_kwargs = deserialize_value(data.get("init_kwargs", {}))
areal/experimental/openai/proxy/proxy_rollout_server.py:323:    method_name = data.get("method")
areal/experimental/openai/proxy/proxy_rollout_server.py:324:    args = deserialize_value(data.get("args", []))
areal/experimental/openai/proxy/proxy_rollout_server.py:325:    kwargs = deserialize_value(data.get("kwargs", {}))
areal/experimental/openai/proxy/proxy_rollout_server.py:437:            existing_sid = _api_key_to_session.get(session_api_key)
areal/experimental/openai/proxy/proxy_rollout_server.py:439:                existing_session = _session_cache.get(existing_sid)
areal/experimental/openai/proxy/proxy_rollout_server.py:656:            if isinstance(msg.get("content"), list):
areal/experimental/openai/proxy/proxy_rollout_server.py:660:                    if isinstance(block, dict) and block.get("type") == "text":
areal/experimental/openai/proxy/proxy_rollout_server.py:661:                        text_parts.append(block.get("text", ""))
areal/experimental/openai/proxy/proxy_rollout_server.py:726:    is_streaming = anthropic_request.get("stream", False)
areal/experimental/openai/proxy/proxy_rollout_server.py:761:                    model=anthropic_request.get("model", "default"),
areal/engine/vllm_remote.py:29:from areal.utils import logging, perf_tracer, stats_tracker
areal/engine/vllm_remote.py:79:                        if content.get("type") == "image_url":
areal/engine/vllm_remote.py:117:            prompt_logprobs = meta_info.get("prompt_logprobs", [])
areal/engine/vllm_remote.py:283:        triton_cache_path = _env.get("TRITON_CACHE_PATH", TRITON_CACHE_PATH)
areal/engine/vllm_remote.py:286:        vllm_cache_path = _env.get("VLLM_CACHE_ROOT")
areal/engine/vllm_remote.py:473:        return stats_tracker.export_all(reduce_group=None)
areal/experimental/openai/proxy/workflow.py:14:from areal.utils import logging, stats_tracker
areal/experimental/openai/proxy/workflow.py:165:        task_id = workflow_context.get().task_id
areal/experimental/openai/proxy/workflow.py:210:            stats_tracker.get(workflow_context.stat_scope()).scalar(reward=last_reward)
areal/experimental/openai/proxy/workflow.py:248:            stats_tracker.get(workflow_context.stat_scope()).scalar(reward=last_reward)
areal/experimental/openai/tool_call_parser.py:67:                    name=tool.get("name"),
areal/experimental/openai/tool_call_parser.py:68:                    description=tool.get("description"),
areal/experimental/openai/tool_call_parser.py:69:                    parameters=tool.get("parameters"),
areal/tools/profile_engines.py:428:    model_path = args.model_path or MODEL_PATHS.get(
areal/tools/plot_session_trace.py:373:    offsets = df.get("finalized_ts_offset")
areal/tools/plot_session_trace.py:458:            phases_data = original_record.get("_phases_data")
areal/tools/plot_session_trace.py:472:                phase_executions = phases_data.get(phase_name, [])
areal/tools/plot_session_trace.py:479:                    start_ts = execution.get("start_ts")
areal/tools/plot_session_trace.py:480:                    end_ts = execution.get("end_ts")
areal/tools/plot_session_trace.py:500:            style = SEGMENT_STYLES.get(segment_key, {})
areal/tools/plot_session_trace.py:501:            color = style.get("color", "#cccccc")
areal/tools/plot_session_trace.py:502:            label_text = segment_label or style.get("label", segment_key)
areal/tools/plot_session_trace.py:614:        color = rank_colors.get(value, palette[0])
areal/tools/plot_session_trace.py:905:            for idx in combined_step_indices.get(step, []):
areal/tools/plot_session_trace.py:1075:        y_min = summary.get("y_min", math.nan)
areal/tools/plot_session_trace.py:1076:        y_max = summary.get("y_max", math.nan)
areal/tools/plot_session_trace.py:1129:        x_min = summary.get("x_min", math.nan)
areal/tools/plot_session_trace.py:1130:        x_max = summary.get("x_max", math.nan)
areal/tools/plot_session_trace.py:1137:                (summary.get("p50"), "p50", "#1f77b4", "dot"),
areal/tools/plot_session_trace.py:1138:                (summary.get("p95"), "p95", "#d62728", "dash"),
areal/tools/plot_session_trace.py:1322:            session_count = step_counts.get(idx - 1, 0) if not step_counts.empty else 0
areal/experimental/models/archon/parallel_dims.py:397:        return self._meshes.get(name)
areal/tools/perf_trace_converter.py:31:    args = event.get("args")
areal/tools/perf_trace_converter.py:34:    rank = args.get("rank")
areal/tools/perf_trace_converter.py:58:    args = event.get("args")
areal/tools/perf_trace_converter.py:61:    role = args.get("role")
areal/tools/perf_trace_converter.py:147:        original_pid = event.get("pid")
areal/tools/perf_trace_converter.py:152:        original_tid = event.get("tid")
areal/tools/perf_trace_converter.py:188:        next_tid = tid_counters.get(new_pid, new_pid)
areal/tools/perf_trace_converter.py:199:        original_pid = event.get("pid")
areal/tools/perf_trace_converter.py:205:        original_tid = event.get("tid")
areal/tools/perf_trace_converter.py:217:        process_name = existing_process_names.get((rank, role, original_pid))
areal/tools/perf_trace_converter.py:253:        thread_name = existing_thread_names.get(
areal/tools/perf_trace_converter.py:329:        if event.get("ph") == "M":
areal/tools/perf_trace_converter.py:330:            name = event.get("name")
areal/tools/perf_trace_converter.py:331:            args = event.get("args", {})
areal/tools/perf_trace_converter.py:332:            pid = event.get("pid")
areal/tools/perf_trace_converter.py:333:            tid = event.get("tid")
areal/tools/perf_trace_converter.py:337:                    pname = args.get("name")
areal/tools/perf_trace_converter.py:343:                    tname = args.get("name")
areal/tools/perf_trace_converter.py:363:        if event.get("ph") in ("s", "t", "f") and "id" in event:
areal/tools/perf_trace_converter.py:367:        args = event.get("args")
areal/tools/perf_trace_converter.py:390:        if event.get("ph") in ("s", "t", "f") and "id" in event:
areal/tools/perf_trace_converter.py:395:        args = event.get("args")
areal/tools/perf_trace_converter.py:409:            _rank_sort_key(event.get("args", {}).get("rank")),
areal/tools/perf_trace_converter.py:410:            _role_sort_key(event.get("args", {}).get("role")),
areal/tools/perf_trace_converter.py:411:            _metadata_name_sort_key(event.get("name")),
areal/tools/perf_trace_converter.py:412:            _value_sort_key(event.get("pid")),
areal/tools/perf_trace_converter.py:413:            _value_sort_key(event.get("tid")),
areal/tools/perf_trace_converter.py:419:            event.get("ts", 0),
areal/tools/perf_trace_converter.py:420:            _value_sort_key(event.get("pid")),
areal/tools/perf_trace_converter.py:421:            _value_sort_key(event.get("tid")),
areal/tools/profile_fsdp.py:274:    model_path = args.model_path or MODEL_PATHS.get(
areal/tools/validation_base.py:95:            raw_deps = data.get("project", {}).get("dependencies", [])
areal/tools/validation_base.py:278:        submodules = self.CUDA_SUBMODULES.get(pkg_name, [])
areal/tools/validation_base.py:312:                import_name = self.PACKAGE_IMPORT_MAP.get(
areal/tools/validation_base.py:321:                import_name = self.PACKAGE_IMPORT_MAP.get(
areal/tools/validation_base.py:324:                required = dep_info.get("required", True)
areal/tools/validation_base.py:345:                        pkg_name, dep_info, import_results.get(pkg_name), required=True
areal/tools/validation_base.py:357:                        pkg_name, dep_info, import_results.get(pkg_name), required=False
areal/tools/validation_base.py:364:                required = dep_info.get("required", True)
areal/tools/validation_base.py:366:                    pkg_name, dep_info, import_results.get(pkg_name), required=required
areal/tools/validation_base.py:377:        import_name = self.PACKAGE_IMPORT_MAP.get(pkg_name, pkg_name.replace("-", "_"))
areal/tools/validation_base.py:398:        version_ok, version_msg = self.check_version(pkg_name, dep_info.get("spec", ""))
areal/experimental/models/archon/qwen2/model/args.py:64:            attn_type=kwargs.get("attn_type", BaseModelArgs.attn_type),
areal/utils/timeutil.py:186:    def get(self, step: int) -> float:
areal/utils/timeutil.py:192:        return self._get(step)
areal/utils/timeutil.py:194:    def _get(self, step):
areal/utils/timeutil.py:199:        return self.get(step=self.total_iters)
areal/utils/timeutil.py:204:    def _get(self, *args, **kwargs) -> float:
areal/utils/timeutil.py:212:    def _get(self, step: int) -> float:
areal/utils/timeutil.py:222:    def _get(self, step: int) -> float:
areal/utils/timeutil.py:235:    def _get(self, step: int) -> float:
areal/utils/timeutil.py:263:                abs(self.schedulers[i + 1].get(0) - self.schedulers[i].final_value)
areal/utils/timeutil.py:271:                    f"next init value is {self.schedulers[i + 1].get(0)}."
areal/utils/timeutil.py:274:    def get(self, step: int) -> float:
areal/utils/timeutil.py:279:                return s.get(step)
areal/utils/offload.py:49:    return os.environ.get("TMS_INIT_ENABLE", "0") == "1"
areal/utils/name_resolve.py:98:    def get(self, name):
areal/utils/name_resolve.py:124:                return self.get(name)
areal/utils/name_resolve.py:175:                self.get(name)
areal/utils/name_resolve.py:239:    def get(self, name):
areal/utils/name_resolve.py:349:    def get(self, name):
areal/utils/name_resolve.py:378:                    rs.append(self.get(key))
areal/utils/name_resolve.py:515:                existing_value, _ = self._client.get(name)
areal/utils/name_resolve.py:570:        value, _ = self._client.get(name)
areal/utils/name_resolve.py:639:    def get(self, name):
areal/utils/name_resolve.py:667:        value, _ = self._client.get(name)
areal/utils/name_resolve.py:794:    def get(self, key: str):
areal/utils/name_resolve.py:968:                existing_value = ray.get(self._kv_store.get.remote(name))
areal/utils/name_resolve.py:977:                lease_id = ray.get(
areal/utils/name_resolve.py:982:                ray.get(self._kv_store.put.remote(name, value))
areal/utils/name_resolve.py:1021:        existing_value = ray.get(self._kv_store.get.remote(name))
areal/utils/name_resolve.py:1030:        ray.get(self._kv_store.delete.remote(name))
areal/utils/name_resolve.py:1036:            count = ray.get(self._kv_store.delete_prefix.remote(name_root))
areal/utils/name_resolve.py:1052:    def get(self, name: str):
areal/utils/name_resolve.py:1070:        value = ray.get(self._kv_store.get.remote(name))
areal/utils/name_resolve.py:1079:            pairs = ray.get(self._kv_store.get_prefix.remote(name_root))
areal/utils/name_resolve.py:1087:            pairs = ray.get(self._kv_store.get_prefix.remote(name_root))
areal/utils/name_resolve.py:1100:                return self.get(name)
areal/utils/name_resolve.py:1174:                self.get(name)
areal/utils/name_resolve.py:1194:                            success = ray.get(
areal/utils/stats_tracker.py:107:    def scalar(self, **kwargs):
areal/utils/stats_tracker.py:185:        reduce_type = self.reduce_types.get(key, ReduceType.SCALAR)
areal/utils/stats_tracker.py:305:def get(name: str = ""):
areal/utils/stats_tracker.py:322:        tracker = get(tracker_key)
areal/experimental/models/archon/qwen3/model/args.py:102:            attn_type=kwargs.get("attn_type", BaseModelArgs.attn_type),
areal/utils/data.py:28:    am = data.get("attention_mask")
areal/utils/data.py:32:    cu = data.get("cu_seqlens")
areal/utils/data.py:36:    mmi = data.get("multi_modal_input")
areal/utils/data.py:186:            merged_multi_modal.extend(td.get(mm_key, [{} for _ in range(bs)]))
areal/experimental/models/archon/qwen3/model/state_dict_adapter.py:303:        hf_abstract_key = self._hf_expert_abstract_keys.get(archon_abstract_key)
areal/utils/perf_tracer.py:190:        alias = _CATEGORY_ALIASES.get(lowered)
areal/utils/perf_tracer.py:530:        self.counters[name] = self.counters.get(name, 0) + value
areal/utils/perf_tracer.py:541:        current = self._active_phases.get(phase)
areal/utils/perf_tracer.py:560:        status = payload.get("status")
areal/utils/perf_tracer.py:564:            record.reason = payload.get("reason")
areal/utils/perf_tracer.py:596:            for entry in self.phases.get(phase, [])
areal/utils/perf_tracer.py:731:        name = payload.get("name")
areal/utils/perf_tracer.py:735:            …2897 tokens truncated…,
areal/trainer/rl_trainer.py:352:                    stats_tracker.record_timing("recompute_logp"),
areal/trainer/rl_trainer.py:364:                    stats_tracker.record_timing("ref_logp"),
areal/trainer/rl_trainer.py:375:                stats_tracker.record_timing("compute_advantage"),
areal/trainer/rl_trainer.py:389:                stats_tracker.record_timing("train_step"),
areal/trainer/rl_trainer.py:402:                    stats_tracker.record_timing("critic_train_step"),
areal/trainer/rl_trainer.py:417:                stats_tracker.record_timing("update_weights"),
areal/trainer/rl_trainer.py:437:                stats_tracker.record_timing("save"),
areal/trainer/rl_trainer.py:447:                stats_tracker.record_timing("checkpoint_for_recover"),
areal/trainer/rl_trainer.py:459:                stats_tracker.record_timing("eval"),
areal/trainer/rl_trainer.py:475:                stats_tracker.record_timing("clear_batches"),
areal/models/tree_attn/constants.py:9:BLOCK_SIZE = int(os.environ.get("AREAL_FLEX_ATTENTION_BLOCK_SIZE", "128"))
areal/models/tree_attn/constants.py:10:USE_TRITON_TREE_ATTN = int(os.environ.get("AREAL_USE_TRITON_TREE_ATTN", "0")) == 1
areal/trainer/rw/rw_engine.py:8:from areal.utils import logging, stats_tracker
areal/trainer/rw/rw_engine.py:19:    @stats_tracker.scope_func_wrapper("rw")
areal/trainer/rw/rw_engine.py:34:    @stats_tracker.scope_func_wrapper("rw-eval")
areal/trainer/rw/rw_engine.py:69:        stats_tracker.denominator(
areal/trainer/rw/rw_engine.py:72:        stats_tracker.stat(
areal/models/tree_attn/module_fsdp.py:23:_FLEX_DYNAMIC = not (os.environ.get("AREAL_DISABLE_FLEX_ATTENTION_DYNAMIC", "0") == "1")
areal/models/tree_attn/module_fsdp.py:94:    tree_triton_data = kwargs.get("tree_triton_data", None)
areal/models/tree_attn/module_fsdp.py:118:        tree_block_mask = kwargs.get("tree_block_mask", None)
areal/trainer/rl_trainer_qun_team.py:42:from areal.utils import logging, perf_tracer, seeding, stats_tracker
areal/trainer/rl_trainer_qun_team.py:328:                stats_tracker.record_timing("eval"),
areal/trainer/rl_trainer_qun_team.py:350:                stats_tracker.record_timing("rollout"),
areal/trainer/rl_trainer_qun_team.py:371:                    stats_tracker.record_timing("critic_values"),
areal/trainer/rl_trainer_qun_team.py:383:                    stats_tracker.record_timing("recompute_logp"),
areal/trainer/rl_trainer_qun_team.py:399:                    stats_tracker.record_timing("compute_ig_reward"),
areal/trainer/rl_trainer_qun_team.py:428:                    stats_tracker.record_timing("ref_logp"),
areal/trainer/rl_trainer_qun_team.py:439:                stats_tracker.record_timing("compute_advantage"),
areal/trainer/rl_trainer_qun_team.py:453:                stats_tracker.record_timing("train_step"),
areal/trainer/rl_trainer_qun_team.py:466:                    stats_tracker.record_timing("critic_train_step"),
areal/trainer/rl_trainer_qun_team.py:481:                stats_tracker.record_timing("update_weights"),
areal/trainer/rl_trainer_qun_team.py:501:                stats_tracker.record_timing("save"),
areal/trainer/rl_trainer_qun_team.py:511:                stats_tracker.record_timing("checkpoint_for_recover"),
areal/trainer/rl_trainer_qun_team.py:523:                stats_tracker.record_timing("eval"),
areal/trainer/rl_trainer_qun_team.py:539:                stats_tracker.record_timing("clear_batches"),
areal/trainer/sft_trainer.py:25:from areal.utils import logging, perf_tracer, seeding, stats_tracker
areal/trainer/sft_trainer.py:158:                stats_tracker.record_timing("load_bcast"),
areal/trainer/sft_trainer.py:171:                stats_tracker.record_timing("train_step"),
areal/trainer/sft_trainer.py:185:                stats_tracker.record_timing("save"),
areal/trainer/sft_trainer.py:195:                stats_tracker.record_timing("checkpoint_for_recover"),
areal/trainer/sft_trainer.py:207:                stats_tracker.record_timing("eval"),
areal/trainer/sft_trainer.py:221:                stats_tracker.record_timing("clear_batches"),
areal/infra/workflow_context.py:44:def get() -> WorkflowContext:
areal/infra/workflow_context.py:46:    return _current_context.get()
areal/infra/workflow_context.py:50:    """Get the appropriate stats_tracker scope based on current context.
areal/infra/workflow_context.py:60:        return "eval-rollout" if get().is_eval else "rollout"
areal/optimizer/distrib_optimizer_muon_core_13_1.py:890:            use_muon = self.optimizer.param_groups[group_index].get("use_muon", False)
areal/optimizer/distrib_optimizer_muon_core_13_1.py:1149:            sharding_type = (metadata or {}).get(
areal/optimizer/muon.py:174:            if not group.get("use_muon", False):
areal/optimizer/muon.py:242:            if not group.get('use_muon', False):
areal/optimizer/muon.py:303:            if group.get('use_muon', False):
areal/infra/utils/http.py:57:                ctx = _session.get(url, timeout=timeo)
areal/workflow/multi_turn.py:14:from areal.utils import logging, stats_tracker
areal/workflow/multi_turn.py:126:        stats_tracker.get(workflow_context.stat_scope()).scalar(
areal/infra/utils/ray.py:25:    return ray.get(future)
areal/workflow/rlvr.py:14:from areal.utils import logging, stats_tracker
areal/workflow/rlvr.py:129:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/infra/utils/ray_placement_group.py:104:        ray.get(pg.ready(), timeout=timeout)
areal/workflow/openai/math_agent.py:32:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/openai/math_agent.py:33:        base_url = extra_kwargs.get("base_url", None) or os.getenv("OPENAI_BASE_URL")
areal/workflow/openai/math_agent.py:34:        api_key = [REDACTED]("api_key", None) or os.getenv("OPENAI_API_KEY")
areal/workflow/openai/math_agent.py:55:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/openai/math_agent.py:56:        base_url = extra_kwargs.get("base_url", None) or os.getenv("OPENAI_BASE_URL")
areal/workflow/openai/math_agent.py:57:        api_key = [REDACTED]("api_key", None) or os.getenv("OPENAI_API_KEY")
areal/workflow/openai/math_agent.py:134:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/openai/math_agent.py:135:        base_url = extra_kwargs.get("base_url", None) or os.getenv("OPENAI_BASE_URL")
areal/workflow/openai/math_agent.py:136:        api_key = [REDACTED]("api_key", None) or os.getenv("OPENAI_API_KEY")
areal/infra/rpc/rpc_server.py:93:                    work_item = _engine_work_queue.get()
areal/infra/rpc/rpc_server.py:156:        count = data.get("count")
areal/infra/rpc/rpc_server.py:190:            resp = requests.get(url, timeout=2)
areal/infra/rpc/rpc_server.py:230:        role = data.get("role")
areal/infra/rpc/rpc_server.py:231:        worker_index = data.get("worker_index")
areal/infra/rpc/rpc_server.py:232:        command = data.get("command")  # Optional custom module path
areal/infra/rpc/rpc_server.py:368:        role = data.get("role")
areal/infra/rpc/rpc_server.py:369:        worker_index = data.get("worker_index")
areal/infra/rpc/rpc_server.py:436:        config = data.get("config")
areal/infra/rpc/rpc_server.py:440:        rank = data.get("rank")
areal/infra/rpc/rpc_server.py:474:        env_payload = data.get("env")
areal/infra/rpc/rpc_server.py:533:        engine = data.get("engine")
areal/infra/rpc/rpc_server.py:534:        engine_name = data.get("engine_name")
areal/infra/rpc/rpc_server.py:536:        init_args = deserialize_value(data.get("init_args", []))
areal/infra/rpc/rpc_server.py:537:        init_kwargs = deserialize_value(data.get("init_kwargs", {}))
areal/infra/rpc/rpc_server.py:636:        method_name = data.get("method")
areal/infra/rpc/rpc_server.py:637:        engine_name = data.get("engine_name")
areal/infra/rpc/rpc_server.py:638:        raw_args = data.get("args", [])
areal/infra/rpc/rpc_server.py:639:        raw_kwargs = data.get("kwargs", {})
areal/infra/rpc/rpc_server.py:870:        shard_ids = data.get("shard_ids", [])
areal/workflow/rlvr_qun_team.py:15:from areal.utils import logging, stats_tracker
areal/workflow/rlvr_qun_team.py:138:        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
areal/workflow/rlvr_qun_team.py:156:        gt_text = data.get("answer")
areal/workflow/rlvr_qun_team.py:372:            gt_text = data.get("answer")
areal/infra/rpc/rtensor.py:111:        async with session.get(url) as resp:
areal/infra/rpc/rtensor.py:137:        return ray.get([s.shard_id for s in shards])
areal/infra/rpc/rtensor.py:291:            attn_mask = obj.get("attention_mask", None)
areal/infra/rpc/rtensor.py:680:        tensor = _storage.get(shard_id)
areal/workflow/anthropic/math_agent.py:35:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/anthropic/math_agent.py:36:        base_url = extra_kwargs.get("base_url", None) or os.getenv("ANTHROPIC_BASE_URL")
areal/workflow/anthropic/math_agent.py:37:        api_key = [REDACTED]("api_key", None) or os.getenv("ANTHROPIC_API_KEY")
areal/infra/rpc/serialization.py:163:        return dtype_map.get(torch_dtype, np.float32)
areal/infra/rpc/serialization.py:484:        if value.get("type") == "dataclass":
areal/infra/rpc/serialization.py:501:        if value.get("type") == "tokenizer":
areal/infra/rpc/serialization.py:511:        if value.get("type") == "ndarray":
areal/infra/rpc/serialization.py:521:        if value.get("type") == "tensor":
areal/workflow/anthropic/claude_math_agent.py:112:        base_url = extra_kwargs.get("base_url", None) or os.getenv("ANTHROPIC_BASE_URL")
areal/workflow/anthropic/claude_math_agent.py:113:        api_key = [REDACTED]("api_key", None) or os.environ.get(
areal/workflow/anthropic/claude_math_agent.py:129:                        extra_kwargs.get("max_completion_tokens", 1024)
areal/workflow/anthropic/claude_math_agent.py:143:            max_turns=self.kwargs.get("max_turns", 10),
areal/workflow/vision_rlvr.py:13:from areal.utils import logging, stats_tracker
areal/workflow/vision_rlvr.py:99:        stats_tracker.get(workflow_context.stat_scope()).scalar(reward=reward)
areal/workflow/langchain/math_agent.py:90:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/langchain/math_agent.py:91:        base_url = extra_kwargs.get("base_url", None) or os.getenv("OPENAI_BASE_URL")
areal/workflow/langchain/math_agent.py:92:        api_key = [REDACTED]("api_key", None) or os.getenv("OPENAI_API_KEY")
areal/workflow/langchain/math_agent.py:99:            temperature=self.kwargs.get("temperature", 1.0),
areal/workflow/langchain/math_agent.py:100:            top_p=self.kwargs.get("top_p", 1.0),
areal/workflow/langchain/math_agent.py:101:            max_tokens=self.kwargs.get("max_completion_tokens", 1024),
areal/workflow/langchain/math_agent.py:134:        http_client = extra_kwargs.get("http_client", None)
areal/workflow/langchain/math_agent.py:135:        base_url = extra_kwargs.get("base_url", None) or os.getenv("OPENAI_BASE_URL")
areal/workflow/langchain/math_agent.py:136:        api_key = [REDACTED]("api_key", None) or os.getenv("OPENAI_API_KEY")
areal/workflow/langchain/math_agent.py:143:            temperature=self.kwargs.get("temperature", 1.0),
areal/workflow/langchain/math_agent.py:144:            top_p=self.kwargs.get("top_p", 1.0),
areal/workflow/langchain/math_agent.py:145:            max_tokens=self.kwargs.get("max_completion_tokens", 1024),
areal/infra/scheduler/ray.py:109:                ray.get(ref)
areal/infra/scheduler/ray.py:278:            worker_ports = ray.get(
areal/infra/scheduler/ray.py:613:        return self._worker_info_by_id.get(worker_id, None)
areal/infra/scheduler/ray.py:669:                result = ray.get(ref, timeout=http_timeout)
areal/reward/code/toolchain.py:30:        bins = _LANGUAGE_BINARIES.get(lang)
areal/reward/code/toolchain.py:50:        missing_bins = {lang: _LANGUAGE_BINARIES.get(lang, []) for lang in missing}
areal/reward/code/sandbox.py:136:    return _LANG_ALIASES.get(lang.strip().lower())
areal/infra/scheduler/slurm.py:277:            response = requests.get(url, timeout=2.0)
areal/infra/scheduler/slurm.py:311:                error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/slurm.py:314:                error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/slurm.py:353:                addr = name_resolve.get(key)
areal/infra/scheduler/slurm.py:474:                if result.get("status") != "success":
areal/infra/scheduler/slurm.py:478:                        result.get("error", "Unknown error"),
areal/infra/scheduler/slurm.py:483:                forked_pid = result.get("pid")
areal/infra/scheduler/slurm.py:554:                        result.get("message", f"Killed forked worker {role}/{idx}")
areal/infra/scheduler/slurm.py:566:        target_workers = self._workers.get(target_role, [])
areal/infra/scheduler/slurm.py:1171:        job_id = self._jobs.get(role)
areal/infra/scheduler/slurm.py:1235:                    detail = (await response.json()).get("error", "Unknown error")
areal/infra/scheduler/slurm.py:1328:                        return result.get("result")
areal/infra/scheduler/slurm.py:1330:                        error_detail = (await response.json()).get(
areal/infra/scheduler/slurm.py:1338:                        error_detail = (await response.json()).get(
areal/infra/scheduler/slurm.py:1439:                    return deserialize_value(result.get("result"))
areal/infra/scheduler/slurm.py:1441:                    error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/slurm.py:1459:                    error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/slurm.py:1579:                            return deserialize_value(result.get("result"))
areal/infra/scheduler/slurm.py:1581:                            error_detail = (await response.json()).get(
areal/infra/scheduler/slurm.py:1602:                            error_detail = (await response.json()).get(
areal/infra/scheduler/local.py:172:        cuda_visible = os.environ.get(current_platform.device_control_env_var)
areal/infra/scheduler/local.py:292:                if result.get("status") != "success":
areal/infra/scheduler/local.py:296:                        result.get("error", "Unknown error"),
areal/infra/scheduler/local.py:301:                forked_pid = result.get("pid")
areal/infra/scheduler/local.py:360:                        result.get("message", f"Killed forked worker {role}/{idx}")
areal/infra/scheduler/local.py:372:        target_workers = self._workers.get(target_role, [])
areal/infra/scheduler/local.py:834:            response = requests.get(url, timeout=2.0)
areal/infra/scheduler/local.py:867:                error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/local.py:870:                error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/local.py:1016:                    detail = (await response.json()).get("error", "Unknown error")
areal/infra/scheduler/local.py:1115:                        return result.get("result")
areal/infra/scheduler/local.py:1118:                        error_detail = (await response.json()).get(
areal/infra/scheduler/local.py:1127:                        error_detail = (await response.json()).get(
areal/infra/scheduler/local.py:1389:                            result_data = (await response.json()).get("result")
areal/infra/scheduler/local.py:1399:                            error_detail = (await response.json()).get(
areal/infra/scheduler/local.py:1407:                            error_detail = (await response.json()).get(
areal/infra/scheduler/local.py:1487:            result = response.json().get("result")
areal/infra/scheduler/local.py:1493:            error_detail = response.json().get("detail", "Unknown error")
areal/infra/scheduler/local.py:1497:            error_detail = response.json().get("detail", "Unknown error")
areal/infra/remote_inf_engine.py:810:                req.metadata.get("return_routed_experts", False)
areal/infra/workflow_executor.py:32:from areal.utils import logging, perf_tracer, stats_tracker
areal/infra/workflow_executor.py:845:        input_ids = traj.get("input_ids")
areal/infra/workflow_executor.py:846:        rewards = traj.get("rewards")
areal/infra/workflow_executor.py:847:        loss_mask = traj.get("loss_mask")
areal/infra/workflow_executor.py:848:        attention_mask = traj.get("attention_mask")
areal/infra/workflow_executor.py:1100:                    stats_tracker.get("rollout").scalar(accepted=1)
areal/infra/workflow_executor.py:1114:                stats_tracker.get("rollout").scalar(rejected=1)
areal/infra/workflow_executor.py:1129:                stats_tracker.get("rollout").scalar(rejected=1)
areal/infra/controller/rollout_controller.py:542:            meta = deserialize_value(payload.get("meta"))
areal/infra/controller/rollout_controller.py:549:            meta = deserialize_value(payload.get("meta"))
areal/infra/controller/rollout_controller.py:550:            param_specs = deserialize_value(payload.get("param_specs"))
areal/infra/controller/rollout_controller.py:559:            meta = deserialize_value(payload.get("meta"))
areal/infra/controller/rollout_controller.py:576:            task_id = payload.get("task_id")
areal/infra/controller/rollout_controller.py:1059:                    stats[k] += v * raw_stats.get(k + "__count", 0)
areal/infra/controller/train_controller.py:20:from areal.utils import logging, stats_tracker
areal/infra/controller/train_controller.py:418:        stats = stats_tracker.export_all()
areal/infra/launcher/vllm_server.py:41:    triton_cache_path = _env.get("TRITON_CACHE_PATH", TRITON_CACHE_PATH)
areal/infra/launcher/vllm_server.py:45:    vllm_cache_path = _env.get("VLLM_CACHE_ROOT")
areal/infra/launcher/vllm_server.py:71:            response = requests.get(
areal/infra/launcher/ray.py:204:                ray.get(placement_group.ready(), timeout=30)
areal/infra/launcher/ray.py:312:                    r = ray.get(future, timeout=0.1)
areal/infra/launcher/ray.py:524:    if config.get("enable_offload", False):
areal/infra/launcher/slurm.py:605:    if config.get("enable_offload", False):
areal/infra/launcher/sglang_server.py:47:    triton_cache_path = _env.get("TRITON_CACHE_PATH", TRITON_CACHE_PATH)
areal/infra/launcher/sglang_server.py:72:            response = requests.get(
areal/infra/platforms/platform.py:141:            return os.environ.get(cls.device_control_env_var, "").split(",")
areal/infra/launcher/local.py:95:        self._gpu_devices: list[str] = os.environ.get(
areal/infra/launcher/local.py:103:                f" set to: `{os.environ.get(current_platform.device_control_env_var, '')}`."
areal/infra/launcher/local.py:232:                self._job_states[job_name] = PROCESS_STATUS_TO_JOB_STATE.get(
areal/infra/launcher/local.py:364:    if config.get("enable_offload", False):
areal/infra/async_task_runner.py:616:                result = self.output_queue.get(timeout=wait_time)
areal/dataset/code/nemotron_competitive.py:50:        rcp = sample.get("responses_create_params") or {}
areal/dataset/code/nemotron_competitive.py:51:        messages = rcp.get("input") or []
areal/dataset/code/nemotron_competitive.py:52:        verifier = sample.get("verifier_metadata") or {}
areal/dataset/code/nemotron_competitive.py:53:        unit_tests = verifier.get("unit_tests") or {}
areal/dataset/code/nemotron_competitive.py:56:            "test_inputs": list(unit_tests.get("inputs") or []),
areal/dataset/code/nemotron_competitive.py:57:            "test_outputs": list(unit_tests.get("outputs") or []),
areal/dataset/torl_data.py:32:    response = requests.get(url, stream=True)
```

### Tool Output - 2026-04-27 23:38:03 EDT

```
Chunk ID: 9587b6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1683
Output:
import uuid
import asyncio
from collections.abc import Callable
from typing import Any

import torch
from transformers import PreTrainedTokenizerFast

from areal import workflow_context
from areal.api.cli_args import GenerationHyperparameters
from areal.api.engine_api import InferenceEngine
from areal.api.io_struct import ModelRequest, ModelResponse
from areal.api.reward_api import AsyncRewardWrapper
from areal.api.workflow_api import RolloutWorkflow
from areal.utils import logging, stats_tracker
from areal.utils.dynamic_import import import_from_string
from areal.utils.perf_tracer import (
    atrace_session_phase,
    session_context,
    trace_session,
)

logger = logging.getLogger("RLVRWorkflow")


def default_get_input_ids_fn(
    data: Any,
    tokenizer: PreTrainedTokenizerFast,
    enable_thinking: bool,
) -> list[int]:
    input_ids = tokenizer.apply_chat_template(
        data,
        tokenize=True,
        add_generation_prompt=True,
        enable_thinking=enable_thinking,
    )
    return list(input_ids)


def default_data_extract_prompt_fn(data: dict[str, Any]) -> Any:
    return data["messages"]


class RLVRWorkflow(RolloutWorkflow):
    """Single-turn reward learning workflow supporting optional thinking tokens."""

    def __init__(
        self,
        reward_fn: Callable[..., Any] | str,
        gconfig: GenerationHyperparameters,
        tokenizer: PreTrainedTokenizerFast | str,
        enable_thinking: bool = False,
        rollout_stat_scope: str = "rollout",
        ig_reward_params=None,
        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
        | str = default_get_input_ids_fn,
        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
        | str = default_data_extract_prompt_fn,
        reward_timeout_seconds: float = 15.0,
    ):
        self.reward_fn = reward_fn
        self.tokenizer = tokenizer
        if isinstance(self.tokenizer, str):
            from areal.utils.hf_utils import load_hf_tokenizer

            tokenizer = load_hf_tokenizer(self.tokenizer)
            self.tokenizer = tokenizer
        self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
        self.rollout_stat_scope = rollout_stat_scope
        self.enable_thinking = enable_thinking
        self.ig_reward_params = ig_reward_params
        self.reward_timeout_seconds = reward_timeout_seconds
        if not isinstance(reward_fn, str):
            self.async_reward_fn = AsyncRewardWrapper(
                reward_fn, timeout_seconds=reward_timeout_seconds
            )
        # Support string paths for get_input_ids_fn
        if isinstance(get_input_ids_fn, str):
            get_input_ids_fn = import_from_string(get_input_ids_fn)
        self.get_input_ids_fn = get_input_ids_fn
        # Support string paths for data_extract_prompt_fn
        if isinstance(data_extract_prompt_fn, str):
            data_extract_prompt_fn = import_from_string(data_extract_prompt_fn)
        self.data_extract_prompt_fn = data_extract_prompt_fn

    @trace_session("reward")
    async def _compute_rewards(
        self,
        resp: ModelResponse,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> float:
        """Decode completion and compute reward.

        Traces reward phase execution for SessionTracer. Decodes output tokens
        to string, calls async reward function, and logs metric to stats tracker.

        Returns
        -------
        float
            Reward value.
        """
        completions_str = self.tokenizer.decode(resp.output_tokens)
        reward = await self.async_reward_fn(
            prompt_str,
            completions_str,
            resp.input_tokens,
            resp.output_tokens,
            **task_data,
        )

        return reward

    @session_context()
    async def _collect_samples(
        self,
        engine: InferenceEngine,
        req: ModelRequest,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[ModelResponse, float]:
        """Generate one sample and compute its reward.

        Registers a new session for this sample, calls engine.agenerate,
        computes reward, and logs metrics. SessionTracer automatically
        tracks generate and reward phases via @trace_session decorators.

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward = await self._compute_rewards(resp, prompt_str, task_data)

        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)

        return resp, reward

    async def _compute_ig_probes(
            self,
            engine: InferenceEngine,
            resp: ModelResponse,
            data: dict[str, Any],
            outcome_reward: float
    ) -> dict[str, list[float]] | None:
        """
        利用热乎的 KV Cache，异步并发计算所有中间步的 IG Logprobs，并直接结算过程奖励。
        这样完全解耦了 Actor 进程，避免了多卡同步死锁和 FSDP Padding 带来的内存溢出。
        """
        if not self.ig_reward_params:
            return None

        gt_text = data.get("answer")
        if not gt_text:
            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
            return None

        # 1. 构造 Target 后缀
        gt_ids = self.tokenizer.encode(gt_text, add_special_tokens=False)
        bridge_text = getattr(self.ig_reward_params, "bridge_text", "\n\nTherefore, the answer is ")
        bridge_ids = self.tokenizer.encode(bridge_text, add_special_tokens=False)
        boxed_first_half_ids = self.tokenizer.encode("\\boxed{", add_special_tokens=False)
        boxed_second_half_ids = self.tokenizer.encode("}", add_special_tokens=False)

        ig_gt_ids = bridge_ids + boxed_first_half_ids + gt_ids + boxed_second_half_ids
        target_len = len(gt_ids)
        tail_len = len(boxed_second_half_ids)

        # 2. 定位所有步骤的切分点 (根据 sep_token)
        sep_token = getattr(self.ig_reward_params, "sep_token", "\n")
        sep_ids = self.tokenizer.encode(sep_token, add_special_tokens=False)
        if not sep_ids:
            return None
        sep_id = sep_ids[-1]

        step_offsets = [i for i, token_id in enumerate(resp.output_tokens) if token_id == sep_id]
        if not step_offsets or step_offsets[-1] != len(resp.output_tokens) - 1:
```

### Tool Output - 2026-04-27 23:38:03 EDT

```
Chunk ID: d8fa5a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2907
Output:
"""Subprocess sandbox for executing untrusted code in multiple languages.

Supports Python (interpreted), C++/Java (compiled), and JavaScript/Node
(interpreted). For compiled languages the compile step happens once per
submission and the resulting artifact is reused across all unit tests —
critical for GRPO with ``n_samples * max_tests`` executions per problem.

Isolation layers (per execution):
1. Separate subprocess with its own address space.
2. ``resource.setrlimit`` caps on address space, CPU time, and core dumps.
   Java skips RLIMIT_AS because the JVM reserves the entire heap up-front;
   we cap the JVM heap with ``-Xmx`` instead.
3. New process group (``os.setsid``) so a hung interpreter and any forks it
   spawned get cleaned up together on timeout.
4. Ephemeral tempdir as CWD.
"""

from __future__ import annotations

import os
import resource
import shutil
import signal
import subprocess
import tempfile
from dataclasses import dataclass, field

from areal.utils import logging

logger = logging.getLogger("CodeSandbox")


@dataclass
class ExecutionResult:
    stdout: str
    stderr: str
    returncode: int
    timeout: bool
    error: str | None = None


@dataclass
class CompileResult:
    """Outcome of the compile (or stage-only) step.

    ``ok`` is False when the compiler returned non-zero or the toolchain is
    missing. The reward fn treats a failed compile as 0 reward.
    """

    ok: bool
    workdir: str
    run_cmd: list[str]
    stderr: str = ""
    error: str | None = None
    needs_jvm_xmx: bool = False
    skip_address_space_limit: bool = False


@dataclass
class LanguageSpec:
    """Toolchain configuration for one language.

    ``compile_cmd`` is None for interpreted languages; the source file is
    just staged on disk.

    Format placeholders (substituted at compile/run time):
        {src}      absolute path to the source file
        {exe}      absolute path to the compiled executable (compiled langs only)
        {workdir}  the per-submission working directory
    """

    name: str
    source_filename: str
    run_cmd: list[str]
    compile_cmd: list[str] | None = None
    exe_name: str | None = None  # only for compiled languages
    needs_jvm_xmx: bool = False
    # JVMs (Java) and V8 (Node) reserve large virtual-memory segments at
    # startup that exceed any reasonable RSS-based cap. Setting RLIMIT_AS
    # tight enough to bound RSS would kill them before main runs, so we
    # skip RLIMIT_AS for these and rely on per-runtime heap caps instead.
    skip_address_space_limit: bool = False
    extra_run_env: dict[str, str] = field(default_factory=dict)


LANGUAGE_SPECS: dict[str, LanguageSpec] = {
    "python": LanguageSpec(
        name="python",
        source_filename="solution.py",
        run_cmd=["python3", "-u", "{src}"],
    ),
    "cpp": LanguageSpec(
        name="cpp",
        source_filename="solution.cpp",
        compile_cmd=["g++", "-O2", "-std=c++17", "-pipe", "{src}", "-o", "{exe}"],
        exe_name="solution",
        run_cmd=["{exe}"],
    ),
    "java": LanguageSpec(
        name="java",
        # Class must match the filename for `java Main` to find it.
        source_filename="Main.java",
        compile_cmd=["javac", "{src}"],
        # JVM heap cap is filled in at run time from java_xmx_mb.
        run_cmd=["java", "-Xmx{xmx}m", "-cp", "{workdir}", "Main"],
        needs_jvm_xmx=True,
        skip_address_space_limit=True,
    ),
    "javascript": LanguageSpec(
        name="javascript",
        source_filename="solution.js",
        # `--max-old-space-size` is the per-process JS heap cap V8 honors.
        run_cmd=["node", "--max-old-space-size={node_old_mb}", "{src}"],
        skip_address_space_limit=True,
    ),
}


_LANG_ALIASES = {
    "python": "python",
    "py": "python",
    "cpp": "cpp",
    "c++": "cpp",
    "cxx": "cpp",
    "java": "java",
    "javascript": "javascript",
    "js": "javascript",
    "node": "javascript",
}


def canonical_language(lang: str) -> str | None:
    """Normalize a language alias to one of LANGUAGE_SPECS keys, or None."""
    if not lang:
        return None
    return _LANG_ALIASES.get(lang.strip().lower())


def _set_rlimits(memory_mb: int | None, cpu_seconds: int) -> None:
    if memory_mb is not None:
        mem_bytes = memory_mb * 1024 * 1024
        resource.setrlimit(resource.RLIMIT_AS, (mem_bytes, mem_bytes))
    resource.setrlimit(resource.RLIMIT_CPU, (cpu_seconds, cpu_seconds))
    resource.setrlimit(resource.RLIMIT_CORE, (0, 0))


def compile_code(
    language: str,
    code: str,
    compile_timeout: float = 30.0,
) -> CompileResult:
    """Stage source for interpreted languages or compile for compiled ones.

    The returned ``CompileResult`` owns a tempdir; pass it to ``run_artifact``
    one or more times, then call :func:`cleanup_compile_result` (or wrap with
    a context manager) to delete the tempdir.

    On compile failure the workdir is cleaned up immediately and ``ok=False``
    is returned.
    """
    canon = canonical_language(language)
    if canon is None:
        return CompileResult(
            ok=False, workdir="", run_cmd=[], error=f"unknown language: {language!r}"
        )
    spec = LANGUAGE_SPECS[canon]

    if not code.strip():
        return CompileResult(ok=False, workdir="", run_cmd=[], error="empty code")

    workdir = tempfile.mkdtemp(prefix=f"arealcode_{canon}_")
    try:
        src_path = os.path.join(workdir, spec.source_filename)
        with open(src_path, "w", encoding="utf-8") as f:
            f.write(code)

        exe_path = os.path.join(workdir, spec.exe_name) if spec.exe_name else ""

        if spec.compile_cmd is not None:
            cmd = [
                arg.format(src=src_path, exe=exe_path, workdir=workdir)
                for arg in spec.compile_cmd
            ]
            try:
                proc = subprocess.run(
                    cmd,
                    cwd=workdir,
                    capture_output=True,
                    text=True,
                    timeout=compile_timeout,
                )
            except FileNotFoundError as e:
                shutil.rmtree(workdir, ignore_errors=True)
                return CompileResult(
                    ok=False,
                    workdir="",
                    run_cmd=[],
                    error=f"toolchain missing for {canon}: {e}",
                )
            except subprocess.TimeoutExpired:
                shutil.rmtree(workdir, ignore_errors=True)
                return CompileResult(
                    ok=False,
                    workdir="",
                    run_cmd=[],
                    error=f"compile timeout after {compile_timeout}s",
                )

            if proc.returncode != 0:
                stderr = proc.stderr or ""
                shutil.rmtree(workdir, ignore_errors=True)
                return CompileResult(
                    ok=False,
                    workdir="",
                    run_cmd=[],
                    stderr=stderr,
                    error="compile failed",
                )

        run_cmd_template = list(spec.run_cmd)
        return CompileResult(
            ok=True,
            workdir=workdir,
            run_cmd=[
                arg.replace("{src}", src_path)
                .replace("{exe}", exe_path)
                .replace("{workdir}", workdir)
                for arg in run_cmd_template
            ],
            needs_jvm_xmx=spec.needs_jvm_xmx,
            skip_address_space_limit=spec.skip_address_space_limit,
        )
    except Exception as e:
        shutil.rmtree(workdir, ignore_errors=True)
        return CompileResult(
            ok=False, workdir="", run_cmd=[], error=f"compile exception: {e}"
        )


def cleanup_compile_result(result: CompileResult) -> None:
    if result.workdir and os.path.isdir(result.workdir):
        shutil.rmtree(result.workdir, ignore_errors=True)


def run_artifact(
    artifact: CompileResult,
    stdin: str = "",
    timeout: float = 5.0,
    memory_mb: int = 2048,
    java_xmx_mb: int = 2048,
) -> ExecutionResult:
    """Execute a compiled/staged artifact against one stdin payload.

    For Java the run command is finalized here using ``java_xmx_mb``; callers
    pass a single value for both the address-space cap (skipped for Java) and
    the JVM heap cap.
    """
    if not artifact.ok:
        return ExecutionResult(
            stdout="",
            stderr=artifact.stderr,
            returncode=-1,
            timeout=False,
            error=artifact.error or "compile failed",
        )

    cpu_limit = max(int(timeout) + 2, 2)
    # JVM and V8 reserve large virtual-memory ranges at startup; RLIMIT_AS
    # would kill them before main runs. Their per-runtime heap caps
    # (-Xmx, --max-old-space-size) bound the actual JS/JVM heap.
    address_space_cap = None if artifact.skip_address_space_limit else memory_mb

    cmd = [
        arg.replace("{xmx}", str(java_xmx_mb)).replace(
            "{node_old_mb}", str(max(memory_mb - 64, 64))
        )
        for arg in artifact.run_cmd
    ]

    def _preexec() -> None:
        _set_rlimits(address_space_cap, cpu_limit)
        os.setsid()

    proc: subprocess.Popen | None = None
    try:
        proc = subprocess.Popen(
            cmd,
            stdin=subprocess.PIPE,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=artifact.workdir,
            preexec_fn=_preexec,
            text=True,
        )
        try:
            stdout, stderr = proc.communicate(input=stdin, timeout=timeout)
            return ExecutionResult(
                stdout=stdout or "",
                stderr=stderr or "",
                returncode=proc.returncode,
                timeout=False,
            )
        except subprocess.TimeoutExpired:
            try:
                os.killpg(os.getpgid(proc.pid), signal.SIGKILL)
            except ProcessLookupError:
                pass
            try:
                stdout, stderr = proc.communicate(timeout=1.0)
            except Exception:
                stdout, stderr = "", ""
            return ExecutionResult(
                stdout=stdout or "",
                stderr=stderr or "",
                returncode=-1,
                timeout=True,
            )
    except FileNotFoundError as e:
        return ExecutionResult(
            stdout="", stderr="", returncode=-1, timeout=False, error=str(e)
        )
    except Exception as e:
        if proc is not None:
            try:
                os.killpg(os.getpgid(proc.pid), signal.SIGKILL)
            except Exception:
                pass
        return ExecutionResult(
            stdout="", stderr="", returncode=-1, timeout=False, error=str(e)
        )


def run_python(
    code: str,
    stdin: str = "",
    timeout: float = 5.0,
    memory_mb: int = 2048,
) -> ExecutionResult:
    """Backward-compat shim: stage + run a Python program in one shot."""
    artifact = compile_code("python", code)
    try:
        return run_artifact(artifact, stdin=stdin, timeout=timeout, memory_mb=memory_mb)
    finally:
        cleanup_compile_result(artifact)


def normalize_output(text: str) -> str:
    """Canonical form for stdout comparison: LF line endings, right-trim each
    line, drop trailing blank lines."""
    text = text.replace("\r\n", "\n").replace("\r", "\n")
    text = "\n".join(line.rstrip() for line in text.split("\n"))
    return text.rstrip("\n")


def outputs_match(produced: str, expected: str) -> bool:
    return normalize_output(produced) == normalize_output(expected)
```

### Tool Call - 2026-04-27 23:38:09 EDT

```
exec_command {"cmd":"sed -n '1,230p' areal/utils/stats_tracker.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":14000}
```

### Tool Output - 2026-04-27 23:38:10 EDT

```
Chunk ID: d27b05
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2133
Output:
import time
from collections import defaultdict
from contextlib import contextmanager
from enum import Enum, auto
from threading import Lock

import torch
import torch.distributed as dist

from areal.infra.platforms import current_platform
from areal.utils import logging
from areal.utils.datapack import flat2d

logger = logging.getLogger("StatsTracker")


class ReduceType(Enum):
    AVG_MIN_MAX = auto()
    AVG = auto()
    SUM = auto()
    MIN = auto()
    MAX = auto()
    SCALAR = auto()


MOE_AUX_LOSSES = {}


class DistributedStatsTracker:
    def __init__(self, name: str = ""):
        self.lock = Lock()
        self.scope_stack = []
        if name:
            self.scope_stack.append(name.strip("/"))
        self.denominators = {}  # key -> denominator key
        self.reduce_types = {}  # key -> ReduceType

        self.stats = defaultdict(list)

    def scope(self, name):
        """Context manager for hierarchical scoping"""
        with self.lock:
            return self.Scope(self, name)

    def scope_func_wrapper(self, name):
        """Decorator for hierarchical scoping"""

        def decorator(func):
            def wrapper(*args, **kwargs):
                with self.scope(name):
                    return func(*args, **kwargs)

            return wrapper

        return decorator

    class Scope:
        def __init__(self, tracker, name):
            self.tracker = tracker
            self.name = name.strip("/")

        def __enter__(self):
            self.tracker.scope_stack.append(self.name)
            return self

        def __exit__(self, exc_type, exc_val, exc_tb):
            self.tracker.scope_stack.pop()

    def _get_full_key(self, key):
        """Combine scope stack with current key"""
        if not self.scope_stack:
            return key
        return "/".join(self.scope_stack + [key])

    @contextmanager
    def disable_scope(self):
        tmp = self.scope_stack
        self.scope_stack = []
        yield
        self.scope_stack = tmp

    @contextmanager
    def record_timing(self, key):
        start_time = time.perf_counter()
        try:
            yield
        finally:
            with self.lock:
                # NOTE: timing records are fixed under the "timeperf" scope
                full_key = f"timeperf/{key}"
                self._set_reduce_type(full_key, ReduceType.SCALAR)
                self.stats[full_key].append(time.perf_counter() - start_time)

    def denominator(self, **kwargs):
        with self.lock:
            for key, value in kwargs.items():
                if not isinstance(value, torch.Tensor) or value.dtype != torch.bool:
                    raise ValueError(
                        f"`{key}` must be a pytorch bool tensor: {value.dtype}"
                    )
                if value.numel() == 0:
                    raise ValueError(f"`{key}` must be non-empty")
                full_key = self._get_full_key(key)
                self._set_reduce_type(full_key, ReduceType.SUM)
                self.stats[full_key].append(value.detach().clone())

    def scalar(self, **kwargs):
        with self.lock:
            for key, value in kwargs.items():
                full_key = self._get_full_key(key)
                self._set_reduce_type(full_key, ReduceType.SCALAR)
                self.stats[full_key].append(float(value))

    def stat(
        self,
        denominator: str,
        reduce_type: ReduceType | None = None,
        **kwargs,
    ):
        """Record multiple values from a dictionary"""
        with self.lock:
            for key, value in kwargs.items():
                if not isinstance(value, torch.Tensor) or value.dtype != torch.float:
                    raise ValueError(
                        f"`{key}` should be a pytorch float tensor: {value.dtype}"
                    )
                if value.numel() == 0:
                    raise ValueError(f"`{key}` should be non-empty")
                if reduce_type == ReduceType.SCALAR:
                    raise ValueError("Cannot use the scalar reduce type for a tensor")
                full_key = self._get_full_key(key)

                denorm = self._get_full_key(denominator)
                if denorm not in self.stats or not self.stats[denorm]:
                    raise ValueError(f"Denominator `{denorm}` does not exist")
                for x, y in zip(self.stats[denorm], self.stats[full_key] + [value]):
                    assert x.shape == y.shape, (x.shape, y.shape)
                self.denominators[full_key] = denorm

                if reduce_type is None:
                    reduce_type = ReduceType.AVG_MIN_MAX
                self._set_reduce_type(full_key, reduce_type)
                self.stats[full_key].append(value.detach().clone())

    def _set_reduce_type(self, key, reduce_type):
        if not isinstance(reduce_type, ReduceType):
            raise ValueError("reduce_type must be a ReduceType enum")
        self.reduce_types[key] = reduce_type

    def export(self, key=None, reduce_group=None, reset=True) -> dict[str, float]:
        """Get aggregated statistics"""
        with self.lock:
            if key is not None:
                full_key = self._get_full_key(key)
                result = self._aggregate(full_key, reduce_group)
                if reset:
                    if full_key in self.denominators:
                        self.denominators.pop(full_key)
                    if full_key in self.reduce_types:
                        self.denominators.pop(full_key)
                    self.stats.pop(full_key)
                return result

            # synchronize keys across ranks
            keys = list(self.stats.keys())
            if reduce_group is not None:
                all_keys = [None for _ in range(dist.get_world_size(reduce_group))]
                dist.all_gather_object(all_keys, keys, group=reduce_group)
                # Should ensure that the orders are the same
                keys = sorted(list(set(flat2d(all_keys))))
            results = {}
            for key in keys:
                results.update(self._aggregate(key, reduce_group))
            if reset:
                self.denominators = {}
                self.reduce_types = {}
                self.stats = defaultdict(list)
            results = {
                k: v.cpu().item() if torch.is_tensor(v) else v
                for k, v in results.items()
            }
            return results

    def _aggregate(self, key, reduce_group):
        reduce_type = self.reduce_types.get(key, ReduceType.SCALAR)

        result = {}
        if reduce_type == ReduceType.AVG_MIN_MAX:
            result["/".join([key, "avg"])] = self._avg_of(key, reduce_group)
            result["/".join([key, "min"])] = self._min_of(key, reduce_group)
            result["/".join([key, "max"])] = self._max_of(key, reduce_group)
        elif reduce_type == ReduceType.AVG:
            result[key] = self._avg_of(key, reduce_group)
        elif reduce_type == ReduceType.SUM:
            result[key] = self._sum_of(key, reduce_group)
        elif reduce_type == ReduceType.MIN:
            result[key] = self._min_of(key, reduce_group)
        elif reduce_type == ReduceType.MAX:
            result[key] = self._max_of(key, reduce_group)
        elif reduce_type == ReduceType.SCALAR:
            if current_platform.is_initialized():
                device = current_platform.device_type
            else:
                device = "cpu"
            value = torch.tensor(
                sum(self.stats[key]), dtype=torch.float32, device=device
            )
            cnt = torch.tensor(len(self.stats[key]), dtype=torch.float32, device=device)
            if reduce_group is not None:
                dist.all_reduce(value, group=reduce_group)
                dist.all_reduce(cnt, group=reduce_group)
            result[key] = float(value / cnt)
            result[key + "__count"] = int(cnt)
        else:
            raise ValueError(f"Unknown reduce type: {reduce_type}")

        keys_to_pop = [k for k, v in result.items() if v is None]
        for k in keys_to_pop:
            result.pop(k)
        return result

    def _sum_of(self, key, reduce_group):
        values = self.stats[key]
        if key not in self.denominators:
            x = sum([x.sum() for x in values])
            if reduce_group is not None:
                dist.all_reduce(x, group=reduce_group)
        else:
            denominator = self.denominators[key]
            if denominator not in self.stats:
```

### Assistant - 2026-04-27 23:38:24 EDT

```
因为 reward 是在 `ProcessPoolExecutor` 子进程里算的，直接在 reward 函数里调 `stats_tracker` 不会回到主训练进程。我会让 code reward 在可选模式下返回一个诊断 dict，再由 workflow 在主进程统一写到 `stats_tracker`，这样 SwanLab/TensorBoard/日志导出都能拿到。
```

### Tool Call - 2026-04-27 23:38:24 EDT

```
exec_command {"cmd":"rg -n \"class ModelResponse|finish_reason|output_len|output_tokens\" areal/api areal/engine -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Call - 2026-04-27 23:38:24 EDT

```
exec_command {"cmd":"sed -n '1,180p' areal/api/io_struct.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":8000}
```

### Tool Call - 2026-04-27 23:38:24 EDT

```
exec_command {"cmd":"sed -n '160,260p' tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":10000}
```

### Tool Output - 2026-04-27 23:38:24 EDT

```
Chunk ID: 8febfd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 734
Output:
areal/engine/vllm_ext/areal_vllm_server.py:293:            finish_reason=FinishReason.ABORT,
areal/engine/sglang_remote.py:98:        finish_reason = meta_info["finish_reason"]
areal/engine/sglang_remote.py:99:        stop_reason = finish_reason["type"]
areal/engine/sglang_remote.py:100:        stop_message = finish_reason.get("message", "")
areal/engine/sglang_remote.py:115:                output_tokens=[],
areal/engine/sglang_remote.py:121:        output_tokens = [x[1] for x in meta_info["output_token_logprobs"]]
areal/engine/sglang_remote.py:135:            output_tokens=output_tokens,
areal/engine/megatron_utils/packed_context_parallel.py:85:    output_len = int(cu_seqlens[-1].item())
areal/engine/megatron_utils/packed_context_parallel.py:88:        (output_len, *output.shape[2:]), device=output.device, dtype=output.dtype
areal/engine/vllm_remote.py:101:        stop_reason = meta_info["finish_reason"]
areal/engine/vllm_remote.py:105:            output_tokens = meta_info["logprobs"]["tokens"]
areal/engine/vllm_remote.py:106:            output_tokens = [int(t.split(":")[1]) for t in output_tokens]
areal/engine/vllm_remote.py:110:            output_tokens = [int(t["token"].split(":")[1]) for t in outputs]
areal/engine/vllm_remote.py:137:        if stop_reason == "abort" and len(output_tokens) == 0:
areal/engine/vllm_remote.py:139:                output_tokens=[],
areal/engine/vllm_remote.py:145:            output_tokens=output_tokens,
areal/api/io_struct.py:61:class ModelResponse:
areal/api/io_struct.py:64:    output_tokens: list[int] = field(default_factory=list)
areal/api/io_struct.py:89:    def output_len(self) -> int:
areal/api/io_struct.py:90:        return len(self.output_tokens)
areal/api/io_struct.py:98:        if len(self.output_tokens) == 0:
areal/api/io_struct.py:100:        last_token = self.output_tokens[-1]
areal/api/io_struct.py:106:    def output_tokens_without_stop(self) -> list[int]:
areal/api/io_struct.py:108:            raise ValueError("tokenizer is None, cannot get output_tokens_without_stop")
areal/api/io_struct.py:109:        if self.stop_reason not in ["length", "abort"] and self.output_tokens:
areal/api/io_struct.py:112:                    f"output_tokens does not end with eos or pad token, it ends with {self.output_tokens[-1]}, but stop_reason is {self.stop_reason}"
areal/api/io_struct.py:119:            for tok in reversed(self.output_tokens):
areal/api/io_struct.py:124:            if pad_or_eos_len == len(self.output_tokens):
areal/api/io_struct.py:126:                    "All output_tokens are EOS or PAD tokens; cannot strip stop tokens without removing entire output."
areal/api/io_struct.py:128:            return self.output_tokens[:-pad_or_eos_len]
areal/api/io_struct.py:129:        return self.output_tokens
areal/api/io_struct.py:275:    output_tokens: list[int]
areal/api/cli_args.py:286:            mapping["max_new_tokens"] = "max_output_tokens"
```

### Tool Output - 2026-04-27 23:38:24 EDT

```
Chunk ID: 24bb47
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1463
Output:
import copy
import os
import subprocess
import uuid
from dataclasses import dataclass, field
from typing import TYPE_CHECKING, Any, Literal, Optional

import numpy as np
import torch
import torch.distributed as dist
from PIL.Image import Image as ImageObject
from transformers import PreTrainedTokenizerFast

from areal.api.alloc_mode import AllocationMode
from areal.api.cli_args import GenerationHyperparameters
from areal.infra.platforms import current_platform
from areal.utils import logging

if TYPE_CHECKING:
    from transformers import AutoProcessor

logger = logging.getLogger("IOStruct")


@dataclass
class ModelRequest:
    rid: str = field(default_factory=lambda: str(uuid.uuid4()))
    input_ids: list[int] = field(default_factory=list)
    gconfig: GenerationHyperparameters = field(
        default_factory=GenerationHyperparameters
    )
    metadata: dict[str, Any] = field(default_factory=dict)
    # tokenizer is used for encode-decode in the inference engine
    tokenizer: PreTrainedTokenizerFast | None = None

    # vlm
    image_data: list[str] | None = field(default_factory=list)
    processor: Optional["AutoProcessor"] = None

    # vlm+vllm:
    vision_msg_vllm: list | None = None

    def copy(self):
        return ModelRequest(
            rid=self.rid,
            input_ids=self.input_ids.copy(),
            gconfig=self.gconfig.new(),
            metadata=self.metadata.copy(),
            tokenizer=self.tokenizer,
            image_data=self.image_data.copy() if self.image_data is not None else None,
            processor=self.processor,
            vision_msg_vllm=(
                self.vision_msg_vllm.copy()
                if self.vision_msg_vllm is not None
                else None
            ),
        )


@dataclass
class ModelResponse:
    # outputs
    input_tokens: list[int] = field(default_factory=list)
    output_tokens: list[int] = field(default_factory=list)
    input_logprobs: list[float] = field(default_factory=list)
    output_logprobs: list[float] = field(default_factory=list)
    output_versions: list[int] = field(default_factory=list)
    stop_reason: Literal["length", "stop", "tool_calls", "abort"] = "stop"
    # tokenizer is used for encode-decode in the inference engine
    tokenizer: PreTrainedTokenizerFast | None = None

    # vlm
    input_images: list[ImageObject | str] = field(default_factory=list)
    processor: Optional["AutoProcessor"] = None

    # statistics
    latency: float = float("inf")
    ttft: float = float("inf")  # Time to first token
    itl: list[float] = field(default_factory=list)  # List of inter-token latencies

    # MoE routing (only populated when return_routed_experts=True)
    routed_experts: np.ndarray | None = None

    @property
    def input_len(self) -> int:
        return len(self.input_tokens)

    @property
    def output_len(self) -> int:
        return len(self.output_tokens)

    @property
    def end_with_stop(self) -> bool:
        if self.tokenizer is None:
            raise ValueError("tokenizer is None, cannot check end_with_stop")
        eos_id = self.tokenizer.eos_token_id
        pad_id = self.tokenizer.pad_token_id
        if len(self.output_tokens) == 0:
            return False
        last_token = self.output_tokens[-1]
        return (eos_id is not None and last_token == eos_id) or (
            pad_id is not None and last_token == pad_id
        )

    @property
    def output_tokens_without_stop(self) -> list[int]:
        if self.tokenizer is None:
            raise ValueError("tokenizer is None, cannot get output_tokens_without_stop")
        if self.stop_reason not in ["length", "abort"] and self.output_tokens:
            if not self.end_with_stop:
                raise ValueError(
                    f"output_tokens does not end with eos or pad token, it ends with {self.output_tokens[-1]}, but stop_reason is {self.stop_reason}"
                )
            pad_or_eos_len = 0
            eos_id = self.tokenizer.eos_token_id
            pad_id = self.tokenizer.pad_token_id
            stop_tokens = {eos_id, pad_id}
            stop_tokens.discard(None)
            for tok in reversed(self.output_tokens):
                if tok in stop_tokens:
                    pad_or_eos_len += 1
                else:
                    break
            if pad_or_eos_len == len(self.output_tokens):
                raise ValueError(
                    "All output_tokens are EOS or PAD tokens; cannot strip stop tokens without removing entire output."
                )
            return self.output_tokens[:-pad_or_eos_len]
        return self.output_tokens


@dataclass
class FinetuneSpec:
    total_train_epochs: int
    dataset_size: int
    train_batch_size: int

    @property
    def total_train_steps(self):
        # assuming drop_last
        return self.total_train_epochs * (self.dataset_size // self.train_batch_size)

    @property
    def steps_per_epoch(self):
        return self.dataset_size // self.train_batch_size


@dataclass
class ParamSpec:
    name: str
    shape: tuple
    dtype: str

    @property
    def size(self) -> int:
        """Param bytes"""
        return getattr(torch, self.dtype).itemsize * np.prod(self.shape)


def get_versioned_lora_name(lora_name: str, version: int) -> str:
    """Get versioned LoRA adapter name (e.g., 'lora-v1')."""
    return f"{lora_name}-v{version}"


@dataclass
class WeightUpdateMeta:
    type: Literal["disk", "nccl"]
    path: str | None = None
    alloc_mode: AllocationMode | None = None

    nccl_master_address: str | None = None
    nccl_master_port: int | None = None
    nccl_group_name: str | None = None
    weight_chunked_mem_mb: int = 1024

    use_lora: bool = False
    lora_name: str = ""
    lora_int_id: int = 0
    base_model_name: str = ""
    peft_config: dict = field(default_factory=dict)
```

### Tool Output - 2026-04-27 23:38:24 EDT

````
Chunk ID: 1d377f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 613
Output:
def test_extract_code_picks_last_fence():
    text = (
        "First try:\n```python\nprint('first')\n```\n"
        "Second try:\n```python\nprint('second')\n```\n"
    )
    got = extract_code(text, expected_language="python")
    assert got is not None
    assert "second" in got[1]


# end-to-end reward fn ---------------------------------------------------------


def test_reward_fn_python_pass_rate():
    if not TOOLCHAIN.get("python"):
        pytest.skip("python toolchain not available")
    completion = "Here's the answer:\n```python\nx = int(input())\nprint(x * 2)\n```\n"
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n", "10\n", "100\n"],
        test_outputs=["2", "20", "200"],
        language="python",
        per_test_timeout=5.0,
        max_tests=15,
        memory_mb=512,
    )
    assert reward == 1.0


def test_reward_fn_cpp_pass_rate():
    if not TOOLCHAIN.get("cpp"):
        pytest.skip("cpp toolchain not available")
    completion = (
        "```cpp\n"
        "#include <iostream>\nint main(){int x; std::cin >> x; std::cout << x * 2 << '\\n';}\n"
        "```\n"
    )
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["3\n", "7\n"],
        test_outputs=["6", "14"],
        language="cpp",
        per_test_timeout=10.0,
        max_tests=15,
        memory_mb=1024,
        compile_timeout=30.0,
    )
    assert reward == 1.0


def test_reward_fn_no_fence_returns_zero():
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions="I would solve this by ...",  # no code block
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n"],
        test_outputs=["2"],
        language="python",
    )
    assert reward == 0.0


def test_reward_fn_compile_failure_returns_zero():
    if not TOOLCHAIN.get("cpp"):
        pytest.skip("cpp toolchain not available")
    completion = "```cpp\nint main(){ return 0 }\n```"
    reward = nemotron_competitive_reward_fn(
        prompt="dummy",
        completions=completion,
        prompt_ids=[],
        completion_ids=[],
        test_inputs=["1\n"],
        test_outputs=["2"],
        language="cpp",
        compile_timeout=10.0,
    )
    assert reward == 0.0
````

### Tool Call - 2026-04-27 23:38:32 EDT

```
exec_command {"cmd":"rg -n \"output_len|total_len|seq_len|stop_length|truncated\" areal examples -S","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":5000}
```

### Tool Output - 2026-04-27 23:38:32 EDT

```
Chunk ID: 993117
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 6671
Output:
Total output lines: 277

areal/engine/fsdp_utils/parallel.py:211:    def seq_len_divisor(self) -> int:
areal/engine/fsdp_utils/parallel.py:212:        # 1. Sequence Parallel requires that seq_len be divisible by TP degree.
areal/engine/fsdp_utils/parallel.py:213:        # 2. Ulysses Sequence Parallel requires that seq_len be divisible by SP degree.
areal/engine/fsdp_engine.py:1382:            mb["cu_seq_lens_q"] = mb["cu_seq_lens_k"] = mb["cu_seqlens"]
areal/engine/fsdp_engine.py:1383:            padded_mb["cu_seq_lens_q"] = padded_mb["cu_seq_lens_k"] = padded_mb[
areal/engine/fsdp_engine.py:1513:        # inputs (padded_mbs) has batch dim (1, seq_len), squeeze to match logits (seq_len,)
areal/engine/fsdp_engine.py:1543:        # inputs (padded_mbs) has batch dim (1, seq_len), squeeze to match logits (seq_len,)
areal/engine/megatron_utils/packed_context_parallel.py:82:    # shape = [batch_size, seq_len] + list(output.shape[2:])
areal/engine/megatron_utils/packed_context_parallel.py:83:    # [1, packed, dim] -> [batch_size, seq_len, dim]
areal/engine/megatron_utils/packed_context_parallel.py:85:    output_len = int(cu_seqlens[-1].item())
areal/engine/megatron_utils/packed_context_parallel.py:86:    # output shape: [total_packed_seq_len] + list(output.shape[2:]
areal/engine/megatron_utils/packed_context_parallel.py:88:        (output_len, *output.shape[2:]), device=output.device, dtype=output.dtype
areal/engine/megatron_utils/packed_context_parallel.py:99:        seq_len = cu_seqlens[i + 1] - cu_seqlens[i]
areal/engine/megatron_utils/packed_context_parallel.py:100:        splitted_seq_len = (cu_seqlens[i + 1] - cu_seqlens[i]) // cp_size
areal/engine/megatron_utils/packed_context_parallel.py:101:        half_splitted_seq_len = splitted_seq_len // 2
areal/engine/megatron_utils/packed_context_parallel.py:104:            (seq_len, *output.shape[2:]), device=output.device, dtype=output.dtype
areal/engine/megatron_utils/packed_context_parallel.py:111:                o[start : start + half_splitted_seq_len],
areal/engine/megatron_utils/packed_context_parallel.py:112:                o[start + half_splitted_seq_len : start + splitted_seq_len],
areal/engine/megatron_utils/packed_context_parallel.py:114:            tmp[j * half_splitted_seq_len : (j + 1) * half_splitted_seq_len] = o0
areal/engine/megatron_utils/packed_context_parallel.py:115:            splitted_start = seq_len - (j + 1) * half_splitted_seq_len
areal/engine/megatron_utils/packed_context_parallel.py:116:            splitted_end = seq_len - j * half_splitted_seq_len
areal/engine/megatron_utils/packed_context_parallel.py:119:        output_new[cu_seqlens[i] : cu_seqlens[i + 1]] = tmp[:seq_len]
areal/engine/megatron_engine.py:633:                seq_length=mb_list.max_seqlen,  # no use when input_shapes was set
areal/tools/profile_engines.py:180:        seq_lens=inputs_config["seq_lens"],
areal/tools/profile_engines.py:181:        total_len=inputs_config["total_len"],
areal/tools/profile_engines.py:281:        seq_lens=inputs_config["seq_lens"],
areal/tools/profile_engines.py:282:        total_len=inputs_config["total_len"],
areal/tools/profile_engines.py:433:    # Parse seq_lens if provided
areal/tools/profile_engines.py:434:    seq_lens = None
areal/tools/profile_engines.py:435:    if args.seq_lens:
areal/tools/profile_engines.py:436:        seq_lens = [int(x.strip()) for x in args.seq_lens.split(",")]
areal/tools/profile_engines.py:437:        args.batch_size = len(seq_lens)
areal/tools/profile_engines.py:443:    # Generate seq_lens if not provided
areal/tools/profile_engines.py:444:    if seq_lens is None:
areal/tools/profile_engines.py:445:        from areal.tools.profiling_utils.utils import generate_random_seq_lens
areal/tools/profile_engines.py:447:        seq_lens = generate_random_seq_lens(args.batch_size, args.total_len)
areal/tools/profile_engines.py:451:        "seq_lens": seq_lens,
areal/tools/profile_engines.py:452:        "total_len": args.total_len,
areal/tools/profile_engines.py:468:    print(f"Sequence lengths: {seq_lens}")
areal/tools/profile_engines.py:469:    print(f"Total tokens: {sum(seq_lens)}")
areal/tools/profiling_utils/utils.py:6:def generate_random_seq_lens(
areal/tools/profiling_utils/utils.py:8:    total_len: int,
areal/tools/profiling_utils/utils.py:9:    min_seq_len: int = 64,
areal/tools/profiling_utils/utils.py:11:    """Generate random sequence lengths that sum to total_len.
areal/tools/profiling_utils/utils.py:18:        total_len: Target total length (sum of all sequence lengths).
areal/tools/profiling_utils/utils.py:19:        min_seq_len: Minimum length for each sequence.
areal/tools/profiling_utils/utils.py:22:        List of sequence lengths that sum to total_len.
areal/tools/profiling_utils/utils.py:24:    seq_lens = []
areal/tools/profiling_utils/utils.py:25:    remaining = total_len
areal/tools/profiling_utils/utils.py:29:        min_len = max(min_seq_len, avg_remaining // 2)
areal/tools/profiling_utils/utils.py:30:        max_len = min(remaining - min_seq_len * (batch_size - i - 1), avg_remaining * 2)
areal/tools/profiling_utils/utils.py:32:        seq_lens.append(length)
areal/tools/profiling_utils/utils.py:35:    seq_lens.append(remaining)
areal/tools/profiling_utils/utils.py:36:    return seq_lens
areal/tools/profiling_utils/__init__.py:3:from areal.tools.profiling_utils.utils import generate_random_seq_lens
areal/tools/profiling_utils/__init__.py:6:    "generate_random_seq_lens",
areal/tools/profile_archon.py:147:    seq_lens: list[int] | None,
areal/tools/profile_archon.py:148:    total_len: int,
areal/tools/profile_archon.py:155:        batch_size: Number of sequences if seq_lens is None.
areal/tools/profile_archon.py:156:        seq_lens: Explicit sequence lengths. If None, generates random lengths.
areal/tools/profile_archon.py:157:        total_len: Total token count when seq_lens is None.
areal/tools/profile_archon.py:162:        dict with input_ids, cu_seqlens, max_seqlen, and seq_lens.
areal/tools/profile_archon.py:164:    if seq_lens is None:
areal/tools/profile_archon.py:165:        from areal.tools.profiling_utils.utils import generate_random_seq_lens
areal/tools/profile_archon.py:167:        seq_lens = generate_random_seq_lens(batch_size, total_len)
areal/tools/profile_archon.py:169:    actual_total_len = sum(seq_lens)
areal/tools/profile_archon.py:171:        100, vocab_size - 100, (1, actual_total_len), device=device
areal/tools/profile_archon.py:176:        [0] + list(itertools.accumulate(seq_lens)),
areal/tools/profile_archon.py:180:    max_seqlen = max(seq_lens)
areal/tools/profile_archon.py:186:        "seq_lens": seq_lens,
areal/tools/profile_archon.py:234:    # Parse seq_lens if provided
areal/tools/profile_archon.py:235:    seq_lens = None
areal/tools/profile_archon.py:236:    if args.seq_lens:
areal/tools/profile_archon.py:237:        seq_lens = [int(x.strip()) for x in args.seq_lens.split(",")]
areal/tools/profile_archon.py:238:        args.batch_size = len(seq_lens)
areal/tools/profile_archon.py:252:    if seq_lens:
areal/tools/profile_archon.py:253:        print(f"Sequence lengths: {seq_lens}")
areal/tools/profile_archon.py:255:        print(f"Total length: {args.total_len} (random variable lengths)")
areal/tools/profile_archon.py:289:        seq_lens=seq_lens,
areal/tools/profile_archon.py:290:        total_len=args.total_len,
areal/tools/profile_archon.py:296:    print(f"  Sequences: {len(inputs['seq_lens'])}")
areal/tools/profile_archon.py:297:    print(f"  Sequence lengths: {inputs['seq_lens']}")
areal/tools/profile_archon.py:298:    print(f"  Total tokens: {sum(inputs['seq_lens'])}")
examples/search_agent/tongyi_deepresearch/tool_visit.py:30:    truncated_tokens = tokens[:max_tokens]
examples/search_agent/tongyi_deepresearch/tool_visit.py:31:    return encoding.decode(truncated_tokens)
areal/tools/profile_fsdp.py:192:    seq_lens: list[int] | None,
areal/tools/profile_fsdp.py:193:    total_len: int,
areal/tools/profile_fsdp.py:200:        batch_size: Number of sequences if seq_lens is None.
areal/tools/profile_fsdp.py:201:        seq_lens: Explicit sequence lengths. If None, generates random lengths.
areal/tools/profile_fsdp.py:202:        total_len: Total token count when seq_lens is None.
areal/tools/profile_fsdp.py:207:        dict with input_ids, attention_mask, and seq_lens.
areal/tools/profile_fsdp.py:209:    if seq_lens is None:
areal/tools/profile_fsdp.py:210:        from areal.tools.profiling_utils.utils import generate_random_seq_lens
areal/tools/profile_fsdp.py:212:        seq_lens = generate_random_seq_lens(batch_size, total_len)
areal/tools/profile_fsdp.py:214:    max_seqlen = max(seq_lens)
areal/tools/profile_fsdp.py:222:    for i, seq_len in enumerate(seq_lens):
areal/tools/profile_fsdp.py:223:        input_ids[i, :seq_len] = torch.randint(
areal/tools/profile_fsdp.py:224:            100, vocab_size - 100, (seq_len,), device=device
areal/tools/profile_fsdp.py:226:        attention_mask[i, :seq_len] = 1
areal/tools/profile_fsdp.py:231:        "seq_lens": seq_lens,
areal/tools/profile_fsdp.py:278:    # Parse seq_lens if provided
areal/tools/profile_fsdp.py:279:    seq_lens = None
areal/tools/profile_fsdp.py:280:    if args.seq_lens:
areal/tools/profile_fsdp.py:281:        seq_lens = [int(x.strip()) for x in args.seq_lens.split(",")]
areal/tools/profile_fsdp.py:282:        args.batch_size = len(seq_lens)
areal/tools/profile_fsdp.py:297:    if seq_lens:
areal/tools/profile_fsdp.py:298:        print(f"Sequence lengths: {seq_lens}")
areal/tools/profile_fsdp.py:300:        print(f"Total length: {args.total_len} (random variable lengths)")
areal/tools/profile_fsdp.py:324:        seq_lens=seq_lens,
areal/tools/profile_fsdp.py:325:        total_len=args.total_len,
areal/tools/profile_fsdp.py:331:    print(f"  Sequences: {len(inputs['seq_lens'])}")
areal/tools/profile_fsdp.py:332:    print(f"  Sequence lengths: {inputs['seq_lens']}")
areal/tools/profile_fsdp.py:333:    print(f"  Total tokens (non-pad…1671 tokens truncated…im]
areal/experimental/models/archon/attention/varlen.py:295:        batch, n_heads, seq_len, head_dim = q.shape
areal/workflow/rlvr.py:161:        loss_mask = [0] * resp.input_len + [1] * resp.output_len
areal/experimental/models/archon/attention/sdpa.py:18:    seq_len: int,
areal/experimental/models/archon/attention/sdpa.py:31:        seq_len: Total sequence length (should equal cu_seqlens[-1]).
areal/experimental/models/archon/attention/sdpa.py:36:        Attention mask of shape [seq_len, seq_len].
areal/experimental/models/archon/attention/sdpa.py:49:    positions = torch.arange(seq_len, device=device)
areal/experimental/models/archon/attention/sdpa.py:57:    mask = torch.full((seq_len, seq_len), float("-inf"), device=device, dtype=dtype)
areal/experimental/models/archon/attention/sdpa.py:98:            q: Query tensor, shape [batch, heads, seq_len, head_dim]
areal/experimental/models/archon/attention/sdpa.py:99:            k: Key tensor, shape [batch, heads, seq_len, head_dim]
areal/experimental/models/archon/attention/sdpa.py:100:            v: Value tensor, shape [batch, heads, seq_len, head_dim]
areal/experimental/models/archon/attention/sdpa.py:108:            Attention output, shape [batch, heads, seq_len, head_dim]
areal/experimental/models/archon/attention/sdpa.py:110:        seq_len = q.shape[2]
areal/experimental/models/archon/attention/sdpa.py:112:        attn_mask = create_block_causal_mask_2d(cu_seqlens, seq_len, q.device, q.dtype)
areal/experimental/models/archon/parallel_dims.py:371:    def seq_len_divisor(self) -> int:
areal/workflow/rlvr_qun_team.py:286:        seq_len = len(resp.input_tokens) + len(resp.output_tokens)
areal/workflow/rlvr_qun_team.py:289:        token_level_rewards = [0.0] * seq_len
areal/workflow/rlvr_qun_team.py:290:        token_level_len_penalties = [0.0] * seq_len
areal/workflow/rlvr_qun_team.py:291:        step_reward_mask = [0.0] * seq_len
areal/workflow/rlvr_qun_team.py:292:        step_boundary_mask = [1.0] * seq_len
areal/workflow/rlvr_qun_team.py:298:            if 0 <= reward_idx < seq_len:
areal/workflow/rlvr_qun_team.py:302:            if abs_idx < seq_len:
areal/workflow/rlvr_qun_team.py:342:        loss_mask = [0] * resp.input_len + [1] * resp.output_len
areal/workflow/rlvr_qun_team.py:364:                        # 最后的 unsqueeze(0) 会完美兼容 actor 里的 shape (bs, seq_len)
areal/experimental/models/archon/qwen2/model/args.py:28:    max_seq_len: int = 32768
areal/experimental/models/archon/qwen2/model/args.py:59:            max_seq_len=getattr(hf_config, "max_position_embeddings", 32768),
areal/workflow/vision_rlvr.py:142:        loss_mask = [0] * resp.input_len + [1] * resp.output_len
areal/experimental/models/archon/qwen2/model/model.py:300:            self.model_args.max_seq_len,
areal/experimental/models/archon/qwen2/model/rope.py:15:    dim: int, max_seq_len: int, base: float = 10_000.0
areal/experimental/models/archon/qwen2/model/rope.py:21:        max_seq_len: Maximum sequence length.
areal/experimental/models/archon/qwen2/model/rope.py:25:        RoPE cache tensor of shape [max_seq_len, dim * 2].
areal/experimental/models/archon/qwen2/model/rope.py:29:    # Create position indexes `[0, 1, ..., max_seq_len - 1]`
areal/experimental/models/archon/qwen2/model/rope.py:30:    t = torch.arange(max_seq_len, dtype=freqs.dtype, device=freqs.device)
areal/experimental/models/archon/qwen2/model/rope.py:33:    # a shape of [max_seq_len, dim // 2]
areal/experimental/models/archon/qwen2/model/rope.py:38:    # Size: [max_seq_len, (dim * 2)]
areal/experimental/models/archon/qwen2/model/rope.py:65:            Shape is [bsz, seq_len, num_heads, head_dim].
areal/experimental/models/archon/qwen2/model/rope.py:102:        xq: Query tensor, shape [bsz, seq_len, num_heads, head_dim].
areal/experimental/models/archon/qwen2/model/rope.py:103:        xk: Key tensor, shape [bsz, seq_len, num_kv_heads, head_dim].
areal/experimental/models/archon/qwen2/model/rope.py:104:        rope_cache: RoPE cache tensor, shape [max_seq_len, head_dim * 2].
areal/experimental/models/archon/qwen2/model/rope.py:105:        positions: Position indices, shape [1, seq_len] or [bsz, seq_len].
areal/experimental/models/archon/qwen2/model/rope.py:120:    # [bsz, seq_len, 1, head_dim]
areal/experimental/models/archon/qwen2/model/rope.py:124:    # xq:  [bsz, seq_len, num_heads, head_dim]
areal/experimental/models/archon/qwen2/model/rope.py:125:    # xk:  [bsz, seq_len, num_kv_heads, head_dim]
areal/experimental/models/archon/ulysses.py:30:    total_len = tensor.shape[-1]
areal/experimental/models/archon/ulysses.py:32:    if total_len % cp_size != 0:
areal/experimental/models/archon/ulysses.py:34:            f"Tensor length {total_len} not aligned to cp_size {cp_size}. "
areal/experimental/models/archon/ulysses.py:35:            "Ensure pad_mb_list is called with batch_align_to=seq_len_divisor first."
areal/experimental/models/archon/ulysses.py:38:    chunk_size = total_len // cp_size
areal/experimental/models/archon/qwen3/model/args.py:39:    max_seq_len: int = 4096
areal/experimental/models/archon/qwen3/model/args.py:94:            max_seq_len=getattr(hf_config, "max_position_embeddings", 4096),
areal/experimental/models/archon/qwen3/model/model.py:404:            self.model_args.max_seq_len,
areal/experimental/models/archon/moe/router.py:140:            x: Input tensor, shape (num_tokens, dim) or (batch_size * seq_len, dim).
areal/experimental/models/archon/moe/router.py:203:            init_std: Standard deviation for truncated normal initialization.
areal/experimental/models/archon/moe/moe.py:114:            x: Input tensor, shape (batch_size, seq_len, dim).
areal/experimental/models/archon/moe/moe.py:117:            Output tensor, shape (batch_size, seq_len, dim).
areal/experimental/models/archon/moe/grouped_experts.py:201:        """Initialize weights using truncated normal distribution.
areal/models/transformers/ulyssess_patch.py:110:                            seq_len = original_visual_mask.shape[1]
areal/models/transformers/ulyssess_patch.py:111:                            local_seq_len = seq_len // current_ulysses_sp_size
areal/models/transformers/ulyssess_patch.py:112:                            start_idx = rank * local_seq_len
areal/models/transformers/ulyssess_patch.py:113:                            end_idx = start_idx + local_seq_len
areal/models/tree_attn/tree.py:287:        describing the batch of sequences. Shape: [batch_size, seq_len].
areal/models/tree_attn/tree.py:599:        seq_len = positions.numel()
areal/models/tree_attn/tree.py:600:        if seq_len == 0:
areal/models/tree_attn/tree.py:604:        _apply_causal_mask_blockwise(mask, positions, seq_len, device)
areal/models/tree_attn/tree.py:612:    seq_len: int,
areal/models/tree_attn/tree.py:617:    Instead of creating one huge tril_indices(seq_len, seq_len), we process
areal/models/tree_attn/tree.py:634:    num_blocks = (seq_len + block_size - 1) // block_size
areal/models/tree_attn/tree.py:638:        row_end = min((block_row + 1) * block_size, seq_len)
areal/models/tree_attn/tree.py:654:            col_end = min((block_col + 1) * block_size, seq_len)
areal/models/fsdp/ulysses.py:215:    _, total_seq_len = input_ids_rmpad.shape
areal/models/fsdp/ulysses.py:216:    pad_size = (sp_size - total_seq_len % sp_size) % sp_size
areal/models/tree_attn/functional.py:248:        Each logprobs tensor has shape (seq_len - 1,) where seq_len is the
areal/models/tree_attn/functional.py:352:        sequence_id to the corresponding tensor of shape (seq_len - 1,).
areal/models/tree_attn/functional.py:494:        sequence_id to the corresponding tensor of shape (seq_len - 1,).
areal/models/tree_attn/functional.py:568:    max_seq_len: int | None = None,
areal/models/tree_attn/functional.py:584:    max_seq_len : int or None, default=None
areal/models/tree_attn/functional.py:593:        Tensor of shape (batch_size, max_seq_len) with merged results.
areal/models/tree_attn/functional.py:614:        return torch.full((batch_size, max_seq_len or 0), padding_value, device=device)
areal/models/tree_attn/functional.py:622:    if max_seq_len is None:
areal/models/tree_attn/functional.py:623:        max_seq_len = max(t.shape[0] for t in combined.values()) if combined else 0
areal/models/tree_attn/functional.py:627:        (batch_size, max_seq_len), padding_value, dtype=dtype, device=device
areal/models/tree_attn/functional.py:634:        seq_len = min(tensor.shape[0], max_seq_len)
areal/models/tree_attn/functional.py:635:        output[seq_id, :seq_len] = tensor[:seq_len]
areal/models/tree_attn/module_fsdp.py:39:    seq_len: int,
areal/models/tree_attn/module_fsdp.py:50:        Dense attention mask of shape (seq_len, seq_len).
areal/models/tree_attn/module_fsdp.py:51:    seq_len : int
areal/models/tree_attn/module_fsdp.py:74:        Q_LEN=seq_len,
areal/models/tree_attn/module_fsdp.py:75:        KV_LEN=seq_len,
areal/models/mcore/common.py:58:        "variable_seq_lengths": True,
areal/models/tree_attn/module_megatron.py:129:                seq_len = attention_mask.shape[0]
areal/models/tree_attn/module_megatron.py:131:                    attention_mask, seq_len, attention_mask.device
areal/models/tree_attn/module_archon.py:98:            q: Query tensor, shape [batch, heads, seq_len, head_dim]
areal/models/tree_attn/module_archon.py:99:            k: Key tensor, shape [batch, heads, seq_len, head_dim]
areal/models/tree_attn/module_archon.py:100:            v: Value tensor, shape [batch, heads, seq_len, head_dim]
areal/models/tree_attn/module_archon.py:110:            Attention output, shape [batch, heads, seq_len, head_dim]
areal/models/tree_attn/module_archon.py:112:        # Input: [batch, heads, seq_len, head_dim]
areal/models/tree_attn/module_archon.py:113:        batch, n_heads, seq_len, head_dim = q.shape
areal/models/mcore/registry.py:175:            rope_scaling_args["seq_len_interpolation_factor"] = hf_config.rope_scaling[
```

### Assistant - 2026-04-27 23:38:41 EDT

```
我会改三处：code reward 增加可选诊断返回；RLVR workflow 识别诊断返回并记录标量；Nemotron code 训练入口默认打开这个诊断模式。指标会进入现有 `rollout/...` scope，不改变训练用的 reward 值。
```

### Assistant - 2026-04-27 23:39:23 EDT

代码路径已经接上了。接下来我补一个小测试，保证默认仍然返回 float，同时诊断模式能给出 no_code/compile_error/pass-rate 这些训练日志字段。

### Tool Call - 2026-04-27 23:39:35 EDT

```
exec_command {"cmd":"uv run pytest tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:39:35 EDT

```
exec_command {"cmd":"uv run ruff check areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:39:35 EDT

```
Chunk ID: c64b54
Wall time: 0.0000 seconds
Process exited with code 127
Original token count: 8
Output:
zsh:1: command not found: uv
```

### Tool Output - 2026-04-27 23:39:35 EDT

```
Chunk ID: 9f2234
Wall time: 0.0000 seconds
Process exited with code 127
Original token count: 8
Output:
zsh:1: command not found: uv
```

### Assistant - 2026-04-27 23:39:45 EDT

这个环境里没有 `uv`，所以我先改用现有 Python/pytest 路径验证。如果本机有项目 venv，我会优先用 `.venv/bin/python`。

### Tool Call - 2026-04-27 23:39:45 EDT

```
exec_command {"cmd":"ls -la .venv/bin/python .venv/bin/pytest .venv/bin/ruff","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:39:45 EDT

```
exec_command {"cmd":"python3 --version","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Call - 2026-04-27 23:39:45 EDT

```
exec_command {"cmd":"git diff -- areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":20000}
```

### Tool Output - 2026-04-27 23:39:45 EDT

```
Chunk ID: ef18f3
Wall time: 0.0000 seconds
Process exited with code 2
Original token count: 48
Output:
ls: cannot access '.venv/bin/python': No such file or directory
ls: cannot access '.venv/bin/pytest': No such file or directory
ls: cannot access '.venv/bin/ruff': No such file or directory
```

### Tool Output - 2026-04-27 23:39:45 EDT

```
Chunk ID: c556df
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 4
Output:
Python 3.12.3
```

### Tool Output - 2026-04-27 23:39:45 EDT

````
Chunk ID: 1bf098
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2518
Output:
diff --git a/areal/reward/code/competitive_exec.py b/areal/reward/code/competitive_exec.py
index bcb65db3..cd38d4b7 100644
--- a/areal/reward/code/competitive_exec.py
+++ b/areal/reward/code/competitive_exec.py
@@ -82,8 +82,9 @@ def nemotron_competitive_reward_fn(
     memory_mb: int = 2048,
     compile_timeout: float = 30.0,
     java_xmx_mb: int = 2048,
+    return_diagnostics: bool = False,
     **kwargs,
-) -> float:
+) -> float | dict[str, float]:
     """Pass rate over (a sample of) unit tests.
 
     :param language: Source language for the model output. May be overridden
@@ -97,18 +98,40 @@ def nemotron_competitive_reward_fn(
     :param compile_timeout: Wall-clock seconds for the compile step (compiled
         languages only). Compile happens once per submission.
     :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
+    :param return_diagnostics: When true, return a dictionary with the reward
+        and per-sample execution outcome counters for training-time logging.
     """
+    diagnostics = {
+        "reward": 0.0,
+        "no_tests": 0.0,
+        "mismatched_tests": 0.0,
+        "no_code": 0.0,
+        "compile_error": 0.0,
+        "timeout_tests": 0.0,
+        "runtime_error_tests": 0.0,
+        "wrong_answer_tests": 0.0,
+        "passed_tests": 0.0,
+        "sampled_tests": 0.0,
+    }
+
+    def _finish(reward: float) -> float | dict[str, float]:
+        diagnostics["reward"] = reward
+        return diagnostics if return_diagnostics else reward
+
     if not test_inputs or not test_outputs:
-        return 0.0
+        diagnostics["no_tests"] = 1.0
+        return _finish(0.0)
     if len(test_inputs) != len(test_outputs):
         logger.warning(
             f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
         )
-        return 0.0
+        diagnostics["mismatched_tests"] = 1.0
+        return _finish(0.0)
 
     extracted = extract_code(completions, expected_language=language)
     if extracted is None:
-        return 0.0
+        diagnostics["no_code"] = 1.0
+        return _finish(0.0)
     detected_lang, code = extracted
 
     artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
@@ -116,7 +139,8 @@ def nemotron_competitive_reward_fn(
         # Compile failure or unsupported language — caller already logged in
         # CompileResult.error/stderr; treat as zero reward.
         cleanup_compile_result(artifact)
-        return 0.0
+        diagnostics["compile_error"] = 1.0
+        return _finish(0.0)
 
     try:
         n = len(test_inputs)
@@ -127,6 +151,7 @@ def nemotron_competitive_reward_fn(
             # across all rollouts of the same problem in a GRPO group.
             rng = random.Random(prompt)
             idx = rng.sample(range(n), max_tests)
+        diagnostics["sampled_tests"] = float(len(idx))
 
         passed = 0
         for i in idx:
@@ -137,11 +162,18 @@ def nemotron_competitive_reward_fn(
                 memory_mb=memory_mb,
                 java_xmx_mb=java_xmx_mb,
             )
-            if result.timeout or result.error is not None or result.returncode != 0:
+            if result.timeout:
+                diagnostics["timeout_tests"] += 1.0
+                continue
+            if result.error is not None or result.returncode != 0:
+                diagnostics["runtime_error_tests"] += 1.0
                 continue
             if outputs_match(result.stdout, test_outputs[i]):
                 passed += 1
+            else:
+                diagnostics["wrong_answer_tests"] += 1.0
 
-        return passed / len(idx)
+        diagnostics["passed_tests"] = float(passed)
+        return _finish(passed / len(idx))
     finally:
         cleanup_compile_result(artifact)
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..06aed3a0 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -54,10 +54,11 @@ class RLVRWorkflow(RolloutWorkflow):
         ig_reward_params=None,
         get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
         | str = default_get_input_ids_fn,
-        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
-        | str = default_data_extract_prompt_fn,
-        reward_timeout_seconds: float = 15.0,
-    ):
+        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
+        | str = default_data_extract_prompt_fn,
+        reward_timeout_seconds: float = 15.0,
+        log_reward_diagnostics: bool = False,
+    ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
         if isinstance(self.tokenizer, str):
@@ -68,8 +69,9 @@ class RLVRWorkflow(RolloutWorkflow):
         self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
         self.rollout_stat_scope = rollout_stat_scope
         self.enable_thinking = enable_thinking
-        self.ig_reward_params = ig_reward_params
-        self.reward_timeout_seconds = reward_timeout_seconds
+        self.ig_reward_params = ig_reward_params
+        self.reward_timeout_seconds = reward_timeout_seconds
+        self.log_reward_diagnostics = log_reward_diagnostics
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,27 +91,39 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
-        """Decode completion and compute reward.
+    ) -> tuple[float, dict[str, float]]:
+        """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
         to string, calls async reward function, and logs metric to stats tracker.
 
         Returns
         -------
-        float
-            Reward value.
-        """
-        completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
-            prompt_str,
-            completions_str,
-            resp.input_tokens,
-            resp.output_tokens,
-            **task_data,
-        )
-
-        return reward
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
+        """
+        completions_str = self.tokenizer.decode(resp.output_tokens)
+        reward_kwargs = dict(task_data)
+        if self.log_reward_diagnostics:
+            reward_kwargs["return_diagnostics"] = True
+        reward_result = await self.async_reward_fn(
+            prompt_str,
+            completions_str,
+            resp.input_tokens,
+            resp.output_tokens,
+            **reward_kwargs,
+        )
+
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,11 +147,19 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
-
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
-
-        return resp, reward
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
+
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
+
+        return resp, reward
 
     async def _compute_ig_probes(
             self,
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..0795284c 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            log_reward_diagnostics=True,
         )
 
         eval_workflow_kwargs_map = {}
diff --git a/tests/test_code_sandbox_multilang.py b/tests/test_code_sandbox_multilang.py
index f759a38a..998a5830 100644
--- a/tests/test_code_sandbox_multilang.py
+++ b/tests/test_code_sandbox_multilang.py
@@ -189,6 +189,30 @@ def test_reward_fn_python_pass_rate():
     assert reward == 1.0
 
 
+def test_reward_fn_diagnostics_mode_reports_test_outcomes():
+    if not TOOLCHAIN.get("python"):
+        pytest.skip("python toolchain not available")
+    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
+    diagnostics = nemotron_competitive_reward_fn(
+        prompt="dummy",
+        completions=completion,
+        prompt_ids=[],
+        completion_ids=[],
+        test_inputs=["1\n", "10\n"],
+        test_outputs=["2", "21"],
+        language="python",
+        per_test_timeout=5.0,
+        max_tests=15,
+        memory_mb=512,
+        return_diagnostics=True,
+    )
+    assert diagnostics["reward"] == 0.5
+    assert diagnostics["sampled_tests"] == 2.0
+    assert diagnostics["passed_tests"] == 1.0
+    assert diagnostics["wrong_answer_tests"] == 1.0
+    assert diagnostics["no_code"] == 0.0
+
+
 def test_reward_fn_cpp_pass_rate():
     if not TOOLCHAIN.get("cpp"):
         pytest.skip("cpp toolchain not available")
````

### Tool Call - 2026-04-27 23:39:51 EDT

```
exec_command {"cmd":"python3 -m pytest tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:39:51 EDT

```
exec_command {"cmd":"python3 -m ruff check areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:39:52 EDT

```
Chunk ID: c8b933
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 11
Output:
/usr/bin/python3: No module named pytest
```

### Tool Output - 2026-04-27 23:39:52 EDT

```
Chunk ID: bb213c
Wall time: 0.0422 seconds
Process exited with code 1
Original token count: 366
Output:
I001 [*] Import block is un-sorted or un-formatted
  --> areal/workflow/rlvr_qun_team.py:1:1
   |
 1 | / import uuid
 2 | | import asyncio
 3 | | from collections.abc import Callable
 4 | | from typing import Any
 5 | |
 6 | | import torch
 7 | | from transformers import PreTrainedTokenizerFast
 8 | |
 9 | | from areal import workflow_context
10 | | from areal.api.cli_args import GenerationHyperparameters
11 | | from areal.api.engine_api import InferenceEngine
12 | | from areal.api.io_struct import ModelRequest, ModelResponse
13 | | from areal.api.reward_api import AsyncRewardWrapper
14 | | from areal.api.workflow_api import RolloutWorkflow
15 | | from areal.utils import logging, stats_tracker
16 | | from areal.utils.dynamic_import import import_from_string
17 | | from areal.utils.perf_tracer import (
18 | |     atrace_session_phase,
19 | |     session_context,
20 | |     trace_session,
21 | | )
   | |_^
22 |
23 |   logger = logging.getLogger("RLVRWorkflow")
   |
help: Organize imports

F541 [*] f-string without any placeholders
   --> areal/workflow/rlvr_qun_team.py:180:28
    |
178 |         gt_text = data.get("answer")
179 |         if not gt_text:
180 |             logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
    |                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
181 |             return None
    |
help: Remove extraneous `f` prefix

Found 2 errors.
[*] 2 fixable with the `--fix` option.
```

### Assistant - 2026-04-27 23:39:57 EDT

`python3 -m ruff` 可用，但 pytest 没装。Ruff 指出这个 workflow 文件里还有一个既有的空 f-string，我顺手修掉，再跑一次静态检查。

### Tool Call - 2026-04-27 23:39:58 EDT

```
exec_command {"cmd":"python3 -m ruff check --fix areal/workflow/rlvr_qun_team.py areal/reward/code/competitive_exec.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:39:59 EDT

```
Chunk ID: 9eecca
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 10
Output:
Found 2 errors (2 fixed, 0 remaining).
```

### Tool Call - 2026-04-27 23:40:05 EDT

```
exec_command {"cmd":"python3 -m ruff check areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:40:05 EDT

```
exec_command {"cmd":"python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:40:05 EDT

```
exec_command {"cmd":"git diff --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:40:05 EDT

```
Chunk ID: 508965
Wall time: 0.0424 seconds
Process exited with code 0
Original token count: 5
Output:
All checks passed!
```

### Tool Output - 2026-04-27 23:40:05 EDT

```
Chunk ID: 35e639
Wall time: 0.0283 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Output - 2026-04-27 23:40:05 EDT

```
Chunk ID: 282def
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 76
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++----
 areal/workflow/rlvr_qun_team.py       | 78 ++++++++++++++++++++++-------------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 +++++++++++
 4 files changed, 114 insertions(+), 35 deletions(-)
```

### Tool Call - 2026-04-27 23:40:14 EDT

```
exec_command {"cmd":"sed -n '1,170p' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":13000}
```

### Tool Call - 2026-04-27 23:40:14 EDT

```
exec_command {"cmd":"sed -n '72,180p' areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:40:14 EDT

```
exec_command {"cmd":"git status --short","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Output - 2026-04-27 23:40:14 EDT

```
Chunk ID: f5e0db
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1538
Output:
import asyncio
import uuid
from collections.abc import Callable
from typing import Any

import torch
from transformers import PreTrainedTokenizerFast

from areal import workflow_context
from areal.api.cli_args import GenerationHyperparameters
from areal.api.engine_api import InferenceEngine
from areal.api.io_struct import ModelRequest, ModelResponse
from areal.api.reward_api import AsyncRewardWrapper
from areal.api.workflow_api import RolloutWorkflow
from areal.utils import logging, stats_tracker
from areal.utils.dynamic_import import import_from_string
from areal.utils.perf_tracer import (
    atrace_session_phase,
    session_context,
    trace_session,
)

logger = logging.getLogger("RLVRWorkflow")


def default_get_input_ids_fn(
    data: Any,
    tokenizer: PreTrainedTokenizerFast,
    enable_thinking: bool,
) -> list[int]:
    input_ids = tokenizer.apply_chat_template(
        data,
        tokenize=True,
        add_generation_prompt=True,
        enable_thinking=enable_thinking,
    )
    return list(input_ids)


def default_data_extract_prompt_fn(data: dict[str, Any]) -> Any:
    return data["messages"]


class RLVRWorkflow(RolloutWorkflow):
    """Single-turn reward learning workflow supporting optional thinking tokens."""

    def __init__(
        self,
        reward_fn: Callable[..., Any] | str,
        gconfig: GenerationHyperparameters,
        tokenizer: PreTrainedTokenizerFast | str,
        enable_thinking: bool = False,
        rollout_stat_scope: str = "rollout",
        ig_reward_params=None,
        get_input_ids_fn: Callable[[Any, PreTrainedTokenizerFast, bool], list[int]]
        | str = default_get_input_ids_fn,
        data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
        | str = default_data_extract_prompt_fn,
        reward_timeout_seconds: float = 15.0,
        log_reward_diagnostics: bool = False,
    ):
        self.reward_fn = reward_fn
        self.tokenizer = tokenizer
        if isinstance(self.tokenizer, str):
            from areal.utils.hf_utils import load_hf_tokenizer

            tokenizer = load_hf_tokenizer(self.tokenizer)
            self.tokenizer = tokenizer
        self.gconfig = gconfig.new_with_stop_and_pad_token_ids(self.tokenizer)
        self.rollout_stat_scope = rollout_stat_scope
        self.enable_thinking = enable_thinking
        self.ig_reward_params = ig_reward_params
        self.reward_timeout_seconds = reward_timeout_seconds
        self.log_reward_diagnostics = log_reward_diagnostics
        if not isinstance(reward_fn, str):
            self.async_reward_fn = AsyncRewardWrapper(
                reward_fn, timeout_seconds=reward_timeout_seconds
            )
        # Support string paths for get_input_ids_fn
        if isinstance(get_input_ids_fn, str):
            get_input_ids_fn = import_from_string(get_input_ids_fn)
        self.get_input_ids_fn = get_input_ids_fn
        # Support string paths for data_extract_prompt_fn
        if isinstance(data_extract_prompt_fn, str):
            data_extract_prompt_fn = import_from_string(data_extract_prompt_fn)
        self.data_extract_prompt_fn = data_extract_prompt_fn

    @trace_session("reward")
    async def _compute_rewards(
        self,
        resp: ModelResponse,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[float, dict[str, float]]:
        """Decode completion and compute reward.

        Traces reward phase execution for SessionTracer. Decodes output tokens
        to string, calls async reward function, and logs metric to stats tracker.

        Returns
        -------
        tuple[float, dict[str, float]]
            Reward value and optional diagnostic scalars.
        """
        completions_str = self.tokenizer.decode(resp.output_tokens)
        reward_kwargs = dict(task_data)
        if self.log_reward_diagnostics:
            reward_kwargs["return_diagnostics"] = True
        reward_result = await self.async_reward_fn(
            prompt_str,
            completions_str,
            resp.input_tokens,
            resp.output_tokens,
            **reward_kwargs,
        )

        if isinstance(reward_result, dict):
            reward = float(reward_result["reward"])
            diagnostics = {
                f"reward_diagnostics/{key}": float(value)
                for key, value in reward_result.items()
                if key != "reward" and isinstance(value, int | float)
            }
            return reward, diagnostics

        return float(reward_result), {}

    @session_context()
    async def _collect_samples(
        self,
        engine: InferenceEngine,
        req: ModelRequest,
        prompt_str: str,
        task_data: dict[str, Any],
    ) -> tuple[ModelResponse, float]:
        """Generate one sample and compute its reward.

        Registers a new session for this sample, calls engine.agenerate,
        computes reward, and logs metrics. SessionTracer automatically
        tracks generate and reward phases via @trace_session decorators.

        Returns
        -------
        tuple[ModelResponse, float]
            Model response and reward value.
        """
        async with atrace_session_phase("generate"):
            resp = await engine.agenerate(req)

        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)

        scalars = {
            "reward": reward,
            "output_len": resp.output_len,
            "total_len": resp.input_len + resp.output_len,
            "stop_length": float(resp.stop_reason == "length"),
            "stop_abort": float(resp.stop_reason == "abort"),
        }
        scalars.update(reward_diagnostics)
        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)

        return resp, reward

    async def _compute_ig_probes(
            self,
            engine: InferenceEngine,
            resp: ModelResponse,
            data: dict[str, Any],
            outcome_reward: float
    ) -> dict[str, list[float]] | None:
```

### Tool Output - 2026-04-27 23:40:14 EDT

```
Chunk ID: d046b9
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1027
Output:
def nemotron_competitive_reward_fn(
    prompt: str,
    completions: str,
    prompt_ids: list[int],
    completion_ids: list[int],
    test_inputs: list[str] | None = None,
    test_outputs: list[str] | None = None,
    language: str = "python",
    per_test_timeout: float = 5.0,
    max_tests: int = 15,
    memory_mb: int = 2048,
    compile_timeout: float = 30.0,
    java_xmx_mb: int = 2048,
    return_diagnostics: bool = False,
    **kwargs,
) -> float | dict[str, float]:
    """Pass rate over (a sample of) unit tests.

    :param language: Source language for the model output. May be overridden
        by ``task_data["language"]`` injected by the dataset loader. Defaults
        to ``"python"`` for Nemotron-RL-coding-competitive_coding.
    :param per_test_timeout: Wall-clock seconds for each test case.
    :param max_tests: Upper bound on tests executed per reward call; tests
        beyond this cap are sampled deterministically per ``prompt``.
    :param memory_mb: Address-space cap for each test subprocess (Python/C++/
        Node). Java uses ``java_xmx_mb`` instead.
    :param compile_timeout: Wall-clock seconds for the compile step (compiled
        languages only). Compile happens once per submission.
    :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
    :param return_diagnostics: When true, return a dictionary with the reward
        and per-sample execution outcome counters for training-time logging.
    """
    diagnostics = {
        "reward": 0.0,
        "no_tests": 0.0,
        "mismatched_tests": 0.0,
        "no_code": 0.0,
        "compile_error": 0.0,
        "timeout_tests": 0.0,
        "runtime_error_tests": 0.0,
        "wrong_answer_tests": 0.0,
        "passed_tests": 0.0,
        "sampled_tests": 0.0,
    }

    def _finish(reward: float) -> float | dict[str, float]:
        diagnostics["reward"] = reward
        return diagnostics if return_diagnostics else reward

    if not test_inputs or not test_outputs:
        diagnostics["no_tests"] = 1.0
        return _finish(0.0)
    if len(test_inputs) != len(test_outputs):
        logger.warning(
            f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
        )
        diagnostics["mismatched_tests"] = 1.0
        return _finish(0.0)

    extracted = extract_code(completions, expected_language=language)
    if extracted is None:
        diagnostics["no_code"] = 1.0
        return _finish(0.0)
    detected_lang, code = extracted

    artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
    if not artifact.ok:
        # Compile failure or unsupported language — caller already logged in
        # CompileResult.error/stderr; treat as zero reward.
        cleanup_compile_result(artifact)
        diagnostics["compile_error"] = 1.0
        return _finish(0.0)

    try:
        n = len(test_inputs)
        if n <= max_tests:
            idx = list(range(n))
        else:
            # Seeding with the prompt keeps the sampled subset identical
            # across all rollouts of the same problem in a GRPO group.
            rng = random.Random(prompt)
            idx = rng.sample(range(n), max_tests)
        diagnostics["sampled_tests"] = float(len(idx))

        passed = 0
        for i in idx:
            result = run_artifact(
                artifact,
                stdin=test_inputs[i],
                timeout=per_test_timeout,
                memory_mb=memory_mb,
                java_xmx_mb=java_xmx_mb,
            )
            if result.timeout:
                diagnostics["timeout_tests"] += 1.0
                continue
            if result.error is not None or result.returncode != 0:
                diagnostics["runtime_error_tests"] += 1.0
                continue
            if outputs_match(result.stdout, test_outputs[i]):
                passed += 1
            else:
                diagnostics["wrong_answer_tests"] += 1.0

        diagnostics["passed_tests"] = float(passed)
        return _finish(passed / len(idx))
    finally:
        cleanup_compile_result(artifact)
```

### Tool Output - 2026-04-27 23:40:14 EDT

```
Chunk ID: c4e26e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 41
Output:
 M areal/reward/code/competitive_exec.py
 M areal/workflow/rlvr_qun_team.py
 M examples/code/nemotron_code_rl.py
 M tests/test_code_sandbox_multilang.py
?? .codex
```

### Tool Call - 2026-04-27 23:40:22 EDT

```
exec_command {"cmd":"find .codex -maxdepth 3 -type f -o -type d","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:40:22 EDT

```
exec_command {"cmd":"git diff --numstat -- areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Call - 2026-04-27 23:40:23 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py areal/reward/code/competitive_exec.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Output - 2026-04-27 23:40:23 EDT

```
Chunk ID: c65291
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2
Output:
.codex
```

### Tool Output - 2026-04-27 23:40:23 EDT

```
Chunk ID: 7d9b1f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 10
Output:
50	28	areal/workflow/rlvr_qun_team.py
```

### Tool Output - 2026-04-27 23:40:23 EDT

```
Chunk ID: 279ff8
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 53
Output:
areal/workflow/rlvr_qun_team.py:       Python script, Unicode text, UTF-8 text executable, with CRLF, LF line terminators
areal/reward/code/competitive_exec.py: Python script, Unicode text, UTF-8 text executable
```

### Tool Call - 2026-04-27 23:40:32 EDT

```
exec_command {"cmd":"ls -la .codex","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Output - 2026-04-27 23:40:32 EDT

```
Chunk ID: 486b71
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 11
Output:
-r--r--r-- 1 ag ag 0 Apr 25 05:19 .codex
```

### Tool Call - 2026-04-27 23:40:41 EDT

```
exec_command {"cmd":"perl -0pi -e 's/(?<!\\r)\\n/\\r\\n/g' areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Output - 2026-04-27 23:40:41 EDT

```
Chunk ID: b3846d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:40:52 EDT

```
exec_command {"cmd":"file areal/workflow/rlvr_qun_team.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":2000}
```

### Tool Call - 2026-04-27 23:40:52 EDT

```
exec_command {"cmd":"python3 -m ruff check areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Call - 2026-04-27 23:40:52 EDT

```
exec_command {"cmd":"python3 -m py_compile areal/reward/code/competitive_exec.py areal/workflow/rlvr_qun_team.py examples/code/nemotron_code_rl.py tests/test_code_sandbox_multilang.py","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:40:52 EDT

```
Chunk ID: 5234c6
Wall time: 0.0425 seconds
Process exited with code 0
Original token count: 28
Output:
areal/workflow/rlvr_qun_team.py: Python script, Unicode text, UTF-8 text executable, with CRLF line terminators
```

### Tool Output - 2026-04-27 23:40:52 EDT

```
Chunk ID: 069a8f
Wall time: 0.0573 seconds
Process exited with code 0
Original token count: 5
Output:
All checks passed!
```

### Tool Output - 2026-04-27 23:40:52 EDT

```
Chunk ID: 689f1f
Wall time: 0.0684 seconds
Process exited with code 0
Original token count: 0
Output:
```

### Tool Call - 2026-04-27 23:40:59 EDT

````
exec_command {"cmd":"python3 -c \"from areal.reward.code.competitive_exec import nemotron_competitive_reward_fn; r=nemotron_competitive_reward_fn(prompt='dummy', completions='```python\\nx = int(input())\\nprint(x * 2)\\n```', prompt_ids=[], completion_ids=[], test_inputs=['1\\n','10\\n'], test_outputs=['2','21'], return_diagnostics=True); print(r)\"","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
````

### Tool Output - 2026-04-27 23:40:59 EDT

```
Chunk ID: 98d434
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 18
Output:
zsh: parse error near `)'
zsh:1: parse error in command substitution
```

### Tool Call - 2026-04-27 23:41:06 EDT

```
exec_command {"cmd":"python3 -c 'from areal.reward.code.competitive_exec import nemotron_competitive_reward_fn; fence=chr(96)*3; completion=fence+\"python\\nx = int(input())\\nprint(x * 2)\\n\"+fence; r=nemotron_competitive_reward_fn(prompt=\"dummy\", completions=completion, prompt_ids=[], completion_ids=[], test_inputs=[\"1\\n\",\"10\\n\"], test_outputs=[\"2\",\"21\"], return_diagnostics=True); print(r)'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":12000}
```

### Tool Output - 2026-04-27 23:41:06 EDT

```
Chunk ID: dd20e4
Wall time: 0.1536 seconds
Process exited with code 1
Original token count: 112
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
  File "/home/ag/projects/AReaL_v1/areal/__init__.py", line 5, in <module>
    from .infra import (
  File "/home/ag/projects/AReaL_v1/areal/infra/__init__.py", line 3, in <module>
    from . import workflow_context
  File "/home/ag/projects/AReaL_v1/areal/infra/workflow_context.py", line 8, in <module>
    import aiohttp
ModuleNotFoundError: No module named 'aiohttp'
```

### Tool Call - 2026-04-27 23:41:18 EDT

```
exec_command {"cmd":"git diff --stat","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":4000}
```

### Tool Call - 2026-04-27 23:41:18 EDT

```
exec_command {"cmd":"git diff -- areal/workflow/rlvr_qun_team.py | sed -n '1,220p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":16000}
```

### Tool Call - 2026-04-27 23:41:18 EDT

```
exec_command {"cmd":"git diff -- areal/reward/code/competitive_exec.py tests/test_code_sandbox_multilang.py examples/code/nemotron_code_rl.py | sed -n '1,260p'","workdir":"/home/ag/projects/AReaL_v1","yield_time_ms":1000,"max_output_tokens":18000}
```

### Tool Output - 2026-04-27 23:41:18 EDT

```
Chunk ID: 6fabf5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 80
Output:
 areal/reward/code/competitive_exec.py | 46 +++++++++++++++++++++++++++++------
 areal/workflow/rlvr_qun_team.py       | 42 ++++++++++++++++++++++++--------
 examples/code/nemotron_code_rl.py     |  1 +
 tests/test_code_sandbox_multilang.py  | 24 ++++++++++++++++++
 4 files changed, 96 insertions(+), 17 deletions(-)
```

### Tool Output - 2026-04-27 23:41:18 EDT

```
Chunk ID: 82bc47
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1002
Output:
diff --git a/areal/workflow/rlvr_qun_team.py b/areal/workflow/rlvr_qun_team.py
index 1db0d323..ed139273 100644
--- a/areal/workflow/rlvr_qun_team.py
+++ b/areal/workflow/rlvr_qun_team.py
@@ -1,5 +1,5 @@
-import uuid
 import asyncio
+import uuid
 from collections.abc import Callable
 from typing import Any
 
@@ -57,6 +57,7 @@ class RLVRWorkflow(RolloutWorkflow):
         data_extract_prompt_fn: Callable[[dict[str, Any]], Any]
         | str = default_data_extract_prompt_fn,
         reward_timeout_seconds: float = 15.0,
+        log_reward_diagnostics: bool = False,
     ):
         self.reward_fn = reward_fn
         self.tokenizer = tokenizer
@@ -70,6 +71,7 @@ class RLVRWorkflow(RolloutWorkflow):
         self.enable_thinking = enable_thinking
         self.ig_reward_params = ig_reward_params
         self.reward_timeout_seconds = reward_timeout_seconds
+        self.log_reward_diagnostics = log_reward_diagnostics
         if not isinstance(reward_fn, str):
             self.async_reward_fn = AsyncRewardWrapper(
                 reward_fn, timeout_seconds=reward_timeout_seconds
@@ -89,7 +91,7 @@ class RLVRWorkflow(RolloutWorkflow):
         resp: ModelResponse,
         prompt_str: str,
         task_data: dict[str, Any],
-    ) -> float:
+    ) -> tuple[float, dict[str, float]]:
         """Decode completion and compute reward.
 
         Traces reward phase execution for SessionTracer. Decodes output tokens
@@ -97,19 +99,31 @@ class RLVRWorkflow(RolloutWorkflow):
 
         Returns
         -------
-        float
-            Reward value.
+        tuple[float, dict[str, float]]
+            Reward value and optional diagnostic scalars.
         """
         completions_str = self.tokenizer.decode(resp.output_tokens)
-        reward = await self.async_reward_fn(
+        reward_kwargs = dict(task_data)
+        if self.log_reward_diagnostics:
+            reward_kwargs["return_diagnostics"] = True
+        reward_result = await self.async_reward_fn(
             prompt_str,
             completions_str,
             resp.input_tokens,
             resp.output_tokens,
-            **task_data,
+            **reward_kwargs,
         )
 
-        return reward
+        if isinstance(reward_result, dict):
+            reward = float(reward_result["reward"])
+            diagnostics = {
+                f"reward_diagnostics/{key}": float(value)
+                for key, value in reward_result.items()
+                if key != "reward" and isinstance(value, int | float)
+            }
+            return reward, diagnostics
+
+        return float(reward_result), {}
 
     @session_context()
     async def _collect_samples(
@@ -133,9 +147,17 @@ class RLVRWorkflow(RolloutWorkflow):
         async with atrace_session_phase("generate"):
             resp = await engine.agenerate(req)
 
-        reward = await self._compute_rewards(resp, prompt_str, task_data)
+        reward, reward_diagnostics = await self._compute_rewards(resp, prompt_str, task_data)
 
-        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(reward=reward)
+        scalars = {
+            "reward": reward,
+            "output_len": resp.output_len,
+            "total_len": resp.input_len + resp.output_len,
+            "stop_length": float(resp.stop_reason == "length"),
+            "stop_abort": float(resp.stop_reason == "abort"),
+        }
+        scalars.update(reward_diagnostics)
+        stats_tracker.get(workflow_context.stat_scope(self.rollout_stat_scope)).scalar(**scalars)
 
         return resp, reward
 
@@ -155,7 +177,7 @@ class RLVRWorkflow(RolloutWorkflow):
 
         gt_text = data.get("answer")
         if not gt_text:
-            logger.warning(f"[IG-Rollout] Enabled but no 'answer' in data.")
+            logger.warning("[IG-Rollout] Enabled but no 'answer' in data.")
             return None
 
         # 1. 构造 Target 后缀
```

### Tool Output - 2026-04-27 23:41:18 EDT

````
Chunk ID: af3ad7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1389
Output:
diff --git a/areal/reward/code/competitive_exec.py b/areal/reward/code/competitive_exec.py
index bcb65db3..cd38d4b7 100644
--- a/areal/reward/code/competitive_exec.py
+++ b/areal/reward/code/competitive_exec.py
@@ -82,8 +82,9 @@ def nemotron_competitive_reward_fn(
     memory_mb: int = 2048,
     compile_timeout: float = 30.0,
     java_xmx_mb: int = 2048,
+    return_diagnostics: bool = False,
     **kwargs,
-) -> float:
+) -> float | dict[str, float]:
     """Pass rate over (a sample of) unit tests.
 
     :param language: Source language for the model output. May be overridden
@@ -97,18 +98,40 @@ def nemotron_competitive_reward_fn(
     :param compile_timeout: Wall-clock seconds for the compile step (compiled
         languages only). Compile happens once per submission.
     :param java_xmx_mb: JVM heap cap for Java submissions (``-Xmx``).
+    :param return_diagnostics: When true, return a dictionary with the reward
+        and per-sample execution outcome counters for training-time logging.
     """
+    diagnostics = {
+        "reward": 0.0,
+        "no_tests": 0.0,
+        "mismatched_tests": 0.0,
+        "no_code": 0.0,
+        "compile_error": 0.0,
+        "timeout_tests": 0.0,
+        "runtime_error_tests": 0.0,
+        "wrong_answer_tests": 0.0,
+        "passed_tests": 0.0,
+        "sampled_tests": 0.0,
+    }
+
+    def _finish(reward: float) -> float | dict[str, float]:
+        diagnostics["reward"] = reward
+        return diagnostics if return_diagnostics else reward
+
     if not test_inputs or not test_outputs:
-        return 0.0
+        diagnostics["no_tests"] = 1.0
+        return _finish(0.0)
     if len(test_inputs) != len(test_outputs):
         logger.warning(
             f"Mismatched test inputs/outputs: {len(test_inputs)} vs {len(test_outputs)}"
         )
-        return 0.0
+        diagnostics["mismatched_tests"] = 1.0
+        return _finish(0.0)
 
     extracted = extract_code(completions, expected_language=language)
     if extracted is None:
-        return 0.0
+        diagnostics["no_code"] = 1.0
+        return _finish(0.0)
     detected_lang, code = extracted
 
     artifact = compile_code(detected_lang, code, compile_timeout=compile_timeout)
@@ -116,7 +139,8 @@ def nemotron_competitive_reward_fn(
         # Compile failure or unsupported language — caller already logged in
         # CompileResult.error/stderr; treat as zero reward.
         cleanup_compile_result(artifact)
-        return 0.0
+        diagnostics["compile_error"] = 1.0
+        return _finish(0.0)
 
     try:
         n = len(test_inputs)
@@ -127,6 +151,7 @@ def nemotron_competitive_reward_fn(
             # across all rollouts of the same problem in a GRPO group.
             rng = random.Random(prompt)
             idx = rng.sample(range(n), max_tests)
+        diagnostics["sampled_tests"] = float(len(idx))
 
         passed = 0
         for i in idx:
@@ -137,11 +162,18 @@ def nemotron_competitive_reward_fn(
                 memory_mb=memory_mb,
                 java_xmx_mb=java_xmx_mb,
             )
-            if result.timeout or result.error is not None or result.returncode != 0:
+            if result.timeout:
+                diagnostics["timeout_tests"] += 1.0
+                continue
+            if result.error is not None or result.returncode != 0:
+                diagnostics["runtime_error_tests"] += 1.0
                 continue
             if outputs_match(result.stdout, test_outputs[i]):
                 passed += 1
+            else:
+                diagnostics["wrong_answer_tests"] += 1.0
 
-        return passed / len(idx)
+        diagnostics["passed_tests"] = float(passed)
+        return _finish(passed / len(idx))
     finally:
         cleanup_compile_result(artifact)
diff --git a/examples/code/nemotron_code_rl.py b/examples/code/nemotron_code_rl.py
index 3d51090f..0795284c 100644
--- a/examples/code/nemotron_code_rl.py
+++ b/examples/code/nemotron_code_rl.py
@@ -65,6 +65,7 @@ def main(args):
             tokenizer=config.tokenizer_path,
             enable_thinking=config.gconfig.enable_thinking,
             reward_timeout_seconds=config.code_reward.reward_timeout_seconds,
+            log_reward_diagnostics=True,
         )
 
         eval_workflow_kwargs_map = {}
diff --git a/tests/test_code_sandbox_multilang.py b/tests/test_code_sandbox_multilang.py
index f759a38a..998a5830 100644
--- a/tests/test_code_sandbox_multilang.py
+++ b/tests/test_code_sandbox_multilang.py
@@ -189,6 +189,30 @@ def test_reward_fn_python_pass_rate():
     assert reward == 1.0
 
 
+def test_reward_fn_diagnostics_mode_reports_test_outcomes():
+    if not TOOLCHAIN.get("python"):
+        pytest.skip("python toolchain not available")
+    completion = "```python\nx = int(input())\nprint(x * 2)\n```"
+    diagnostics = nemotron_competitive_reward_fn(
+        prompt="dummy",
+        completions=completion,
+        prompt_ids=[],
+        completion_ids=[],
+        test_inputs=["1\n", "10\n"],
+        test_outputs=["2", "21"],
+        language="python",
+        per_test_timeout=5.0,
+        max_tests=15,
+        memory_mb=512,
+        return_diagnostics=True,
+    )
+    assert diagnostics["reward"] == 0.5
+    assert diagnostics["sampled_tests"] == 2.0
+    assert diagnostics["passed_tests"] == 1.0
+    assert diagnostics["wrong_answer_tests"] == 1.0
+    assert diagnostics["no_code"] == 0.0
+
+
 def test_reward_fn_cpp_pass_rate():
     if not TOOLCHAIN.get("cpp"):
         pytest.skip("cpp toolchain not available")
````

### Assistant - 2026-04-27 23:41:37 EDT

```
已加上训练期统计，默认在 `examples/code/nemotron_code_rl.py` 里打开。

改动点：
- [competitive_exec.py](/home/ag/projects/AReaL_v1/areal/reward/code/competitive_exec.py:72)：`return_diagnostics=True` 时返回 reward 诊断信息，不影响默认 float reward。
- [rlvr_qun_team.py](/home/ag/projects/AReaL_v1/areal/workflow/rlvr_qun_team.py:45)：workflow 会把诊断指标写进 `stats_tracker`。
- [nemotron_code_rl.py](/home/ag/projects/AReaL_v1/examples/code/nemotron_code_rl.py:57)：code 训练入口默认启用 `log_reward_diagnostics=True`。
- [test_code_sandbox_multilang.py](/home/ag/projects/AReaL_v1/tests/test_code_sandbox_multilang.py:192)：补了诊断模式测试。

训练日志里会多出这些 rollout scope 下的指标：
- `rollout/output_len`
- `rollout/total_len`
- `rollout/stop_length`
- `rollout/stop_abort`
- `rollout/reward_diagnostics/no_tests`
- `rollout/reward_diagnostics/mismatched_tests`
- `rollout/reward_diagnostics/no_code`
- `rollout/reward_diagnostics/compile_error`
- `rollout/reward_diagnostics/timeout_tests`
- `rollout/reward_diagnostics/runtime_error_tests`
- `rollout/reward_diagnostics/wrong_answer_tests`
- `rollout/reward_diagnostics/passed_tests`
- `rollout/reward_diagnostics/sampled_tests`

这样你可以直接看横盘主要来自哪里：格式没出代码、编译失败、运行错误、超时、还是单纯 wrong answer。13k 平均长度的话，重点先看 `stop_length`，如果很高，建议优先把 `max_new_tokens` 降到 4096/8192 做对照实验。

验证：
- `python3 -m ruff check ...` 通过
- `python3 -m py_compile ...` 通过
- 没跑 pytest：当前环境没有 `uv`，系统 Python 也缺 `pytest/aiohttp` 依赖。
```

### User - 2026-04-27 23:47:00 EDT

之前跑过max_len=8k，rollout/reward 平均比16k低0.1左右

### Assistant - 2026-04-27 23:47:10 EDT

````
那说明 16k 对当前模型不是纯浪费，至少多出来的 token 在提高 pass rate。我的建议不是直接降到 8k，而是先用刚加的统计拆因：

- 如果 `stop_length` 在 16k 仍然很高：模型还经常被截断，8k 低 0.1 很正常，先别降长度。
- 如果 `output_len` 均值 13k、`stop_length` 不高：模型确实倾向长思考，但可能有冗余，可以试 `12k` 而不是 `8k`。
- 如果 `no_code/compile_error/runtime_error` 高：问题不是难度本身，更多是格式/代码质量，筛数据不一定先解决。
- 如果主要是 `wrong_answer_tests`，且 partial pass 多：就是能力/难度问题，适合按 pass@16 或 reward 方差筛 curriculum。

你现在这个现象下，我会优先跑一个 `12k` 对照，而不是回 8k：

```bash
gconfig.max_new_tokens=12288 \
eval_gconfig.max_new_tokens=12288 \
sglang.context_length=14336 \
actor.mb_spec.max_tokens_per_mb=14336
```

同时看 `reward / output_len / stop_length` 的性价比。如果 12k reward 接近 16k，但长度明显低，就用 12k；如果也掉很多，说明这批 competitive coding 对小模型确实依赖长 rollout，那就保留 16k，转向数据筛选。
````
